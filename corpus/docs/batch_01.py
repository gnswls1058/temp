"""DOC-001 ~ DOC-015 : Project Echo (2024) 인증 시스템 개선 아크."""

DOCUMENTS = [
    {
        "id": "DOC-001",
        "title": "소셜 로그인 통합 정책 1차 논의",
        "department": "서비스기획팀",
        "type": "기획자와의 회의록",
        "project": "Project Echo",
        "date": "2024-07-11",
        "systems": ["NEXUS", "BlueGate"],
        "features": ["로그인", "소셜 로그인"],
        "participants": ["송지은", "김도윤"],
        "concepts": ["계정 통합", "이메일 중복", "정책 미확정"],
        "body": """## 논의 배경

현재 NEXUS 는 이메일 가입 고객과 소셜 로그인 고객을 별도 계정으로 관리합니다. 같은 사람이 이메일로 가입한 뒤 카카오 로그인을 시도하면 신규 계정이 하나 더 생기고, 고객지원팀에 "주문 내역이 안 보인다"는 문의가 이번 달에만 30건 넘게 들어왔습니다.

## 기획 요구사항

송지은님이 정리해온 요구사항은 다음과 같습니다.

- 동일 이메일이면 기존 계정에 소셜 계정을 연결한다
- 연결 시점에 비밀번호 재확인을 받는다
- 마이페이지에서 연결된 소셜 계정을 해제할 수 있다

## 쟁점

김도윤님이 지적한 부분은 소셜 제공자가 이메일을 항상 내려주지 않는다는 점입니다. 애플 로그인은 사용자가 이메일 가리기를 선택하면 릴레이 주소가 오고, 이 값은 계정 식별자로 쓰기 어렵습니다.

| 제공자 | 이메일 제공 | 비고 |
| --- | --- | --- |
| 카카오 | 선택 동의 | 미동의 시 null |
| 네이버 | 필수 동의 | |
| 애플 | 릴레이 가능 | 해제 시 값 변경됨 |

이메일이 없는 경우 자동 연결은 불가능하고, 결국 사용자에게 "기존 계정과 연결하시겠습니까" 화면을 띄워야 한다는 결론에 가까워졌습니다.

## 결정 사항

- 이메일이 확인되는 제공자에 한해 자동 연결 후보로 제시한다
- 자동 연결이라도 사용자 확인 단계를 반드시 거친다
- 이메일 미제공 케이스는 별도 계정으로 생성하고, 마이페이지에서 수동 연결을 지원한다

## 미확정

계정 연결 해제 시 해당 소셜로만 가입한 고객을 어떻게 처리할지는 결론이 나지 않았습니다. 비밀번호가 없는 계정이라 로그인 수단이 사라지는 문제가 있습니다. 다음 주 2차 회의에서 다시 다룹니다.

## Action Item

- 송지은님: 연결 해제 시나리오 정책안 작성
- 김도윤님: 제공자별 이메일 제공 여부 실제 응답 샘플 확보
""",
    },
    {
        "id": "DOC-002",
        "title": "Echo 킥오프 - 인증 서버 분리 범위 확정",
        "department": "플랫폼개발팀",
        "type": "개발자 회의록",
        "project": "Project Echo",
        "date": "2024-07-18",
        "systems": ["BlueGate", "NEXUS"],
        "features": ["로그인", "인증"],
        "participants": ["김도윤", "조아름", "임채원"],
        "concepts": ["서비스 분리", "JWT", "마이그레이션"],
        "body": """## 논의 배경

인증 로직이 NEXUS 안에 섞여 있어서 회원 도메인을 손댈 때마다 로그인 회귀 테스트를 전부 돌려야 하는 상황입니다. Echo 에서 인증을 BlueGate 로 떼어내기로 했고, 오늘은 어디까지 떼어낼지 경계를 정했습니다.

## 분리 범위

BlueGate 로 이관:

- 로그인 / 로그아웃
- 토큰 발급 및 재발급
- 소셜 OAuth 콜백 처리
- 접근 제어 정책 평가

NEXUS 에 잔류:

- 회원 프로필
- 약관 동의 이력
- 탈퇴 및 휴면 상태 관리

## 쟁점

조아름님이 토큰 서명 키를 어디서 관리할지 물었습니다. 지금은 애플리케이션 설정에 들어 있어서 배포 없이 교체가 불가능합니다. 키 회전을 하려면 별도 저장소가 필요하다는 데 동의했고, 다만 이번 스프린트 범위는 아니라고 정리했습니다.

두 번째 쟁점은 마이그레이션 방식입니다. 한 번에 전환하면 롤백이 어렵고, 병행 운영하면 세션 불일치가 생깁니다. 임채원님이 일정상 병행 기간을 2주 이상 잡기 어렵다고 했습니다.

## 결정 사항

- BlueGate 를 신규 서비스로 띄우고 NEXUS 는 검증만 위임한다
- 기존 토큰은 만료까지 그대로 두고 신규 발급분부터 BlueGate 서명을 쓴다
- 키 회전 저장소는 Echo 2차 범위로 미룬다

## 보류

접근 제어 정책을 코드로 둘지 DB 로 둘지는 보류했습니다. 운영에서 정책을 바꿀 일이 얼마나 되는지 데이터가 없어서, 데이터팀에 최근 6개월 권한 변경 이력을 요청하기로 했습니다.
""",
    },
    {
        "id": "DOC-003",
        "title": "BlueGate 로그인 인증 플로우 설계",
        "department": "플랫폼개발팀",
        "type": "비즈니스 기능 설계",
        "project": "Project Echo",
        "date": "2024-07-25",
        "systems": ["BlueGate", "NEXUS"],
        "features": ["로그인", "인증"],
        "participants": ["김도윤"],
        "concepts": ["JWT", "Access Token", "Refresh Token", "Edge Case"],
        "body": """## 배경

인증을 BlueGate 로 분리하면서 로그인 한 번에 어떤 토큰이 어떻게 발급되는지 기준을 잡아둡니다. 지금까지는 로그인 응답에 토큰 하나만 내려주고 만료되면 재로그인시키는 구조였는데, 앱에서 로그인 화면이 자꾸 뜬다는 불만이 계속 나왔습니다.

## 정상 Flow

1. 클라이언트가 이메일과 비밀번호로 `POST /auth/login` 호출
2. BlueGate 가 NEXUS 회원 조회 API 로 자격 검증
3. 검증 성공 시 Access Token(30분), Refresh Token(14일) 발급
4. Refresh Token 은 해시만 저장소에 저장하고 원본은 클라이언트만 보관
5. Access Token 만료 시 `POST /auth/token/refresh` 로 재발급

## 상태 전이

```text
UNAUTHENTICATED -> AUTHENTICATED (로그인 성공)
AUTHENTICATED -> AUTHENTICATED (토큰 재발급)
AUTHENTICATED -> UNAUTHENTICATED (로그아웃, Refresh 만료, 강제 로그아웃)
```

## Validation

| 항목 | 규칙 | 실패 시 |
| --- | --- | --- |
| 이메일 형식 | RFC 준수 | `AUTH_1001` |
| 비밀번호 시도 횟수 | 10분간 5회 | `AUTH_1005` 잠금 |
| 휴면 회원 | 로그인 차단 | `AUTH_1010` 휴면 해제 유도 |
| 탈퇴 회원 | 로그인 차단 | `AUTH_1011` |

## Edge Case

- **동시 로그인**: 같은 계정으로 여러 기기에서 로그인하는 것을 허용합니다. 기기별 Refresh Token 을 따로 발급하고, 로그아웃은 해당 기기 토큰만 폐기합니다.
- **비밀번호 변경 직후**: 변경 시각 이후 발급된 토큰만 유효하도록 `pwd_changed_at` 을 클레임에 넣고 검증합니다.
- **휴면 전환과 로그인 동시 발생**: 배치가 휴면 처리하는 도중 로그인이 들어오면 회원 상태를 조회 시점 기준으로 판단하기 때문에 통과할 수 있습니다. 로그인 성공 후에도 첫 API 호출에서 상태를 다시 확인합니다.
- **Refresh Token 탈취**: 현재 설계로는 탈취를 감지할 수 없습니다. 회전 도입 여부는 별도 논의가 필요합니다.

## 고민 사항

Access Token 만료를 30분으로 잡았는데, 짧게 하면 재발급 트래픽이 늘고 길게 하면 강제 로그아웃 반영이 늦습니다. 운영 중 강제 로그아웃 요청이 실제로 얼마나 오는지 보고 조정하는 편이 맞다고 봅니다.
""",
    },
    {
        "id": "DOC-004",
        "title": "로그인 API v1 명세",
        "department": "플랫폼개발팀",
        "type": "API 명세서",
        "project": "Project Echo",
        "date": "2024-08-01",
        "systems": ["BlueGate"],
        "features": ["로그인"],
        "participants": [],
        "concepts": ["Access Token", "Error Code"],
        "deprecated": True,
        "body": """> 이 문서는 v1 기준입니다. Refresh Token 회전이 적용된 이후 동작은 v2 문서를 참고하세요.

## Endpoint

```http
POST /auth/login
Content-Type: application/json
```

인증 헤더는 필요하지 않습니다.

## Request

| 필드 | 타입 | 필수 | 설명 |
| --- | --- | --- | --- |
| email | string | Y | 로그인 이메일 |
| password | string | Y | 평문 비밀번호 (TLS 구간 암호화에 의존) |
| deviceId | string | N | 기기 식별자. 없으면 서버가 생성 |

```json
{
  "email": "test-user@example.com",
  "password": "********",
  "deviceId": "device-7f21"
}
```

## Response

```json
{
  "accessToken": "eyJhbGciOi...",
  "refreshToken": "rt_8f2c1d...",
  "expiresIn": 1800,
  "memberId": 10241
}
```

`expiresIn` 은 초 단위이며 Access Token 에만 해당합니다. Refresh Token 만료는 응답에 포함하지 않습니다.

## Error Code

| 코드 | HTTP | 설명 |
| --- | --- | --- |
| AUTH_1001 | 400 | 이메일 형식 오류 |
| AUTH_1002 | 401 | 이메일 또는 비밀번호 불일치 |
| AUTH_1005 | 429 | 로그인 시도 횟수 초과 |
| AUTH_1010 | 403 | 휴면 회원 |
| AUTH_1011 | 403 | 탈퇴 회원 |

## 주의사항

`AUTH_1002` 는 이메일이 없는 경우와 비밀번호가 틀린 경우를 구분하지 않습니다. 계정 존재 여부가 노출되지 않도록 의도한 동작이므로 클라이언트에서 메시지를 나누지 마세요.
""",
    },
    {
        "id": "DOC-005",
        "title": "member_auth 테이블 설계안",
        "department": "백엔드개발팀",
        "type": "DB 테이블 설계안",
        "project": "Project Echo",
        "date": "2024-08-05",
        "systems": ["NEXUS", "BlueGate"],
        "features": ["로그인", "소셜 로그인"],
        "participants": ["이서연", "김도윤"],
        "concepts": ["테이블 분리", "Unique Constraint", "소셜 계정 연결"],
        "body": """## 배경

지금은 `member` 테이블에 비밀번호 해시와 소셜 제공자 정보가 같이 들어 있습니다. 소셜 제공자가 늘어날 때마다 컬럼을 추가하는 구조라서 이번에 인증 수단을 별도 테이블로 뺍니다.

## 현재 문제

- `social_provider`, `social_id` 컬럼이 하나뿐이라 계정 하나에 소셜 하나만 연결 가능
- 비밀번호 없는 소셜 전용 회원 때문에 `password_hash` 가 nullable
- 인증 수단 변경 이력이 남지 않음

## 변경안

| 컬럼 | 타입 | 제약 | 설명 |
| --- | --- | --- | --- |
| id | BIGINT | PK | |
| member_id | BIGINT | FK -> member.id | |
| auth_type | VARCHAR(20) | NOT NULL | PASSWORD / KAKAO / NAVER / APPLE |
| credential | VARCHAR(255) | NULL | 비밀번호 해시 또는 제공자 사용자 ID |
| linked_at | DATETIME | NOT NULL | |
| unlinked_at | DATETIME | NULL | 연결 해제 시각 (soft delete) |

인덱스와 제약:

```sql
ALTER TABLE member_auth
  ADD CONSTRAINT uk_member_auth_type
  UNIQUE (member_id, auth_type, unlinked_at);

CREATE INDEX idx_member_auth_lookup
  ON member_auth (auth_type, credential);
```

## 설계 고민

이서연님이 unique 제약에 `unlinked_at` 을 넣는 방식에 이견을 냈습니다. MySQL 은 NULL 을 중복으로 보지 않기 때문에 해제된 행이 여러 개 쌓이면 "카카오를 연결했다 끊었다 다시 연결" 시나리오에서 제약이 원하는 대로 걸리지 않습니다.

대안으로 나온 것은 두 가지입니다.

1. 해제 시 행을 삭제하고 별도 `member_auth_history` 에 이력을 남긴다
2. `unlinked_at` 기본값을 `'9999-12-31'` 로 두고 NULL 을 쓰지 않는다

김도윤님은 이력 조회 빈도가 낮으니 1안이 단순하다는 의견이었고, 저는 삭제 후 이력 테이블을 다시 만드는 게 결국 같은 복잡도라고 봤습니다. 오늘은 결론을 내지 못했고, 다음 백엔드 주간 회의에서 정합니다.

## soft delete 여부

`member` 자체는 탈퇴 후 30일 보관 정책이 있어 soft delete 를 유지합니다. `member_auth` 는 회원이 탈퇴하면 즉시 물리 삭제해도 되는지 정보보안팀 확인이 필요합니다.
""",
    },
    {
        "id": "DOC-006",
        "title": "토큰 재발급 API v1 명세",
        "department": "플랫폼개발팀",
        "type": "API 명세서",
        "project": "Project Echo",
        "date": "2024-08-12",
        "systems": ["BlueGate"],
        "features": ["인증"],
        "participants": [],
        "concepts": ["Refresh Token", "재시도"],
        "deprecated": True,
        "body": """## Endpoint

```http
POST /auth/token/refresh
```

Access Token 이 만료되었을 때 Refresh Token 으로 새 Access Token 을 받습니다. v1 에서는 Refresh Token 자체는 그대로 재사용합니다.

## Request

```json
{
  "refreshToken": "rt_8f2c1d...",
  "deviceId": "device-7f21"
}
```

## Response

```json
{
  "accessToken": "eyJhbGciOi...",
  "expiresIn": 1800
}
```

Refresh Token 은 응답에 포함되지 않습니다. 클라이언트는 기존 값을 계속 보관합니다.

## Error Code

| 코드 | HTTP | 설명 |
| --- | --- | --- |
| AUTH_2001 | 401 | Refresh Token 없음 또는 형식 오류 |
| AUTH_2002 | 401 | 만료된 Refresh Token |
| AUTH_2003 | 401 | 폐기된 Refresh Token (로그아웃, 강제 로그아웃) |
| AUTH_2004 | 409 | deviceId 불일치 |

## 주의사항

클라이언트가 `AUTH_2002` 또는 `AUTH_2003` 을 받으면 저장된 토큰을 지우고 로그인 화면으로 보내야 합니다. 이 응답을 받고도 같은 토큰으로 재시도하는 구현이 있는데, 잠금 카운터가 올라가서 정상 재로그인까지 막힐 수 있습니다.

앱에서 401 을 받으면 무조건 재발급을 시도하는 인터셉터가 있다면, 재발급 요청 자체가 401 을 반환했을 때는 재귀 호출이 되지 않도록 예외 처리해 주세요.
""",
    },
    {
        "id": "DOC-007",
        "title": "새벽 시간대 로그아웃 급증 현상 조사",
        "department": "플랫폼개발팀",
        "type": "이슈 정리",
        "project": "Project Echo",
        "date": "2024-08-20",
        "systems": ["BlueGate"],
        "features": ["인증", "로그인"],
        "participants": ["오세훈", "김도윤"],
        "concepts": ["JWT", "Refresh Token", "시계 오차"],
        "body": """## 현상

8월 18일 새벽 2시부터 4시 사이에 `AUTH_2002`(만료된 Refresh Token) 응답이 평소의 20배로 늘었습니다. 같은 시간대에 앱 재로그인 화면 진입 이벤트도 함께 튀었습니다.

## 재현 조건

특정 사용자를 재현하지는 못했습니다. 로그를 보면 발급된 지 3일밖에 안 된 토큰인데 만료로 판정된 건이 다수입니다.

## 로그 분석

```text
2024-08-18 02:14:07 refresh_denied token_issued=2024-08-15T09:02:11 exp=2024-08-29T09:02:11 now=2024-08-31T02:14:07
```

`now` 값이 실제 시각보다 이틀 앞서 찍힌 인스턴스가 있었습니다. 오세훈님이 확인한 결과 8월 17일 저녁에 교체된 노드 두 대에서 NTP 동기화가 실패한 상태로 기동되었습니다.

## 원인 후보

1. NTP 동기화 실패로 인한 인스턴스 시계 오차
2. 토큰 만료 계산에 UTC 와 KST 가 섞여 들어감
3. Redis 에 저장된 폐기 목록 TTL 오설정

2번은 코드를 확인했고 모두 UTC 기준이라 배제했습니다. 3번은 폐기 목록에 없는 토큰까지 거부되었으므로 해당 없습니다.

## 실제 원인

1번이 맞습니다. 시계가 앞선 노드에 라우팅된 요청만 만료 판정을 받았고, 다른 노드로 재시도하면 정상 통과했습니다. 앱이 첫 실패에 곧바로 토큰을 지워버려서 사용자 입장에서는 강제 로그아웃으로 보였습니다.

## 임시 대응

- 해당 노드 2대 재기동 후 NTP 재동기화 (8월 18일 09:40 완료)

## 근본 해결

- 기동 시 시계 오차가 임계값을 넘으면 헬스체크를 실패시키는 방향으로 오세훈님이 작업 예정
- 김도윤님 제안으로 만료 판정에 30초 유예를 두기로 함

## 재발 방지

시계 오차 지표를 대시보드에 추가합니다. 앱 쪽에서 재발급 실패 시 한 번은 재시도하도록 바꾸는 건 모바일팀과 협의가 필요합니다. 아직 전달하지 않았습니다.
""",
    },
    {
        "id": "DOC-008",
        "title": "Refresh Token 회전 도입 여부 논의",
        "department": "플랫폼개발팀",
        "type": "개발자 회의록",
        "project": "Project Echo",
        "date": "2024-08-27",
        "systems": ["BlueGate"],
        "features": ["인증"],
        "participants": ["조아름", "김도윤", "한지민"],
        "concepts": ["Refresh Token", "탈취 감지", "동시성"],
        "body": """## 논의 배경

v1 재발급 방식은 Refresh Token 을 계속 재사용합니다. 조아름님이 보안 점검에서 토큰이 유출되면 만료까지 14일간 무제한으로 악용 가능하다는 점을 지적했고, 회전 방식 도입을 검토했습니다.

## 논의 내용

회전 방식은 재발급할 때마다 Refresh Token 을 새로 주고 이전 것을 폐기합니다. 이전 토큰이 다시 사용되면 탈취로 보고 해당 계정의 모든 토큰을 폐기합니다.

한지민님이 앱 관점에서 문제를 제기했습니다. 앱은 화면 여러 개가 동시에 API 를 호출하고, 401 을 받은 요청들이 각각 재발급을 시도합니다. 회전을 적용하면 두 번째 요청이 이미 폐기된 토큰을 쓰게 되어 정상 사용자가 전부 로그아웃됩니다.

## 쟁점

| 방안 | 장점 | 단점 |
| --- | --- | --- |
| 회전 없음 (현행) | 단순, 동시 요청 안전 | 탈취 감지 불가 |
| 즉시 폐기 회전 | 탈취 감지 확실 | 동시 요청 시 오탐 |
| 유예 기간 회전 | 오탐 감소 | 유예 시간 동안은 탈취 토큰도 유효 |

## 결정 사항

유예 기간을 둔 회전으로 갑니다. 이전 토큰을 60초 동안 "회전됨" 상태로 유지하고, 이 기간에 들어온 요청은 새 토큰을 다시 내려줍니다. 60초가 지난 뒤 사용되면 탈취로 판정합니다.

## Action Item

- 김도윤님: 회전 상태를 어디에 저장할지 검토. Redis 유력하지만 장애 시 전원 로그아웃 위험이 있어 대안 필요
- 한지민님: 앱 재발급 요청을 하나로 합치는 큐잉 구조 검토
- 조아름님: 탈취 판정 시 알림 정책 초안

## 추후 논의

유예 60초가 적절한지는 근거가 없습니다. 배포 후 회전 재사용 요청의 시간 분포를 보고 조정합니다.
""",
    },
    {
        "id": "DOC-009",
        "title": "Refresh Token 회전 설계",
        "department": "플랫폼개발팀",
        "type": "비즈니스 기능 설계",
        "project": "Project Echo",
        "date": "2024-09-03",
        "systems": ["BlueGate"],
        "features": ["인증"],
        "participants": ["김도윤"],
        "concepts": ["Refresh Token", "동시성", "탈취 감지", "보상 처리"],
        "body": """## 배경

8월 27일 회의 결정에 따라 유예 기간이 있는 Refresh Token 회전을 설계합니다. 핵심 목표는 탈취 감지이고, 정상 사용자의 동시 요청에서 오탐이 나지 않는 것이 제약 조건입니다.

## 토큰 상태

```text
ACTIVE    - 정상 사용 가능
ROTATED   - 회전됨. 유예 60초 동안 재사용 시 새 토큰 재발급
REVOKED   - 폐기됨. 사용 시 계정 전체 토큰 폐기
EXPIRED   - 기간 만료
```

## 정상 Flow

1. 클라이언트가 Refresh Token `A` 로 재발급 요청
2. 서버가 `A` 를 ROTATED 로 바꾸고 `rotated_at` 기록, 새 토큰 `B` 발급
3. `B` 는 ACTIVE, 부모 토큰으로 `A` 를 기록
4. 유예 시간 내에 `A` 로 다시 들어오면 `A` 의 자식인 `B` 를 그대로 재사용해 응답

## Sequence

```text
App(요청1) -> BlueGate : refresh(A)
BlueGate  -> Store     : A = ROTATED, B 생성
BlueGate  -> App(요청1): B
App(요청2) -> BlueGate : refresh(A)   # 동시 요청
BlueGate  -> Store     : A 상태 확인 = ROTATED, 유예 내
BlueGate  -> App(요청2): B            # 새로 만들지 않고 자식 반환
```

## Edge Case

- **유예 초과 후 재사용**: 탈취로 판정하고 해당 `member_id` 의 모든 Refresh Token 을 REVOKED 로 바꿉니다. 이 경우 정상 사용자도 로그아웃되므로 알림을 보냅니다.
- **회전 직후 서버 장애**: 새 토큰은 저장됐는데 응답이 유실되면 클라이언트는 여전히 `A` 를 갖고 있습니다. 유예 기간이 이 상황을 흡수합니다.
- **저장소 장애**: 상태 저장소가 죽으면 회전 판정을 할 수 없습니다. 이때는 회전을 건너뛰고 기존 토큰을 그대로 유효 처리합니다. 보안보다 가용성을 택한 선택이고, 장애 시간 동안 탈취 감지가 동작하지 않는다는 점을 명시해 둡니다.
- **로그아웃과 재발급 동시 발생**: 로그아웃이 먼저 커밋되면 재발급은 REVOKED 를 보고 실패합니다. 이 경우는 탈취 판정을 하지 않습니다. 로그아웃으로 폐기된 것과 탈취로 폐기된 것을 사유 컬럼으로 구분합니다.

## 고려한 방법

토큰 상태를 DB 에 두는 안도 검토했습니다. 재발급 트래픽이 초당 수백 건이라 DB 부하가 부담스러워 Redis 를 택했습니다. 다만 위에서 적은 대로 Redis 장애 시 동작을 명확히 정의해야 해서, 결과적으로 예외 처리가 더 늘었습니다.

## 최종 선택

Redis 해시에 토큰 상태를 저장하고 TTL 은 Refresh 만료와 동일하게 둡니다. 탈취 판정 이벤트는 별도 토픽으로 발행해 정보보안팀이 구독합니다.
""",
    },
    {
        "id": "DOC-010",
        "title": "[리뷰] TokenService 회전 로직 1차",
        "department": "플랫폼개발팀",
        "type": "코드리뷰",
        "project": "Project Echo",
        "date": "2024-09-10",
        "systems": ["BlueGate"],
        "features": ["인증"],
        "participants": ["김도윤", "이서연"],
        "concepts": ["동시성", "Transaction", "예외 처리"],
        "body": """## 현재 구현

`TokenService.rotate()` 에서 기존 토큰 조회 후 상태를 ROTATED 로 바꾸고 새 토큰을 저장합니다.

```java
var token = tokenStore.find(refreshToken);
if (token.status() == ACTIVE) {
    tokenStore.markRotated(token.id());
    var issued = issue(token.memberId(), token.deviceId());
    tokenStore.linkChild(token.id(), issued.id());
    return issued;
}
```

## 리뷰 의견

이서연님: 조회와 상태 변경 사이가 원자적이지 않습니다. 동시 요청 두 건이 모두 ACTIVE 를 읽으면 새 토큰이 두 개 생기고, 둘 중 하나는 부모 링크가 덮어써집니다. 설계 문서에서 막으려던 상황이 그대로 재현될 수 있습니다.

Redis 라면 `SET key value NX` 또는 Lua 스크립트로 상태 전이와 자식 생성을 한 번에 처리하는 편이 안전합니다.

김도윤님: 동의합니다. 다만 Lua 안에서 토큰 생성까지 하기는 어려워서, 자식 토큰 ID 를 먼저 만들고 스크립트에는 전이와 링크만 넘기는 형태로 가겠습니다.

이서연님: `token.status()` 가 ROTATED 인 분기가 없습니다. 유예 기간 내 재사용이 여기로 오는데 현재 코드는 그냥 null 을 반환하고 상위에서 401 이 나갑니다.

## 수정 사항

- 상태 전이와 자식 링크를 Lua 스크립트 한 번으로 처리
- ROTATED 분기 추가. 유예 내면 자식 토큰 반환, 초과면 `TokenTheftDetectedException`
- 저장소 예외를 그대로 던지지 않고 `TokenStoreUnavailableException` 으로 감싸서 상위에서 우회 정책 적용

## 남은 확인

유예 시간을 설정값으로 뺐는데 기본값이 코드에 60 으로 하드코딩되어 있습니다. 설정 누락 시 기동 실패로 바꿀지, 기본값을 유지할지 결정이 필요합니다. 일단 머지하고 후속 PR 에서 정리하기로 했습니다.
""",
    },
    {
        "id": "DOC-011",
        "title": "Echo Sprint 3 진행 현황",
        "department": "프로젝트관리팀",
        "type": "프로젝트 진행상황",
        "project": "Project Echo",
        "date": "2024-09-12",
        "systems": ["BlueGate", "NEXUS"],
        "features": ["인증", "로그인"],
        "participants": ["임채원", "한지민"],
        "concepts": ["Blocker", "일정 지연"],
        "body": """## 스프린트 개요

기간은 9월 2일부터 9월 13일까지입니다. 목표는 회전 방식 재발급을 스테이징에 올리는 것이었습니다.

## 진행 현황

| 항목 | 담당 | 상태 |
| --- | --- | --- |
| 회전 설계 문서화 | 백엔드 | 완료 |
| TokenService 회전 구현 | 백엔드 | 완료 |
| Redis 상태 저장소 구성 | DevOps | 완료 |
| 앱 재발급 큐잉 | 모바일 | 진행 중 |
| 스테이징 배포 | DevOps | 대기 |
| 탈취 알림 연동 | 정보보안 | 대기 |

## Blocker

앱 재발급 큐잉이 예상보다 오래 걸리고 있습니다. 한지민님 확인 결과 기존 네트워크 계층이 요청별로 토큰을 읽어가는 구조라 큐잉을 넣으려면 인터셉터를 전면 수정해야 합니다. 스프린트 내 완료가 어려워 보입니다.

앱 큐잉 없이 서버만 배포하면 유예 60초로 대부분 흡수되긴 하지만, 앱 백그라운드 복귀 시점에 요청이 몰리는 케이스는 여전히 위험합니다.

## Risk

- 앱 수정이 다음 스프린트로 밀리면 서버 배포도 함께 미뤄야 할 수 있습니다
- 탈취 알림 연동은 정보보안팀 일정이 확정되지 않았습니다

## 다음 스프린트

앱 인터셉터 수정을 최우선으로 두고, 서버는 유예 시간을 설정으로 조정 가능하게 만들어 두기로 했습니다. 임채원님이 릴리스 일정을 2주 뒤로 조정하는 안을 준비합니다.
""",
    },
    {
        "id": "DOC-012",
        "title": "2024 코드리뷰 규칙",
        "department": "백엔드개발팀",
        "type": "팀 Ground Rule",
        "project": None,
        "date": "2024-09-18",
        "systems": [],
        "features": [],
        "participants": [],
        "concepts": ["코드리뷰", "PR", "팀 규칙"],
        "deprecated": True,
        "body": """> 2026년 개정판이 나온 뒤로는 이 문서를 기준으로 삼지 않습니다. 최신 규칙 문서를 확인하세요.

## 리뷰어 지정

- PR 당 리뷰어는 2명을 지정합니다
- 리뷰어 중 최소 1명은 해당 도메인 담당자여야 합니다
- Approve 2개가 모여야 머지할 수 있습니다

## 리뷰 응답 시간

- 평일 기준 24시간 안에 첫 코멘트를 남깁니다
- 24시간이 지나면 작성자가 다른 리뷰어를 추가 지정할 수 있습니다

## PR 크기

변경 라인 수 500 이하를 권장합니다. 넘어가면 PR 설명에 왜 나눌 수 없었는지 적어 주세요.

## 코멘트 작성

- 반드시 고쳐야 하는 것과 취향 차이를 구분해서 적습니다
- 취향 차이는 앞에 `nit:` 을 붙입니다
- "이건 왜 이렇게 했나요" 대신 "이렇게 하면 어떤 문제가 있어서 이렇게 바꾸는 게 좋겠습니다" 형태로 근거를 함께 씁니다

## 예외

- 장애 대응 핫픽스는 Approve 1개로 머지할 수 있습니다. 대신 다음 영업일에 사후 리뷰를 등록합니다
- 문서, 주석, 오타 수정은 Approve 1개면 됩니다
""",
    },
    {
        "id": "DOC-013",
        "title": "로컬 개발 환경 설정 가이드",
        "department": "백엔드개발팀",
        "type": "매뉴얼 / 정의서",
        "project": None,
        "date": "2024-09-24",
        "systems": ["NEXUS", "ORBIT", "BlueGate"],
        "features": [],
        "participants": ["오세훈"],
        "concepts": ["개발 환경", "Docker"],
        "body": """## 준비물

- JDK 17
- Docker Desktop
- 사내 VPN 접속 권한

VPN 은 사내 시스템 접근용입니다. 신청은 인프라 요청 채널로 하시면 오세훈님이 처리해 줍니다.

## 실행 순서

1. 저장소를 클론하고 `local` 프로파일로 실행합니다
2. `docker compose up -d` 로 MySQL, Redis 를 띄웁니다
3. 초기 스키마는 애플리케이션 기동 시 자동 적용됩니다

```bash
docker compose up -d
./gradlew bootRun --args='--spring.profiles.active=local'
```

## 서비스별 포트

| 서비스 | 포트 |
| --- | --- |
| NEXUS | 8081 |
| ORBIT | 8082 |
| BlueGate | 8080 |
| MySQL | 3306 |
| Redis | 6379 |

## 테스트 계정

로컬 기동 시 시드 데이터로 테스트 계정이 자동 생성됩니다. 계정 정보는 사내 비밀 저장소에서 확인하세요. 문서나 채팅에 붙여넣지 마세요.

## 자주 겪는 문제

- **기동 직후 401 이 계속 나올 때**: BlueGate 가 아직 안 떠 있는 경우가 대부분입니다. 헬스체크가 200 인지 먼저 확인하세요.
- **포트 충돌**: 이전 컨테이너가 남아 있을 수 있습니다. `docker compose down` 후 다시 올리세요.
- **스키마 불일치**: 브랜치를 바꾼 뒤에는 볼륨을 지우고 다시 올리는 편이 빠릅니다.
""",
    },
    {
        "id": "DOC-014",
        "title": "OAuth 콜백 state 검증 누락",
        "department": "정보보안팀",
        "type": "이슈 정리",
        "project": "Project Echo",
        "date": "2024-10-02",
        "systems": ["BlueGate"],
        "features": ["소셜 로그인", "인증"],
        "participants": ["조아름", "김도윤"],
        "concepts": ["CSRF", "OAuth", "보안 점검"],
        "body": """## 현상

정기 보안 점검 중 소셜 로그인 콜백 처리에서 `state` 파라미터를 검증하지 않는 것을 확인했습니다. 인가 요청 시 `state` 를 생성해 보내기는 하지만, 콜백에서 돌려받은 값을 비교하지 않고 코드만 사용합니다.

## 영향 범위

- 카카오, 네이버, 애플 콜백 전부 동일
- 공격자가 자신의 인가 코드로 피해자 세션에 계정을 연결시키는 시나리오가 가능합니다
- 실제 악용 흔적은 로그에서 확인되지 않았습니다

## 재현

로컬에서 인가 요청 후 콜백 URL 의 `state` 를 임의 문자열로 바꿔도 로그인이 정상 처리되는 것을 확인했습니다.

## 분석

`OAuthCallbackHandler` 가 `code` 만 읽고 있습니다. `state` 생성 시 세션이나 저장소에 보관하는 코드 자체가 없어서 비교할 대상도 없는 상태입니다.

## 조치

- `state` 를 생성할 때 5분 TTL 로 저장소에 보관하고 콜백에서 일치 여부를 검증
- 검증 실패 시 `AUTH_3001` 로 거부하고 보안 로그에 기록
- 소셜 계정 연결은 로그인 상태에서만 허용하도록 별도 확인 추가

김도윤님이 이번 주 안에 작업하기로 했습니다.

## 재발 방지

조아름님이 인증 관련 신규 엔드포인트에 대한 보안 체크리스트를 만들기로 했습니다. PR 템플릿에 체크 항목을 넣을지는 백엔드팀과 협의 후 결정합니다.

## 상태

조치 진행 중입니다. 검증 로직 배포 전까지는 소셜 계정 연결 기능 노출을 보류합니다.
""",
    },
    {
        "id": "DOC-015",
        "title": "Echo Release 준비 현황",
        "department": "프로젝트관리팀",
        "type": "프로젝트 진행상황",
        "project": "Project Echo",
        "date": "2024-10-15",
        "systems": ["BlueGate"],
        "features": ["인증", "로그인", "소셜 로그인"],
        "participants": ["임채원", "윤수빈"],
        "concepts": ["릴리스", "QA", "롤백"],
        "body": """## 릴리스 범위

- 인증 기능의 BlueGate 이관
- Refresh Token 회전 적용
- OAuth `state` 검증 추가

소셜 계정 통합 기능은 정책 미확정으로 이번 릴리스에서 제외했습니다.

## 완료

- 서버 구현 및 스테이징 배포
- 앱 재발급 큐잉 반영
- 보안 점검 지적 사항 조치

## 진행 중

윤수빈님이 QA 시나리오 42건 중 35건을 수행했고, 남은 항목은 동시 로그인과 강제 로그아웃 관련입니다. 현재까지 발견된 결함은 3건이며 모두 앱 화면 처리 이슈입니다.

## 대기

- 운영 배포 승인
- 모니터링 대시보드 항목 추가

## Risk

기존 토큰과 신규 토큰이 2주간 공존합니다. 이 기간에는 두 가지 검증 경로가 동시에 살아 있어서, 문제가 생기면 어느 경로인지 구분할 수 있도록 로그에 토큰 버전을 남기기로 했습니다.

롤백은 신규 발급을 중단하고 기존 방식으로 되돌리는 형태입니다. 이미 발급된 회전 토큰은 만료까지 유효하므로 롤백 후에도 재발급이 가능해야 합니다. 이 경로를 QA 에서 한 번 더 확인해 달라고 요청했습니다.

## 예정 일정

- 10월 21일 운영 배포
- 10월 28일 배포 후 회고
""",
    },
]
