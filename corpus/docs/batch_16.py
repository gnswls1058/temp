"""DOC-226 ~ DOC-240 : 결제 수단 확대와 팀 규칙 (2025~2026).

일부 문서는 주제가 겹치되 관점이 다릅니다(결제 실패 정책 / PG 장애 가이드 / 재시도 설계).
"""

DOCUMENTS = [
    {
        "id": "DOC-226",
        "title": "결제 실패 처리 정책",
        "department": "백엔드개발팀",
        "type": "매뉴얼 / 정의서",
        "project": None,
        "date": "2026-04-06",
        "systems": ["ORBIT"],
        "features": ["결제"],
        "participants": [],
        "concepts": ["에러 처리", "재시도", "정책"],
        "body": """## 목적

결제가 실패했을 때 시스템과 사용자가 각각 무엇을 해야 하는지 정리합니다. PG 장애 대응은 별도 문서를 참고하세요.

## 실패 분류

| 그룹 | 대표 코드 | 원인 | 재시도 |
| --- | --- | --- | --- |
| LIMIT | 한도 초과 | 카드 한도 | 같은 카드로는 무의미 |
| BALANCE | 잔액 부족 | 계좌/한도 | 무의미 |
| CARD_INVALID | 유효기간 만료, 정지 카드 | 카드 문제 | 무의미 |
| DECLINED | 카드사 거절 | 카드사 판단 | 무의미 |
| SYSTEM | timeout, 5xx | 일시적 | 유효 |
| UNKNOWN | 매핑되지 않은 코드 | - | 유효로 간주 |

## 사용자 안내

그룹별로 다른 문구와 액션을 노출합니다.

| 그룹 | 문구 | 버튼 |
| --- | --- | --- |
| LIMIT | 카드 한도를 초과했습니다 | 다른 결제수단 |
| BALANCE | 잔액이 부족합니다 | 다른 결제수단 |
| CARD_INVALID | 카드 정보를 확인해 주세요 | 카드 관리 |
| DECLINED | 카드사에서 거절했습니다 | 다른 결제수단 |
| SYSTEM | 잠시 후 다시 시도해 주세요 | 재시도 |
| UNKNOWN | 잠시 후 다시 시도해 주세요 | 재시도 |

## 시스템 처리

- 실패해도 `payment` 행은 남깁니다. `FAILED` 상태로 기록합니다
- 포인트와 쿠폰을 이미 사용했으면 되돌립니다
- 재고 예약은 유지합니다. 결제 시한(30분) 안에는 다시 시도할 수 있어야 합니다
- 재고 예약이 만료되면 주문이 자동 취소됩니다

## 재시도

같은 조건으로 재시도할 때는 같은 중복 방지 키를 씁니다. 결제 수단이나 금액이 바뀌면 새 키를 만듭니다.

`SYSTEM` 그룹은 재시도가 유효하지만 자동 재시도는 하지 않습니다. 사용자가 버튼을 눌러야 합니다. 자동 재시도는 중복 승인 위험이 있습니다.

## 매핑되지 않은 코드

PG 가 새 코드를 내려주면 `UNKNOWN` 으로 처리되고 로그에 남습니다. 주 1회 로그를 확인해 매핑을 보완합니다.

매핑 표는 설정으로 관리하므로 배포 없이 추가할 수 있습니다.

## 확인

결제 실패가 갑자기 늘면 특정 그룹에 몰려 있는지 봅니다. `SYSTEM` 이 늘면 PG 문제이고, `DECLINED` 가 늘면 특정 카드사 문제일 수 있습니다.
""",
    },
    {
        "id": "DOC-227",
        "title": "카드사 거절 사유가 안내되지 않는 문제",
        "department": "백엔드개발팀",
        "type": "이슈 정리",
        "project": None,
        "date": "2026-04-13",
        "systems": ["ORBIT"],
        "features": ["결제"],
        "participants": [],
        "concepts": ["에러 처리", "매핑", "외부 연동"],
        "body": """## 현상

결제가 실패했는데 화면에 "잠시 후 다시 시도해 주세요"만 나옵니다. 실제로는 카드 한도 초과인데 재시도 안내가 나가서 사용자가 같은 카드로 반복 시도합니다.

## 재현

특정 카드사 카드로 한도를 넘긴 결제를 시도하면 재현됩니다.

## 분석

PG 응답을 확인했습니다.

```json
{
  "resultCode": "F0413",
  "resultMessage": "승인거절(한도초과)",
  "issuerCode": "BC"
}
```

매핑 표에 `F0413` 이 없습니다. 매핑되지 않은 코드는 `UNKNOWN` 그룹으로 처리되어 재시도 안내가 나갑니다.

매핑 표를 확인해 보니 60개 코드가 등록되어 있는데, 이번 달 로그에서 등장한 코드는 87개입니다. 27개가 미등록입니다.

미등록 코드는 카드사별로 다른 코드 체계를 쓰기 때문입니다. 같은 "한도 초과"도 카드사마다 코드가 다릅니다.

## 영향 범위

전체 결제 실패 중 `UNKNOWN` 비중이 19% 입니다. 이들 사용자는 잘못된 안내를 받고 있습니다.

## 조치

**즉시**: 로그에서 발견된 미등록 코드 27개를 매핑 표에 추가했습니다. 설정 변경이라 배포 없이 반영됐습니다.

추가 후 `UNKNOWN` 비중이 19% 에서 3% 로 떨어졌습니다.

**구조 개선**: 코드가 계속 늘어나므로 수동 관리는 한계가 있습니다. PG 가 제공하는 표준 분류 필드가 있는지 확인했더니 `declineCategory` 라는 값이 있었습니다. 우리가 쓰지 않고 있었습니다.

이 값을 1차 기준으로 쓰고, 세부 코드는 보조로 사용하도록 바꿉니다.

## 남은 작업

`declineCategory` 기반 매핑으로 전환하는 작업이 남았습니다. PG 문서의 분류와 우리 그룹을 대응시켜야 합니다.

주 1회 `UNKNOWN` 로그를 확인하는 절차는 유지합니다. 전환 후에도 새 값이 나올 수 있습니다.

## 배운 점

외부 응답에서 우리가 안 쓰는 필드가 있는지 확인하지 않았습니다. 연동 시 응답 스펙 전체를 검토하는 절차가 필요합니다.
""",
    },
    {
        "id": "DOC-228",
        "title": "PG 장애 시 대응 가이드",
        "department": "DevOps팀",
        "type": "매뉴얼 / 정의서",
        "project": None,
        "date": "2026-04-20",
        "systems": ["ORBIT"],
        "features": ["결제"],
        "participants": [],
        "concepts": ["장애 대응", "외부 시스템 장애", "에스컬레이션"],
        "body": """## 판단

PG 장애와 우리 장애를 먼저 구분합니다.

| 확인 | PG 장애 | 우리 장애 |
| --- | --- | --- |
| PG 응답 시간 | 상승 | 정상 |
| 우리 애플리케이션 오류율 | 정상 | 상승 |
| PG 상태 페이지 | 공지 있음 | 없음 |
| `PENDING` 건수 | 급증 | 정상 |

결제 실패 그룹 분포도 단서가 됩니다. `SYSTEM` 이 급증하면 PG 쪽일 가능성이 높습니다.

## PG 장애 확인 시

**1. 상황 공유**

공유 채널에 즉시 알립니다. 원인을 몰라도 "결제 실패가 늘고 있고 PG 응답이 느립니다" 수준으로 먼저 공유하세요.

**2. PG 담당자 연락**

연락처는 사내 연락망에 있습니다. 장애 인지 여부와 예상 복구 시간을 확인합니다.

**3. 사용자 안내**

기획팀에 안내 문구 노출을 요청합니다. 결제 화면 상단에 배너를 띄웁니다.

**4. 결제 차단 여부 판단**

장애가 길어지면 결제 시도 자체를 막을지 결정합니다. **리드가 판단합니다.**

| 상황 | 권장 |
| --- | --- |
| 산발적 실패 (성공률 80% 이상) | 유지 |
| 대부분 실패 (성공률 30% 미만) | 차단 검토 |
| `PENDING` 급증 | 차단 검토 |

`PENDING` 이 급증하면 실제 승인 여부를 알 수 없는 결제가 쌓입니다. 이 상태가 계속되면 사후 정리 부담이 커집니다.

## 복구 후

**1. PENDING 정리 확인**

보정 배치가 `PENDING` 건을 모두 확정했는지 확인합니다. 30분 초과 알림이 남아 있으면 수동 확인이 필요합니다.

**2. 영향 주문 정리**

결제는 됐는데 주문이 없는 건이 있는지 확인합니다.

```sql
SELECT p.id, p.order_id, p.status, p.pg_transaction_id
FROM payment p
WHERE p.status = 'PENDING'
  AND p.created_at BETWEEN :start AND :end;
```

**3. 고객 안내**

영향받은 주문 목록을 고객지원팀에 전달합니다.

## 절대 하지 말 것

- `PENDING` 건을 일괄 `FAILED` 처리하지 마세요
- PG 관리자 화면에서 임의로 취소하지 마세요
- 자동 재시도를 켜지 마세요. 중복 승인이 발생합니다

## 사후

P1 로 처리하고 일주일 안에 보고서를 작성합니다. PG 측 원인과 복구 조치를 함께 기록합니다.
""",
    },
    {
        "id": "DOC-229",
        "title": "결제 재시도 정책 설계",
        "department": "백엔드개발팀",
        "type": "비즈니스 기능 설계",
        "project": None,
        "date": "2026-04-27",
        "systems": ["ORBIT"],
        "features": ["결제"],
        "participants": [],
        "concepts": ["재시도", "멱등성", "Edge Case"],
        "body": """## 배경

재시도가 어디서 어떻게 일어나는지 흩어져 있습니다. 클라이언트 재시도, 서버 내부 재시도, 보정 배치 재조회가 각각 다른 규칙을 씁니다. 한 곳에 정리합니다.

## 재시도 주체

| 주체 | 대상 | 규칙 |
| --- | --- | --- |
| 클라이언트 | 결제 요청 | 사용자 조작. 자동 재시도 없음 |
| 서버 | PG 조회 | 자동. 최대 2회 |
| 서버 | PG 승인 | **재시도하지 않음** |
| 보정 배치 | PG 조회 | 단계별 주기 |
| 재시도 큐 | 포인트 취소, 재고 복원 | 지수 백오프 최대 5회 |

## PG 승인을 재시도하지 않는 이유

승인 요청이 timeout 되면 PG 가 실제로 승인했는지 알 수 없습니다. 재시도하면 두 번 승인될 수 있습니다.

대신 `PENDING` 으로 두고 보정 배치가 조회로 확정합니다. 조회는 멱등하므로 안전합니다.

## 클라이언트 재시도

자동 재시도를 하지 않습니다. 사용자가 버튼을 눌러야 합니다.

같은 조건이면 같은 멱등키를 씁니다. 서버가 이미 처리 중이면 `PAY_1003` 을 반환하고 클라이언트는 상태 조회로 넘어갑니다.

## 후속 처리 재시도

결제 실패 후 포인트를 되돌리거나 재고를 복원하는 작업입니다. 이들은 멱등하므로 재시도해도 안전합니다.

```text
1차: 즉시
2차: 5초 후
3차: 30초 후
4차: 5분 후
5차: 30분 후
이후: 실패 처리 및 알림
```

참조키가 있어 중복 실행이 되지 않습니다.

## Edge Case

- **재시도 중 원래 요청이 성공**: 참조키가 같으므로 두 번째는 멱등 응답을 받습니다. 문제없습니다.
- **재시도 큐 자체가 유실**: 인스턴스 종료 시 큐가 비워집니다. graceful shutdown 으로 대부분 처리되지만 유실 가능성이 남습니다. 일 1회 대사 배치가 미처리 건을 찾습니다.
- **5회 실패 후**: 알림을 보내고 수동 처리 대상으로 남깁니다. 자동으로 포기하지 않습니다.
- **PG 조회도 계속 실패**: 30분 후 온콜 알림. 자동 확정하지 않습니다.

## 하지 않기로 한 것

**자동 결제 재시도**: 사용자 개입 없이 실패한 결제를 다시 시도하는 기능입니다. 편리하지만 중복 승인 위험과 사용자가 인지하지 못하는 결제가 생깁니다. 도입하지 않습니다.

**Circuit Breaker**: 12월에 검토했으나 결제는 차단해도 사용자가 할 수 있는 게 없어 실익이 적다고 판단했습니다. 커넥션 풀 분리로 장애 전파만 막습니다.
""",
    },
    {
        "id": "DOC-230",
        "title": "간편결제 수단 추가 도입 검토",
        "department": "백엔드개발팀",
        "type": "개발자 회의록",
        "project": None,
        "date": "2026-05-04",
        "systems": ["ORBIT"],
        "features": ["결제"],
        "participants": ["김도윤", "강민재"],
        "concepts": ["결제 수단", "외부 연동", "범위 산정"],
        "body": """## 논의 배경

현재 결제 수단은 카드, 계좌이체, 간편결제 1종입니다. 간편결제 비중이 계속 늘고 있어 추가 도입을 검토합니다.

## 현황

강민재님이 가져온 수단별 비중입니다.

| 수단 | 비중 | 전년 대비 |
| --- | --- | --- |
| 카드 | 51% | -9%p |
| 간편결제 A | 38% | +11%p |
| 계좌이체 | 11% | -2%p |

간편결제 하나만 지원하는데도 38% 입니다. 추가하면 전환율이 오를 것으로 봅니다.

## 기술 검토

김도윤님이 정리한 작업입니다.

**공통 구조 필요**: 지금은 결제 수단별 분기가 `PaymentService` 안에 if 문으로 들어가 있습니다. 수단이 늘면 감당이 안 됩니다. 전략 패턴으로 분리해야 합니다.

**수단별 차이**:

| 항목 | 카드 | 간편결제 A | 신규 후보 |
| --- | --- | --- | --- |
| 부분 취소 | 지원 | 지원 | 미지원 |
| 정기 결제 | 빌링키 | 지원 | 미확인 |
| 승인 방식 | 동기 | 리다이렉트 후 콜백 | 리다이렉트 |
| 취소 기한 | 365일 | 180일 | 확인 필요 |

부분 취소 미지원이 문제입니다. Falcon 에서 부분 취소를 만들었는데 지원하지 않는 수단이 늘어납니다.

## 결정 사항

- 결제 수단별 전략을 인터페이스로 분리하는 리팩터링을 먼저 합니다
- 신규 수단은 그 위에 추가합니다
- 부분 취소 미지원 수단은 화면에서 사전에 안내합니다

## 부분 취소 미지원 처리

Falcon 회고에서 남은 과제였습니다. 이번에 같이 처리합니다.

주문에 부분 취소가 필요할 가능성(여러 상품)이 있고 결제 수단이 부분 취소를 지원하지 않으면, 주문서에서 안내를 표시합니다.

## Action Item

- 김도윤님: 결제 수단 전략 분리 설계
- 강민재님: 신규 수단 계약 조건 확인 (취소 기한, 정기 결제 지원 여부)
""",
    },
    {
        "id": "DOC-231",
        "title": "간편결제 연동 API 명세",
        "department": "백엔드개발팀",
        "type": "API 명세서",
        "project": None,
        "date": "2026-05-13",
        "systems": ["ORBIT"],
        "features": ["결제"],
        "participants": [],
        "concepts": ["결제 수단", "리다이렉트", "콜백"],
        "body": """## 흐름

간편결제는 카드와 달리 외부 페이지로 이동했다가 돌아옵니다.

```text
1. 결제 준비 요청  -> 결제 URL 수신
2. 사용자를 결제 URL 로 이동
3. 사용자가 외부에서 인증 및 승인
4. 우리 콜백 URL 로 리다이렉트
5. 승인 확정 요청
6. 결과 표시
```

## 1. 결제 준비

```http
POST /payments/prepare
Idempotency-Key: {key}
```

```json
{
  "orderId": 90112,
  "method": "EASY_PAY_B",
  "totalAmount": 34000,
  "pointAmount": 2500,
  "returnUrl": "https://app.example.com/payments/callback"
}
```

응답:

```json
{
  "paymentId": 771903,
  "status": "PENDING",
  "redirectUrl": "https://easypay.example.com/checkout/xyz",
  "expiresAt": "2026-05-13T14:52:00+09:00"
}
```

`expiresAt` 은 보통 10분입니다. 지나면 처음부터 다시 해야 합니다.

## 2. 콜백 수신

사용자가 승인하면 `returnUrl` 로 돌아옵니다.

```text
GET /payments/callback?paymentId=771903&result=SUCCESS&token=abc123
```

콜백 파라미터를 그대로 믿지 않습니다. 승인 확정 요청으로 실제 상태를 확인합니다.

## 3. 승인 확정

```http
POST /payments/{paymentId}/confirm
```

```json
{
  "token": "abc123"
}
```

응답은 카드 결제와 동일한 형식입니다.

## 주의사항

**콜백 유실**: 사용자가 승인 후 브라우저를 닫으면 콜백이 오지 않습니다. 결제는 이미 승인됐을 수 있습니다. 보정 배치가 `PENDING` 건을 조회해 확정합니다.

**콜백 중복**: 같은 콜백이 여러 번 올 수 있습니다. 확정 요청은 멱등하게 처리되므로 문제없습니다.

**앱 복귀**: 앱에서는 외부 브라우저나 앱으로 이동했다가 돌아옵니다. 딥링크로 복귀하며, 복귀하지 못한 경우 앱 재실행 시 진행 중 결제를 확인해 상태 화면을 보여줍니다.

## Error Code

| 코드 | HTTP | 설명 |
| --- | --- | --- |
| PAY_3001 | 400 | 지원하지 않는 결제 수단 |
| PAY_3002 | 409 | 준비 시한 만료 |
| PAY_3003 | 400 | 토큰 불일치 |
| PAY_3004 | 402 | 외부에서 승인 거절 |
""",
    },
    {
        "id": "DOC-232",
        "title": "payment_method 테이블 설계",
        "department": "백엔드개발팀",
        "type": "DB 테이블 설계안",
        "project": None,
        "date": "2026-05-18",
        "systems": ["ORBIT"],
        "features": ["결제"],
        "participants": [],
        "concepts": ["결제 수단", "설정", "확장성"],
        "body": """## 배경

결제 수단이 코드에 enum 으로 박혀 있습니다. 수단을 추가하거나 일시 중단하려면 배포가 필요합니다.

## payment_method

| 컬럼 | 타입 | 설명 |
| --- | --- | --- |
| code | VARCHAR(24) | PK. CARD / TRANSFER / EASY_PAY_A / EASY_PAY_B |
| name | VARCHAR(50) | 사용자 노출 이름 |
| provider | VARCHAR(24) | PG 또는 제공사 식별자 |
| flow_type | VARCHAR(16) | DIRECT / REDIRECT |
| enabled | BOOLEAN | 사용 여부 |
| supports_partial_cancel | BOOLEAN | 부분 취소 지원 |
| supports_recurring | BOOLEAN | 정기 결제 지원 |
| cancel_period_days | INT | 취소 가능 기간 |
| min_amount | INT | 최소 결제 금액 |
| max_amount | INT | 최대 결제 금액 |
| display_order | INT | 화면 노출 순서 |

## 왜 DB 로 옮기는가

- 장애 시 특정 수단만 배포 없이 끌 수 있습니다
- 부분 취소 지원 여부를 화면에서 미리 판단할 수 있습니다
- 취소 기한이 수단마다 달라 코드에 흩어져 있던 값을 모읍니다

## 코드에 남는 것

결제 수단의 **동작**은 코드에 있습니다. 각 수단마다 승인, 취소 구현이 다릅니다.

```text
DB:   어떤 수단이 활성인가, 어떤 제약이 있는가
코드: 그 수단을 어떻게 처리하는가
```

DB 에 새 코드를 넣어도 대응하는 구현이 없으면 동작하지 않습니다. 기동 시 DB 의 수단 코드와 구현된 전략을 대조해 불일치를 경고합니다.

권한 설계에서 쓴 방식과 같습니다.

## 사용 예

주문서에서 결제 수단 목록을 내려줄 때 금액 조건으로 거릅니다.

```sql
SELECT code, name, supports_partial_cancel
FROM payment_method
WHERE enabled = true
  AND :orderAmount BETWEEN min_amount AND max_amount
ORDER BY display_order;
```

## 캐시

수단 목록은 자주 바뀌지 않습니다. 5분 캐시합니다.

다만 장애 시 즉시 꺼야 하므로 관리 화면에서 변경하면 캐시를 즉시 무효화합니다.

## 마이그레이션

기존 enum 값을 그대로 코드로 씁니다. 값이 같으므로 기존 데이터(`payment.method`)와 호환됩니다.
""",
    },
    {
        "id": "DOC-233",
        "title": "[리뷰] 결제 수단별 전략 분리",
        "department": "백엔드개발팀",
        "type": "코드리뷰",
        "project": None,
        "date": "2026-05-22",
        "systems": ["ORBIT"],
        "features": ["결제"],
        "participants": ["김도윤", "노태윤"],
        "concepts": ["설계", "확장성", "인터페이스"],
        "body": """## 현재 구현

결제 수단별 처리를 인터페이스로 분리했습니다.

```java
public interface PaymentStrategy {
    String methodCode();
    PrepareResult prepare(PrepareCommand command);
    ApproveResult approve(ApproveCommand command);
    CancelResult cancel(CancelCommand command);
}
```

## 리뷰 의견

노태윤님: `prepare` 가 모든 수단에 있는데 카드는 준비 단계가 없습니다. 카드 구현에서 빈 메서드를 반환하게 되면 인터페이스가 잘못된 겁니다.

김도윤님: 흐름이 다르다는 걸 인터페이스에 반영하겠습니다. `DirectPaymentStrategy` 와 `RedirectPaymentStrategy` 로 나누고 공통 부분만 상위 인터페이스에 두겠습니다.

노태윤님: 취소는 어떤가요. 부분 취소를 지원하지 않는 수단은 `cancel(부분금액)` 이 호출되면 어떻게 되나요.

김도윤님: 지금은 예외를 던집니다. 다만 호출하는 쪽이 미리 알 수 있어야 합니다. `payment_method.supports_partial_cancel` 을 서비스 계층에서 확인하고 거부하도록 하겠습니다.

노태윤님: 그 판단이 두 곳에 생깁니다. 서비스에서 확인하고 전략에서도 확인하는 형태요. 중복이지만 방어로는 맞습니다. 다만 오류 코드가 달라지지 않게 통일해 주세요.

김도윤님: 둘 다 `PAY_2007` 로 맞추겠습니다.

노태윤님: 전략을 어떻게 찾나요. 맵으로 주입받는 형태인데, DB 에 있는 코드에 대응하는 전략이 없으면 런타임에 null 이 됩니다.

김도윤님: 기동 시 대조하고 없으면 경고를 남깁니다. 조회 시에는 명시적 예외를 던지도록 바꾸겠습니다.

## 수정 사항

- Direct / Redirect 인터페이스 분리
- 부분 취소 지원 여부를 서비스 계층에서 사전 확인
- 오류 코드 통일 (`PAY_2007`)
- 전략 미존재 시 명시적 예외

## 남은 확인

기존 카드/계좌이체 구현을 새 구조로 옮기는 작업이 남았습니다. 한 번에 옮기면 위험하므로 수단별로 나눠 배포합니다. 이행 기간에는 기존 분기와 새 전략이 공존합니다.
""",
    },
    {
        "id": "DOC-234",
        "title": "간편결제 승인 직후 취소되는 사례",
        "department": "백엔드개발팀",
        "type": "이슈 정리",
        "project": None,
        "date": "2026-06-03",
        "systems": ["ORBIT"],
        "features": ["결제"],
        "participants": [],
        "concepts": ["외부 연동", "콜백", "상태 불일치"],
        "body": """## 현상

신규 간편결제(EASY_PAY_B)로 결제한 주문 중 일부가 승인 직후 취소 상태가 됩니다. 사용자는 결제 완료 화면을 봤는데 주문 내역에는 취소로 표시됩니다.

6월 1~2일 사이 11건 발생했습니다.

## 분석

`payment_history` 를 보면 순서가 이렇습니다.

```text
14:22:31  APPROVE_REQUESTED
14:22:34  APPROVED
14:22:36  CANCEL_REQUESTED   reason=SYSTEM
14:22:36  CANCELED
```

승인 2초 뒤에 시스템이 취소를 요청했습니다.

취소를 요청한 것은 주문 생성 실패 보상 처리였습니다. 승인은 됐는데 주문 상태 갱신에서 실패해 결제를 되돌린 것입니다.

주문 갱신 실패 원인을 보니 재고 예약이 이미 만료되어 있었습니다. 간편결제는 외부 페이지에서 시간을 쓰기 때문에 준비부터 확정까지 오래 걸립니다. 재고 예약 시한(30분)을 넘긴 경우입니다.

## 재현

1. 주문 생성 (재고 예약)
2. 간편결제 준비
3. 외부 페이지에서 30분 이상 머무름
4. 승인 완료 후 복귀
5. 주문 확정 실패 → 결제 취소

## 원인

재고 예약 시한과 결제 흐름 시간이 맞지 않습니다. 카드 결제는 몇 초 만에 끝나지만 리다이렉트 방식은 훨씬 오래 걸릴 수 있습니다.

취소 자체는 설계대로 동작한 것입니다. 결제만 되고 주문이 없는 상태보다는 낫습니다.

문제는 사용자가 결제 완료 화면을 본다는 점입니다. 외부 결제 페이지가 성공 화면을 먼저 보여주고 우리 쪽으로 돌아옵니다.

## 조치

**즉시**: 결제 준비 시 재고 예약 시한을 연장합니다. 리다이렉트 방식은 60분으로 둡니다.

**화면**: 콜백 복귀 후 확정이 끝날 때까지 "결제 확인 중" 화면을 보여줍니다. 완료 화면은 확정 후에만 표시합니다.

**안내**: 확정 실패 시 명확히 안내하고 결제가 취소되었음을 알립니다. 지금은 주문 내역에서 발견하게 됩니다.

## 남은 문제

60분으로 늘려도 그보다 오래 걸리는 경우가 있을 수 있습니다. 근본적으로는 결제 준비 시한(10분)과 재고 예약 시한을 연동해야 합니다.

결제 준비가 만료되면 재고 예약도 함께 해제하는 방향으로 정리가 필요합니다. 별도 과제로 등록했습니다.
""",
    },
    {
        "id": "DOC-235",
        "title": "정기 결제 도입 요건 논의",
        "department": "서비스기획팀",
        "type": "기획자와의 회의록",
        "project": None,
        "date": "2026-06-16",
        "systems": ["ORBIT"],
        "features": ["결제"],
        "participants": ["강민재", "김도윤"],
        "concepts": ["정기 결제", "요구사항", "정책"],
        "body": """## 논의 배경

정기 배송 상품을 준비하면서 정기 결제가 필요해졌습니다. 매달 같은 날 자동으로 결제됩니다.

## 요구사항

- 월 단위 자동 결제
- 사용자가 언제든 해지 가능
- 결제 실패 시 재시도
- 결제 예정 3일 전 알림

## 쟁점

**결제 실패 처리**

김도윤님이 물었습니다. 카드 한도 초과로 실패하면 며칠 뒤 다시 시도할지, 바로 해지할지입니다.

강민재님은 3일 간격으로 3회 시도 후 해지를 제안했습니다. 다만 그 사이 배송을 보낼지가 문제입니다.

결제가 확정되기 전에는 배송하지 않기로 정리했습니다. 결제 성공 후 배송이 시작됩니다.

**금액 변동**

상품 가격이 바뀌면 다음 결제부터 반영합니다. 다만 사용자에게 미리 알려야 합니다. 가격 인상은 30일 전 고지가 필요합니다.

**포인트와 쿠폰**

정기 결제에 포인트를 쓸 수 있게 할지 논의했습니다. 매달 잔액이 다르므로 부족하면 실패합니다.

김도윤님은 정기 결제에서는 포인트와 쿠폰을 쓰지 않는 편이 단순하다고 했습니다. 강민재님도 동의했습니다.

## 결정 사항

- 월 1회, 사용자가 지정한 날짜에 결제
- 해당 날짜가 없는 달(31일)은 말일에 결제
- 결제 실패 시 3일 간격 3회 재시도, 이후 자동 해지
- 결제 성공 후 배송 시작
- 포인트와 쿠폰은 사용 불가
- 결제 3일 전, 실패 시, 해지 시 알림

## 미확정

해지 시점의 처리를 정하지 못했습니다. 이미 결제한 회차의 배송이 남아 있으면 그것은 진행하고 다음 회차부터 중단하는 것이 맞는데, 사용자가 "환불해 달라"고 할 수 있습니다.

부분 환불 규정을 정해야 합니다. 다음 회의에서 다룹니다.

## Action Item

- 강민재님: 해지 및 환불 정책안
- 김도윤님: 기술 검토 및 일정 산정
""",
    },
    {
        "id": "DOC-236",
        "title": "정기 결제 기술 검토",
        "department": "백엔드개발팀",
        "type": "개발자 회의록",
        "project": None,
        "date": "2026-06-22",
        "systems": ["ORBIT"],
        "features": ["결제"],
        "participants": ["김도윤", "노태윤"],
        "concepts": ["정기 결제", "빌링키", "배치"],
        "body": """## 논의 배경

정기 결제 요건을 받아 기술적으로 무엇이 필요한지 정리합니다.

## 빌링키

정기 결제는 카드 정보를 매번 받지 않고 빌링키로 결제합니다. 빌링키는 PG 가 발급하고 우리는 키만 보관합니다.

카드 번호는 저장하지 않습니다. 마지막 4자리와 카드사만 표시용으로 보관합니다.

## 결제 수단별 지원

노태윤님이 확인한 내용입니다.

| 수단 | 정기 결제 |
| --- | --- |
| 카드 | 빌링키 지원 |
| 간편결제 A | 지원 |
| 간편결제 B | 미지원 |
| 계좌이체 | 미지원 |

지원하는 수단만 정기 결제에 노출합니다. `payment_method.supports_recurring` 을 씁니다.

## 결제 실행

매일 새벽 배치가 그날 결제 대상을 찾아 실행합니다.

김도윤님이 우려한 것은 실행 시각입니다. 새벽에 몰아서 실행하면 PG 에 부하가 걸리고, 실패해도 사용자가 즉시 알기 어렵습니다.

노태윤님은 오전 시간대로 분산하자고 제안했습니다. 구독 시작 시각을 기준으로 분산하면 자연스럽게 나뉩니다.

## 빌링키 만료

카드 유효기간이 지나면 빌링키가 무효가 됩니다. 사전에 알 수 있으므로 만료 30일 전에 카드 갱신을 안내합니다.

## 재시도

3일 간격 3회입니다. 재시도도 배치가 처리합니다.

중복 방지 키는 `{subscriptionId}-{회차}-{시도횟수}` 로 구성합니다. 같은 회차의 같은 시도는 중복되지 않고, 재시도는 새 시도로 인식됩니다.

## 상태

```text
ACTIVE      정상
PAST_DUE    결제 실패, 재시도 중
CANCELED    사용자 해지
EXPIRED     재시도 소진으로 자동 해지
```

## Action Item

- 김도윤님: 설계 문서 작성
- 노태윤님: 빌링키 발급 연동 확인

## 추후 논의

정기 결제 중 상품이 판매 중지되면 어떻게 할지 정하지 않았습니다. 자동 해지할지, 대체 상품을 제안할지 기획팀 확인이 필요합니다.
""",
    },
    {
        "id": "DOC-237",
        "title": "정기 결제 설계 초안",
        "department": "백엔드개발팀",
        "type": "비즈니스 기능 설계",
        "project": None,
        "date": "2026-06-26",
        "systems": ["ORBIT"],
        "features": ["결제"],
        "participants": [],
        "concepts": ["정기 결제", "배치", "Edge Case", "멱등성"],
        "body": """## 배경

정기 결제를 설계합니다. 기존 결제 흐름을 재사용하되 실행 주체가 사용자가 아니라 배치라는 점이 다릅니다.

## 데이터

`subscription`

| 컬럼 | 설명 |
| --- | --- |
| id | PK |
| member_id | |
| product_id | |
| billing_key_id | 빌링키 참조 |
| status | ACTIVE / PAST_DUE / CANCELED / EXPIRED |
| billing_day | 결제일 (1~31) |
| next_billing_at | 다음 결제 예정 시각 |
| retry_count | 현재 회차 재시도 횟수 |
| cycle_count | 총 결제 회차 |
| started_at | |
| canceled_at | |

`subscription_billing`

회차별 결제 이력입니다. `(subscription_id, cycle_number)` 에 unique 를 겁니다.

## 결제 Flow

1. 배치가 `next_billing_at <= now` 이고 `status IN (ACTIVE, PAST_DUE)` 인 구독 조회
2. 주문 생성
3. 빌링키로 결제 요청 (멱등키 = `{subscriptionId}-{cycle}-{retry}`)
4. 성공: 다음 결제일 계산, `retry_count` 초기화
5. 실패: `PAST_DUE` 로 전이, `retry_count` 증가, 3일 후로 `next_billing_at` 설정
6. 재시도 3회 실패: `EXPIRED` 로 전이, 알림

## 결제일 계산

`billing_day` 가 31인데 2월이면 말일로 조정합니다. 다음 달 계산 시에는 원래 값(31)을 기준으로 다시 계산합니다. 한 번 28일로 밀리면 계속 28일이 되는 문제를 막습니다.

## Edge Case

- **배치 중복 실행**: 멱등키로 중복 결제를 막습니다. `subscription_billing` unique 도 이중 방어입니다.
- **결제 중 사용자 해지**: 해지 시 `status` 를 확인하고, 결제가 진행 중이면 해당 회차는 완료합니다. 다음 회차부터 중단합니다.
- **빌링키 만료**: 결제 시 PG 가 오류를 반환합니다. 재시도해도 같으므로 즉시 `PAST_DUE` 로 두고 카드 갱신을 안내합니다. 3일 재시도를 기다리지 않습니다.
- **상품 판매 중지**: 결제 전에 확인하고 중지된 상품이면 결제하지 않습니다. 처리 방식은 기획 확인 대기 중입니다.
- **가격 변동**: 결제 시점 가격을 사용합니다. 인상인 경우 30일 전 고지가 있었는지 확인하고, 없으면 이전 가격으로 결제합니다.
- **재고 부족**: 정기 배송 상품은 재고를 예약해 두는 것이 맞지만 현재 구조에 없습니다. 재고가 없으면 결제하지 않고 알림을 보냅니다.

## 미해결

**해지 시 환불**: 이미 결제한 회차의 환불 기준이 정해지지 않았습니다. 배송 전이면 전액, 배송 후면 반품 절차가 자연스럽지만 확정이 필요합니다.

**재고 예약**: 정기 배송인데 재고가 없어 결제를 못 하는 상황이 반복되면 구독 의미가 없습니다. 별도 재고 확보 방식이 필요할 수 있습니다.

두 항목 모두 기획팀 회신 대기 중이며 이 문서는 초안 상태입니다.
""",
    },
    {
        "id": "DOC-238",
        "title": "결제 수단 확대 진행 현황",
        "department": "프로젝트관리팀",
        "type": "프로젝트 진행상황",
        "project": None,
        "date": "2026-07-01",
        "systems": ["ORBIT"],
        "features": ["결제"],
        "participants": [],
        "concepts": ["일정", "Risk", "결제 수단"],
        "body": """## 범위

- 결제 수단 전략 분리
- `payment_method` 설정 테이블
- 간편결제 B 추가
- 정기 결제 (2단계)

## 완료

- 결제 수단 전략 인터페이스 분리
- `payment_method` 테이블 및 관리 화면
- 간편결제 B 연동 및 배포
- 부분 취소 미지원 수단 사전 안내

## 진행 중

- 카드/계좌이체 전략 이행 (기존 분기 제거)
- 정기 결제 설계

## 대기

- 정기 결제 구현 (설계 미확정 항목 있음)
- 빌링키 관리 화면

## 간편결제 B 지표

5월 25일 배포 후 5주 지표입니다.

| 항목 | 값 |
| --- | --- |
| 전체 결제 중 비중 | 9.4% |
| 결제 성공률 | 96.1% |
| 평균 결제 소요 시간 | 41초 |

카드(4초)에 비해 오래 걸립니다. 리다이렉트 방식 특성입니다.

6월 초 발생한 승인 후 취소 이슈는 재고 예약 시한 연장으로 해결됐습니다. 이후 재발하지 않았습니다.

## Risk

정기 결제 설계에 미확정 항목이 두 개 있습니다. 해지 시 환불 기준과 재고 확보 방식입니다. 기획팀 회신이 2주째 없습니다.

설계가 확정되지 않으면 구현을 시작할 수 없습니다. 정기 배송 상품 출시 일정과 연결되어 있어 지연되면 영향이 있습니다.

기존 결제 분기 제거 작업이 남아 있습니다. 이행 기간에 두 구조가 공존하는데, 이 상태가 길어지면 오히려 복잡합니다. 7월 중 마무리 목표입니다.

## 다음

- 7월 중 전략 이행 완료
- 정기 결제 정책 확정 요청 재발송
""",
    },
    {
        "id": "DOC-239",
        "title": "회의 운영 규칙",
        "department": "프로젝트관리팀",
        "type": "팀 Ground Rule",
        "project": None,
        "date": "2025-03-25",
        "systems": [],
        "features": [],
        "participants": [],
        "concepts": ["회의", "팀 규칙", "기록"],
        "body": """## 회의 소집

- 목적과 결정할 사항을 미리 공유합니다
- 참석자를 최소로 유지합니다. 정보 공유만 필요한 사람은 회의록으로 충분합니다
- 관련 자료를 하루 전에 공유합니다

"논의합니다"만 있는 소집은 준비 없이 시간을 씁니다. 무엇을 정할지 적어 주세요.

## 시간

- 기본 30분입니다. 30분으로 안 되면 안건이 너무 많은 것입니다
- 시작 시각에 시작합니다. 늦은 사람을 기다리지 않습니다
- 끝나면 남은 시간을 채우지 않고 종료합니다

## 진행

- 소집자가 진행을 맡습니다
- 안건에서 벗어난 논의는 별도 회의로 넘깁니다
- 결론이 나지 않으면 "다음에 무엇을 준비해서 언제 다시 볼지"를 정하고 마칩니다

결론 없이 끝나는 것 자체는 문제가 아닙니다. 다음 단계 없이 끝나는 것이 문제입니다.

## 기록

모든 회의는 회의록을 남깁니다. 소집자가 작성하고 당일 안에 공유합니다.

포함할 것:

- 논의 배경
- 주요 쟁점과 각 입장
- 결정 사항
- 미확정 사항과 다음 단계
- Action Item (담당자와 기한)

**반대 의견도 기록합니다.** 나중에 왜 그 결정을 했는지 돌아볼 때 필요합니다.

## Action Item

- 담당자와 기한을 반드시 적습니다
- "검토한다"가 아니라 "무엇을 언제까지 한다"로 씁니다
- 다음 회의 시작 시 이전 Action Item 을 먼저 확인합니다

## 정기 회의

- 주간 회의는 30분, 스프린트 회고는 1시간
- 안건이 없으면 취소합니다. 정기라는 이유로 모이지 않습니다
""",
    },
    {
        "id": "DOC-240",
        "title": "장애 대응 규칙",
        "department": "DevOps팀",
        "type": "팀 Ground Rule",
        "project": None,
        "date": "2025-11-27",
        "systems": [],
        "features": [],
        "participants": [],
        "concepts": ["장애 대응", "팀 규칙", "에스컬레이션"],
        "body": """## 등급

| 등급 | 기준 |
| --- | --- |
| P1 | 서비스 전체 또는 핵심 기능(로그인, 주문, 결제) 불가 |
| P2 | 일부 기능 장애, 성능 저하 |
| P3 | 소수 사용자 영향, 우회 가능 |

판단이 애매하면 높은 등급으로 시작합니다. 낮추는 것은 쉽지만 늦게 올리면 대응이 늦습니다.

## 대응 우선순위

```text
1. 복구
2. 원인 파악
3. 재발 방지
```

순서를 지킵니다. 원인을 모르더라도 복구가 우선입니다. 롤백으로 복구할 수 있으면 원인 분석보다 롤백을 먼저 합니다.

## 공유

- 인지 즉시 공유 채널에 알립니다. 원인을 몰라도 됩니다
- 상황이 바뀌면 갱신합니다. 침묵하지 마세요
- 복구되면 종료를 명시적으로 알립니다

혼자 붙잡고 있는 것이 가장 나쁩니다.

## 역할

P1 에서는 역할을 나눕니다.

| 역할 | 하는 일 |
| --- | --- |
| 대응 리드 | 판단과 지시 |
| 조사 | 원인 분석 |
| 커뮤니케이션 | 상황 공유, 관련 팀 연락 |

한 사람이 조사와 공유를 같이 하면 둘 다 늦어집니다.

## 하지 말 것

- 원인을 모른 채 데이터를 수정하지 않습니다
- 여러 사람이 동시에 다른 조치를 하지 않습니다. 무엇이 효과가 있었는지 알 수 없습니다
- 복구 후 바로 흩어지지 않습니다. 재발 여부를 확인합니다

## 사후

- P1, P2 는 24시간 안에 간단한 기록을 남깁니다
- P1 은 일주일 안에 보고서를 작성합니다

보고서에는 시간순 경과, 원인, 조치, 재발 방지를 씁니다.

**책임 소재를 다루지 않습니다.** 누가 실수했는지가 아니라 왜 그 실수가 가능했는지를 봅니다. 사람이 실수해도 시스템이 막아야 합니다.

## 훈련

분기마다 장애 대응 훈련을 합니다. 실제 장애 상황을 가정하고 절차를 따라가 봅니다. 연락망과 권한이 실제로 동작하는지 확인하는 것이 목적입니다.
""",
    },
]
