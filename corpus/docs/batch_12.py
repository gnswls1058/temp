"""DOC-166 ~ DOC-180 : 리워드 2차 개선 - 등급, 재입고 알림, 선착순 쿠폰 (2026 Q1~Q2)."""

DOCUMENTS = [
    {
        "id": "DOC-166",
        "title": "고객 등급별 적립률 개편 요구사항",
        "department": "서비스기획팀",
        "type": "기획자와의 회의록",
        "project": "Project Nova",
        "date": "2026-01-21",
        "systems": ["MARS", "NEXUS"],
        "features": ["리워드 지급", "포인트"],
        "participants": ["강민재", "박준호"],
        "concepts": ["등급", "적립률", "기술적 제약"],
        "body": """## 논의 배경

지금은 모든 고객에게 동일하게 1% 적립합니다. 구매액이 큰 고객에게 더 주는 등급제를 도입하려 합니다.

## 기획안

| 등급 | 조건 (최근 6개월 구매액) | 적립률 |
| --- | --- | --- |
| BASIC | 0원 이상 | 1.0% |
| SILVER | 30만원 이상 | 1.5% |
| GOLD | 100만원 이상 | 2.0% |
| VIP | 300만원 이상 | 3.0% |

등급은 매월 1일에 산정합니다.

## 개발 관점

박준호님이 확인한 제약입니다.

**적립 시점의 등급**: 주문 시점 등급으로 적립하는데, 배송 완료 시점에 적립이 일어납니다. 그 사이 월이 바뀌면 등급이 달라질 수 있습니다. 어느 시점 등급을 쓸지 정해야 합니다.

**부분 취소 재계산**: 부분 취소 시 남은 금액으로 적립을 재계산하는 로직이 있습니다. 등급이 붙으면 "어느 등급의 요율로 재계산하는가"가 추가됩니다.

**구매액 산정**: 취소된 주문을 빼야 합니다. 취소 시점이 산정 기준 기간을 넘어가면 계산이 복잡해집니다.

## 결정 사항

- 적립 요율은 **주문 시점 등급**을 사용합니다. 주문 시 등급과 요율을 주문에 함께 기록합니다.
- 부분 취소 재계산도 주문에 기록된 요율을 씁니다.
- 구매액은 취소분을 제외한 실 결제액 기준입니다.

주문에 요율을 기록하면 나중에 등급이 바뀌어도 과거 주문의 적립이 흔들리지 않습니다.

## 미확정

등급 하락 시 안내를 어떻게 할지 정하지 못했습니다. 아무 안내 없이 떨어지면 문의가 생기고, 미리 알리면 하락 직전 구매를 유도하는 효과와 불쾌감이 함께 있습니다.

기획팀에서 다른 서비스 사례를 정리한 뒤 다음에 다시 봅니다.

## Action Item

- 강민재님: 등급 하락 안내 정책안
- 박준호님: 구매액 산정 기준 상세 정의
""",
    },
    {
        "id": "DOC-167",
        "title": "등급 산정 기준 기술 검토",
        "department": "백엔드개발팀",
        "type": "개발자 회의록",
        "project": "Project Nova",
        "date": "2026-01-26",
        "systems": ["MARS", "ORBIT"],
        "features": ["포인트"],
        "participants": ["박준호", "배현우"],
        "concepts": ["배치", "집계", "정합성"],
        "body": """## 논의 배경

등급 산정에 필요한 구매액을 어디서 계산할지 정합니다. 주문 데이터는 ORBIT 에 있고 등급은 MARS 가 관리합니다.

## 검토안

**1. MARS 가 매월 ORBIT 에 조회**

회원 480만 명에 대해 6개월치 주문을 조회하면 부하가 큽니다. 배현우님 추정으로 단순 집계에도 수십 분이 걸립니다.

**2. ORBIT 이 집계해서 MARS 에 전달**

ORBIT 이 자기 데이터를 집계하므로 효율적입니다. 다만 ORBIT 에 등급 관련 책임이 생깁니다.

**3. 데이터 파이프라인에서 집계 후 양쪽에 제공**

배현우님이 제안한 방식입니다. 이미 주문 데이터를 분석용으로 적재하고 있으므로 여기서 집계하면 운영 DB 부하가 없습니다.

## 쟁점

박준호님은 3안의 지연을 우려했습니다. 분석 파이프라인은 하루 단위 적재라 최신 데이터가 반영되지 않습니다.

배현우님은 등급 산정이 월 1회이므로 하루 지연은 문제가 아니라고 했습니다. 다만 월말 마지막 날 주문이 반영되지 않을 수 있어 산정 시점을 매월 1일이 아니라 2일로 미루자고 제안했습니다.

## 결정 사항

- 3안으로 갑니다. 데이터 파이프라인에서 집계합니다
- 산정은 매월 2일 새벽에 수행합니다
- 집계 결과는 MARS 의 `member_grade` 테이블에 적재합니다
- 원본 대사를 위해 집계 기준 기간과 대상 주문 수를 함께 기록합니다

## 우려

집계가 파이프라인에 의존하면 파이프라인 장애 시 등급 산정이 밀립니다. 실패 시 이전 등급을 유지하고 알림을 보내기로 했습니다. 등급이 잘못 내려가는 것보다 늦게 올라가는 편이 낫습니다.

## Action Item

- 배현우님: 집계 쿼리 및 적재 파이프라인
- 박준호님: `member_grade` 테이블 설계
""",
    },
    {
        "id": "DOC-168",
        "title": "재입고 알림 기능 요건 정리",
        "department": "서비스기획팀",
        "type": "기획자와의 회의록",
        "project": None,
        "date": "2026-01-29",
        "systems": ["ORBIT", "NEXUS"],
        "features": ["상품"],
        "participants": ["강민재", "정하늘"],
        "concepts": ["알림", "요구사항", "동시성"],
        "body": """## 논의 배경

품절 상품 노출 논의에서 나온 재입고 알림을 구체화합니다.

## 요구사항

- 품절 상품 상세에서 알림 신청
- 재입고되면 신청자에게 푸시 발송
- 알림은 1회만. 발송 후 신청 해제
- 신청 목록을 마이페이지에서 확인 및 취소

## 쟁점

**동시 접속 문제**: 인기 상품에 신청자가 수천 명이면 재입고 순간 알림을 받은 사람들이 동시에 접속합니다. 재고가 10개인데 알림을 3,000명에게 보내면 대부분 실패합니다.

강민재님은 그래도 알려야 한다는 입장입니다. 알림을 안 보내면 신청한 의미가 없습니다.

정하늘님은 알림에 재고 수량을 표시하거나, 나눠 보내는 방법을 제안했습니다.

**신청 상한**: 한 사용자가 무제한으로 신청할 수 있으면 알림이 과도해집니다.

## 결정 사항

- 알림은 신청 순서대로 나눠 발송합니다. 배치 크기는 재고 수량의 10배
- 첫 배치 발송 후 5분 뒤 다음 배치. 재고가 소진되면 중단
- 사용자당 신청 상한 50개
- 신청 후 30일이 지나면 자동 만료

## 재고 소진 시 중단

재고가 다 팔리면 남은 배치를 보내지 않습니다. 이미 없는 상품 알림을 받는 것이 가장 나쁜 경험입니다.

다만 신청은 유지합니다. 다음 재입고 때 대상이 됩니다.

## 미확정

알림 발송 순서를 신청 순으로 할지, 등급이나 구매 이력을 반영할지 의견이 갈렸습니다. 신청 순이 공정하지만 VIP 우대 요구가 있을 수 있습니다.

이번에는 신청 순으로 시작하고, 등급제 도입 이후 재검토합니다.

## Action Item

- 강민재님: 알림 문구 및 마이페이지 화면 요건
- 정하늘님: 상품 상세 신청 버튼 반영
""",
    },
    {
        "id": "DOC-169",
        "title": "재입고 알림 처리 설계",
        "department": "백엔드개발팀",
        "type": "비즈니스 기능 설계",
        "project": None,
        "date": "2026-02-04",
        "systems": ["ORBIT", "NEXUS"],
        "features": ["상품"],
        "participants": [],
        "concepts": ["알림", "배치", "동시성", "Edge Case"],
        "body": """## 배경

재입고 시 신청자에게 알림을 나눠 보냅니다. 재고가 소진되면 중단합니다.

## 데이터

`restock_subscription`

| 컬럼 | 설명 |
| --- | --- |
| id | PK |
| member_id | |
| product_id | |
| status | ACTIVE / NOTIFIED / EXPIRED / CANCELED |
| subscribed_at | |
| notified_at | |

`(member_id, product_id, status)` 에 부분 unique 를 두어 같은 상품에 중복 신청을 막습니다.

## 재입고 감지

재고 조정 이력에서 `RESTOCK` 타입이 발생하고 `available` 이 0에서 양수로 바뀌면 알림 대상입니다.

```text
직전 available == 0  AND  현재 available > 0  -> 재입고 이벤트
```

단순히 재고가 늘었다고 보내면 안 됩니다. 재고가 5에서 10으로 늘어난 것은 재입고 알림 대상이 아닙니다.

## 발송 Flow

1. 재입고 이벤트 수신
2. 해당 상품의 `ACTIVE` 신청자를 신청 순으로 조회
3. 배치 크기 = 현재 재고 × 10
4. 첫 배치 발송, 상태를 `NOTIFIED` 로 변경
5. 5분 대기
6. 재고 확인. 남아 있으면 다음 배치, 없으면 중단

## Edge Case

- **발송 중 재고 소진**: 다음 배치 전에 확인하므로 최대 한 배치분이 헛발송됩니다. 완전히 막을 수는 없습니다.
- **발송 중 재입고 추가**: 배치가 진행 중인데 재고가 더 들어오면 새 이벤트가 발생합니다. 같은 상품에 두 개의 발송 작업이 도는 것을 막기 위해 상품 단위 잠금을 겁니다.
- **알림 후 신청 복원 요청**: 알림을 받았지만 못 산 사용자가 다시 신청할 수 있어야 합니다. `NOTIFIED` 상태에서 재신청을 허용합니다.
- **탈퇴 회원**: 발송 대상에서 제외하고 `EXPIRED` 로 정리합니다.
- **상품 판매 중지**: 재입고가 있어도 판매하지 않으므로 발송하지 않습니다.
- **푸시 미허용 사용자**: 알림 자체를 보낼 수 없습니다. 신청 시 푸시 권한을 확인하고 없으면 안내합니다. 이메일 대체는 즉시성이 떨어져 하지 않습니다.

## 발송 배치 크기

재고의 10배로 정했습니다. 근거는 약한데, 알림 수신자 중 실제 구매 전환이 10% 정도일 것이라는 추정입니다.

실제 전환율을 측정한 뒤 조정해야 합니다. 설정값으로 빼둡니다.
""",
    },
    {
        "id": "DOC-170",
        "title": "리뷰 작성 리워드 정책 논의",
        "department": "서비스기획팀",
        "type": "기획자와의 회의록",
        "project": "Project Nova",
        "date": "2026-02-11",
        "systems": ["MARS", "ORBIT"],
        "features": ["리워드 지급", "포인트"],
        "participants": ["송지은", "박준호"],
        "concepts": ["어뷰징", "정책", "중복 지급"],
        "body": """## 논의 배경

상품 리뷰 작성 시 포인트를 지급하려 합니다. 리뷰 수를 늘리는 것이 목적입니다.

## 기획안

| 유형 | 포인트 |
| --- | --- |
| 텍스트 리뷰 | 100 |
| 사진 포함 리뷰 | 500 |
| 동영상 리뷰 | 1,000 |

## 어뷰징 우려

박준호님이 지적한 시나리오입니다.

1. 리뷰 작성 후 포인트를 받고 리뷰 삭제
2. 저가 상품을 반복 구매하며 리뷰 작성
3. 의미 없는 사진(빈 화면 등)으로 사진 리뷰 포인트 수령
4. 리뷰 작성 후 주문 취소

## 대응

**1번**: 리뷰 삭제 시 포인트를 회수합니다. 이미 사용했으면 회수 불가로 집계합니다.

**2번**: 주문 건당 1회, 고객당 월 10회로 제한합니다.

**3번**: 자동 판별이 어렵습니다. 신고 기반으로 처리하고, 신고가 확인되면 포인트를 회수합니다.

**4번**: 배송 완료 후에만 리뷰를 쓸 수 있게 합니다. 취소되면 리뷰 자체가 불가능합니다.

## 쟁점

송지은님은 월 10회 제한이 너무 빡빡하다고 봤습니다. 실제 구매가 많은 사용자가 걸립니다.

박준호님은 상한 없이 시작하면 나중에 낮추기 어렵다고 했습니다. 혜택을 줄이는 변경은 반발이 큽니다.

## 결정 사항

- 월 20회로 시작합니다. 데이터를 보고 조정합니다
- 배송 완료 후 30일 안에만 작성 가능
- 리뷰 삭제 시 포인트 회수
- 참조키는 `REVIEW:{orderItemId}` 로 하여 주문 상품당 1회를 보장

## 미확정

사진 리뷰의 품질 기준을 정하지 못했습니다. 자동 판별을 시도할지, 신고에만 의존할지 결론이 없습니다. 초기에는 신고 기반으로 가고 데이터를 보기로 했습니다.
""",
    },
    {
        "id": "DOC-171",
        "title": "분석용 데이터 적재 파이프라인 개선",
        "department": "데이터팀",
        "type": "개발자 회의록",
        "project": None,
        "date": "2026-02-18",
        "systems": ["ORBIT", "MARS", "NEXUS"],
        "features": [],
        "participants": [],
        "concepts": ["집계", "파이프라인", "지연"],
        "body": """## 논의 배경

등급 산정이 파이프라인에 의존하게 되면서 안정성 요구가 올라갔습니다. 현재 구조를 점검합니다.

## 현재 구조

```text
운영 DB -> 일 1회 전체 스냅샷 -> 분석 저장소 -> 집계 -> 리포트
```

전체 스냅샷 방식이라 주문 테이블(4,200만 행)을 매일 통째로 읽습니다. 새벽 2시에 시작해 4시간 걸립니다.

## 문제

- 운영 DB 부하가 큽니다. 새벽 배치들과 시간이 겹칩니다
- 실패 시 재실행에 또 4시간이 걸립니다
- 최신 데이터 반영이 하루 늦습니다

## 검토안

**변경분만 적재(CDC)**: 변경 로그를 읽어 증분 적재합니다. 부하가 줄고 지연도 짧아집니다. 다만 인프라가 추가되고 초기 구축에 시간이 걸립니다.

**시간대 분산**: 테이블별로 시간을 나눠 적재합니다. 부하는 분산되지만 총 시간은 그대로입니다.

**증분 쿼리**: `updated_at` 기준으로 변경분만 조회합니다. 구현이 단순하지만 삭제된 행을 감지하지 못합니다.

## 쟁점

삭제 감지가 문제입니다. 주문은 삭제하지 않지만 장바구니나 알림은 삭제됩니다. 증분 쿼리로는 이를 알 수 없습니다.

주문과 결제처럼 삭제가 없는 테이블은 증분 쿼리로 충분합니다. 삭제가 있는 테이블만 전체 스냅샷을 유지하는 절충안이 나왔습니다.

## 결정 사항

- 주문, 결제, 리워드 이력은 `updated_at` 증분 적재로 전환
- 나머지는 전체 스냅샷 유지하되 시간대를 분산
- CDC 도입은 이번 분기에는 하지 않고 다음 분기 검토

## 예상 효과

증분 전환 대상이 전체 데이터의 70% 이므로 적재 시간이 4시간에서 1시간대로 줄 것으로 봅니다.

## 남은 확인

`updated_at` 이 없는 테이블이 몇 개 있습니다. 컬럼 추가가 필요하고, 기존 행을 채우는 작업이 따릅니다. 대상 테이블 목록을 정리하기로 했습니다.
""",
    },
    {
        "id": "DOC-172",
        "title": "member_grade 테이블 설계",
        "department": "백엔드개발팀",
        "type": "DB 테이블 설계안",
        "project": "Project Nova",
        "date": "2026-02-20",
        "systems": ["MARS"],
        "features": ["포인트", "리워드 지급"],
        "participants": [],
        "concepts": ["등급", "이력", "Index"],
        "body": """## 테이블

### member_grade (현재 등급)

| 컬럼 | 타입 | 제약 | 설명 |
| --- | --- | --- | --- |
| member_id | BIGINT | PK | |
| grade | VARCHAR(16) | NOT NULL | BASIC / SILVER / GOLD / VIP |
| reward_rate | DECIMAL(4,3) | NOT NULL | 적립률. 0.010 = 1% |
| calculated_at | DATETIME | NOT NULL | 산정 시각 |
| base_amount | INT | NOT NULL | 산정 기준 구매액 |
| valid_from | DATE | NOT NULL | |

### member_grade_history

| 컬럼 | 설명 |
| --- | --- |
| id | PK |
| member_id | |
| from_grade | 이전 등급 |
| to_grade | 변경 후 등급 |
| base_amount | 산정 근거 금액 |
| calculated_at | |

```sql
CREATE INDEX idx_grade_history_member
  ON member_grade_history (member_id, calculated_at DESC);
CREATE UNIQUE INDEX uk_grade_history_period
  ON member_grade_history (member_id, calculated_at);
```

## reward_rate 를 저장하는 이유

등급과 요율의 매핑은 설정으로 관리할 수도 있습니다. 하지만 요율이 바뀌면 과거 산정 결과가 소급해서 달라집니다.

산정 시점의 요율을 저장하면 "이 회원이 이번 달에 몇 %로 적립받는지"가 확정됩니다.

## base_amount 를 저장하는 이유

문의 대응에 필요합니다. "왜 등급이 안 올랐냐"는 문의에 산정 기준 금액을 보여줄 수 있어야 합니다.

## 주문에 요율 기록

`order` 테이블에도 `applied_reward_rate` 를 추가합니다. 주문 시점 요율로 적립하고, 부분 취소 재계산도 이 값을 씁니다.

등급 테이블을 참조하면 나중에 등급이 바뀐 뒤 재계산할 때 다른 요율이 적용됩니다.

## 산정 배치

매월 2일 새벽에 실행합니다. 데이터 파이프라인 집계 결과를 읽어 적재합니다.

- 등급이 바뀐 회원만 `member_grade_history` 에 기록
- 변동 없으면 `calculated_at` 만 갱신
- 파이프라인 결과가 없으면 배치를 중단하고 알림

## 유의

480만 회원 전체를 갱신하므로 배치 시간이 깁니다. 1,000건 단위로 나눠 처리하고, 중단되면 이어서 실행할 수 있게 커서를 남깁니다.
""",
    },
    {
        "id": "DOC-173",
        "title": "[리뷰] 등급 산정 배치 구현",
        "department": "백엔드개발팀",
        "type": "코드리뷰",
        "project": "Project Nova",
        "date": "2026-02-25",
        "systems": ["MARS"],
        "features": ["포인트"],
        "participants": ["박준호", "배현우"],
        "concepts": ["배치", "등급", "재실행"],
        "body": """## 현재 구현

파이프라인 집계 결과를 읽어 등급을 갱신합니다.

```java
var results = gradeAggregationReader.read(targetMonth);
for (var row : results) {
    var grade = GradePolicy.of(row.purchaseAmount());
    gradeRepository.upsert(row.memberId(), grade, row.purchaseAmount());
    if (isChanged(row.memberId(), grade)) {
        historyRepository.save(...);
    }
}
```

## 리뷰 의견

배현우님: 집계 결과가 비어 있을 때 처리가 없습니다. 파이프라인이 실패해 빈 결과가 오면 아무 회원도 갱신되지 않고 조용히 끝납니다. 설계에는 "결과가 없으면 중단하고 알림"이라고 되어 있습니다.

박준호님: 추가하겠습니다. 결과 건수가 예상 회원 수의 90% 미만이면 중단하는 조건도 넣겠습니다.

배현우님: `isChanged` 를 위해 회원마다 현재 등급을 조회합니다. 480만 번 조회하면 배치가 매우 느려집니다. upsert 결과나 이전 값을 한 번에 읽어오는 방식이 낫습니다.

박준호님: 1,000건 단위로 현재 등급을 일괄 조회한 뒤 메모리에서 비교하겠습니다.

배현우님: 재실행 시 `member_grade_history` 에 중복이 들어갑니다. unique 제약이 `(member_id, calculated_at)` 인데 `calculated_at` 을 실행 시각으로 넣으면 재실행 때 값이 달라져 중복이 통과합니다.

박준호님: `calculated_at` 을 산정 대상 월의 고정 시각으로 넣겠습니다. 그러면 재실행해도 같은 값이라 중복이 막힙니다.

## 수정 사항

- 집계 결과 건수 검증 후 중단 조건 추가
- 현재 등급 일괄 조회로 변경
- `calculated_at` 을 산정 기준 월의 고정 시각으로 설정
- 진행 커서를 배치 이력에 저장

## 남은 확인

등급이 내려가는 회원에게 알림을 보낼지 정해지지 않았습니다. 기획팀 결론이 나올 때까지 배치는 등급만 갱신하고 알림은 보내지 않습니다.
""",
    },
    {
        "id": "DOC-174",
        "title": "회원 등급 조회 API 명세",
        "department": "백엔드개발팀",
        "type": "API 명세서",
        "project": "Project Nova",
        "date": "2026-03-04",
        "systems": ["MARS"],
        "features": ["포인트"],
        "participants": [],
        "concepts": ["등급", "조회"],
        "body": """## Endpoint

```http
GET /rewards/grade
Authorization: Bearer {accessToken}
```

## Response

```json
{
  "memberId": 1024,
  "grade": "GOLD",
  "rewardRate": 0.020,
  "baseAmount": 1240000,
  "calculatedAt": "2026-03-02T04:12:00+09:00",
  "nextGrade": {
    "grade": "VIP",
    "requiredAmount": 3000000,
    "remainingAmount": 1760000
  },
  "nextCalculationAt": "2026-04-02T04:00:00+09:00"
}
```

`nextGrade` 는 다음 등급까지 필요한 금액입니다. 최고 등급이면 null 입니다.

## 등급 기준

| 등급 | 최근 6개월 구매액 | 적립률 |
| --- | --- | --- |
| BASIC | 0원 이상 | 1.0% |
| SILVER | 30만원 이상 | 1.5% |
| GOLD | 100만원 이상 | 2.0% |
| VIP | 300만원 이상 | 3.0% |

구매액은 취소와 환불을 제외한 실 결제 금액입니다. 포인트로 결제한 금액은 포함되지 않습니다.

## Error Code

| 코드 | HTTP | 설명 |
| --- | --- | --- |
| REWARD_4001 | 404 | 등급 정보 없음 (신규 회원) |
| REWARD_4002 | 403 | 조회 불가 회원 상태 |

`REWARD_4001` 은 아직 등급 산정이 되지 않은 신규 회원입니다. 클라이언트는 BASIC 으로 표시하세요.

## 주의사항

`baseAmount` 는 산정 시점 기준입니다. 이후 구매는 반영되지 않으며 다음 산정 때 포함됩니다. 화면에 "3월 2일 기준" 같은 안내를 함께 표시하세요.

주문 시 적용되는 적립률은 주문에 별도로 기록됩니다. 이 API 의 `rewardRate` 는 현재 값이며, 과거 주문의 적립률과 다를 수 있습니다.
""",
    },
    {
        "id": "DOC-175",
        "title": "선착순 쿠폰 발급 요건 정리",
        "department": "서비스기획팀",
        "type": "기획자와의 회의록",
        "project": "Project Nova",
        "date": "2026-03-11",
        "systems": ["MARS"],
        "features": ["쿠폰 지급"],
        "participants": ["강민재", "송지은"],
        "concepts": ["선착순", "동시성", "요구사항"],
        "body": """## 논의 배경

이벤트에서 선착순 쿠폰을 쓰고 싶다는 요청이 계속 있었습니다. Nova 1차에서 제외했던 항목입니다.

## 요구사항

- 수량이 정해진 쿠폰을 먼저 신청한 순서대로 지급
- 소진되면 즉시 마감 표시
- 남은 수량을 화면에 실시간 표시
- 고객당 1개

## 쟁점

**실시간 수량 표시**: 남은 수량을 정확히 보여주려면 매번 조회해야 합니다. 이벤트 시작 직후 수천 명이 동시에 화면을 보면 조회만으로 부하가 큽니다.

강민재님은 정확한 숫자보다 "얼마 남지 않음" 같은 단계 표시로도 충분하다고 했습니다.

**마감 후 요청**: 소진된 뒤에도 요청이 계속 들어옵니다. 이 요청들을 빠르게 거부해야 합니다.

**중복 신청**: 고객당 1개인데 동시에 여러 번 누르면 여러 개 받을 수 있습니다.

## 결정 사항

- 남은 수량은 구간으로 표시합니다 (충분 / 100개 미만 / 마감 임박 / 마감)
- 구간 정보는 10초 캐시합니다
- 소진 후에는 캐시된 마감 상태로 즉시 거부합니다
- 고객당 1개는 발급 참조키로 보장합니다

## 발급 순서

정확한 "선착순"을 보장할지 논의했습니다. 네트워크 지연 때문에 사용자가 먼저 눌렀다고 먼저 도착하지 않습니다.

서버 도착 순서를 기준으로 하고, 이를 사용자에게 명시합니다. "네트워크 상황에 따라 순서가 달라질 수 있습니다"

## 미확정

대기열을 둘지 정하지 않았습니다. 대량 트래픽이 예상되면 대기열이 필요하지만, 인프라가 추가됩니다. 첫 이벤트 규모를 보고 판단합니다.

## Action Item

- 강민재님: 첫 이벤트 예상 규모 산정
- 개발: 동시성 처리 방안 검토
""",
    },
    {
        "id": "DOC-176",
        "title": "선착순 발급 동시성 처리 검토",
        "department": "백엔드개발팀",
        "type": "개발자 회의록",
        "project": "Project Nova",
        "date": "2026-03-17",
        "systems": ["MARS"],
        "features": ["쿠폰 지급"],
        "participants": ["박준호", "김도윤"],
        "concepts": ["동시성", "선착순", "Redis"],
        "body": """## 논의 배경

선착순 쿠폰은 정해진 수량을 여러 명이 동시에 가져가는 구조입니다. 재고 차감과 비슷하지만 트래픽 집중도가 훨씬 높습니다.

## 검토안

**1. DB 조건부 갱신**

재고에서 쓴 방식입니다.

```sql
UPDATE coupon_quantity
SET issued = issued + 1
WHERE coupon_id = :id AND issued < total;
```

정확하지만 같은 행에 동시 갱신이 몰리면 락 경합이 심합니다. 초당 수천 건이면 대기가 길어집니다.

**2. Redis 카운터**

`DECR` 로 수량을 줄이고 0 미만이면 거부합니다. 매우 빠릅니다. 다만 Redis 장애 시 수량 관리가 불가능하고, 실제 발급(DB)과 카운터가 어긋날 수 있습니다.

**3. Redis 로 선별 후 DB 확정**

Redis 로 후보를 뽑고 DB 에서 실제 발급합니다. 빠르면서 최종 정합성은 DB 가 보장합니다.

## 쟁점

김도윤님은 3안에서 Redis 통과 후 DB 발급이 실패하는 경우를 물었습니다. 그러면 수량은 줄었는데 쿠폰은 안 나갑니다.

박준호님은 실패 시 Redis 카운터를 되돌리면 된다고 했습니다. 되돌리기가 실패하면 수량이 손실되지만, 발급 수가 초과되는 것보다는 낫습니다.

## 결정 사항

- **3안**으로 갑니다
- Redis `DECR` 후 DB 발급, 실패 시 `INCR` 로 복원
- 복원 실패는 로그와 알림. 자동 재시도하지 않음
- Redis 장애 시에는 선착순 발급 자체를 중단합니다. DB 만으로 처리하지 않습니다

DB 폴백을 두지 않는 이유는 장애 상황에서 락 경합으로 서비스 전체가 느려질 수 있기 때문입니다. 이벤트 하나를 위해 전체를 위험에 빠뜨리지 않습니다.

## 회원당 1개

Redis `SET key value NX` 로 회원별 발급 여부를 먼저 확인합니다. 이것도 실패 시 복원이 필요합니다.

순서는 회원 중복 확인 → 수량 차감 → DB 발급입니다. 역순으로 복원합니다.

## Action Item

- 박준호님: 설계 문서 작성
- 김도윤님: 복원 실패 시 대사 방법 검토
""",
    },
    {
        "id": "DOC-177",
        "title": "선착순 쿠폰 발급 설계",
        "department": "백엔드개발팀",
        "type": "비즈니스 기능 설계",
        "project": "Project Nova",
        "date": "2026-03-24",
        "systems": ["MARS"],
        "features": ["쿠폰 지급"],
        "participants": [],
        "concepts": ["선착순", "동시성", "보상 처리", "Edge Case"],
        "body": """## 배경

Redis 로 선별하고 DB 로 확정하는 구조입니다. 각 단계 실패 시 되돌리는 처리가 핵심입니다.

## Redis 키

```text
coupon:{couponId}:remaining    남은 수량 (정수)
coupon:{couponId}:issued:{memberId}   회원 발급 여부 (플래그)
```

TTL 은 이벤트 종료 + 1일로 둡니다.

## 발급 Flow

```text
1. SET coupon:{id}:issued:{memberId} 1 NX EX 86400
   실패(이미 존재) -> 이미 발급됨 (COUPON_2002)

2. DECR coupon:{id}:remaining
   결과 < 0 -> INCR 로 복원 후 소진 (COUPON_2003)

3. DB 에 coupon_issue 저장
   실패 -> INCR 복원, 회원 플래그 삭제, 오류 반환

4. 성공 응답
```

## 초기화

이벤트 시작 시 `remaining` 을 총 수량으로 설정합니다. 이미 키가 있으면 덮어쓰지 않습니다. 중복 초기화로 수량이 리셋되면 초과 발급됩니다.

```text
SET coupon:{id}:remaining {total} NX
```

## Edge Case

- **DB 저장 성공, 응답 유실**: 클라이언트가 재시도하면 1단계에서 이미 발급됨으로 걸립니다. 이때 기존 발급 정보를 반환합니다.
- **Redis 복원 실패**: 수량이 실제보다 적게 남습니다. 발급이 덜 나가는 방향이라 안전합니다. 알림만 보내고 이벤트 종료 후 대사합니다.
- **회원 플래그 삭제 실패**: 해당 회원이 재시도해도 이미 발급됨으로 나옵니다. 실제로는 발급받지 못한 상태입니다. 문의가 오면 수동 지급합니다.
- **Redis 장애**: 발급을 중단하고 "일시적으로 이용할 수 없습니다"를 반환합니다. DB 로 폴백하지 않습니다.
- **이벤트 종료 후 요청**: 쿠폰 정의의 발급 기간을 확인해 거부합니다. Redis 조회 전에 확인합니다.
- **수량 소진 후 취소된 발급**: 발급 취소 시 수량을 복원할지 정해야 합니다. 복원하면 이미 마감 표시를 본 사용자가 다시 받을 수 있어 혼란스럽습니다. 복원하지 않기로 했습니다.

## 대사

이벤트 종료 후 Redis 발급 수와 DB `coupon_issue` 건수를 비교합니다. 차이가 있으면 복원 실패 건입니다.

DB 가 기준이므로 DB 건수가 실제 발급 수입니다. Redis 차이는 손실된 수량으로 집계만 합니다.

## 남은 고민

수량 표시를 10초 캐시하는데, 마감 직전에는 캐시 때문에 "남음"으로 보이다가 요청 시 마감이 나옵니다. 사용자 경험이 좋지 않지만, 실시간 조회는 부하가 큽니다.

마감 임박 구간에서는 캐시 시간을 2초로 줄이는 방안을 검토 중입니다.
""",
    },
    {
        "id": "DOC-178",
        "title": "3월 등급 산정 결과가 일부 회원에서 누락",
        "department": "데이터팀",
        "type": "이슈 정리",
        "project": "Project Nova",
        "date": "2026-04-01",
        "systems": ["MARS"],
        "features": ["포인트"],
        "participants": [],
        "concepts": ["배치", "집계", "정합성"],
        "body": """## 현상

3월 2일 등급 산정 후 일부 회원의 등급이 갱신되지 않았습니다. 문의로 발견됐고 확인 결과 약 3만 명이 대상입니다.

## 분석

배치는 정상 종료했고 처리 건수는 445만 건입니다. 전체 활성 회원 수(448만)와 3만 건 차이가 납니다.

집계 쿼리를 확인했습니다.

```sql
SELECT member_id, SUM(paid_amount) AS purchase_amount
FROM order_summary
WHERE ordered_at >= :from AND ordered_at < :to
  AND status IN ('DELIVERED', 'PARTIAL_CANCELED')
GROUP BY member_id;
```

집계 대상이 주문이 있는 회원만입니다. 최근 6개월 주문이 없는 회원은 결과에 포함되지 않습니다.

이들은 구매액 0원이므로 BASIC 이어야 합니다. 기존에 SILVER 이상이던 회원이 갱신되지 않아 등급을 유지하고 있었습니다.

## 영향 범위

- 등급이 유지된 회원 약 3만 명
- 실제보다 높은 적립률이 적용됨

3월 한 달간 초과 적립된 포인트는 약 180만 포인트입니다.

## 판단

기술적으로는 오류지만 사용자에게는 유리하게 작용했습니다. 회수하지 않기로 했습니다.

기획팀과 협의한 결과 소급 적용은 하지 않고 4월 산정부터 정상화합니다.

## 조치

집계 결과에 없는 회원을 BASIC 으로 처리하도록 배치를 수정했습니다.

```text
집계 결과에 있는 회원  -> 결과대로 산정
집계 결과에 없는 회원  -> 구매액 0 으로 간주, BASIC
```

검증 조건도 수정했습니다. 기존에는 "결과 건수가 예상 회원 수의 90% 미만이면 중단"이었는데, 이 조건이 문제를 가렸습니다. 445만/448만 = 99.3% 이므로 통과했습니다.

이제 처리 대상을 집계 결과가 아니라 활성 회원 목록으로 잡습니다.

## 재발 방지

배치가 "몇 건 처리했는가"만 보고 "누구를 처리했는가"를 보지 않았습니다. 처리 대상 정의가 잘못되면 건수 검증으로는 잡히지 않습니다.

산정 후 등급별 회원 수 분포를 이전 달과 비교하는 검증을 추가했습니다. 분포가 크게 달라지면 알림이 갑니다.
""",
    },
    {
        "id": "DOC-179",
        "title": "리워드 2차 개선 진행 현황",
        "department": "프로젝트관리팀",
        "type": "프로젝트 진행상황",
        "project": "Project Nova",
        "date": "2026-04-08",
        "systems": ["MARS"],
        "features": ["포인트", "쿠폰 지급", "리워드 지급"],
        "participants": ["임채원", "박준호"],
        "concepts": ["일정", "Risk"],
        "body": """## 범위

Nova 1차 이후 남은 항목과 신규 요구사항입니다.

- 회원 등급별 적립률
- 선착순 쿠폰 발급
- 리뷰 작성 리워드
- 쿠폰 사용 조건 데이터화

## 완료

- 등급 산정 배치 및 테이블
- 회원 등급 조회 API
- 주문에 적용 요율 기록
- 3월 산정 누락 이슈 대응

## 진행 중

- 선착순 발급 구현 (60%)
- 리뷰 리워드 지급 연동

## 대기

- 쿠폰 사용 조건 데이터화
- 등급 하락 안내 (정책 미확정)

## Risk

박준호님이 지적한 사항입니다. 선착순 발급은 Redis 의존도가 높은데, 장애 시 발급을 중단하는 설계입니다. 이벤트 당일 Redis 에 문제가 생기면 이벤트 자체가 실패합니다.

Redis 이중화 상태를 DevOps 팀과 확인이 필요합니다.

첫 선착순 이벤트 규모가 확정되지 않았습니다. 예상 트래픽을 모르면 부하 테스트 기준을 잡을 수 없습니다. 기획팀에 재요청했습니다.

등급 하락 안내는 두 달째 미확정입니다. 다음 산정(5월)까지 정해지지 않으면 안내 없이 하락하는 상태가 계속됩니다.

## 다음

- 4월 중 선착순 부하 테스트
- 5월 초 첫 선착순 이벤트 목표
- 쿠폰 조건 데이터화는 6월 이후
""",
    },
    {
        "id": "DOC-180",
        "title": "이벤트 쿠폰 등록 및 발급 절차",
        "department": "서비스기획팀",
        "type": "매뉴얼 / 정의서",
        "project": None,
        "date": "2026-04-14",
        "systems": ["MARS", "AIMS"],
        "features": ["쿠폰 지급"],
        "participants": [],
        "concepts": ["운영", "쿠폰", "체크리스트"],
        "body": """## 쿠폰 등록

AIMS 운영 > 쿠폰 관리 > 신규 등록에서 진행합니다.

입력 항목:

| 항목 | 설명 |
| --- | --- |
| 쿠폰 코드 | 영문 대문자와 하이픈. 등록 후 변경 불가 |
| 쿠폰명 | 사용자에게 보이는 이름 |
| 할인 유형 | 정액 / 정률 |
| 할인 값 | 금액 또는 비율 |
| 최소 주문 금액 | |
| 최대 할인 금액 | 정률일 때 필수 |
| 유효일수 | 발급일로부터 |
| 발급 기간 | 시작일 ~ 종료일 |

## 발급 방식

**대량 발급**: 대상 고객 목록을 올려 일괄 발급합니다. 1,000명 단위로 나뉘어 처리되며, 이미 받은 고객은 자동으로 건너뜁니다.

**선착순 발급**: 수량을 지정하면 사용자가 직접 신청해 받습니다. 등록 시 총 수량을 반드시 입력하세요.

**자동 발급**: 가입, 첫 구매 등 조건에 따라 자동 지급됩니다. 개발 작업이 필요하므로 사전에 요청하세요.

## 등록 전 체크리스트

- [ ] 쿠폰 코드가 기존과 중복되지 않는가
- [ ] 정률 할인에 최대 할인 금액을 넣었는가
- [ ] 유효일수가 이벤트 기간과 맞는가
- [ ] 최소 주문 금액이 할인액보다 큰가
- [ ] 선착순이면 수량을 입력했는가

정률 할인에 최대 금액을 빼먹으면 고가 상품에서 할인액이 무한정 커집니다. 실제로 사고가 있었습니다.

## 발급 후 확인

- 발급 건수가 예상과 맞는지 확인
- 샘플 계정으로 실제 사용이 되는지 확인
- 대량 발급은 실패 건수를 반드시 확인

## 회수

잘못 발급한 쿠폰은 회수할 수 있습니다. 이미 사용된 쿠폰은 회수되지 않습니다.

대량 회수는 데이터팀에 요청하세요. 화면에서 하나씩 처리하면 중간 실패 시 상태 파악이 어렵습니다.

## 주의

- 선착순 수량은 등록 후 늘릴 수 없습니다. 늘리려면 새 쿠폰을 만드세요
- 발급 기간이 시작된 뒤 할인 조건을 바꾸지 마세요. 이미 발급된 쿠폰에는 반영되지 않습니다
""",
    },
]
