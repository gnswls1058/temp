"""DOC-256 ~ DOC-270 : 인증 2차 개선, 비회원 주문, 온보딩 (2025~2026)."""

DOCUMENTS = [
    {
        "id": "DOC-256",
        "title": "토큰 서명 키 회전 저장소 도입 논의",
        "department": "플랫폼개발팀",
        "type": "개발자 회의록",
        "project": "Project Echo",
        "date": "2026-02-12",
        "systems": ["BlueGate"],
        "features": ["인증"],
        "participants": ["조아름", "김도윤"],
        "concepts": ["키 관리", "JWT", "보안"],
        "body": """## 논의 배경

2024년 Echo 킥오프에서 미뤄둔 항목입니다. 토큰 서명 키가 애플리케이션 설정에 들어 있어 배포 없이 교체할 수 없습니다.

조아름님이 연간 보안 점검에서 다시 지적했습니다.

## 현황

- 서명 키는 환경 변수로 주입
- 2024년 이후 한 번도 교체하지 않음
- 유출 시 전 사용자 토큰을 위조할 수 있음

## 요구사항

- 배포 없이 키를 교체할 수 있어야 합니다
- 교체 중에도 이전 키로 서명된 토큰이 유효해야 합니다
- 키 이력을 남겨야 합니다

## 검토안

**1. 외부 키 관리 서비스**

가장 안전합니다. 키가 애플리케이션 밖에 있고 접근 이력이 남습니다. 다만 매 검증마다 외부 호출이 생기면 느립니다. 키를 캐시하면 회전 반영이 늦습니다.

**2. DB 저장 + 캐시**

키를 DB 에 두고 애플리케이션이 주기적으로 읽습니다. 구현이 단순하고 회전 반영도 조절 가능합니다. DB 가 유출되면 키도 함께 유출됩니다.

**3. 혼합**

키 자체는 외부 서비스에 두고, 어떤 키가 활성인지만 DB 로 관리합니다.

## 쟁점

김도윤님은 3안이 복잡하다고 봤습니다. 외부 호출은 여전히 필요합니다.

조아름님은 DB 에 평문으로 두는 것은 받아들이기 어렵다고 했습니다. 다만 마스터 키로 암호화해서 저장하면 절충이 가능합니다.

## 결정 사항

- 키를 **암호화해서 DB 에 저장**합니다. 마스터 키만 환경 변수로 둡니다
- 애플리케이션은 5분마다 활성 키를 갱신합니다
- 회전 시 이전 키를 즉시 폐기하지 않고 "검증 전용"으로 유지합니다
- 검증 전용 기간은 Access Token 만료(30분)의 두 배인 1시간

## 키 상태

```text
ACTIVE      서명과 검증에 사용
VERIFY_ONLY 검증만 사용 (회전 직후)
RETIRED     사용하지 않음
```

## 회전 주기

6개월로 정했습니다. 자동 회전은 하지 않고 운영자가 수동으로 실행합니다.

자동 회전은 문제가 생겼을 때 원인 파악이 어렵습니다. 우선 수동으로 시작하고 안정화되면 자동화를 검토합니다.

## Action Item

- 김도윤님: 키 저장소 설계 및 구현
- 조아름님: 마스터 키 보관 방식 확정
""",
    },
    {
        "id": "DOC-257",
        "title": "토큰 재발급 API v2 명세",
        "department": "플랫폼개발팀",
        "type": "API 명세서",
        "project": "Project Echo",
        "date": "2026-02-27",
        "systems": ["BlueGate"],
        "features": ["인증"],
        "participants": [],
        "concepts": ["Refresh Token", "회전", "버전 변경"],
        "body": """## 변경 개요

v1 은 Refresh Token 을 재사용했습니다. v2 는 회전 방식이며 매번 새 토큰을 발급합니다.

| 항목 | v1 | v2 |
| --- | --- | --- |
| Refresh Token | 재사용 | 매번 갱신 |
| 응답에 Refresh 포함 | 없음 | 포함 |
| 탈취 감지 | 불가 | 유예 초과 재사용 시 감지 |
| 키 버전 | 없음 | 토큰 헤더에 포함 |

v1 은 2026년 8월 종료합니다.

## Endpoint

```http
POST /auth/token/refresh
```

## Request

```json
{
  "refreshToken": "rt_8f2c1d...",
  "deviceId": "device-7f21"
}
```

## Response

```json
{
  "accessToken": "eyJhbGciOi...",
  "refreshToken": "rt_9a3e2f...",
  "expiresIn": 1800
}
```

**응답의 `refreshToken` 을 반드시 저장하세요.** 이전 토큰은 유예 시간(60초) 후 무효가 됩니다.

## 유예 기간

같은 Refresh Token 으로 60초 안에 다시 요청하면 같은 결과를 반환합니다. 앱에서 여러 요청이 동시에 재발급을 시도하는 상황을 위한 것입니다.

60초가 지난 뒤 이전 토큰을 사용하면 탈취로 판정하고 해당 계정의 모든 토큰을 폐기합니다.

## Error Code

| 코드 | HTTP | 설명 |
| --- | --- | --- |
| AUTH_2001 | 401 | 형식 오류 |
| AUTH_2002 | 401 | 만료 |
| AUTH_2003 | 401 | 폐기됨 (로그아웃) |
| AUTH_2004 | 409 | deviceId 불일치 |
| AUTH_2005 | 401 | 탈취 판정으로 전체 폐기됨 |

`AUTH_2005` 를 받으면 저장된 토큰을 모두 지우고 로그인 화면으로 보내세요. 사용자에게는 "보안을 위해 다시 로그인해 주세요" 정도로 안내합니다.

## 키 회전 대응

Access Token 헤더에 키 버전(`kid`)이 포함됩니다. 클라이언트는 이 값을 볼 필요가 없습니다.

서버가 키를 회전해도 이전 키로 서명된 토큰은 1시간 동안 유효합니다. 클라이언트 조치는 필요 없습니다.

## 주의사항

재발급 요청을 동시에 여러 개 보내지 마세요. 하나의 요청으로 모으고 결과를 공유하는 구조를 권장합니다. 유예 기간이 대부분 흡수하지만 완전하지 않습니다.
""",
    },
    {
        "id": "DOC-258",
        "title": "[리뷰] 서명 키 회전 적용",
        "department": "플랫폼개발팀",
        "type": "코드리뷰",
        "project": "Project Echo",
        "date": "2026-03-06",
        "systems": ["BlueGate"],
        "features": ["인증"],
        "participants": ["김도윤", "조아름"],
        "concepts": ["키 관리", "캐시", "보안"],
        "body": """## 현재 구현

키를 DB 에서 읽어 캐시하고, 서명 시 활성 키를, 검증 시 키 버전에 맞는 키를 씁니다.

```java
public String sign(Claims claims) {
    var key = keyCache.getActiveKey();
    return jwtBuilder.setHeaderParam("kid", key.version())
                     .signWith(key.material())
                     .compact();
}
```

## 리뷰 의견

조아름님: 캐시가 5분 주기로 갱신되는데, 키가 유출되어 긴급히 폐기해야 하는 상황에서 5분을 기다려야 합니다.

김도윤님: 긴급 폐기는 별도 경로를 두겠습니다. 폐기 신호를 받으면 캐시를 즉시 비웁니다.

조아름님: 복호화한 키를 메모리에 얼마나 들고 있나요. 힙 덤프에 남을 수 있습니다.

김도윤님: 현재는 캐시에 계속 있습니다. 완전히 막기는 어렵지만 `byte[]` 로 다루고 교체 시 명시적으로 0으로 채우겠습니다.

조아름님: 검증 시 `kid` 를 신뢰하는 구조입니다. 공격자가 `kid` 를 조작해 RETIRED 키를 지정하면 어떻게 되나요.

김도윤님: RETIRED 키는 캐시에 로드하지 않습니다. 없는 키 버전이면 검증 실패입니다.

조아름님: `kid` 가 없는 토큰은요. v1 시절 발급된 토큰입니다.

김도윤님: 기본 키로 검증합니다. v1 종료 시점(8월)에 이 경로를 제거해야 합니다. TODO 를 남기겠습니다.

조아름님: 키 조회 실패 시 동작이 없습니다. DB 가 죽으면 캐시가 갱신되지 않는데, 캐시된 값으로 계속 동작하나요.

김도윤님: 그렇습니다. 갱신 실패는 로그만 남기고 기존 캐시를 유지합니다. 인증이 멈추는 것보다 낫습니다.

## 수정 사항

- 긴급 폐기 시 캐시 즉시 무효화 경로 추가
- 키 자료를 `byte[]` 로 다루고 교체 시 초기화
- `kid` 없는 토큰 처리에 종료 예정 TODO 명시
- 캐시 갱신 실패 시 기존 값 유지 및 경고 로그

## 남은 확인

키 회전을 실제로 수행하는 절차 문서가 없습니다. 누가 언제 어떻게 실행하는지 정리가 필요합니다. 운영 매뉴얼에 추가하기로 했습니다.
""",
    },
    {
        "id": "DOC-259",
        "title": "signing_key 테이블 설계",
        "department": "플랫폼개발팀",
        "type": "DB 테이블 설계안",
        "project": "Project Echo",
        "date": "2026-03-02",
        "systems": ["BlueGate"],
        "features": ["인증"],
        "participants": [],
        "concepts": ["키 관리", "암호화", "이력"],
        "body": """## 테이블

| 컬럼 | 타입 | 제약 | 설명 |
| --- | --- | --- | --- |
| id | BIGINT | PK | |
| version | VARCHAR(16) | UNIQUE | 토큰 헤더의 kid |
| algorithm | VARCHAR(16) | NOT NULL | HS256 등 |
| encrypted_material | VARBINARY(512) | NOT NULL | 마스터 키로 암호화된 키 |
| status | VARCHAR(16) | NOT NULL | ACTIVE / VERIFY_ONLY / RETIRED |
| activated_at | DATETIME | NOT NULL | |
| retired_at | DATETIME | NULL | |
| created_by | BIGINT | NOT NULL | 생성한 운영자 |

```sql
CREATE UNIQUE INDEX uk_signing_key_version ON signing_key (version);
CREATE INDEX idx_signing_key_status ON signing_key (status, activated_at DESC);
```

## ACTIVE 는 하나만

동시에 두 개의 활성 키가 있으면 안 됩니다. DB 제약으로 강제하기 어려워 애플리케이션에서 보장합니다.

회전은 트랜잭션 안에서 처리합니다.

```sql
UPDATE signing_key SET status = 'VERIFY_ONLY' WHERE status = 'ACTIVE';
INSERT INTO signing_key (version, ..., status) VALUES (..., 'ACTIVE');
```

기동 시 `ACTIVE` 가 하나가 아니면 오류로 종료합니다. 잘못된 상태로 서비스하지 않습니다.

## 키 자료 암호화

마스터 키로 암호화해서 저장합니다. 마스터 키는 환경 변수로 주입되며 DB 에 없습니다.

DB 만 유출되면 서명 키를 복원할 수 없습니다. 마스터 키까지 함께 유출되어야 합니다.

## 상태 전이

```text
(생성) -> ACTIVE
ACTIVE -> VERIFY_ONLY   (새 키로 회전)
VERIFY_ONLY -> RETIRED  (유예 1시간 경과)
```

`RETIRED` 로 바뀌면 캐시에서 제거되고 해당 키로 서명된 토큰은 검증에 실패합니다.

## 긴급 폐기

유출이 의심되면 유예 없이 바로 `RETIRED` 로 보냅니다. 해당 키로 서명된 모든 토큰이 즉시 무효가 되어 전 사용자가 로그아웃됩니다.

영향이 크므로 SYSTEM_ADMIN 권한과 사유 입력이 필요합니다.

## 이력

`created_by` 와 상태 변경을 감사 로그에 남깁니다. 키 관리는 가장 민감한 조작이므로 누가 언제 했는지 반드시 추적할 수 있어야 합니다.

## 보관

`RETIRED` 키도 삭제하지 않습니다. 과거 토큰의 서명을 검증해야 하는 조사 상황이 있을 수 있습니다.

다만 3년이 지나면 삭제합니다.
""",
    },
    {
        "id": "DOC-260",
        "title": "키 회전 직후 일부 요청에서 인증 실패",
        "department": "플랫폼개발팀",
        "type": "이슈 정리",
        "project": "Project Echo",
        "date": "2026-03-20",
        "systems": ["BlueGate"],
        "features": ["인증"],
        "participants": [],
        "concepts": ["키 관리", "캐시", "배포"],
        "body": """## 현상

3월 19일 스테이징에서 첫 키 회전을 수행했습니다. 회전 직후 약 4분간 인증 실패가 발생했습니다.

## 로그

```text
14:02:11 key.rotate version=v2 activated
14:02:14 token.verify failed kid=v2 reason=UNKNOWN_KEY_VERSION
14:02:14 token.verify failed kid=v2 reason=UNKNOWN_KEY_VERSION
14:06:33 key.cache.refresh loaded=2
14:06:34 token.verify ok kid=v2
```

새 키로 서명된 토큰을 다른 인스턴스가 검증하지 못했습니다.

## 원인

키 캐시가 5분 주기로 갱신됩니다. 회전을 수행한 인스턴스는 즉시 새 키를 갖지만, 다른 인스턴스는 최대 5분 뒤에 알게 됩니다.

그 사이 회전한 인스턴스가 발급한 토큰이 다른 인스턴스로 라우팅되면 알 수 없는 키 버전이 됩니다.

설계 시 "이전 키로 서명된 토큰이 유효해야 한다"는 것만 고려했고, "새 키를 아직 모르는 인스턴스"는 생각하지 못했습니다.

## 조치

**즉시**: 회전 후 캐시 갱신 주기(5분)만큼 기다린 뒤 새 키로 서명을 시작하도록 바꿉니다.

```text
1. 새 키를 PENDING 상태로 저장
2. 캐시 갱신 주기 + 여유(총 10분) 대기
3. 모든 인스턴스가 키를 알게 됨
4. PENDING -> ACTIVE 전환
```

`PENDING` 상태를 추가했습니다. 검증에는 쓰이지만 서명에는 쓰이지 않습니다.

**대안 검토**: 캐시를 쓰지 않고 매번 DB 조회하는 방법도 있지만 검증 빈도가 매우 높아 부담이 큽니다.

키 변경 시 인스턴스에 알림을 보내는 방법도 있으나 인프라가 추가됩니다. 회전이 6개월에 한 번이라 10분 대기로 충분합니다.

## 결과

수정 후 스테이징에서 다시 회전했고 실패가 없었습니다.

## 배운 점

여러 인스턴스가 공유 상태를 캐시할 때, 상태를 바꾸는 쪽과 읽는 쪽의 시차를 항상 고려해야 합니다.

"이전 것을 얼마나 유지할까"는 생각했는데 "새것을 언제부터 쓸까"는 놓쳤습니다. 양방향으로 봐야 합니다.

## 운영 배포 전 확인

운영 회전 절차 문서에 대기 시간을 명시했습니다. 회전 실행 후 10분간 서명 전환을 기다린다는 것을 절차에 넣었습니다.
""",
    },
    {
        "id": "DOC-261",
        "title": "신규 입사자 개발 환경 준비",
        "department": "백엔드개발팀",
        "type": "매뉴얼 / 정의서",
        "project": None,
        "date": "2026-01-16",
        "systems": ["NEXUS", "ORBIT", "MARS", "BlueGate", "AIMS"],
        "features": [],
        "participants": [],
        "concepts": ["온보딩", "개발 환경"],
        "body": """## 첫날

1. 사내 계정 생성 (인사팀에서 안내)
2. VPN 신청
3. 코드 저장소 권한 요청
4. 개발 도구 설치

권한 요청은 팀 리드가 승인합니다. 처리에 반나절 정도 걸립니다.

## 개발 환경

로컬 환경 설정은 별도 가이드 문서를 따르세요. JDK, Docker, IDE 설정이 정리되어 있습니다.

첫 실행에서 막히면 혼자 오래 붙잡지 말고 물어보세요. 대부분 이미 겪은 문제입니다.

## 서비스 구조 파악

전체 구조는 아키텍처 개요 문서를 보세요. 처음부터 전부 이해할 필요는 없습니다.

권장 순서입니다.

1. 서비스 5개가 각각 무엇을 담당하는지
2. 자기 팀이 주로 다루는 서비스 하나를 깊게
3. 그 서비스가 다른 서비스를 어떻게 호출하는지

## 첫 작업

첫 PR 은 작은 것으로 시작합니다. 문서 수정이나 간단한 버그 수정을 권장합니다.

목적은 기능 구현이 아니라 개발-리뷰-배포 흐름을 한 번 겪어보는 것입니다.

## 읽어두면 좋은 문서

| 문서 | 이유 |
| --- | --- |
| 코드리뷰 규칙 | PR 을 올리기 전에 |
| PR 작성 규칙 | 같은 이유 |
| Git Branch 전략 | 브랜치를 만들기 전에 |
| 배포 규칙 | 첫 배포 전에 |
| 개인정보 취급 주의사항 | 코드를 쓰기 전에 |
| 서비스 아키텍처 개요 | 전체 그림 |

## 접근 권한

기본으로 부여되는 것과 요청해야 하는 것이 있습니다.

| 대상 | 기본 |
| --- | --- |
| 코드 저장소 | 요청 후 부여 |
| 스테이징 | 기본 부여 |
| 로그 대시보드 | 기본 부여 |
| AIMS (마스킹 조회) | 요청 후 부여 |
| AIMS (개인정보 상세) | 별도 승인 필요 |
| 운영 DB | 건별 요청 |

개인정보에 접근하는 권한은 업무상 필요할 때만 요청하세요.

## 물어보기

모르는 것을 물어보는 것이 빠릅니다. 30분 이상 막히면 물어보세요.

질문할 때는 무엇을 시도했고 어떤 결과가 나왔는지 함께 알려주면 답이 빨라집니다.
""",
    },
    {
        "id": "DOC-262",
        "title": "서비스 아키텍처 개요",
        "department": "백엔드개발팀",
        "type": "매뉴얼 / 정의서",
        "project": None,
        "date": "2025-02-19",
        "systems": ["NEXUS", "ORBIT", "MARS", "BlueGate", "AIMS"],
        "features": [],
        "participants": [],
        "concepts": ["아키텍처", "서비스 경계"],
        "body": """## 서비스 구성

| 서비스 | 담당 | 주요 데이터 |
| --- | --- | --- |
| NEXUS | 회원, 프로필, 계정 상태 | member, member_auth |
| ORBIT | 상품, 주문, 결제, 배송 | product, orders, payment, shipping |
| MARS | 적립금, 쿠폰, 리워드 | reward_history, coupon |
| BlueGate | 인증, 토큰, 접근 제어 | signing_key, token |
| AIMS | 운영자 도구 | admin_user, audit_log |

## 호출 관계

```text
Client -> BlueGate  (로그인, 토큰)
Client -> NEXUS     (프로필)
Client -> ORBIT     (상품, 주문, 결제)
Client -> MARS      (포인트 조회)

ORBIT  -> NEXUS     (회원 확인)
ORBIT  -> MARS      (포인트 사용/회수)
MARS   -> NEXUS     (회원 상태 확인)
BlueGate -> NEXUS   (자격 검증)
AIMS   -> 전부      (운영 조회 및 조치)
```

순환 호출은 없습니다. NEXUS 는 다른 서비스를 호출하지 않습니다.

## 경계 원칙

**데이터 소유**: 각 서비스가 자기 데이터를 소유합니다. 다른 서비스의 DB 를 직접 조회하지 않습니다.

**복제 허용**: 조회 성능을 위해 값을 복제할 수 있습니다. 단 "그 시점 값"이 의미 있는 경우에만 복제합니다.

예: 주문에 상품명을 복사합니다. 상품명이 나중에 바뀌어도 주문 시점 이름을 보여주는 것이 맞습니다.

**이벤트와 동기 호출**: 결과를 알아야 다음 단계로 갈 수 있으면 동기 호출, 알려주기만 하면 되면 이벤트입니다.

이벤트만으로 정합성을 보장하지 않습니다. 대조 배치가 안전망입니다.

## 인증

모든 요청은 BlueGate 가 발급한 토큰을 사용합니다.

- 사용자 토큰: 클라이언트용
- 서비스 토큰: 서비스 간 호출용

서비스 토큰으로 사용자 API 를 호출할 수 없고, 반대도 마찬가지입니다.

## 데이터 저장소

- 주 저장소: MySQL (서비스별 분리)
- 캐시: Redis (서비스별 논리 분리)
- 분석: 별도 저장소로 일 1회 적재

서비스가 다른 서비스의 DB 에 접근하는 경로는 없습니다.

## 배포

서비스별로 독립 배포합니다. 다만 API 가 바뀌면 소비자에게 미리 공유해야 합니다.

하위 호환은 최소 6개월 유지합니다.

## 알아두면 좋은 것

- 결제와 적립금은 항상 함께 움직입니다. 한쪽만 보면 상태를 오해합니다
- 정합성 대조 배치가 여러 곳에 있습니다. 정합성이 의심되면 정합성 대조 결과를 먼저 보세요
- `PENDING` 상태는 "아직 모른다"는 뜻입니다. 실패가 아닙니다
""",
    },
    {
        "id": "DOC-263",
        "title": "서비스 경계 재정의 논의",
        "department": "백엔드개발팀",
        "type": "개발자 회의록",
        "project": None,
        "date": "2026-07-07",
        "systems": ["NEXUS", "ORBIT", "MARS"],
        "features": [],
        "participants": ["김도윤", "노태윤", "박준호"],
        "concepts": ["서비스 경계", "기술 부채", "확장성"],
        "body": """## 논의 배경

ORBIT 이 계속 커지고 있습니다. 상품, 주문, 결제, 배송, 리뷰, 문의가 모두 들어 있습니다. 배포 단위가 커서 작은 변경에도 전체를 배포합니다.

## 현황

노태윤님이 정리한 ORBIT 코드 규모입니다.

| 도메인 | 비중 |
| --- | --- |
| 주문/결제 | 38% |
| 상품/카테고리 | 24% |
| 배송 | 16% |
| 리뷰 | 12% |
| 문의 | 10% |

## 분리 후보

**리뷰와 문의**: 주문과 느슨하게 연결되어 있습니다. 리뷰는 주문 상품을 참조하고 문의는 주문을 참조하지만, 그 외에는 독립적입니다.

**배송**: 주문과 강하게 연결됩니다. 배송 상태가 주문 상태에 반영됩니다.

**상품**: 주문이 상품을 참조하지만 반대는 없습니다. 분리 가능해 보입니다.

## 쟁점

김도윤님은 분리 자체가 목적이 되면 안 된다고 했습니다. 서비스를 나누면 네트워크 호출이 늘고 트랜잭션이 깨집니다.

박준호님은 MARS 분리 경험을 이야기했습니다. 포인트를 분리한 뒤 정합성 문제가 계속 나왔고, 대사 배치를 여러 개 만들어야 했습니다.

노태윤님은 배포 단위 문제는 인정하지만, 지금 겪는 불편이 분리 비용보다 크지 않다고 봤습니다.

## 결정 사항

**당장 분리하지 않습니다.**

대신 다음을 합니다.

- ORBIT 내부에서 도메인 경계를 명확히 합니다. 패키지 구조를 도메인 단위로 정리하고 도메인 간 직접 참조를 줄입니다
- 도메인 간 호출을 인터페이스로 추상화합니다. 나중에 분리할 때 그 지점이 경계가 됩니다
- 리뷰와 문의를 우선 정리 대상으로 삼습니다

## 판단 기준

분리를 다시 검토할 조건을 정했습니다.

- 특정 도메인의 배포 빈도가 다른 도메인의 3배를 넘을 때
- 특정 도메인의 부하 특성이 크게 달라 독립 확장이 필요할 때
- 팀이 나뉘어 같은 코드를 자주 충돌할 때

지금은 어느 조건도 해당하지 않습니다.

## Action Item

- 노태윤님: 리뷰/문의 도메인 패키지 정리
- 김도윤님: 도메인 간 인터페이스 정의
""",
    },
    {
        "id": "DOC-264",
        "title": "비회원 주문 도입 검토",
        "department": "서비스기획팀",
        "type": "기획자와의 회의록",
        "project": None,
        "date": "2026-05-28",
        "systems": ["ORBIT", "NEXUS"],
        "features": ["주문", "회원가입"],
        "participants": ["강민재", "이서연"],
        "concepts": ["비회원", "개인정보", "요구사항"],
        "body": """## 논의 배경

가입 없이 주문할 수 있게 해달라는 요청이 있습니다. 가입 단계에서 이탈하는 사용자를 잡기 위함입니다.

## 기획 요구사항

- 가입 없이 주문과 결제
- 주문번호와 연락처로 주문 조회
- 나중에 가입하면 비회원 주문을 연결

## 개발 관점

이서연님이 정리한 문제입니다.

**고객 ID 의존**: 주문, 결제, 배송이 모두 `member_id` 를 필수로 요구합니다. null 을 허용하면 여러 곳에서 분기가 생깁니다.

**포인트와 쿠폰**: 비회원은 적립과 사용이 불가능합니다. 주문 흐름에서 이 부분을 건너뛰어야 합니다.

**개인정보**: 고객 정보 없이 주문하려면 이름, 연락처, 주소를 주문에 직접 저장해야 합니다. 보관 기간과 삭제 정책이 필요합니다.

**주문 조회**: 로그인이 없으므로 다른 인증 수단이 필요합니다. 주문번호 + 연락처 조합은 추측 가능성이 있습니다.

## 검토안

**1. 완전 비회원**

고객 데이터를 만들지 않습니다. 구현 부담이 큽니다.

**2. 임시 고객 생성**

내부적으로 고객을 만들되 사용자에게는 노출하지 않습니다. 기존 구조를 거의 그대로 쓸 수 있습니다.

## 쟁점

강민재님은 2안이 개인정보를 더 많이 남기는 것 아니냐고 물었습니다.

이서연님은 어차피 주문에 개인정보가 저장되므로 총량은 같다고 답했습니다. 오히려 고객 테이블의 삭제 정책을 그대로 적용할 수 있어 관리가 낫습니다.

## 결정 사항

**2안(임시 고객)으로 갑니다.**

- 고객 상태에 `GUEST` 를 추가합니다
- 로그인할 수 없고 비밀번호가 없습니다
- 포인트와 쿠폰을 사용할 수 없습니다
- 주문 완료 후 90일에 개인정보를 삭제합니다

## 주문 조회 인증

주문번호와 연락처만으로는 부족합니다. 주문 시 조회용 비밀번호를 받거나, 연락처로 인증번호를 보내는 방식이 필요합니다.

인증번호 방식으로 정했습니다. 비밀번호를 하나 더 만들게 하는 것은 부담입니다.

## 가입 시 연결

같은 연락처로 가입하면 비회원 주문을 연결할지 물어봅니다. 자동 연결은 하지 않습니다. 연락처를 물려받은 경우가 있을 수 있습니다.

## 미확정

비회원 주문의 취소와 환불 처리를 정하지 못했습니다. 환불 계좌를 받아야 하는데 비회원은 계좌 정보를 저장할 곳이 없습니다.

다음 회의에서 다룹니다.
""",
    },
    {
        "id": "DOC-265",
        "title": "비회원 주문 조회 API 명세",
        "department": "백엔드개발팀",
        "type": "API 명세서",
        "project": None,
        "date": "2026-06-18",
        "systems": ["ORBIT", "NEXUS"],
        "features": ["주문"],
        "participants": [],
        "concepts": ["비회원", "인증", "Rate Limit"],
        "body": """## 흐름

비회원은 로그인할 수 없으므로 조회 시마다 인증합니다.

```text
1. 주문번호 + 연락처 입력
2. 인증번호 발송
3. 인증번호 확인 -> 임시 조회 토큰 발급
4. 토큰으로 주문 조회
```

## 1. 인증번호 요청

```http
POST /guest-orders/verification
```

```json
{
  "orderNumber": "ORD-2026-0618-0031",
  "phone": "010-0000-0000"
}
```

응답:

```json
{
  "sent": true,
  "expiresIn": 300
}
```

주문번호와 연락처가 일치하지 않아도 `sent: true` 를 반환합니다. 주문 존재 여부를 노출하지 않기 위함입니다. 실제 발송은 일치할 때만 합니다.

## 2. 인증번호 확인

```http
POST /guest-orders/verification/confirm
```

```json
{
  "orderNumber": "ORD-2026-0618-0031",
  "phone": "010-0000-0000",
  "code": "482913"
}
```

응답:

```json
{
  "accessToken": "gt_...",
  "expiresIn": 1800
}
```

이 토큰은 해당 주문 하나만 조회할 수 있습니다. 다른 주문에는 쓸 수 없습니다.

## 3. 주문 조회

```http
GET /guest-orders/{orderNumber}
Authorization: Bearer {guestToken}
```

응답은 회원 주문 조회와 동일한 형식입니다. 단 포인트 관련 필드는 없습니다.

## 제한

| 항목 | 값 |
| --- | --- |
| 인증번호 발송 | 주문당 시간당 3회 |
| 인증 시도 | 발송당 5회 |
| 인증번호 유효 | 5분 |
| 조회 토큰 유효 | 30분 |
| IP 기준 발송 | 시간당 10회 |

## Error Code

| 코드 | HTTP | 설명 |
| --- | --- | --- |
| GUEST_1001 | 400 | 형식 오류 |
| GUEST_1002 | 401 | 인증번호 불일치 |
| GUEST_1003 | 410 | 인증번호 만료 |
| GUEST_1004 | 429 | 시도 횟수 초과 |
| GUEST_1005 | 401 | 조회 토큰이 다른 주문용 |

## 주의사항

인증번호 불일치와 주문 없음을 구분하지 않습니다. 둘 다 `GUEST_1002` 입니다. 주문번호를 추측해 존재를 확인하는 것을 막습니다.

조회 토큰은 주문 단위입니다. 여러 주문을 조회하려면 각각 인증해야 합니다.
""",
    },
    {
        "id": "DOC-266",
        "title": "비회원 주문 데이터 처리 방안",
        "department": "백엔드개발팀",
        "type": "DB 테이블 설계안",
        "project": None,
        "date": "2026-06-12",
        "systems": ["NEXUS", "ORBIT"],
        "features": ["주문", "회원가입"],
        "participants": [],
        "concepts": ["비회원", "개인정보", "보관 기간"],
        "body": """## 방침

비회원도 내부적으로 회원 행을 만듭니다. 기존 구조를 재사용하기 위함입니다.

## member 변경

`status` 에 `GUEST` 를 추가합니다.

| status | 로그인 | 주문 | 포인트 |
| --- | --- | --- | --- |
| GUEST | 불가 | 가능 | 불가 |

`email` 은 비회원에게 없을 수 있으므로 nullable 로 바꿉니다. 대신 `phone_hash` 를 필수로 둡니다.

기존 unique 제약이 `email` 에 걸려 있어 null 을 여러 개 허용해야 합니다. MySQL 은 null 을 중복으로 보지 않으므로 그대로 동작합니다.

## guest_order_contact

비회원 주문의 연락처를 별도 관리합니다.

| 컬럼 | 설명 |
| --- | --- |
| id | PK |
| order_id | FK |
| member_id | GUEST 회원 ID |
| phone_encrypted | 암호화된 연락처 |
| phone_hash | 조회용 해시 |
| email_encrypted | 선택 |
| created_at | |
| deleted_at | 개인정보 삭제 시각 |

```sql
CREATE INDEX idx_guest_contact_hash ON guest_order_contact (phone_hash, order_id);
CREATE INDEX idx_guest_contact_delete ON guest_order_contact (deleted_at, created_at);
```

## 보관 기간

주문 완료 후 90일에 개인정보를 삭제합니다.

| 데이터 | 처리 |
| --- | --- |
| 연락처, 이메일 | 90일 후 삭제 |
| 배송지 | 배송 완료 후 6개월 마스킹 |
| 주문/결제 내역 | 5년 (법정) |
| GUEST 회원 행 | 90일 후 개인정보 제거, 행은 유지 |

행을 유지하는 이유는 주문이 참조하기 때문입니다.

## 가입 시 연결

같은 연락처로 가입하면 비회원 주문을 연결할 수 있습니다.

```text
1. 가입 완료
2. phone_hash 로 GUEST 주문 조회
3. 있으면 "이전 주문을 연결하시겠습니까" 안내
4. 동의 시 주문의 member_id 를 정회원 ID 로 변경
5. GUEST 회원 행은 비활성 처리
```

자동 연결하지 않습니다. 번호를 물려받은 경우 다른 사람의 주문이 보일 수 있습니다.

연결 대상은 90일 이내 주문으로 제한합니다. 그 이후는 개인정보가 삭제되어 대조할 수 없습니다.

## 우려

GUEST 회원이 계속 쌓입니다. 비회원 주문이 하루 500건이면 연 18만 행입니다.

회원 수 통계에서 GUEST 를 제외해야 합니다. 지표 정의에 이미 "전체 회원 = PENDING_VERIFICATION, ACTIVE, DORMANT" 로 되어 있어 자연히 제외됩니다. 정의를 만들어 둔 것이 도움이 됐습니다.
""",
    },
    {
        "id": "DOC-267",
        "title": "[리뷰] 비회원 주문 조회 인증",
        "department": "백엔드개발팀",
        "type": "코드리뷰",
        "project": None,
        "date": "2026-06-25",
        "systems": ["ORBIT"],
        "features": ["주문"],
        "participants": ["이서연", "조아름"],
        "concepts": ["비회원", "보안", "Rate Limit"],
        "body": """## 현재 구현

인증번호를 확인하고 조회 토큰을 발급합니다.

```java
public GuestTokenResponse confirm(ConfirmCommand command) {
    var stored = verificationStore.get(command.orderNumber());
    if (stored == null || !stored.code().equals(command.code())) {
        throw new VerificationFailedException();
    }
    verificationStore.remove(command.orderNumber());
    return GuestTokenResponse.of(issueToken(command.orderNumber()));
}
```

## 리뷰 의견

조아름님: 인증번호 비교가 `equals` 입니다. 타이밍 공격이 이론상 가능합니다. 상수 시간 비교를 쓰세요.

이서연님: 6자리 숫자라 실효성은 낮지만 반영하겠습니다.

조아름님: 실패해도 저장된 인증번호가 남습니다. 시도 횟수 제한이 어디에 있나요.

이서연님: 별도 카운터로 관리합니다. 다만 이 메서드에는 없고 상위 필터에 있습니다.

조아름님: 실패 시 카운터를 올리는 로직이 예외 경로에 있으면 놓치기 쉽습니다. 검증 로직 안에 함께 두는 편이 안전합니다.

조아름님: 인증번호를 어디에 저장하나요. 평문이면 저장소 유출 시 그대로 노출됩니다.

이서연님: Redis 에 평문입니다. TTL 5분이라 위험이 낮다고 봤습니다.

조아름님: 해시로 저장해도 비용이 크지 않습니다. 5분이라도 평문은 피하는 게 좋겠습니다.

조아름님: 발송 API 가 주문 존재 여부와 무관하게 성공을 반환하는 것은 잘 되어 있습니다. 다만 응답 시간이 다르면 존재 여부를 알 수 있습니다. 실제 발송 여부와 관계없이 응답 시간을 맞춰야 합니다.

이서연님: 발송을 비동기로 처리하면 응답 시간이 일정해집니다. 그렇게 바꾸겠습니다.

## 수정 사항

- 상수 시간 비교 적용
- 시도 횟수 카운터를 검증 로직 안으로 이동
- 인증번호를 해시로 저장
- 발송을 비동기 처리하여 응답 시간 균일화

## 남은 확인

IP 기준 제한이 프록시 뒤에서 정확히 동작하는지 확인이 필요합니다. `X-Forwarded-For` 처리를 검토하겠습니다.
""",
    },
    {
        "id": "DOC-268",
        "title": "고객 프로필 이미지 도입 검토",
        "department": "서비스기획팀",
        "type": "기획자와의 회의록",
        "project": None,
        "date": "2025-08-20",
        "systems": ["NEXUS"],
        "features": ["프로필"],
        "participants": ["송지은", "최유진"],
        "concepts": ["프로필", "이미지", "정책"],
        "body": """## 논의 배경

리뷰에 작성자가 표시되는데 모두 같은 기본 이미지입니다. 프로필 이미지를 넣자는 제안입니다.

## 요구사항

- 마이페이지에서 이미지 업로드
- 리뷰, 문의에 표시
- 기본 이미지 제공

## 쟁점

**부적절한 이미지**

최유진님이 가장 먼저 제기한 문제입니다. 누구나 이미지를 올릴 수 있으면 부적절한 내용이 올라옵니다. 검수 체계가 없습니다.

송지은님은 신고 기반으로 대응하자고 했습니다. 사전 검수는 인력이 필요합니다.

**필요성**

최유진님은 프로필 이미지가 실제로 필요한지 물었습니다. 쇼핑 서비스에서 다른 사용자의 얼굴을 보는 것이 구매에 도움이 되는지 명확하지 않습니다.

리뷰 신뢰도를 높이는 효과가 있다는 주장이 있지만 근거가 약합니다.

## 대안

기본 이미지를 여러 개 제공하고 그중에서 고르게 하는 방식입니다. 업로드가 없으므로 검수 문제가 없습니다.

닉네임 첫 글자를 색상 배경에 표시하는 방식도 있습니다. 구현이 가장 단순합니다.

## 결정 사항

**이미지 업로드는 하지 않습니다.**

대신:

- 닉네임 첫 글자 + 자동 배정 색상으로 아바타를 만듭니다
- 색상은 고객 ID 해시로 결정하여 일관되게 유지합니다
- 사용자가 색상을 바꿀 수 있게 합니다

## 근거

- 업로드 이미지의 검수 부담이 기대 효과보다 큽니다
- 저장소 비용이 발생합니다
- 개인 사진이 올라가면 개인정보 이슈가 생깁니다

## 추후

수요가 확인되면 다시 검토합니다. 지금은 사용자 요청이 있었던 것도 아니고 내부 제안에서 시작됐습니다.

## 부수 결정

닉네임 기능이 없다는 것을 이 논의에서 발견했습니다. 리뷰에는 이름 일부를 마스킹해서 표시하고 있습니다("홍*동").

닉네임 도입은 별도로 검토합니다.
""",
    },
    {
        "id": "DOC-269",
        "title": "이미지 저장소 선정 검토",
        "department": "DevOps팀",
        "type": "개발자 회의록",
        "project": None,
        "date": "2025-09-02",
        "systems": ["ORBIT"],
        "features": ["상품"],
        "participants": ["오세훈", "최유진"],
        "concepts": ["저장소", "CDN", "비용"],
        "body": """## 논의 배경

상품 이미지가 늘면서 저장소와 전송 비용이 커지고 있습니다. 현재 구조를 점검합니다.

## 현황

- 원본 이미지를 저장소에 보관
- 요청 시 크기별로 변환해서 제공
- 변환 결과를 CDN 에 캐시

## 문제

**변환 부하**: 변환을 애플리케이션 서버가 합니다. 이미지가 큰 경우 CPU 를 많이 씁니다.

**캐시 미스**: CDN 캐시가 만료되면 다시 변환합니다. 상품 수가 많아 캐시 히트율이 낮습니다.

**원본 크기**: 판매자가 올리는 원본이 10MB 를 넘는 경우가 있습니다.

## 검토안

**1. 업로드 시 사전 변환**

업로드할 때 필요한 크기를 모두 만들어 저장합니다. 조회 시 변환이 없습니다. 저장 용량이 늘지만 CPU 는 아낍니다.

**2. 이미지 처리 전용 서비스**

변환을 별도 서비스로 분리합니다. 애플리케이션 서버 부하가 사라지지만 인프라가 늘어납니다.

**3. CDN 의 이미지 변환 기능**

CDN 이 제공하는 변환 기능을 씁니다. 우리가 변환하지 않아도 됩니다. 비용이 추가되고 CDN 종속이 생깁니다.

## 쟁점

최유진님은 필요한 크기가 몇 종류인지 물었습니다. 확인해 보니 목록 썸네일, 상세 대표, 확대 보기 3종입니다.

3종뿐이면 사전 변환이 합리적입니다. 원본까지 4개를 저장해도 부담이 크지 않습니다.

오세훈님은 나중에 크기가 추가되면 전체를 다시 변환해야 한다고 지적했습니다. 12만 상품이면 시간이 걸립니다.

## 결정 사항

**1안(사전 변환)으로 갑니다.**

- 업로드 시 3종을 만들어 저장
- 원본은 보관하되 서비스에 제공하지 않음
- 크기 추가 시 배치로 일괄 변환

원본을 보관하는 이유는 나중에 크기가 추가될 때 필요하기 때문입니다.

## 원본 크기 제한

업로드 시 10MB 로 제한하고, 긴 변 4,000px 로 리사이즈해서 원본으로 저장합니다. 그보다 큰 원본은 필요 없습니다.

## 미결

기존 이미지의 사전 변환 작업이 필요합니다. 12만 상품 × 3종 = 36만 건입니다. 언제 실행할지는 정하지 않았습니다.

## Action Item

- 오세훈님: 변환 배치 및 실행 계획
- 최유진님: 클라이언트에서 크기별 URL 사용하도록 수정
""",
    },
    {
        "id": "DOC-270",
        "title": "인증 시스템 2차 개선 현황",
        "department": "프로젝트관리팀",
        "type": "프로젝트 진행상황",
        "project": "Project Echo",
        "date": "2026-04-15",
        "systems": ["BlueGate"],
        "features": ["인증", "로그인"],
        "participants": [],
        "concepts": ["일정", "키 관리", "Risk"],
        "body": """## 범위

2024년 Echo 1차에서 미뤄둔 항목과 신규 요구사항입니다.

- 서명 키 회전 저장소
- 토큰 재발급 API v2 정식화
- v1 API 종료 준비

## 완료

- `signing_key` 테이블 및 암호화 저장
- 키 회전 절차 구현
- 캐시 전파 문제 해결 (PENDING 상태 추가)
- 재발급 API v2 문서화

## 진행 중

- 운영 키 회전 절차 문서
- v1 사용 클라이언트 파악

## 대기

- 첫 운영 키 회전 (5월 예정)
- v1 API 종료 (8월 예정)

## 키 회전 검증

3월 19일 스테이징 첫 회전에서 4분간 인증 실패가 있었습니다. 캐시 전파 시차 문제였고 수정 후 재검증했습니다.

3월 26일, 4월 2일 두 차례 추가 회전에서 문제가 없었습니다.

## v1 종료 준비

v1 재발급 API 를 아직 쓰는 클라이언트가 있는지 확인 중입니다.

| 클라이언트 | v2 전환 |
| --- | --- |
| 웹 | 완료 |
| Android | 완료 (3.14 이상) |
| iOS | 완료 (3.14 이상) |
| 구버전 앱 | 미전환 |

3.14 미만 앱 사용자가 아직 8% 있습니다. 8월까지 5% 미만으로 떨어질 것으로 예상하지만 확실하지 않습니다.

## Risk

v1 종료 시점에 구버전 앱 사용자가 남아 있으면 그들은 로그인이 풀립니다. 강제 업데이트 기준의 "서버 하위 호환 종료" 항목에 해당하므로, 5% 미만이 되면 강제 업데이트를 겁니다.

5% 미만이 되지 않으면 종료를 연기해야 합니다. 판단 시점은 7월 말입니다.

## 다음

- 5월 운영 키 회전
- 7월 말 v1 종료 여부 판단
""",
    },
]
