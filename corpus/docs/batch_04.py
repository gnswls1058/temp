"""DOC-046 ~ DOC-060 : Project Nova 전반 - 리워드 초기 설계와 잔액 문제 발견."""

DOCUMENTS = [
    {
        "id": "DOC-046",
        "title": "리워드 정책 통합 요구사항 정리",
        "department": "서비스기획팀",
        "type": "기획자와의 회의록",
        "project": "Project Nova",
        "date": "2025-07-01",
        "systems": ["MARS"],
        "features": ["리워드 지급", "포인트", "쿠폰 지급"],
        "participants": ["강민재", "박준호"],
        "concepts": ["정책 통합", "적립금", "일정 영향"],
        "body": """## 논의 배경

지금 적립 수단이 세 가지입니다. 구매 적립금, 이벤트 포인트, 프로모션 쿠폰이 각각 다른 규칙으로 돌아가고 화면에서도 따로 보입니다. 사용자 문의 중 "포인트랑 적립금이 뭐가 다르냐"가 꾸준히 들어옵니다.

## 기획 요구사항

강민재님이 정리한 방향입니다.

- 적립금과 이벤트 포인트를 하나의 "포인트"로 통합한다
- 쿠폰은 별도로 유지한다
- 유효기간은 통합 후 하나의 기준으로 관리한다
- 사용 시 소멸 임박 포인트부터 차감한다

## 개발 관점

박준호님이 확인한 현재 구조입니다.

| 수단 | 저장 위치 | 유효기간 |
| --- | --- | --- |
| 구매 적립금 | MARS `reward` | 없음 |
| 이벤트 포인트 | MARS `event_point` | 지급일로부터 90일 |
| 프로모션 쿠폰 | MARS `coupon` | 쿠폰별 상이 |

적립금에 유효기간이 없다가 생기면 기존 잔액을 어떻게 할지가 문제입니다. 소급 적용하면 사용자 자산이 사라지는 셈이라 고지 없이는 할 수 없습니다.

## 결정 사항

- 통합 시점 이전 적립금은 유효기간 없이 유지한다
- 통합 이후 적립분부터 유효기간을 적용한다
- 사용 순서는 유효기간이 있는 것 중 임박한 것부터, 그다음 무기한

## 미확정

무기한 잔액이 남아 있는 한 이 예외 처리가 영구적으로 유지됩니다. 언젠가 정리하려면 별도 공지가 필요한데 시점은 정하지 못했습니다.

## Action Item

- 강민재님: 통합 안내 공지 문구 초안
- 박준호님: 기존 적립금 잔액 분포 확인
""",
    },
    {
        "id": "DOC-047",
        "title": "Nova 킥오프 - MARS 리워드 재설계 범위",
        "department": "백엔드개발팀",
        "type": "개발자 회의록",
        "project": "Project Nova",
        "date": "2025-07-07",
        "systems": ["MARS", "ORBIT"],
        "features": ["리워드 지급", "포인트"],
        "participants": ["박준호", "임채원"],
        "concepts": ["범위 산정", "마이그레이션", "일정"],
        "body": """## 논의 배경

리워드 통합 요구사항을 받아 개발 범위를 정합니다.

## 범위

포함:

- `reward` 와 `event_point` 통합
- 유효기간 및 소멸 배치
- 지급/사용/회수 API 재정비
- ORBIT 주문 연동 변경

제외:

- 쿠폰 구조 변경 (현행 유지)
- 등급별 적립률 개편 (별도 과제)

## 마이그레이션

기존 두 테이블의 잔액을 새 구조로 옮겨야 합니다. 박준호님이 확인한 대상 건수는 적립금 210만 건, 이벤트 포인트 87만 건입니다.

임채원님이 무중단으로 가능한지 물었고, 잔액이 바뀌는 시점에 이관하면 정합성이 깨지기 때문에 짧은 점검 시간을 두는 편이 안전하다고 정리했습니다.

## 결정 사항

- 이관은 서비스 점검 30분 안에 끝나는 규모로 만든다
- 이관 전후 총 잔액 대사 스크립트를 먼저 작성한다
- 롤백은 기존 테이블을 그대로 두는 방식으로 확보한다

## 일정

| 스프린트 | 목표 |
| --- | --- |
| Sprint 1 | 신규 테이블 설계 및 지급 API |
| Sprint 2 | 사용/회수 API, ORBIT 연동 |
| Sprint 3 | 유효기간 및 소멸 배치 |
| Sprint 4 | 마이그레이션 및 QA |

## 추후 논의

등급별 적립률은 이번 범위에서 뺐지만, 테이블 구조가 이를 수용할 수 있어야 나중에 편합니다. 적립 사유 코드를 충분히 확장 가능하게 두기로 했습니다.
""",
    },
    {
        "id": "DOC-048",
        "title": "reward 테이블 설계안",
        "department": "백엔드개발팀",
        "type": "DB 테이블 설계안",
        "project": "Project Nova",
        "date": "2025-07-10",
        "systems": ["MARS"],
        "features": ["리워드 지급", "포인트"],
        "participants": [],
        "concepts": ["잔액 관리", "정합성", "Index"],
        "deprecated": True,
        "body": """> 이 설계는 2025년 8월 잔액 동시성 문제 이후 폐기되었습니다. 현재 구조는 `reward_balance` 와 `reward_history` 분리 설계를 따릅니다.

## 배경

적립금과 이벤트 포인트를 하나로 합치면서 잔액을 어떻게 들고 있을지 정합니다.

## 설계안

회원당 한 행을 두고 잔액을 직접 관리합니다.

| 컬럼 | 타입 | 제약 | 설명 |
| --- | --- | --- | --- |
| id | BIGINT | PK | |
| member_id | BIGINT | UNIQUE | 회원당 1행 |
| balance | INT | NOT NULL DEFAULT 0 | 현재 포인트 잔액 |
| total_earned | BIGINT | NOT NULL DEFAULT 0 | 누적 적립 |
| total_used | BIGINT | NOT NULL DEFAULT 0 | 누적 사용 |
| updated_at | DATETIME | NOT NULL | |

지급과 사용은 이 행을 갱신합니다.

```sql
UPDATE reward
SET balance = balance + :amount,
    total_earned = total_earned + :amount,
    updated_at = NOW()
WHERE member_id = :memberId;
```

## 왜 단일 행인가

조회가 압도적으로 많습니다. 상품 상세, 장바구니, 주문서에서 매번 잔액을 보여줘야 하는데 이력을 합산하면 비용이 큽니다. 잔액을 직접 들고 있으면 인덱스 하나로 끝납니다.

## 이력 관리

지급/사용 이력은 별도 `reward_transaction` 테이블에 남깁니다. 잔액과 이력이 어긋날 수 있어서 일 1회 정합성 대조 배치를 돌립니다.

## 우려 사항

같은 회원에 동시 갱신이 들어오면 `UPDATE ... SET balance = balance + ?` 구문 자체는 원자적이지만, 잔액 부족 검사를 애플리케이션에서 하면 검사와 갱신 사이에 틈이 생깁니다. 사용 시에는 조건을 쿼리에 넣어야 합니다.

```sql
UPDATE reward
SET balance = balance - :amount
WHERE member_id = :memberId AND balance >= :amount;
```

영향 행이 0이면 잔액 부족으로 처리합니다. 이 방식으로 충분한지 리뷰에서 확인이 필요합니다.

## 인덱스

`member_id` unique 하나면 됩니다. 조회 패턴이 단순합니다.
""",
    },
    {
        "id": "DOC-049",
        "title": "리워드 지급 처리 설계",
        "department": "백엔드개발팀",
        "type": "비즈니스 기능 설계",
        "project": "Project Nova",
        "date": "2025-07-15",
        "systems": ["MARS", "ORBIT"],
        "features": ["리워드 지급"],
        "participants": [],
        "concepts": ["중복 지급", "Transaction", "Edge Case", "이벤트"],
        "body": """## 배경

적립금 지급 경로가 여러 개입니다. 주문 완료 적립, 이벤트 보상, 운영자 수동 지급, 보상성 지급이 모두 같은 API 를 씁니다. 경로별로 다른 규칙이 붙어 있어 정리합니다.

## 지급 사유

| 코드 | 설명 | 유효기간 |
| --- | --- | --- |
| ORDER_REWARD | 주문 완료 적립 | 365일 |
| EVENT_REWARD | 이벤트 보상 | 90일 |
| MANUAL_GRANT | 운영자 수동 지급 | 지정 |
| COMPENSATION | 장애 보상 | 365일 |

## 정상 Flow

1. 호출자가 지급 요청 (회원, 금액, 사유, 참조키)
2. 참조키 기준 중복 확인
3. 잔액 증가 및 이력 기록
4. 지급 완료 이벤트 발행

## 중복 방지

참조키(`referenceKey`)를 필수로 받습니다. 주문 적립이면 주문번호, 이벤트면 `이벤트ID:회원ID` 형태입니다. 같은 사유와 참조키 조합은 한 번만 지급합니다.

```json
{
  "memberId": 1024,
  "amount": 3000,
  "reason": "ORDER_REWARD",
  "referenceKey": "ORD-2025-0715-0031"
}
```

## Edge Case

- **지급 후 주문 취소**: 적립된 적립금을 회수해야 합니다. 이미 사용했다면 잔액이 음수가 될 수 있습니다. 음수를 허용할지 사용 시점에 막을지 결정이 필요합니다. 현재는 음수를 허용하고 다음 적립으로 상계합니다.
- **이벤트 중복 응모**: 참조키가 같으므로 두 번째 요청은 무시됩니다. 다만 응답을 성공으로 줄지 실패로 줄지 정해야 합니다. 호출자가 재시도할 수 있으므로 성공(멱등) 응답으로 합니다.
- **지급 대상 회원이 탈퇴**: 탈퇴 회원에게 지급하면 즉시 소멸됩니다. 지급 자체를 막고 `REWARD_1005` 를 반환합니다.
- **금액이 0 이하**: 거부합니다. 회수는 별도 API 를 씁니다.
- **유효기간이 지난 시점의 소급 지급**: 운영자가 과거 날짜로 지급하는 경우가 있습니다. 지급일은 항상 현재로 고정하고 사유에 메모를 남기게 합니다.

## 고민 사항

주문 완료 시점에 적립할지 배송 완료 시점에 적립할지가 정해지지 않았습니다. 주문 완료 시점이면 취소가 잦아 회수가 늘고, 배송 완료면 적립이 늦어 사용자 불만이 있습니다. 기획팀과 다시 이야기해야 합니다.
""",
    },
    {
        "id": "DOC-050",
        "title": "리워드 지급 API v1 명세",
        "department": "백엔드개발팀",
        "type": "API 명세서",
        "project": "Project Nova",
        "date": "2025-07-18",
        "systems": ["MARS"],
        "features": ["리워드 지급"],
        "participants": [],
        "concepts": ["멱등성", "Error Code", "참조키"],
        "body": """## Endpoint

```http
POST /rewards/grant
Authorization: Bearer {serviceToken}
```

서비스 간 호출 전용입니다. 사용자 토큰으로는 호출할 수 없습니다.

## Request

| 필드 | 타입 | 필수 | 설명 |
| --- | --- | --- | --- |
| memberId | number | Y | |
| amount | number | Y | 1 이상 정수 |
| reason | string | Y | ORDER_REWARD / EVENT_REWARD / MANUAL_GRANT / COMPENSATION |
| referenceKey | string | Y | 중복 방지 키. 사유별 유일 |
| expiresAt | string | N | MANUAL_GRANT 일 때만 지정 가능 |
| memo | string | N | 운영 확인용 |

```json
{
  "memberId": 1024,
  "amount": 3000,
  "reason": "ORDER_REWARD",
  "referenceKey": "ORD-2025-0715-0031"
}
```

## Response

```json
{
  "transactionId": 88214,
  "memberId": 1024,
  "granted": 3000,
  "balance": 12400,
  "expiresAt": "2026-07-18T00:00:00+09:00",
  "duplicated": false
}
```

같은 `reason` 과 `referenceKey` 로 다시 호출하면 새로 지급하지 않고 기존 결과를 그대로 반환합니다. 이때 `duplicated` 가 true 입니다. HTTP 상태는 200 입니다.

## Error Code

| 코드 | HTTP | 설명 |
| --- | --- | --- |
| REWARD_1001 | 400 | 금액이 1 미만 |
| REWARD_1002 | 400 | 알 수 없는 사유 코드 |
| REWARD_1003 | 400 | MANUAL_GRANT 가 아닌데 expiresAt 지정 |
| REWARD_1005 | 409 | 지급 불가 회원 상태 (탈퇴, 휴면) |
| REWARD_1009 | 500 | 잔액 갱신 실패 |

## 주의사항

`referenceKey` 는 호출자가 만듭니다. 이벤트 지급에서 `이벤트ID` 만 넣고 회원 ID 를 빼먹으면 한 사람만 받고 나머지가 중복으로 걸립니다. 실제로 스테이징에서 이 실수가 나왔으니 키 구성에 회원 식별자를 반드시 포함하세요.

`duplicated: true` 를 실패로 처리하지 마세요. 재시도 상황에서 정상 응답입니다.
""",
    },
    {
        "id": "DOC-051",
        "title": "적립금 잔액 조회 API 명세",
        "department": "백엔드개발팀",
        "type": "API 명세서",
        "project": "Project Nova",
        "date": "2025-07-22",
        "systems": ["MARS"],
        "features": ["포인트"],
        "participants": [],
        "concepts": ["조회 성능", "소멸 예정"],
        "body": """## Endpoint

```http
GET /rewards/balance
Authorization: Bearer {accessToken}
```

본인 잔액을 조회합니다. 운영자 조회는 `GET /admin/rewards/{memberId}/balance` 를 사용합니다.

## Response

```json
{
  "memberId": 1024,
  "balance": 12400,
  "expiringSoon": {
    "amount": 1500,
    "expiresAt": "2025-08-15T00:00:00+09:00"
  },
  "unlimitedBalance": 3200
}
```

| 필드 | 설명 |
| --- | --- |
| balance | 사용 가능한 전체 잔액 |
| expiringSoon | 30일 이내 소멸 예정 금액과 가장 이른 소멸일 |
| unlimitedBalance | 유효기간 없는 잔액 (통합 이전 적립분) |

소멸 예정이 없으면 `expiringSoon` 은 null 입니다.

## 캐시

잔액은 캐시하지 않습니다. 주문 직후 화면에서 이전 값이 보이면 문의로 이어집니다.

`expiringSoon` 은 계산 비용이 있어 5분 캐시를 둡니다. 소멸일이 분 단위로 바뀌지 않으므로 문제되지 않습니다.

## 주의사항

주문서 화면에서 이 API 를 호출한 뒤 결제까지 시간이 걸리면 잔액이 바뀔 수 있습니다. 결제 시점에 다시 검증하므로 화면 값만 믿고 진행하면 안 됩니다. 사용 요청이 `REWARD_2003`(잔액 부족)으로 실패할 수 있습니다.
""",
    },
    {
        "id": "DOC-052",
        "title": "[리뷰] RewardService 지급 로직 1차",
        "department": "백엔드개발팀",
        "type": "코드리뷰",
        "project": "Project Nova",
        "date": "2025-07-25",
        "systems": ["MARS"],
        "features": ["리워드 지급"],
        "participants": ["박준호", "김도윤"],
        "concepts": ["멱등성", "Transaction", "동시성"],
        "body": """## 현재 구현

```java
@Transactional
public GrantResult grant(GrantCommand command) {
    var existing = transactionRepository
        .findByReasonAndReferenceKey(command.reason(), command.referenceKey());
    if (existing.isPresent()) {
        return GrantResult.duplicated(existing.get());
    }
    rewardRepository.increaseBalance(command.memberId(), command.amount());
    var saved = transactionRepository.save(command.toTransaction());
    return GrantResult.granted(saved);
}
```

## 리뷰 의견

김도윤님: 중복 확인과 저장 사이가 열려 있습니다. 이벤트 지급처럼 동시에 여러 요청이 들어오면 둘 다 중복 없음으로 판정하고 두 번 지급됩니다. `reward_transaction` 에 `(reason, reference_key)` unique 제약이 있는지 확인이 필요합니다.

박준호님: 인덱스만 있고 unique 는 아닙니다. 추가하겠습니다.

김도윤님: unique 를 걸면 두 번째 요청이 예외로 떨어집니다. 그때 예외를 잡아서 기존 건을 조회해 멱등 응답을 돌려주는 처리가 필요합니다. 지금은 예외가 그대로 올라가 500 이 나갑니다.

김도윤님: 잔액 증가와 이력 저장 순서도 뒤집는 게 좋겠습니다. 이력을 먼저 저장하면 unique 위반이 잔액 변경 전에 발생해서 롤백 범위가 작습니다.

박준호님: 같은 트랜잭션이라 결과는 같지만 의도가 명확해지니 바꾸겠습니다.

## 수정 사항

- `reward_transaction` 에 `(reason, reference_key)` unique 제약 추가
- 이력 저장을 먼저 수행
- `DataIntegrityViolationException` 을 잡아 기존 건 조회 후 멱등 응답

## 남은 확인

잔액 갱신은 `balance = balance + ?` 형태라 동시 지급 자체는 안전합니다. 다만 사용 시 잔액 부족 검사는 다른 경로라 별도 리뷰가 필요합니다. 사용 API PR 에서 다시 봅니다.
""",
    },
    {
        "id": "DOC-053",
        "title": "Nova Sprint 1 진행 현황",
        "department": "프로젝트관리팀",
        "type": "프로젝트 진행상황",
        "project": "Project Nova",
        "date": "2025-07-31",
        "systems": ["MARS"],
        "features": ["리워드 지급", "포인트"],
        "participants": [],
        "concepts": ["일정", "Risk"],
        "body": """## 스프린트 개요

7월 21일 ~ 7월 31일. 신규 테이블 설계와 지급 API 구현이 목표였습니다.

## 완료

- 통합 리워드 테이블 설계
- 지급 API 구현 및 리뷰 반영
- 잔액 조회 API
- 지급 이력 중복 방지 제약 추가

## 진행 중

- 사용 API 설계 (60%)
- ORBIT 연동 인터페이스 협의

## 대기

- 유효기간 소멸 배치
- 마이그레이션 스크립트

## Risk

주문 적립 시점(주문 완료 vs 배송 완료)이 확정되지 않아 ORBIT 연동 설계가 두 갈래로 열려 있습니다. 다음 주 기획 회의에서 정해지지 않으면 Sprint 2 범위를 조정해야 합니다.

기존 적립금 210만 건, 이벤트 포인트 87만 건 이관은 아직 실측을 못 했습니다. 스테이징에 운영 규모 데이터를 만들어 놓고 시간을 재봐야 합니다.

## 다음 스프린트

- 사용/회수 API 구현
- ORBIT 연동
- 마이그레이션 시간 측정
""",
    },
    {
        "id": "DOC-054",
        "title": "여름 이벤트 보상 중복 지급 발생",
        "department": "백엔드개발팀",
        "type": "이슈 정리",
        "project": "Project Nova",
        "date": "2025-08-05",
        "systems": ["MARS"],
        "features": ["리워드 지급", "이벤트 보상"],
        "participants": [],
        "concepts": ["중복 지급", "참조키", "회수"],
        "body": """## 현상

8월 4일 여름 이벤트 보상 지급 중 일부 회원이 적립금을 2회 받았습니다. 총 1,142명에게 중복 지급되었고 금액은 342만 적립금입니다.

## 재현 조건

이벤트 배치가 실패해 재실행한 경우에만 발생했습니다.

## 로그 분석

```text
14:02:11 event.batch start eventId=SUMMER-2025 targets=48210
14:19:44 event.batch failed at offset=31200 reason=connection reset
14:31:02 event.batch restart eventId=SUMMER-2025 offset=0
```

재실행이 처음부터 다시 돌았습니다. 이미 지급된 31,200명 중 참조키가 정상적으로 들어간 건은 멱등 처리되어 중복이 없었고, 문제는 참조키 구성이었습니다.

```text
referenceKey = "SUMMER-2025"          # 잘못된 구성
referenceKey = "SUMMER-2025:1024"     # 올바른 구성
```

배치 코드가 회원 ID 를 붙이지 않은 경로가 하나 있었습니다. 응모 이력이 있는 회원과 자동 대상 회원을 다른 함수로 처리하는데, 자동 대상 쪽에만 회원 ID 가 빠져 있었습니다.

그런데 첫 실행에서 자동 대상자 중 첫 번째 사람만 지급되고 나머지는 중복으로 걸렸어야 정상입니다. 확인해 보니 unique 제약이 배포 직전에 추가되어 이벤트 배치가 돈 시점에는 아직 적용 전이었습니다. 결국 중복 방지가 아무것도 동작하지 않았습니다.

## 영향 범위

- 중복 지급 1,142명 / 342만 적립금
- 이 중 이미 사용된 적립금 41만

## 임시 대응

- 이벤트 배치 중단
- 중복 지급 건 목록 추출

## 근본 해결

- 자동 대상 지급 경로의 참조키에 회원 ID 추가 (배포 완료)
- unique 제약 적용 확인
- 배치 재실행 시 처리 완료 offset 을 기록해 이어서 실행

## 회수 처리

이미 사용된 41만 적립금은 회수하면 잔액이 음수가 됩니다. 기획팀 판단으로 사용분은 회수하지 않고 미사용분만 회수하기로 했습니다. 회수 작업은 8월 7일 진행 예정입니다.
""",
    },
    {
        "id": "DOC-055",
        "title": "적립금 유효기간 정책 확정",
        "department": "서비스기획팀",
        "type": "기획자와의 회의록",
        "project": "Project Nova",
        "date": "2025-08-08",
        "systems": ["MARS"],
        "features": ["포인트"],
        "participants": ["강민재", "송지은"],
        "concepts": ["유효기간", "소멸", "고지"],
        "body": """## 논의 배경

통합 이후 적립분에 유효기간을 적용하기로 했습니다. 오늘은 기간과 안내 방식을 정합니다.

## 결정 사항

| 항목 | 결정 |
| --- | --- |
| 주문 적립 유효기간 | 365일 |
| 이벤트 보상 유효기간 | 90일 |
| 소멸 기준 시각 | 만료일 다음 날 00:00 |
| 사전 안내 | 소멸 30일 전, 7일 전 |
| 사용 순서 | 소멸 임박 순 → 무기한 |

## 쟁점

송지은님이 이벤트 보상 90일이 짧다고 봤습니다. 이벤트로 받은 적립금은 금액이 작아서 단독으로 쓰기 어렵고, 다음 주문까지 90일이 넘는 사용자가 40% 가까이 됩니다.

강민재님은 90일이 이벤트 참여를 구매로 연결시키는 장치라는 입장이었습니다. 기간을 늘리면 이벤트 효과 측정이 어려워집니다.

결론적으로 90일을 유지하되, 소멸 7일 전 안내에 "지금 쓸 수 있는 상품" 추천을 붙이는 것으로 절충했습니다.

## 소멸 시각

만료일 당일 자정에 소멸시키면 사용자가 "오늘까지라고 했는데 못 썼다"고 느낍니다. 만료일 다음 날 00:00 로 하고, 화면에는 만료일을 표시합니다.

## 미확정

소멸 예정 적립금을 주문서에서 자동 우선 사용할지, 사용자가 선택하게 할지 정하지 못했습니다. 자동이 편하지만 사용자가 인지하지 못한 채 쓰이는 게 맞는지 의견이 갈렸습니다. 다음 회의에서 다시 봅니다.
""",
    },
    {
        "id": "DOC-056",
        "title": "적립금 소멸 처리 설계",
        "department": "백엔드개발팀",
        "type": "비즈니스 기능 설계",
        "project": "Project Nova",
        "date": "2025-08-12",
        "systems": ["MARS"],
        "features": ["포인트"],
        "participants": [],
        "concepts": ["배치", "유효기간", "Edge Case", "정합성"],
        "body": """## 배경

유효기간 정책이 확정되어 소멸 처리를 설계합니다. 잔액을 단일 값으로 들고 있는 현재 구조에서 "어떤 적립분이 언제 소멸하는지"를 알아야 합니다.

## 문제

`reward.balance` 는 합계만 가지고 있습니다. 소멸시키려면 적립 건별로 남은 금액을 알아야 하는데, 사용 시 어느 적립분에서 차감했는지 기록하지 않고 있습니다.

## 필요한 정보

- 적립 건별 잔여 금액
- 적립 건별 만료일
- 사용 시 차감 대상 적립 건

## 설계

`reward_transaction` 에 잔여 금액 컬럼을 추가합니다.

| 컬럼 | 설명 |
| --- | --- |
| amount | 최초 지급액 |
| remaining_amount | 아직 사용되지 않은 금액 |
| expires_at | 만료일 |

사용 시 만료 임박 순으로 적립 건을 순회하며 `remaining_amount` 를 차감합니다.

```sql
SELECT id, remaining_amount
FROM reward_transaction
WHERE member_id = :memberId
  AND type = 'GRANT'
  AND remaining_amount > 0
  AND (expires_at IS NULL OR expires_at > NOW())
ORDER BY expires_at IS NULL, expires_at, id
FOR UPDATE;
```

## 소멸 배치

매일 새벽 만료된 적립 건의 `remaining_amount` 를 0 으로 만들고 그 합만큼 `reward.balance` 를 줄입니다.

## Edge Case

- **소멸 배치와 사용 동시 발생**: 사용자가 만료 직전 적립금을 쓰는 중에 배치가 돌면 이중 차감이 납니다. 배치도 `FOR UPDATE` 로 잠그고 처리합니다.
- **회수와 소멸 충돌**: 주문 취소로 회수해야 하는 적립분이 이미 소멸된 경우입니다. 회수 대상이 없으므로 잔액에서 직접 차감하고 이력에 사유를 남깁니다.
- **부분 사용된 적립 건**: 3,000점 중 1,000점만 쓰고 만료되면 2,000점이 소멸합니다. 이력에는 소멸 금액만 기록합니다.

## 우려

사용할 때마다 여러 적립 건을 잠그고 갱신합니다. 잔액 하나만 고치던 것에 비해 락 범위가 커집니다. 성능 영향이 어느 정도인지 측정이 필요합니다. 현재 구조의 한계가 여기서 드러나는 것 같습니다.
""",
    },
    {
        "id": "DOC-057",
        "title": "동시 주문 시 적립금 잔액이 음수가 되는 문제",
        "department": "백엔드개발팀",
        "type": "이슈 정리",
        "project": "Project Nova",
        "date": "2025-08-19",
        "systems": ["MARS", "ORBIT"],
        "features": ["포인트", "주문"],
        "participants": ["박준호", "김도윤"],
        "concepts": ["동시성", "잔액 관리", "Race Condition", "설계 변경"],
        "body": """## 현상

스테이징 부하 테스트에서 적립금 잔액이 음수가 되는 케이스가 나왔습니다. 잔액 5,000인 계정에 5,000점 사용 요청을 동시에 10건 넣으면 3~4건이 성공하고 잔액이 -15,000 이 됩니다.

## 재현 조건

같은 회원에 대해 사용 요청이 동시에 들어올 때 재현됩니다. 실제 서비스에서는 사용자가 여러 탭에서 결제하거나 앱 재시도가 겹칠 때 발생 가능합니다.

## 분석

사용 처리는 두 단계입니다.

1. `reward_transaction` 에서 적립 건을 조회해 차감 계획을 만든다
2. `reward.balance` 를 줄인다

설계 검토 때는 `WHERE balance >= :amount` 조건으로 막을 수 있다고 봤습니다. 그런데 소멸 처리 설계가 들어오면서 1단계가 추가됐고, 여기서 조회한 적립 건 목록을 기준으로 차감액을 계산합니다. 동시 요청들이 같은 적립 건 목록을 읽고 각자 차감하면서 합계가 잔액을 넘어섭니다.

`FOR UPDATE` 를 걸면 막히지만, 적립 건이 수백 개인 계정에서 전부 잠그면 대기가 길어집니다. 부하 테스트에서 락 대기 타임아웃도 함께 관측됐습니다.

## 실제 원인

잔액과 이력이 별개 데이터로 존재하고, 둘을 동시에 일관되게 갱신하려니 잠금 범위가 계속 커지는 구조적 문제입니다. 단일 잔액 컬럼 설계 자체가 이런 요구사항과 맞지 않습니다.

## 임시 대응

회원 단위 분산 락을 걸어 사용 요청을 직렬화했습니다. 부하 테스트에서 음수는 사라졌지만 응답 시간이 3배로 늘었습니다. 운영에 이 상태로 올리기는 어렵습니다.

## 근본 해결

김도윤님이 이력 기반(ledger) 구조로 바꾸는 방향을 제안했습니다. 잔액을 저장하지 않고 이력의 합으로 계산하거나, 잔액 테이블을 두더라도 이력이 진실의 원천이 되게 하는 방식입니다.

8월 21일에 별도 회의를 잡고 구조를 다시 봅니다. 마이그레이션 일정에 영향이 있어 PM 에게 공유했습니다.

## 상태

임시 대응 적용, 구조 재검토 진행 중입니다.
""",
    },
    {
        "id": "DOC-058",
        "title": "적립금 잔액 관리 방식 재검토",
        "department": "백엔드개발팀",
        "type": "개발자 회의록",
        "project": "Project Nova",
        "date": "2025-08-21",
        "systems": ["MARS"],
        "features": ["포인트", "리워드 지급"],
        "participants": ["박준호", "김도윤", "배현우"],
        "concepts": ["ledger", "동시성", "설계 변경", "정합성"],
        "body": """## 논의 배경

8월 19일 확인된 잔액 음수 문제로 현재 설계를 다시 봅니다. 임시 대응인 분산 락은 성능 저하가 커서 그대로 갈 수 없습니다.

## 검토한 방안

**1안. 현행 유지 + 분산 락**

구현이 끝나 있어 추가 작업이 적습니다. 다만 회원 단위 직렬화라 이벤트 지급 같은 대량 처리에서 병목이 됩니다.

**2안. 완전 ledger. 잔액 컬럼 없이 이력 합계로 조회**

정합성은 가장 확실합니다. 배현우님이 지적한 대로 이력이 수십만 건인 계정에서 매번 합계를 내면 조회가 느려집니다. 잔액 조회는 서비스에서 가장 빈번한 호출입니다.

**3안. ledger + 잔액 스냅샷 분리**

이력(`reward_history`)을 진실의 원천으로 두고, 조회용 잔액(`reward_balance`)을 별도 테이블에 유지합니다. 잔액 갱신은 이력 삽입과 같은 트랜잭션에서 처리하되, 잔액 행 하나만 잠그므로 락 범위가 작습니다.

## 쟁점

배현우님이 3안에서도 잔액과 이력이 어긋날 수 있지 않냐고 물었습니다. 같은 트랜잭션이면 어긋나지 않지만, 배치나 수동 작업이 한쪽만 건드리면 깨집니다. 대사 배치는 어느 안이든 필요합니다.

김도윤님은 잔액 행에 버전 컬럼을 두고 낙관적 락으로 처리하는 방식을 제안했습니다. 충돌 시 재시도하면 되고, 같은 회원 동시 요청이 많지 않으므로 재시도 비용이 낮습니다.

## 결정 사항

- **3안으로 갑니다.** `reward_history` + `reward_balance` 구조
- 잔액 갱신은 낙관적 락, 충돌 시 3회까지 재시도
- 소멸 처리는 이력의 `remaining_amount` 를 그대로 활용
- 일 1회 대사 배치로 잔액과 이력 합계를 비교

## 일정 영향

박준호님 추정으로 재작업 5일, 마이그레이션 스크립트 수정 2일입니다. Sprint 4 로 잡혀 있던 마이그레이션이 한 스프린트 밀립니다.

## Action Item

- 박준호님: 새 테이블 설계 문서 작성
- 김도윤님: 낙관적 락 재시도 공통 처리 검토
- 배현우님: 대사 배치 쿼리 초안
""",
    },
    {
        "id": "DOC-059",
        "title": "리워드 수동 지급 및 회수 절차",
        "department": "백엔드개발팀",
        "type": "매뉴얼 / 정의서",
        "project": None,
        "date": "2025-08-26",
        "systems": ["MARS", "AIMS"],
        "features": ["리워드 지급", "리워드 회수"],
        "participants": [],
        "concepts": ["운영 대응", "승인 절차"],
        "body": """## 언제 사용하나

- 장애 보상
- 고객 응대 과정에서의 보상 지급
- 잘못 지급된 적립금 회수

이벤트 보상은 이 절차가 아니라 이벤트 배치로 처리합니다.

## 지급 절차

1. AIMS 에서 회원 조회
2. 적립금 탭 > 수동 지급
3. 금액, 사유 코드, 메모 입력
4. 5만 적립금 초과 시 팀 리드 승인 필요

메모에는 근거를 남기세요. "문의 #12345 응대" 처럼 추적 가능한 형태를 권장합니다.

## 회수 절차

회수는 지급보다 조건이 까다롭습니다.

- 사용된 적립금은 회수할 수 없습니다
- 회수 후 잔액이 음수가 되는 요청은 거부됩니다
- 회수는 금액과 무관하게 팀 리드 승인이 필요합니다

대량 회수(50건 초과)는 AIMS 화면이 아니라 데이터팀에 배치 처리를 요청하세요. 화면에서 하나씩 처리하면 중간에 실패했을 때 어디까지 됐는지 파악이 어렵습니다.

## 금지 사항

- DB 에 직접 UPDATE 하지 않습니다. 잔액과 이력이 어긋납니다
- 같은 사유로 여러 번 지급하지 않습니다. 참조키가 중복되면 두 번째는 무시됩니다
- 테스트 목적의 지급은 운영 환경에서 하지 않습니다

## 확인

지급/회수 후 회원의 적립금 이력에 정상 반영됐는지 확인하세요. 반영이 안 보이면 잔액과 이력이 어긋났을 수 있으니 백엔드팀에 알려주세요.
""",
    },
    {
        "id": "DOC-060",
        "title": "Nova Sprint 3 - 설계 변경 반영 현황",
        "department": "프로젝트관리팀",
        "type": "프로젝트 진행상황",
        "project": "Project Nova",
        "date": "2025-08-29",
        "systems": ["MARS"],
        "features": ["포인트", "리워드 지급"],
        "participants": ["임채원", "박준호"],
        "concepts": ["설계 변경", "일정 지연", "Risk"],
        "body": """## 스프린트 개요

8월 18일 ~ 8월 29일. 원래 목표는 유효기간 소멸 배치였으나, 잔액 구조 변경이 들어오면서 범위가 바뀌었습니다.

## 변경 사항

8월 21일 회의에서 잔액 관리 방식을 ledger 기반으로 바꾸기로 했습니다. 기존 `reward` 단일 테이블 설계는 폐기합니다.

## 완료

- 잔액 음수 문제 임시 대응 (분산 락)
- 신규 구조 설계 문서 초안

## 진행 중

- `reward_history` / `reward_balance` 테이블 정의
- 지급 API 재작업
- 낙관적 락 재시도 공통 처리

## 대기

- 소멸 배치 (신규 구조 확정 후 착수)
- 마이그레이션 스크립트 수정
- 정합성 대조 배치

## Risk

전체 일정이 한 스프린트 밀립니다. 박준호님 추정으로 재작업 5일, 마이그레이션 수정 2일입니다.

더 큰 위험은 이미 작성된 API 명세와 코드리뷰 문서가 옛 구조를 기준으로 한다는 점입니다. 지급 API 응답에 `balance` 가 들어 있는데 계산 방식이 바뀝니다. 외부 인터페이스는 유지하되 내부만 바꾸는 방향으로 정리했습니다.

## 다음 스프린트

- 신규 구조 구현 완료
- 소멸 배치 착수
- 마이그레이션 시간 재측정
""",
    },
]
