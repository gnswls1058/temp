"""DOC-241 ~ DOC-255 : 보안, 로그, 데이터 지표 (2025~2026)."""

DOCUMENTS = [
    {
        "id": "DOC-241",
        "title": "로그 보관 정책 재검토",
        "department": "DevOps팀",
        "type": "개발자 회의록",
        "project": None,
        "date": "2025-05-07",
        "systems": [],
        "features": [],
        "participants": ["오세훈", "조아름"],
        "concepts": ["로그", "보관 기간", "비용"],
        "body": """## 논의 배경

로그 저장 비용이 전 분기 대비 40% 늘었습니다. 보관 기간과 수집 범위를 다시 봅니다.

## 현황

| 종류 | 보관 | 일 용량 |
| --- | --- | --- |
| 애플리케이션 로그 | 30일 | 220GB |
| 접근 로그 | 30일 | 140GB |
| 디버그 로그 | 30일 | 310GB |

디버그 로그가 가장 큽니다. 운영에서 `DEBUG` 레벨로 도는 서비스가 있었습니다.

## 논의 내용

오세훈님은 디버그 로그를 운영에서 끄면 절반이 줄어든다고 했습니다. 다만 장애 분석 시 필요한 경우가 있습니다.

절충안으로 평소에는 `INFO` 로 두고, 필요할 때 특정 서비스만 일시적으로 `DEBUG` 로 올릴 수 있게 하기로 했습니다. 설정으로 변경 가능해야 합니다.

조아름님은 보관 기간을 줄이는 데 신중해야 한다고 했습니다. 보안 사고 조사에서 과거 로그가 필요할 수 있습니다.

## 결정 사항

| 종류 | 보관 | 비고 |
| --- | --- | --- |
| 애플리케이션 (INFO 이상) | 30일 | 유지 |
| 접근 로그 | 90일 | 연장. 보안 조사용 |
| 디버그 | 7일 | 단축, 운영 기본 비활성 |

접근 로그는 늘리고 디버그는 줄입니다. 전체 용량은 줄어듭니다.

## 로그 레벨 동적 변경

Feature Flag 로 서비스별 로그 레벨을 바꿀 수 있게 합니다. 장애 대응 중에 배포 없이 상세 로그를 켤 수 있습니다.

켠 뒤 끄는 것을 잊으면 다시 용량이 늘어납니다. 24시간 뒤 자동으로 원래 레벨로 돌아가게 합니다.

## 로그 내용 점검

조아름님이 로그에 개인정보가 있는지 점검하자고 했습니다. 아직 확인한 적이 없습니다.

## Action Item

- 오세훈님: 보관 기간 조정, 로그 레벨 동적 변경
- 조아름님: 로그 내 개인정보 점검
""",
    },
    {
        "id": "DOC-242",
        "title": "애플리케이션 로그에 개인정보가 남는 경로 확인",
        "department": "정보보안팀",
        "type": "이슈 정리",
        "project": None,
        "date": "2025-06-18",
        "systems": ["NEXUS", "ORBIT", "MARS"],
        "features": [],
        "participants": [],
        "concepts": ["개인정보", "로그", "보안"],
        "body": """## 배경

5월 회의에서 요청된 로그 내 개인정보 점검을 수행했습니다.

## 방법

최근 7일 로그에서 패턴 검색을 했습니다.

```text
이메일:     [\\w.+-]+@[\\w-]+\\.[\\w.-]+
전화번호:   01[016789]-?\\d{3,4}-?\\d{4}
카드번호:   \\d{4}-?\\d{4}-?\\d{4}-?\\d{4}
```

## 결과

| 서비스 | 이메일 | 전화번호 | 카드 |
| --- | --- | --- | --- |
| NEXUS | 14,208 | 3,110 | 0 |
| ORBIT | 2,840 | 9,720 | 0 |
| MARS | 62 | 0 | 0 |

카드번호는 없었습니다. 이메일과 전화번호가 다수 발견됐습니다.

## 유입 경로

**1. 예외 메시지 (가장 많음)**

```java
throw new DuplicateEmailException("이미 사용 중인 이메일입니다: " + email);
```

예외 메시지에 값을 넣고, 이 예외가 로그로 기록됩니다.

**2. 요청 본문 로깅**

일부 서비스가 요청 본문 전체를 `DEBUG` 로 남깁니다. 가입 요청이면 이메일과 이름이 그대로 들어갑니다.

**3. 수령지 정보**

주문 처리 로그에 수령자 정보가 포함됩니다.

**4. 외부 응답 저장**

PG 응답을 로그로 남기는 코드가 있었습니다. 다행히 카드번호는 PG 가 이미 마스킹해서 보냅니다.

## 조치

**즉시**

- 요청 본문 전체 로깅 제거
- 예외 메시지에서 개인정보 제거 (회원 ID 로 대체)
- 수령지 로깅 제거

**구조**

민감 필드를 자동으로 마스킹하는 로그 필터를 추가합니다. 필드명 기반으로 `email`, `phone`, `address`, `name` 을 마스킹합니다.

완전하지는 않습니다. 문자열 연결로 만들어진 메시지는 필드명이 없어 걸러지지 않습니다.

**기존 로그**

30일 보관이므로 조치 후 30일이 지나면 자연히 사라집니다. 즉시 삭제할지 검토했으나, 삭제 자체가 로그 무결성을 해칠 수 있어 보관 기간 만료를 기다리기로 했습니다.

## 재발 방지

- 개인정보 취급 매뉴얼에 로그 관련 항목 추가
- 코드리뷰 체크리스트에 "로그에 개인정보가 들어가지 않는가" 추가
- 분기마다 같은 점검을 반복

## 남은 문제

문자열 연결로 만든 로그 메시지는 자동 마스킹이 어렵습니다. 구조화 로깅(필드 단위)으로 전환하면 해결되지만 범위가 큽니다. 별도 과제로 등록했습니다.
""",
    },
    {
        "id": "DOC-243",
        "title": "보안 점검 규칙",
        "department": "정보보안팀",
        "type": "팀 Ground Rule",
        "project": None,
        "date": "2025-07-23",
        "systems": [],
        "features": [],
        "participants": [],
        "concepts": ["보안", "팀 규칙", "점검"],
        "body": """## 정기 점검

| 항목 | 주기 |
| --- | --- |
| 의존성 취약점 스캔 | 주 1회 (자동) |
| 로그 내 개인정보 점검 | 분기 1회 |
| 권한 부여 현황 점검 | 분기 1회 |
| 접근 로그 이상 탐지 | 상시 (자동) |
| 침투 테스트 | 연 1회 (외부) |

## 신규 기능 점검 대상

다음에 해당하면 개발 전 정보보안팀과 협의합니다.

- 인증, 인가와 관련된 변경
- 개인정보를 새로 수집하거나 저장 위치를 바꾸는 경우
- 외부 시스템과 새로 연동하는 경우
- 파일 업로드/다운로드 기능
- 관리자 권한이 필요한 기능

협의는 설계 단계에서 합니다. 구현 후에 지적받으면 되돌리는 비용이 큽니다.

## 코드 작성 시

- 사용자 입력을 신뢰하지 않습니다. 서버에서 반드시 검증합니다
- 쿼리를 문자열로 조립하지 않습니다. 파라미터 바인딩을 사용합니다
- 인증 정보를 코드나 설정 파일에 넣지 않습니다. 비밀 저장소를 사용합니다
- 오류 응답에 내부 구조를 노출하지 않습니다 (스택 트레이스, 테이블명 등)
- 권한 검사는 기본 거부로 둡니다

## 취약점 발견 시

- 발견자는 즉시 정보보안팀에 알립니다
- 공개 채널에 상세 내용을 쓰지 않습니다
- 직접 악용을 시도하지 않습니다. 재현 확인은 최소 범위로 합니다

## 외부 공개

- 외부에 노출되는 엔드포인트는 별도 검토를 받습니다
- 오픈소스로 코드를 공개할 때는 내부 정보(도메인, 인증 정보, 내부 URL)를 제거합니다

## 사고 대응

개인정보 유출이 의심되면 즉시 정보보안팀에 알립니다. 확신이 없어도 알리세요.

- 혼자 조사하지 않습니다
- 로그와 데이터를 변경하지 않습니다
- 외부에 공유하지 않습니다

## 교육

신규 입사자는 입사 후 2주 안에 보안 교육을 이수합니다. 전 구성원은 연 1회 갱신 교육을 받습니다.
""",
    },
    {
        "id": "DOC-244",
        "title": "민감 데이터 암호화 적용 방안",
        "department": "정보보안팀",
        "type": "DB 테이블 설계안",
        "project": None,
        "date": "2026-02-06",
        "systems": ["NEXUS", "ORBIT"],
        "features": [],
        "participants": [],
        "concepts": ["암호화", "개인정보", "키 관리"],
        "body": """## 배경

개인정보 중 일부는 저장 시 암호화가 필요합니다. 현재는 비밀번호만 해시 처리되어 있고 나머지는 평문입니다.

## 대상 분류

| 데이터 | 처리 | 이유 |
| --- | --- | --- |
| 비밀번호 | 단방향 해시 | 복원 불필요 |
| 주민등록번호 | 수집하지 않음 | - |
| 계좌번호 | 양방향 암호화 | 환불 시 필요 |
| 전화번호 | 양방향 암호화 | 조회 필요 |
| 이메일 | 평문 + 해시 컬럼 | 로그인 식별자로 사용 |
| 주소 | 평문 | 검색 필요, 위험도 낮음 |

## 이메일을 암호화하지 않는 이유

로그인 시 이메일로 조회합니다. 암호화하면 조회할 수 없습니다.

결정적 암호화(같은 값이 항상 같은 암호문)를 쓰면 조회가 가능하지만, 빈도 분석에 취약해 암호화 효과가 떨어집니다.

대신 이메일 해시 컬럼을 두고 조회에 씁니다. 원문은 발송에 필요하므로 유지합니다.

## 전화번호 암호화

조회가 필요한데 암호화하면 검색이 안 됩니다. 정확 일치 검색만 지원하기로 하고 해시 컬럼을 함께 둡니다.

| 컬럼 | 용도 |
| --- | --- |
| phone_encrypted | 표시용. 복호화해서 보여줌 |
| phone_hash | 검색용 |

부분 일치 검색(뒷 4자리)은 지원하지 않습니다. 운영에서 필요하다는 요청이 있으면 별도 검토합니다.

## 키 관리

- 암호화 키는 애플리케이션 설정에 두지 않습니다
- 외부 키 관리 서비스에서 조회합니다
- 키 회전 주기는 1년입니다
- 회전 시 이전 키로 암호화된 데이터를 읽을 수 있어야 합니다

암호문에 키 버전을 접두어로 붙입니다.

```text
v2:base64encodedciphertext
```

## 성능

암호화/복호화 비용이 있습니다. 회원 조회가 빈번하므로 영향을 측정해야 합니다.

전화번호는 목록 조회에서 마스킹만 표시하므로 복호화가 필요 없습니다. 상세 조회에서만 복호화합니다.

## 마이그레이션

기존 평문 데이터를 암호화해야 합니다. 480만 행이라 시간이 걸립니다.

1. 암호화 컬럼 추가 (nullable)
2. 배치로 기존 데이터 암호화하며 채움
3. 읽기를 암호화 컬럼 우선으로 전환
4. 평문 컬럼 제거

3단계에서 두 컬럼이 공존합니다. 새로 저장되는 값은 암호화 컬럼에만 들어가야 하며, 평문 컬럼에 쓰는 코드가 남아 있으면 안 됩니다.

## 미확정

계좌번호는 환불 기능에서만 쓰이며 건수가 적습니다. 먼저 적용해 검증한 뒤 전화번호로 확대하는 순서를 제안합니다. 일정은 미정입니다.
""",
    },
    {
        "id": "DOC-245",
        "title": "데이터 추출 요청 API 명세",
        "department": "데이터팀",
        "type": "API 명세서",
        "project": None,
        "date": "2026-03-10",
        "systems": ["AIMS"],
        "features": ["운영 관리"],
        "participants": [],
        "concepts": ["데이터 추출", "권한", "감사"],
        "body": """## 배경

운영에서 데이터 추출 요청이 채팅으로 들어옵니다. 누가 무엇을 요청했고 어떤 데이터가 나갔는지 기록이 없습니다. 요청과 처리를 시스템화합니다.

## 추출 요청

```http
POST /admin/data-exports
Authorization: Bearer {adminToken}
```

필요 권한: `data:export:request`

```json
{
  "templateCode": "ORDER_SUMMARY_BY_PERIOD",
  "parameters": {
    "startDate": "2026-02-01",
    "endDate": "2026-02-28"
  },
  "purpose": "2월 마감 정산 대사",
  "format": "CSV"
}
```

임의 쿼리는 허용하지 않습니다. 사전 정의된 템플릿만 실행할 수 있습니다.

## 템플릿

| 코드 | 설명 | 개인정보 포함 |
| --- | --- | --- |
| ORDER_SUMMARY_BY_PERIOD | 기간별 주문 집계 | 없음 |
| MEMBER_SIGNUP_STATS | 가입 통계 | 없음 |
| REWARD_TRANSACTION | 포인트 거래 내역 | 회원 ID |
| REFUND_LIST | 환불 목록 | 이름, 계좌 |

개인정보가 포함된 템플릿은 추가 승인이 필요합니다.

## Response

```json
{
  "exportId": 1204,
  "status": "PENDING_APPROVAL",
  "estimatedRows": 42000,
  "requiresApproval": true
}
```

## 승인과 다운로드

승인되면 파일이 생성되고 다운로드 링크가 발급됩니다.

- 링크는 24시간 유효합니다
- 다운로드는 1회만 가능합니다
- 다운로드 사실이 감사 로그에 남습니다

## 제한

| 항목 | 값 |
| --- | --- |
| 최대 행 수 | 100만 |
| 기간 조건 | 최대 1년 |
| 동시 요청 | 사용자당 3건 |
| 파일 보관 | 7일 후 삭제 |

## Error Code

| 코드 | HTTP | 설명 |
| --- | --- | --- |
| EXPORT_1001 | 400 | 알 수 없는 템플릿 |
| EXPORT_1002 | 400 | 파라미터 오류 |
| EXPORT_1003 | 403 | 권한 없음 |
| EXPORT_1004 | 409 | 예상 행 수 초과 |
| EXPORT_1005 | 429 | 동시 요청 초과 |

## 주의사항

추출한 파일은 개인 장비에 보관하지 않습니다. 사용 후 삭제하세요.

목적(`purpose`)은 감사 기록에 남습니다. 구체적으로 작성해 주세요.
""",
    },
    {
        "id": "DOC-246",
        "title": "통계 지표 정의 통일",
        "department": "데이터팀",
        "type": "개발자 회의록",
        "project": None,
        "date": "2026-03-26",
        "systems": ["ORBIT", "MARS"],
        "features": [],
        "participants": ["배현우", "임채원"],
        "concepts": ["지표", "정의", "정합성"],
        "body": """## 논의 배경

같은 지표를 화면마다 다르게 계산합니다. "이번 달 매출"이 대시보드와 정산 화면에서 다르게 나옵니다.

## 확인된 불일치

배현우님이 정리한 사례입니다.

| 지표 | 화면 A | 화면 B | 차이 |
| --- | --- | --- | --- |
| 매출 | 결제 완료 금액 | 결제 완료 - 취소 | 취소 반영 |
| 주문 수 | 주문 생성 건수 | 결제 완료 건수 | 미결제 주문 |
| 회원 수 | 전체 | ACTIVE 만 | 휴면/미인증 |
| 재구매율 | 2회 이상 주문 회원 비율 | 최근 6개월 기준 | 기간 |

정의가 없어서 개발자마다 다르게 해석했습니다.

## 결정 사항

지표 정의를 문서로 관리하고, 구현은 그 정의를 따릅니다.

**매출**

```text
매출 = SUM(결제 완료 금액) - SUM(취소 금액)
기준 시각 = 결제 완료 시각 (취소는 취소 시각이 아니라 원 결제 시각에 반영)
포인트 사용액 = 매출에서 제외
```

취소를 원 결제 시각에 반영하는 이유는 월별 매출이 나중에 바뀌는 것을 감수하더라도, 어느 달의 매출인지가 명확해야 하기 때문입니다.

**주문 수**

```text
주문 수 = 결제가 완료된 주문 건수
미결제 주문은 포함하지 않음
부분 취소는 1건으로 계산
전체 취소는 제외
```

**회원 수**

```text
전체 회원 = status IN (PENDING_VERIFICATION, ACTIVE, DORMANT)
활성 회원 = status = ACTIVE
탈퇴 회원은 제외
```

"회원 수"라는 표현을 쓰지 않고 "전체 회원"과 "활성 회원"을 구분해서 씁니다.

**재구매율**

```text
재구매율 = 기간 내 2회 이상 주문한 회원 / 기간 내 1회 이상 주문한 회원
기본 기간 = 최근 6개월
```

## 관리 방식

임채원님이 문서만으로는 지켜지지 않는다고 지적했습니다. 지표 정의와 실제 쿼리가 어긋나도 알 수 없습니다.

집계를 한 곳(`daily_metrics`)에서만 하고 화면은 그 결과를 읽도록 바꾸기로 했습니다. 화면마다 직접 계산하지 않습니다.

## Action Item

- 배현우님: 지표 정의 문서 및 집계 테이블 설계
- 임채원님: 기존 화면의 지표 사용처 목록 정리
""",
    },
    {
        "id": "DOC-247",
        "title": "[리뷰] 일별 지표 집계 쿼리",
        "department": "데이터팀",
        "type": "코드리뷰",
        "project": None,
        "date": "2026-04-02",
        "systems": ["ORBIT"],
        "features": [],
        "participants": [],
        "concepts": ["집계", "Query 최적화", "정합성"],
        "body": """## 현재 구현

일별 매출과 주문 수를 집계합니다.

```sql
INSERT INTO daily_metrics (metric_date, gross_sales, order_count, ...)
SELECT
  DATE(p.approved_at) AS metric_date,
  SUM(p.total_amount - p.canceled_amount) AS gross_sales,
  COUNT(DISTINCT p.order_id) AS order_count
FROM payment p
WHERE p.approved_at >= :from AND p.approved_at < :to
  AND p.status IN ('APPROVED', 'PARTIAL_CANCELED')
GROUP BY DATE(p.approved_at);
```

## 리뷰 의견

**취소 반영 시점**: `canceled_amount` 는 현재 값입니다. 어제 결제된 건이 오늘 취소되면 어제 집계를 다시 계산해야 합니다. 지금은 한 번 집계하면 다시 보지 않습니다.

정의에서 "취소는 원 결제 시각에 반영"으로 정했으므로, 과거 날짜를 재집계하는 처리가 필요합니다. 최근 30일을 매일 다시 집계하는 방식이 단순합니다.

**status 조건**: `CANCELED` 상태가 빠져 있습니다. 전액 취소된 건은 매출이 0 이므로 결과는 같지만, 주문 수 계산에서는 차이가 납니다. 정의상 전체 취소는 주문 수에서 제외하므로 현재 조건이 맞습니다. 다만 의도가 드러나도록 주석이 필요합니다.

**포인트 제외**: 정의에서 포인트 사용액을 매출에서 제외하기로 했는데 쿼리에 반영되지 않았습니다. `total_amount` 는 PG 청구 금액이라 이미 포인트가 빠져 있는지 확인이 필요합니다.

확인 결과 `total_amount` 는 포인트를 제외한 실제 청구액이 맞습니다. 의도대로 동작하지만 컬럼 의미를 아는 사람만 알 수 있습니다. 주석을 남기는 편이 좋겠습니다.

**타임존**: `DATE(p.approved_at)` 이 DB 타임존을 따릅니다. 서버가 UTC 면 한국 기준 날짜와 어긋납니다. 명시적으로 변환해야 합니다.

## 수정 사항

- 최근 30일 재집계 로직 추가
- status 조건에 의도 주석
- 포인트 제외 여부 주석
- 타임존 명시 변환

## 남은 확인

30일 재집계는 30일 이후 취소를 반영하지 못합니다. 취소 가능 기간이 365일이므로 이론상 1년 뒤에도 바뀔 수 있습니다.

월 마감 후 확정된 수치는 바꾸지 않는 것이 회계 관행입니다. 재무팀 확인 후 재집계 기간을 정하기로 했습니다.
""",
    },
    {
        "id": "DOC-248",
        "title": "운영 대시보드 지표 요구사항",
        "department": "프로젝트관리팀",
        "type": "기획자와의 회의록",
        "project": None,
        "date": "2026-04-09",
        "systems": ["AIMS"],
        "features": ["운영 관리"],
        "participants": ["임채원", "배현우"],
        "concepts": ["지표", "대시보드", "우선순위"],
        "body": """## 논의 배경

지표 정의를 통일하면서 대시보드도 정리합니다. 지금은 화면 5개에 지표가 흩어져 있고 서로 값이 다릅니다.

## 사용자별 필요 지표

임채원님이 팀별로 조사한 내용입니다.

**경영/기획**

- 일/주/월 매출
- 주문 수, 객단가
- 신규 가입, 활성 회원
- 재구매율

**운영/CS**

- 오늘 주문 현황
- 처리 대기 문의 수
- 배송 지연 건수
- 환불 대기 건수

**개발/인프라**

- API 응답 시간, 오류율
- 결제 성공률
- 배치 실행 현황

## 쟁점

배현우님은 개발 지표와 비즈니스 지표를 한 대시보드에 넣으면 안 된다고 했습니다. 갱신 주기가 다릅니다. 비즈니스 지표는 일 단위 집계이고 개발 지표는 실시간입니다.

임채원님도 동의했고, 대시보드를 목적별로 나누기로 했습니다.

## 결정 사항

| 대시보드 | 대상 | 갱신 |
| --- | --- | --- |
| 비즈니스 | 경영/기획 | 일 1회 (전일 확정) |
| 운영 | CS/운영 | 5분 |
| 시스템 | 개발/인프라 | 실시간 |

시스템 대시보드는 이미 있는 모니터링 도구를 씁니다. 새로 만들지 않습니다.

## 비즈니스 대시보드

전일 기준 확정 수치만 보여줍니다. 당일 실시간 수치는 넣지 않습니다.

당일 수치를 넣으면 시간대에 따라 값이 계속 바뀌어 오해를 부릅니다. "오늘 매출이 어제보다 적다"는 판단이 오전에는 항상 참입니다.

## 운영 대시보드

처리 대기 항목 중심입니다. "지금 무엇을 해야 하는가"를 보여줍니다.

- 미답변 문의 (SLA 초과 강조)
- 승인 대기 환불
- 배송 지연 주문
- 재고 임박 상품

## 미확정

비즈니스 대시보드에 목표 대비 달성률을 넣을지 정하지 않았습니다. 목표 수치를 어디서 관리할지가 정해지지 않았습니다.

## Action Item

- 배현우님: 비즈니스 대시보드 집계 구현
- 임채원님: 운영 대시보드 항목 확정
""",
    },
    {
        "id": "DOC-249",
        "title": "daily_metrics 집계 테이블 설계",
        "department": "데이터팀",
        "type": "DB 테이블 설계안",
        "project": None,
        "date": "2026-04-22",
        "systems": ["ORBIT"],
        "features": [],
        "participants": [],
        "concepts": ["집계", "지표", "재집계"],
        "body": """## 목적

지표를 한 곳에서 계산해 저장하고, 모든 화면이 이 테이블을 읽습니다. 화면마다 계산하지 않습니다.

## 테이블

| 컬럼 | 타입 | 설명 |
| --- | --- | --- |
| metric_date | DATE | PK |
| gross_sales | BIGINT | 매출 (취소 반영) |
| order_count | INT | 결제 완료 주문 수 |
| canceled_order_count | INT | 전체 취소 주문 수 |
| average_order_value | INT | 객단가 |
| new_member_count | INT | 신규 가입 |
| active_member_count | INT | 활성 회원 (누적) |
| ordering_member_count | INT | 주문한 회원 수 |
| repeat_ordering_member_count | INT | 2회 이상 주문 회원 수 |
| point_used_amount | BIGINT | 포인트 사용액 |
| point_granted_amount | BIGINT | 포인트 지급액 |
| calculated_at | DATETIME | 집계 시각 |
| version | INT | 재집계 횟수 |

## version 컬럼

같은 날짜를 여러 번 집계합니다. 취소가 나중에 반영되기 때문입니다.

`version` 으로 몇 번째 집계인지 알 수 있습니다. 값이 바뀐 이유를 추적할 때 씁니다.

## 재집계 정책

매일 새벽 배치가 다음을 집계합니다.

```text
전일:        신규 집계
최근 30일:   재집계
그 이전:     변경하지 않음
```

30일이 지난 뒤 취소가 발생하면 해당 날짜 매출에 반영되지 않습니다. 대신 취소 발생일 기준 별도 지표(`late_cancel_amount`)로 관리합니다.

재무팀과 협의한 결과, 마감된 월의 수치를 바꾸지 않는 것이 회계 처리에 맞습니다.

## 파생 지표

객단가와 재구매율은 저장된 값에서 계산합니다.

```text
객단가   = gross_sales / order_count
재구매율 = repeat_ordering_member_count / ordering_member_count
```

계산식이 단순하므로 저장하지 않고 조회 시 계산합니다. 다만 `average_order_value` 는 자주 쓰여 저장해 둡니다.

## 조회

```sql
SELECT * FROM daily_metrics
WHERE metric_date BETWEEN :from AND :to
ORDER BY metric_date;
```

기간 조회가 대부분이라 PK 만으로 충분합니다.

## 월별 집계

`monthly_metrics` 를 별도로 두지 않고 일별에서 합산합니다. 한 달이 31행이므로 합산 비용이 없습니다.

단 활성 회원 수처럼 누적 값은 합산할 수 없습니다. 월말 값을 씁니다.

## 검증

집계 후 전일 대비 변동률이 임계값(±50%)을 넘으면 알림을 보냅니다. 집계 오류를 조기에 발견하기 위함입니다.

이벤트나 프로모션으로 실제 변동이 클 수 있으므로 자동으로 중단하지는 않습니다.
""",
    },
    {
        "id": "DOC-250",
        "title": "통계 조회 API 명세",
        "department": "데이터팀",
        "type": "API 명세서",
        "project": None,
        "date": "2026-05-11",
        "systems": ["AIMS"],
        "features": ["운영 관리"],
        "participants": [],
        "concepts": ["지표", "권한", "캐시"],
        "body": """## Endpoint

```http
GET /admin/metrics/daily?from=2026-04-01&to=2026-04-30
Authorization: Bearer {adminToken}
```

필요 권한: `metrics:read`

## Response

```json
{
  "period": { "from": "2026-04-01", "to": "2026-04-30" },
  "summary": {
    "grossSales": 1284000000,
    "orderCount": 42180,
    "averageOrderValue": 30440,
    "newMemberCount": 8210,
    "repeatRate": 0.312
  },
  "daily": [
    {
      "date": "2026-04-01",
      "grossSales": 41200000,
      "orderCount": 1380,
      "newMemberCount": 264
    }
  ]
}
```

`summary` 는 기간 합계입니다. 객단가와 재구매율은 기간 전체 기준으로 다시 계산된 값이며, 일별 값의 평균이 아닙니다.

## 지표 정의

각 지표의 계산 기준은 지표 정의 문서를 따릅니다. 이 API 는 `daily_metrics` 테이블 값을 그대로 반환하며 별도 계산을 하지 않습니다.

| 지표 | 정의 |
| --- | --- |
| grossSales | 결제 완료 금액 - 취소 금액. 포인트 사용액 제외 |
| orderCount | 결제 완료 주문 수. 전체 취소 제외 |
| newMemberCount | 가입 완료(인증 완료) 기준 |
| repeatRate | 기간 내 2회 이상 주문 회원 / 1회 이상 주문 회원 |

## 데이터 기준일

전일까지의 확정 수치만 제공합니다. 당일 데이터는 포함되지 않습니다.

```json
{
  "dataAsOf": "2026-05-10",
  "note": "전일까지의 확정 수치입니다"
}
```

당일 실시간 수치가 필요하면 운영 대시보드 API 를 사용하세요.

## 제한

| 항목 | 값 |
| --- | --- |
| 조회 기간 | 최대 1년 |
| 캐시 | 1시간 |

## Error Code

| 코드 | HTTP | 설명 |
| --- | --- | --- |
| METRIC_1001 | 400 | 기간 오류 (1년 초과, from > to) |
| METRIC_1002 | 403 | 권한 없음 |
| METRIC_1003 | 404 | 해당 기간 집계 없음 |

## 주의사항

최근 30일 데이터는 재집계로 값이 바뀔 수 있습니다. 취소가 반영되기 때문입니다. 캡처해서 보고에 쓸 때는 조회 시점을 함께 기록하세요.
""",
    },
    {
        "id": "DOC-251",
        "title": "화면마다 매출 수치가 다르게 보이는 문제",
        "department": "데이터팀",
        "type": "이슈 정리",
        "project": None,
        "date": "2026-05-19",
        "systems": ["AIMS"],
        "features": ["운영 관리"],
        "participants": [],
        "concepts": ["지표", "정합성", "마이그레이션"],
        "body": """## 현상

집계 테이블을 도입한 뒤에도 화면마다 매출이 다르게 나옵니다. 대시보드는 4월 매출 12억 8,400만원, 정산 화면은 13억 1,200만원입니다.

## 분석

대시보드는 `daily_metrics` 를 읽습니다. 정산 화면은 여전히 직접 계산합니다.

3월 회의에서 "모든 화면이 집계 테이블을 읽는다"고 정했는데, 정산 화면이 전환되지 않았습니다. 전환 대상 목록에 빠져 있었습니다.

차이 2,800만원의 내역을 확인했습니다.

| 항목 | 금액 | 설명 |
| --- | --- | --- |
| 취소 반영 시점 차이 | 2,100만 | 정산은 취소 발생일 기준 |
| 포인트 처리 | 700만 | 정산은 포인트 사용액 포함 |

두 화면이 서로 다른 정의를 씁니다.

## 판단

정산 화면의 계산이 틀린 것은 아닙니다. 회계 관점에서는 취소 발생일 기준이 맞고, 포인트도 매출로 인식하는 기준이 있습니다.

즉 "매출"이라는 같은 단어를 다른 의미로 쓰고 있습니다.

## 조치

지표 이름을 구분합니다.

| 이름 | 정의 | 사용처 |
| --- | --- | --- |
| 순매출 | 결제 - 취소(원결제일 기준), 포인트 제외 | 대시보드, 리포트 |
| 결제액 | 결제 금액 합계, 취소는 발생일 기준 차감 | 정산 |

두 값을 모두 집계 테이블에 저장하고 화면마다 필요한 것을 씁니다. 이름을 다르게 하여 혼동을 막습니다.

## 남은 작업

- `daily_metrics` 에 `payment_amount` 컬럼 추가
- 정산 화면을 집계 테이블 기반으로 전환
- 지표 정의 문서에 두 지표를 모두 기재
- 화면에 지표 이름과 정의 툴팁 표시

## 배운 점

"모든 화면이 집계 테이블을 읽는다"고 정했지만 전환 대상 목록을 만들지 않았습니다. 목록 없이 "모두"라고 하면 빠지는 것이 생깁니다.

전환 대상 화면 목록을 만들고 진행 상황을 추적합니다. 현재 12개 중 9개가 전환됐습니다.
""",
    },
    {
        "id": "DOC-252",
        "title": "[리뷰] 지표 조회 캐시 계층",
        "department": "데이터팀",
        "type": "코드리뷰",
        "project": None,
        "date": "2026-06-08",
        "systems": ["AIMS"],
        "features": ["운영 관리"],
        "participants": ["배현우", "최유진"],
        "concepts": ["캐시", "지표", "성능"],
        "body": """## 현재 구현

지표 조회에 1시간 캐시를 적용했습니다.

```java
@Cacheable(value = "metrics", key = "#from + ':' + #to")
public MetricsResponse getDaily(LocalDate from, LocalDate to) {
    ...
}
```

## 리뷰 의견

최유진님: 캐시 키가 기간 조합입니다. 사용자가 임의 기간을 조회하면 키가 계속 늘어납니다. 캐시가 거의 히트하지 않고 메모리만 씁니다.

배현우님: 날짜별로 캐시하고 조회 시 합치는 방식이 낫겠네요. 4월 1일~30일 조회면 30개 키를 읽어 합칩니다.

최유진님: 그러면 히트율이 올라갑니다. 겹치는 기간을 조회할 때 재사용됩니다.

최유진님: 1시간 캐시인데 집계는 하루 1회입니다. 캐시 시간을 늘려도 되지 않나요.

배현우님: 최근 30일은 재집계로 값이 바뀝니다. 다만 재집계도 하루 1회라 1시간은 짧습니다. 6시간 정도로 늘리고, 집계 배치 완료 시 캐시를 무효화하겠습니다.

최유진님: 무효화가 실패하면 최대 6시간 동안 옛 값이 보입니다. 지표는 실시간성이 낮으니 괜찮을 것 같은데, 배치 직후에 확인하는 사람이 있으면 혼란스럽습니다.

배현우님: 응답에 `dataAsOf` 를 넣고 있습니다. 언제 기준 데이터인지 보이므로 오해는 줄어듭니다.

## 수정 사항

- 캐시 키를 날짜 단위로 변경
- TTL 6시간
- 집계 배치 완료 시 최근 30일 캐시 무효화
- `dataAsOf` 를 캐시된 값과 함께 반환

## 남은 확인

날짜 단위 캐시로 바꾸면 조회당 키 조회가 여러 번 일어납니다. 1년 조회면 365번입니다. 다중 조회(mget)를 지원하는지 확인이 필요합니다.

지원하지 않으면 월 단위로 묶는 절충안을 검토합니다.
""",
    },
    {
        "id": "DOC-253",
        "title": "인프라 비용 절감 방안 논의",
        "department": "DevOps팀",
        "type": "개발자 회의록",
        "project": None,
        "date": "2026-06-15",
        "systems": [],
        "features": [],
        "participants": ["오세훈", "임채원"],
        "concepts": ["비용", "인프라", "우선순위"],
        "body": """## 논의 배경

인프라 비용이 전년 대비 62% 늘었습니다. 트래픽 증가(38%)보다 증가율이 높습니다.

## 비용 구성

오세훈님이 정리한 내역입니다.

| 항목 | 비중 | 전년 대비 |
| --- | --- | --- |
| 컴퓨팅 | 41% | +48% |
| 데이터베이스 | 28% | +71% |
| 스토리지 | 16% | +92% |
| 네트워크 | 9% | +31% |
| 기타 | 6% | +22% |

스토리지 증가율이 가장 높습니다.

## 스토리지

리뷰 이미지가 큰 비중입니다. 원본을 그대로 저장하고 있어 건당 평균 3.8MB 입니다.

리사이즈해서 저장하면 대부분 500KB 이하가 됩니다. 원본이 필요한 경우는 없습니다.

기존 이미지도 일괄 변환할 수 있습니다. 약 40TB 가 6TB 로 줄어듭니다.

## 데이터베이스

인스턴스 크기를 계속 키워왔습니다. 실제 사용률을 보니 CPU 평균 22%, 피크 61% 입니다. 한 단계 낮춰도 여유가 있습니다.

다만 이벤트 시 피크가 높아 위험이 있습니다. 임채원님이 이벤트 일정을 공유하면 그때만 임시로 올리는 방식을 제안했습니다.

## 컴퓨팅

인스턴스 수가 트래픽과 무관하게 고정입니다. 새벽 시간대에도 주간과 같은 수를 유지합니다.

자동 확장을 적용하면 야간 비용이 줄어듭니다. 다만 배치가 새벽에 돌아 무작정 줄일 수는 없습니다.

## 결정 사항

우선순위 순입니다.

1. **리뷰 이미지 리사이즈** — 스토리지 40TB → 6TB 예상. 효과 크고 위험 낮음
2. **로그 보관 최적화** — 이미 일부 적용. 디버그 로그 추가 정리
3. **컴퓨팅 자동 확장** — 야간 축소. 배치 영향 확인 필요
4. **DB 인스턴스 조정** — 마지막. 위험이 가장 큼

## 하지 않기로 한 것

캐시 서버 축소는 하지 않습니다. 비용 비중이 작고 장애 시 영향이 큽니다.

## Action Item

- 오세훈님: 이미지 리사이즈 배치, 자동 확장 검토
- 임채원님: 이벤트 일정 공유 체계
""",
    },
    {
        "id": "DOC-254",
        "title": "관리자 대시보드 항목 우선순위",
        "department": "서비스기획팀",
        "type": "기획자와의 회의록",
        "project": "Project Atlas",
        "date": "2026-06-29",
        "systems": ["AIMS"],
        "features": ["운영 관리"],
        "participants": ["강민재", "임채원"],
        "concepts": ["대시보드", "우선순위", "운영 효율"],
        "body": """## 논의 배경

운영 대시보드 항목을 정합니다. 4월 회의에서 방향은 정했고 구체적인 항목을 확정합니다.

## 후보 항목

임채원님이 팀별 요청을 취합했습니다.

| 항목 | 요청 팀 | 갱신 필요 주기 |
| --- | --- | --- |
| 미답변 문의 수 | CS | 5분 |
| SLA 초과 문의 | CS | 5분 |
| 승인 대기 환불 | CS, 재무 | 5분 |
| 배송 지연 주문 | CS | 1시간 |
| 재고 임박 상품 | 운영 | 1시간 |
| 오늘 주문 수 | 운영, 기획 | 5분 |
| 결제 실패율 | 운영 | 5분 |
| 신규 리뷰 신고 | 운영 | 1시간 |
| 폴링 중단 배송 | CS | 1시간 |

## 쟁점

강민재님은 항목이 많으면 아무도 안 본다고 했습니다. 첫 화면에는 5개 이하가 적당합니다.

임채원님은 팀별로 필요한 것이 다르므로 개인화가 필요하다고 봤습니다. 다만 개인화 기능을 만들면 범위가 커집니다.

## 결정 사항

역할별 기본 화면을 제공합니다. 개인화는 넣지 않습니다.

**CS_AGENT / CS_LEAD**

1. 미답변 문의 (SLA 초과 강조)
2. 승인 대기 환불
3. 배송 지연 주문
4. 폴링 중단 배송

**OPERATOR**

1. 오늘 주문 수
2. 재고 임박 상품
3. 신규 리뷰 신고
4. 결제 실패율

**FINANCE**

1. 승인 대기 환불
2. 오늘 결제액
3. 환불 처리 현황

## 표시 방식

각 항목은 숫자와 함께 "클릭하면 목록으로 이동"합니다. 숫자만 보여주면 다음 행동이 없습니다.

임계값을 넘으면 색으로 강조합니다. 예를 들어 SLA 초과 문의가 5건을 넘으면 붉게 표시합니다.

## 미확정

임계값을 누가 관리할지 정하지 않았습니다. 팀마다 기준이 다를 수 있는데, 설정 화면을 만들면 범위가 커집니다.

초기값을 코드에 두고 운영하면서 조정하기로 했습니다. 자주 바뀌면 그때 설정으로 뺍니다.

## Action Item

- 강민재님: 항목별 표시 문구와 임계값 초기안
- 임채원님: 개발 일정 산정
""",
    },
    {
        "id": "DOC-255",
        "title": "데이터 플랫폼 개선 현황",
        "department": "데이터팀",
        "type": "프로젝트 진행상황",
        "project": None,
        "date": "2026-07-03",
        "systems": ["ORBIT", "MARS", "NEXUS"],
        "features": [],
        "participants": [],
        "concepts": ["파이프라인", "지표", "일정"],
        "body": """## 범위

- 적재 파이프라인 증분 전환
- 지표 정의 통일
- `daily_metrics` 집계
- 데이터 추출 요청 시스템

## 완료

- 주문/결제/리워드 증분 적재 전환
- 지표 정의 문서화
- `daily_metrics` 집계 배치
- 통계 조회 API
- 데이터 추출 요청/승인

## 진행 중

- 화면의 집계 테이블 전환 (12개 중 9개)
- 지표 캐시 계층 개선

## 대기

- 검색 지표 수집 파이프라인
- CDC 도입 검토

## 성과

**적재 시간**

| 시점 | 소요 |
| --- | --- |
| 전환 전 | 4시간 12분 |
| 전환 후 | 1시간 08분 |

증분 전환 대상이 전체의 70% 였고 예상대로 줄었습니다.

**지표 정합성**

집계 테이블 도입 후 화면 간 수치 차이가 대부분 해소됐습니다. 5월에 발견된 정산 화면 불일치는 지표 이름을 분리해 해결했습니다.

## 남은 문제

전환되지 않은 화면 3개가 여전히 직접 계산합니다. 목록은 파악했고 7월 중 완료 목표입니다.

`updated_at` 이 없는 테이블 5개가 전체 스냅샷으로 남아 있습니다. 컬럼 추가와 기존 행 채우기가 필요한데 우선순위가 밀렸습니다.

## 다음 분기

CDC 도입을 재검토합니다. 증분 적재로 상당 부분 해결됐지만 여전히 하루 지연이 있습니다. 실시간에 가까운 지표 요구가 늘면 필요합니다.

현재는 요구가 강하지 않아 우선순위가 높지 않습니다.
""",
    },
]
