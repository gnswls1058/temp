"""DOC-031 ~ DOC-045 : Aurora 후반 - 휴면, 탈퇴, 소셜 계정 통합."""

DOCUMENTS = [
    {
        "id": "DOC-031",
        "title": "휴면 회원 처리 기준 재정비",
        "department": "백엔드개발팀",
        "type": "개발자 회의록",
        "project": "Project Aurora",
        "date": "2025-03-19",
        "systems": ["NEXUS"],
        "features": ["휴면 처리"],
        "participants": ["이서연", "오세훈"],
        "concepts": ["배치", "상태 전이", "부하"],
        "body": """## 논의 배경

회원 상태를 `status` 단일 컬럼으로 통합하면서 휴면 배치도 같이 손봐야 합니다. 기존 배치는 `is_dormant` 를 기준으로 돌고 있어 마이그레이션 이후 동작하지 않습니다.

## 현재 배치 구조

매일 새벽 3시에 전체 회원을 스캔하면서 마지막 로그인이 1년을 넘긴 계정을 휴면 처리합니다. 회원 수가 늘면서 실행 시간이 40분을 넘었고, 그동안 회원 테이블에 락 경합이 생깁니다.

## 논의 내용

오세훈님이 지적한 것은 스캔 범위입니다. 매일 전체를 보는데 실제 대상은 하루 수천 건입니다. `status_changed_at` 이 아니라 `last_login_at` 기준 인덱스가 필요합니다.

이서연님은 배치를 하루 한 번이 아니라 시간대별로 나눠 돌리는 안을 냈습니다. 대상 건수가 균등하지 않아 실효가 크지 않을 수 있다는 지적이 있었습니다.

## 결정 사항

- `last_login_at` 인덱스를 추가하고 조회 조건을 바꾼다
- 한 번에 처리하는 건수를 1,000건으로 제한하고 반복 실행한다
- 처리 결과를 배치 실행 이력 테이블에 남긴다

## 보류

휴면 전환 시점을 1년에서 6개월로 줄이자는 기획팀 요청이 있었는데, 이번 작업에서는 반영하지 않습니다. 대상 건수가 크게 늘어 첫 실행 시 수십만 건이 한 번에 처리됩니다. 기준 변경은 별도 계획이 필요합니다.

## Action Item

- 이서연님: 배치 조회 조건 및 분할 실행 구현
- 오세훈님: 인덱스 추가 시 운영 반영 절차 확인
""",
    },
    {
        "id": "DOC-032",
        "title": "휴면 전환 및 복귀 처리 설계",
        "department": "백엔드개발팀",
        "type": "비즈니스 기능 설계",
        "project": "Project Aurora",
        "date": "2025-03-24",
        "systems": ["NEXUS", "BlueGate"],
        "features": ["휴면 처리", "로그인"],
        "participants": [],
        "concepts": ["상태 전이", "Edge Case", "배치", "동시성"],
        "body": """## 배경

휴면 처리는 개인정보 보호 목적과 어뷰징 방지 목적이 섞여 있습니다. 여기서는 상태 전이와 예외 상황을 정리합니다.

## 전환 조건

- 마지막 로그인으로부터 365일 경과
- 상태가 `ACTIVE` 인 회원만 대상
- 전환 30일 전, 7일 전에 안내 메일 발송

## 전환 시 처리

1. `status` 를 `DORMANT` 로 변경
2. 해당 회원의 모든 Refresh Token 폐기
3. 개인정보 일부를 분리 보관 영역으로 이동
4. 휴면 전환 이력 기록

## 복귀 Flow

1. 로그인 시도 시 `AUTH_1010` 응답
2. 클라이언트가 휴면 해제 화면으로 이동
3. 이메일 인증 후 `ACTIVE` 복귀
4. 분리 보관된 정보 복원

## Edge Case

- **전환 배치 도중 로그인**: 배치가 대상을 조회한 뒤 실제 전환 전에 로그인이 성공할 수 있습니다. 전환 시점에 `last_login_at` 을 다시 확인해 조건을 만족하지 않으면 건너뜁니다.
- **휴면 상태에서 탈퇴 요청**: 로그인이 막혀 있으므로 앱에서는 탈퇴할 수 없습니다. 고객지원팀 요청으로 AIMS 에서 처리합니다.
- **복귀 중 인증 메일 미수신**: 재발송은 가능하지만 휴면 상태에서는 발송 한도를 별도로 둡니다. 일반 인증과 같은 한도를 쓰면 복귀가 막히는 사례가 나옵니다.
- **분리 보관 이동 실패**: 상태만 바뀌고 데이터 이동이 실패하면 반쪽 상태가 됩니다. 상태 변경과 이동을 같은 트랜잭션에 넣기 어려워서, 이동 실패 시 상태를 되돌리는 보상 처리를 둡니다.
- **복귀 직후 재휴면**: `last_login_at` 이 갱신되므로 다시 365일이 필요합니다. 복귀만 하고 서비스를 안 쓰는 계정은 1년 뒤 다시 대상이 됩니다.

## 고려한 방법

분리 보관 대신 컬럼 암호화만 적용하는 안도 있었습니다. 구현은 간단하지만 휴면 회원 데이터를 물리적으로 분리하라는 내부 기준을 만족하지 못해 제외했습니다.
""",
    },
    {
        "id": "DOC-033",
        "title": "회원 조회 API v2 명세",
        "department": "백엔드개발팀",
        "type": "API 명세서",
        "project": "Project Aurora",
        "date": "2025-03-26",
        "systems": ["NEXUS"],
        "features": ["프로필"],
        "participants": [],
        "concepts": ["Authentication", "버전 변경", "마스킹"],
        "body": """## 변경 개요

v1 은 회원 전체 정보를 한 번에 내려줬습니다. v2 는 호출자 권한에 따라 필드를 다르게 반환합니다.

## Endpoint

```http
GET /members/{memberId}
Authorization: Bearer {accessToken}
```

## 권한별 응답 필드

| 필드 | 본인 | 운영자(AIMS) | 타 서비스 |
| --- | --- | --- | --- |
| memberId | O | O | O |
| email | O | 마스킹 | X |
| name | O | O | O |
| phone | O | 마스킹 | X |
| status | O | O | O |
| createdAt | O | O | X |
| lastLoginAt | X | O | X |

마스킹 형식은 `ab***@example.com`, `010-****-1234` 입니다.

## Response

```json
{
  "memberId": 20481,
  "email": "us***@example.com",
  "name": "홍길동",
  "status": "ACTIVE",
  "createdAt": "2025-01-30T10:12:00+09:00"
}
```

## Error Code

| 코드 | HTTP | 설명 |
| --- | --- | --- |
| MEMBER_3001 | 404 | 회원 없음 |
| MEMBER_3002 | 403 | 조회 권한 없음 |
| MEMBER_3003 | 410 | 탈퇴 후 삭제된 회원 |

## 주의사항

`MEMBER_3003` 은 회원이 존재했다가 삭제된 경우입니다. 404 와 구분하는 이유는 주문 이력 화면에서 "탈퇴한 회원" 표시가 필요하기 때문입니다.

타 서비스에서 호출할 때는 서비스 토큰을 사용하며, 이 경우 조회 가능한 필드가 가장 좁습니다. 결제나 리워드에서 회원 이름이 필요한 경우가 있는데 이름은 허용 필드에 포함되어 있습니다.
""",
    },
    {
        "id": "DOC-034",
        "title": "새벽 배치 구간 DB 응답 지연",
        "department": "DevOps팀",
        "type": "이슈 정리",
        "project": "Project Aurora",
        "date": "2025-04-02",
        "systems": ["NEXUS"],
        "features": ["휴면 처리"],
        "participants": ["오세훈"],
        "concepts": ["부하", "배치", "Slow Query"],
        "body": """## 현상

4월 1일 새벽 3시 5분부터 3시 40분까지 NEXUS API 응답 시간이 평소 대비 6배로 올랐습니다. 같은 시간대에 커넥션 풀 대기가 발생했습니다.

## 로그 분석

```sql
-- 실행 시간 12초, 전체 스캔
SELECT id, email, last_login_at
FROM member
WHERE status = 'ACTIVE'
  AND last_login_at < DATE_SUB(NOW(), INTERVAL 365 DAY);
```

`last_login_at` 인덱스가 아직 반영되지 않은 상태에서 분할 실행이 먼저 배포되었습니다. 분할 실행은 같은 조회를 반복하므로 전체 스캔이 여러 번 발생했습니다. 기존보다 오히려 악화됐습니다.

## 영향 범위

- NEXUS 조회 API 지연
- 같은 인스턴스를 쓰는 ORBIT 주문 조회 일부 지연
- 사용자 영향은 새벽 시간대라 제한적

## 임시 대응

배치 스케줄을 중단하고 다음 실행부터 수동 승인으로 전환했습니다.

## 근본 해결

인덱스를 먼저 추가하고 배치를 재개합니다. 오세훈님이 4월 3일 새벽에 온라인 DDL 로 인덱스를 추가할 예정입니다.

분할 실행에서 매번 처음부터 조회하는 대신 마지막 처리 ID 를 커서로 넘기도록 바꾸는 것도 필요합니다. 인덱스가 있어도 반복 조회는 낭비입니다.

## 재발 방지

인덱스 추가와 이를 전제로 한 코드는 배포 순서를 함께 관리해야 합니다. 배포 체크리스트에 "선행 스키마 변경 여부" 항목을 넣기로 했습니다.
""",
    },
    {
        "id": "DOC-035",
        "title": "휴면 안내 메일 발송 시점 논의",
        "department": "서비스기획팀",
        "type": "기획자와의 회의록",
        "project": "Project Aurora",
        "date": "2025-04-08",
        "systems": ["NEXUS"],
        "features": ["휴면 처리"],
        "participants": ["송지은", "문가영"],
        "concepts": ["고객 문의", "정책 미확정"],
        "body": """## 논의 배경

휴면 전환 안내를 30일 전, 7일 전 두 번 보내기로 했는데 고객지원팀에서 문의가 늘었다는 얘기가 나왔습니다.

## 현황

문가영님이 공유한 3월 문의 유형입니다.

| 유형 | 건수 |
| --- | --- |
| 휴면 안내를 받았는데 무슨 뜻인지 모르겠다 | 62 |
| 휴면이 되면 데이터가 지워지나 | 41 |
| 안내를 못 받고 휴면이 됐다 | 18 |

세 번째 유형이 문제입니다. 확인해 보니 마케팅 수신 동의를 안 한 고객에게는 안내 메일이 발송되지 않고 있었습니다.

## 쟁점

휴면 안내는 마케팅이 아니라 서비스 고지에 해당하므로 수신 동의와 무관하게 발송해야 한다는 것이 송지은님 의견입니다. 다만 현재 발송 시스템이 수신 동의 여부로 일괄 필터링하고 있어 유형 구분이 필요합니다.

## 결정 사항

- 발송 유형에 `SERVICE_NOTICE` 를 추가하고 수신 동의 필터에서 제외한다
- 안내 문구에 "데이터는 삭제되지 않고 분리 보관됩니다" 를 명시한다

## 미확정

7일 전 안내를 앱 푸시로도 보낼지는 정하지 못했습니다. 푸시 수신 동의는 별도라 같은 문제가 반복될 수 있고, 휴면 대상이면 앱을 안 쓴 지 오래라 푸시 도달률이 낮을 것 같습니다. 데이터 확인 후 다시 논의합니다.
""",
    },
    {
        "id": "DOC-036",
        "title": "[리뷰] MemberStatusScheduler 커서 기반 분할",
        "department": "백엔드개발팀",
        "type": "코드리뷰",
        "project": "Project Aurora",
        "date": "2025-04-10",
        "systems": ["NEXUS"],
        "features": ["휴면 처리"],
        "participants": ["이서연", "김도윤"],
        "concepts": ["배치", "Query 최적화", "재시도"],
        "body": """## 현재 구현

분할 실행을 커서 기반으로 바꿨습니다. 마지막 처리한 `member.id` 를 기억하고 다음 실행에서 그 이후부터 조회합니다.

```sql
SELECT id, email, last_login_at
FROM member
WHERE status = 'ACTIVE'
  AND last_login_at < :threshold
  AND id > :lastId
ORDER BY id
LIMIT 1000;
```

## 리뷰 의견

김도윤님: 커서를 어디에 저장하나요. 인메모리면 인스턴스가 바뀔 때 처음부터 다시 돕니다.

이서연님: 배치 실행 이력 테이블에 저장합니다. 실행 시작 시 마지막 성공 커서를 읽습니다.

김도윤님: 한 사이클이 끝나면 커서를 0 으로 되돌려야 합니다. 지금 코드에는 리셋 조건이 없어서 조회 결과가 비면 커서가 최대값에 머무릅니다. 다음 날 새로 휴면 대상이 된 회원 중 ID 가 작은 계정은 영영 처리되지 않습니다.

이서연님: 놓쳤습니다. 조회 결과가 비면 커서를 초기화하겠습니다.

김도윤님: `nit:` 인덱스가 `(status, last_login_at)` 인데 정렬은 `id` 로 합니다. 정렬용 파일소트가 생길 수 있으니 실행 계획을 한 번 확인해 주세요.

## 수정 사항

- 조회 결과가 비면 커서 초기화
- 커서를 배치 실행 이력에 저장
- 실행 계획 확인 결과 첨부

## 남은 확인

실행 계획을 보니 대상 건수가 적을 때는 문제없지만 첫 실행처럼 대상이 많으면 정렬 비용이 있습니다. `(status, last_login_at, id)` 로 인덱스를 확장할지 검토가 필요합니다. 이번 PR 에서는 그대로 두고 운영 지표를 보고 판단합니다.
""",
    },
    {
        "id": "DOC-037",
        "title": "회원 탈퇴 API 명세",
        "department": "백엔드개발팀",
        "type": "API 명세서",
        "project": "Project Aurora",
        "date": "2025-04-15",
        "systems": ["NEXUS", "ORBIT"],
        "features": ["회원 탈퇴"],
        "participants": [],
        "concepts": ["Validation", "선행 조건"],
        "body": """## Endpoint

```http
DELETE /members/me
Authorization: Bearer {accessToken}
```

본인만 호출할 수 있습니다. 운영자 강제 탈퇴는 AIMS 전용 API 를 사용합니다.

## Request

```json
{
  "reasonCode": "NOT_USING",
  "reasonDetail": "앱을 잘 사용하지 않아서"
}
```

`reasonCode` 는 필수이며 `reasonDetail` 은 선택입니다.

## 탈퇴 불가 조건

다음 상태에서는 탈퇴할 수 없습니다.

| 조건 | 코드 | 안내 |
| --- | --- | --- |
| 진행 중 주문 존재 | MEMBER_4001 | 배송 완료 후 가능 |
| 취소/환불 처리 중 | MEMBER_4002 | 처리 완료 후 가능 |
| 미사용 쿠폰 존재 | - | 차단하지 않음, 안내만 |

미사용 적립금도 차단하지 않습니다. 소멸 안내만 화면에 표시합니다.

## Response

```json
{
  "memberId": 20481,
  "status": "WITHDRAWN",
  "withdrawnAt": "2025-04-15T11:02:00+09:00",
  "personalDataDeletedAt": "2025-05-15T00:00:00+09:00"
}
```

## Error Code

| 코드 | HTTP | 설명 |
| --- | --- | --- |
| MEMBER_4001 | 409 | 진행 중인 주문 존재 |
| MEMBER_4002 | 409 | 취소 또는 환불 처리 중 |
| MEMBER_4003 | 409 | 이미 탈퇴한 회원 |

## 주의사항

탈퇴 시 ORBIT 에 진행 중 주문을 조회합니다. ORBIT 이 응답하지 않으면 탈퇴를 진행하지 않고 503 을 반환합니다. 확인 없이 탈퇴시키면 배송 중 주문의 수령자 정보를 잃습니다.

탈퇴 즉시 토큰이 전부 폐기되므로 응답을 받은 클라이언트는 로그인 화면으로 이동해야 합니다.
""",
    },
    {
        "id": "DOC-038",
        "title": "회원 탈퇴 및 개인정보 삭제 설계",
        "department": "백엔드개발팀",
        "type": "비즈니스 기능 설계",
        "project": "Project Aurora",
        "date": "2025-04-18",
        "systems": ["NEXUS", "ORBIT", "MARS"],
        "features": ["회원 탈퇴"],
        "participants": [],
        "concepts": ["데이터 삭제", "정합성", "보상 처리", "Edge Case"],
        "body": """## 배경

탈퇴는 여러 서비스에 걸쳐 있습니다. 회원 정보는 NEXUS, 주문 이력은 ORBIT, 적립금은 MARS 에 있고 삭제 기준이 각각 다릅니다.

## 삭제 정책

| 데이터 | 위치 | 처리 |
| --- | --- | --- |
| 회원 개인정보 | NEXUS | 30일 후 삭제 |
| 이메일 해시 | NEXUS | 90일 후 삭제 |
| 주문 이력 | ORBIT | 5년 보관 (법정) |
| 결제 이력 | ORBIT | 5년 보관 |
| 적립금 잔액 | MARS | 즉시 소멸 |
| 쿠폰 | MARS | 즉시 소멸 |

주문 이력은 남기되 회원 식별 정보는 지웁니다. 주문 테이블의 수령자명과 연락처는 배송 완료 후 6개월이 지나면 마스킹합니다.

## 탈퇴 Flow

1. 진행 중 주문 확인 (ORBIT 동기 호출)
2. 회원 상태를 `WITHDRAWN` 으로 변경
3. 토큰 전체 폐기
4. 탈퇴 이벤트 발행
5. MARS 가 이벤트를 받아 적립금과 쿠폰 소멸
6. 30일 후 배치가 개인정보 삭제

## Edge Case

- **이벤트 유실**: MARS 가 이벤트를 못 받으면 적립금이 남습니다. 탈퇴 회원의 적립금은 사용할 수 없으므로 즉각적인 문제는 없지만 정합성이 깨집니다. 일 1회 대조 배치로 탈퇴 회원의 잔여 적립금을 정리합니다.
- **탈퇴 후 주문 취소 발생**: 배송 완료 직후 탈퇴했는데 이후 반품이 접수되는 경우가 있습니다. 환불 계좌 정보를 받을 수 없어 고객지원팀 수동 처리가 필요합니다. 현재 월 2~3건 발생합니다.
- **삭제 배치 실행 중 재가입**: 30일이 지나면 같은 이메일로 재가입이 가능합니다. 삭제 배치가 돌기 전에 재가입하면 이메일 unique 제약에 걸립니다. 삭제 시점을 30일 정각이 아니라 30일 경과 후 첫 배치로 두고, 재가입 판정도 같은 기준을 씁니다.
- **탈퇴 취소 요청**: 정책상 취소는 없습니다. 실수 탈퇴는 30일 안에 고객지원팀을 통해 복구할 수 있게 하자는 의견이 있었지만, 복구 시 적립금은 이미 소멸된 상태라 원상복구가 불가능합니다. 현재는 지원하지 않습니다.

## 고민 사항

적립금 즉시 소멸이 사용자에게 불리하다는 지적이 있습니다. 탈퇴 화면에서 잔여 적립금을 명확히 보여주는 것으로 갈음하고 있는데, 실제로 소멸 후 문의가 들어오는 경우가 있습니다. 정책 재검토가 필요합니다.
""",
    },
    {
        "id": "DOC-039",
        "title": "member_withdrawal 이력 테이블 설계",
        "department": "백엔드개발팀",
        "type": "DB 테이블 설계안",
        "project": "Project Aurora",
        "date": "2025-04-22",
        "systems": ["NEXUS"],
        "features": ["회원 탈퇴"],
        "participants": [],
        "concepts": ["history 테이블", "개인정보", "정합성"],
        "body": """## 배경

탈퇴 사유를 수집하기로 하면서 별도 테이블이 필요해졌습니다. `member` 에 컬럼을 붙이지 않는 이유는 개인정보 삭제 시점이 다르기 때문입니다.

## 테이블

| 컬럼 | 타입 | 제약 | 설명 |
| --- | --- | --- | --- |
| id | BIGINT | PK | |
| member_id | BIGINT | NOT NULL | FK 걸지 않음 |
| email_hash | CHAR(64) | NOT NULL | 재가입 판정용 |
| reason_code | VARCHAR(32) | NOT NULL | |
| reason_detail | VARCHAR(500) | NULL | 사용자 입력 |
| withdrawn_at | DATETIME | NOT NULL | |
| personal_data_deleted_at | DATETIME | NULL | 삭제 배치 처리 시각 |
| hash_deleted_at | DATETIME | NULL | 해시 삭제 시각 |

```sql
CREATE INDEX idx_withdrawal_hash ON member_withdrawal (email_hash, withdrawn_at);
CREATE INDEX idx_withdrawal_pending ON member_withdrawal (personal_data_deleted_at, withdrawn_at);
```

## 왜 FK 를 걸지 않는가

`member` 행은 30일 후 삭제됩니다. FK 가 있으면 삭제가 막히거나 cascade 로 이력까지 지워집니다. 이력은 남아야 하므로 FK 없이 ID 만 보관합니다.

## reason_detail 처리

사용자가 자유 입력한 텍스트라 개인정보가 들어올 수 있습니다. "전화번호가 노출돼서" 같은 내용에 실제 번호를 적는 경우가 있습니다. 개인정보 삭제 시점에 `reason_detail` 도 함께 비웁니다. 통계는 `reason_code` 만으로 냅니다.

## 인덱스 설계

- `idx_withdrawal_hash`: 재가입 시 90일 이내 탈퇴 이력 조회
- `idx_withdrawal_pending`: 삭제 대상 조회. `personal_data_deleted_at IS NULL` 조건이 선행하므로 해당 컬럼을 앞에 뒀습니다

## 고민

같은 이메일로 탈퇴와 재가입을 반복하면 이력이 여러 건 쌓입니다. 재가입 판정은 가장 최근 건만 보면 되는데, 인덱스만으로 최신 건을 뽑기 위해 `withdrawn_at` 을 뒤에 붙였습니다. 어뷰징 분석에는 전체 이력이 유용하므로 삭제하지 않고 유지합니다.
""",
    },
    {
        "id": "DOC-040",
        "title": "Aurora 릴리스 후 1주차 지표 정리",
        "department": "프로젝트관리팀",
        "type": "프로젝트 진행상황",
        "project": "Project Aurora",
        "date": "2025-04-29",
        "systems": ["NEXUS"],
        "features": ["회원가입"],
        "participants": ["임채원", "배현우"],
        "concepts": ["지표", "전환율", "회고"],
        "body": """## 릴리스 개요

4월 21일 운영 배포했습니다. 2단계 가입, 가입 후 이메일 인증, 회원 상태 통합이 포함되었습니다.

## 주요 지표

배현우님이 뽑아준 배포 전후 1주 비교입니다.

| 지표 | 배포 전 | 배포 후 | 변화 |
| --- | --- | --- | --- |
| 가입 시작 대비 완료율 | 58% | 71% | +13%p |
| 평균 가입 소요 시간 | 2분 41초 | 1분 12초 | -55% |
| 이메일 인증 완료율 | 96% (가입 전 필수) | 78% (3일 내) | -18%p |
| 미인증 자동 삭제 | - | 일 평균 214건 |  |

## 해석

가입 완료율은 목표치를 넘었습니다. 다만 이메일 인증 완료율이 78% 로 떨어졌습니다. 가입은 했지만 인증을 안 한 계정이 하루 200건 넘게 삭제되고 있습니다.

이 계정들이 정말 이탈한 것인지, 인증 메일을 못 받은 것인지 구분이 안 됩니다. 메일 도달률 지표가 없어서 확인이 어렵습니다.

## 후속 과제

- 메일 도달률 및 열람률 지표 추가 (데이터팀 요청 등록)
- 미인증 상태 앱 재진입 시 인증 유도 화면 추가 검토
- 3일 유예가 짧은지 검토. 7일로 늘리는 안이 나왔습니다

## 미해결

재가입 제한 로직은 여전히 미배포 상태입니다. MARS 연동 일정이 Nova 프로젝트와 겹쳐 5월 중순 이후로 밀렸습니다.
""",
    },
    {
        "id": "DOC-041",
        "title": "탈퇴 회원 주문 상세 조회 시 오류",
        "department": "백엔드개발팀",
        "type": "이슈 정리",
        "project": "Project Aurora",
        "date": "2025-05-07",
        "systems": ["NEXUS", "ORBIT"],
        "features": ["회원 탈퇴", "주문"],
        "participants": [],
        "concepts": ["정합성", "서비스 간 호출", "예외 처리"],
        "body": """## 현상

AIMS 에서 주문 상세를 조회할 때 500 이 발생합니다. 탈퇴한 회원의 주문에서만 재현됩니다.

## 재현

1. 회원 탈퇴 처리
2. AIMS 에서 해당 회원의 과거 주문 상세 조회
3. 500 응답

## 로그

```text
NullPointerException at OrderDetailAssembler.assemble(OrderDetailAssembler.java:64)
  member response status=410 body={"code":"MEMBER_3003"}
```

## 분석

ORBIT 이 주문 상세를 만들 때 NEXUS 회원 조회 API 를 호출합니다. 탈퇴 후 삭제된 회원은 410 과 `MEMBER_3003` 을 반환하는데, ORBIT 클라이언트가 2xx 가 아니면 null 을 돌려주고 있었습니다. 이후 이름을 참조하다 NPE 가 납니다.

410 응답 자체는 회원 조회 API v2 명세대로 동작한 것입니다. 호출하는 쪽이 이 케이스를 처리하지 않았습니다.

## 조치

- ORBIT 클라이언트에서 410 을 별도로 처리해 "탈퇴한 회원" 표시용 객체를 반환
- 404 와 410 을 구분해 로그 레벨을 다르게 설정

## 재발 방지

서비스 간 호출에서 신규 에러 코드가 추가되면 호출하는 쪽에 공유하는 절차가 없습니다. API 변경 시 소비자 목록을 확인하는 항목을 배포 체크리스트에 추가하자는 의견이 나왔고, 아직 반영되지 않았습니다.

비슷한 패턴이 MARS 에도 있을 수 있어 확인이 필요합니다. 아직 점검하지 않았습니다.
""",
    },
    {
        "id": "DOC-042",
        "title": "소셜 계정 통합 기술 검토",
        "department": "백엔드개발팀",
        "type": "개발자 회의록",
        "project": "Project Aurora",
        "date": "2025-05-13",
        "systems": ["NEXUS", "BlueGate"],
        "features": ["소셜 로그인", "회원가입"],
        "participants": ["김도윤", "한지민"],
        "concepts": ["계정 병합", "정합성", "Edge Case"],
        "body": """## 논의 배경

작년 Echo 에서 미뤄둔 소셜 계정 통합을 이번에 착수합니다. 기획 요구사항은 2024년 7월 문서 그대로이고, 기술 검토만 다시 합니다.

## 통합 시나리오

1. 이메일 회원이 소셜 로그인을 시도
2. 소셜 제공자 이메일이 기존 계정과 일치
3. "기존 계정에 연결하시겠습니까" 확인
4. 비밀번호 재확인 후 `member_auth` 에 행 추가

## 쟁점

한지민님이 앱에서 이미 소셜로만 가입한 계정이 있다는 점을 지적했습니다. 이 계정과 이메일 계정이 각각 존재하면 연결이 아니라 병합입니다. 주문 이력과 적립금을 어느 쪽으로 모을지 정해야 합니다.

김도윤님은 병합은 이번 범위에서 빼자는 입장입니다. 데이터 이관이 들어가면 실패 시 복구가 어렵고, 대상 계정 수를 먼저 확인해야 합니다.

## 결정 사항

- 이번 범위는 **연결만** 지원합니다. 소셜 전용 계정이 이미 있으면 연결을 막고 안내합니다.
- 병합은 별도 과제로 등록합니다.

## 확인 필요

- 이메일이 중복된 계정 쌍이 실제로 몇 건인지 데이터팀에 요청
- 애플 릴레이 이메일 계정은 판정 대상에서 제외

## Action Item

- 김도윤님: 연결 API 설계 초안
- 한지민님: 앱 연결 화면 플로우 확인
""",
    },
    {
        "id": "DOC-043",
        "title": "소셜 계정 연결 API 명세",
        "department": "플랫폼개발팀",
        "type": "API 명세서",
        "project": "Project Aurora",
        "date": "2025-05-20",
        "systems": ["BlueGate", "NEXUS"],
        "features": ["소셜 로그인"],
        "participants": [],
        "concepts": ["OAuth", "Validation", "Error Code"],
        "body": """## Endpoint

```http
POST /auth/social/link
Authorization: Bearer {accessToken}
```

로그인된 상태에서만 호출할 수 있습니다. 비로그인 상태의 자동 연결은 지원하지 않습니다.

## Request

| 필드 | 타입 | 필수 | 설명 |
| --- | --- | --- | --- |
| provider | string | Y | KAKAO / NAVER / APPLE |
| authorizationCode | string | Y | 제공자 인가 코드 |
| password | string | Y | 본인 확인용 현재 비밀번호 |

```json
{
  "provider": "KAKAO",
  "authorizationCode": "auth-code-sample",
  "password": "********"
}
```

소셜 전용 계정(비밀번호 없음)은 이 API 를 쓸 수 없습니다. `AUTH_4005` 가 반환됩니다.

## Response

```json
{
  "provider": "KAKAO",
  "linkedAt": "2025-05-20T15:31:00+09:00",
  "linkedProviders": ["KAKAO"]
}
```

## Error Code

| 코드 | HTTP | 설명 |
| --- | --- | --- |
| AUTH_4001 | 400 | 인가 코드 오류 또는 만료 |
| AUTH_4002 | 409 | 이미 연결된 제공자 |
| AUTH_4003 | 409 | 해당 소셜 계정이 다른 회원에 연결됨 |
| AUTH_4004 | 401 | 비밀번호 불일치 |
| AUTH_4005 | 409 | 비밀번호가 없는 계정 |

## 주의사항

`AUTH_4003` 이 나오면 이미 소셜 전용 계정이 존재한다는 뜻입니다. 계정 병합은 지원하지 않으므로 안내 화면에서 기존 계정으로 로그인하도록 유도해야 합니다.

연결 해제는 `DELETE /auth/social/{provider}` 입니다. 마지막 로그인 수단은 해제할 수 없습니다.
""",
    },
    {
        "id": "DOC-044",
        "title": "[리뷰] 소셜 연결 시 중복 판정 처리",
        "department": "플랫폼개발팀",
        "type": "코드리뷰",
        "project": "Project Aurora",
        "date": "2025-05-27",
        "systems": ["BlueGate", "NEXUS"],
        "features": ["소셜 로그인"],
        "participants": ["김도윤", "이서연"],
        "concepts": ["동시성", "Unique Constraint", "예외 처리"],
        "body": """## 현재 구현

`SocialLinkService.link()` 에서 제공자 사용자 ID 로 기존 연결을 조회하고, 없으면 `member_auth` 에 저장합니다.

## 리뷰 의견

이서연님: 조회 후 저장 사이에 동시 요청이 들어오면 둘 다 통과합니다. `member_auth` 에 `(auth_type, credential)` unique 제약이 없어서 중복 행이 생깁니다. 설계 문서에는 조회용 인덱스만 있고 unique 는 아닙니다.

김도윤님: 맞습니다. 같은 소셜 계정이 여러 회원에 연결되면 로그인 시 어느 회원인지 결정할 수 없습니다. unique 제약을 추가하겠습니다. 다만 연결 해제된 행이 남아 있어 `unlinked_at` 이 있는 행까지 제약에 걸리면 재연결이 막힙니다.

이서연님: 해제 시 행을 지우고 이력 테이블로 옮기는 방식으로 가면 깔끔합니다. 작년 설계 논의에서 결론을 못 냈던 부분인데 이번에 정하는 게 좋겠습니다.

김도윤님: 이번 PR 에서 해제 시 물리 삭제 + `member_auth_history` 적재로 바꾸겠습니다.

## 수정 사항

- `member_auth` 에 `(auth_type, credential)` unique 제약 추가
- 연결 해제 시 행 삭제 후 이력 테이블 적재
- unique 위반 예외를 `AUTH_4003` 으로 변환

## 남은 확인

기존 데이터에 중복이 있는지 먼저 확인해야 제약을 걸 수 있습니다. 운영 데이터 점검 후 마이그레이션 순서를 정하겠습니다. 제약 추가 전까지는 애플리케이션 레벨 방어만 있는 상태입니다.
""",
    },
    {
        "id": "DOC-045",
        "title": "회원 상태별 기능 제한 정의서",
        "department": "백엔드개발팀",
        "type": "매뉴얼 / 정의서",
        "project": None,
        "date": "2025-06-03",
        "systems": ["NEXUS", "ORBIT", "MARS"],
        "features": ["회원가입", "휴면 처리", "회원 탈퇴"],
        "participants": [],
        "concepts": ["상태값 관리", "권한"],
        "body": """## 목적

회원 상태에 따라 어떤 기능이 막히는지 서비스마다 다르게 구현되어 있어서 기준을 한 곳에 모읍니다. 신규 API 를 만들 때 이 표를 기준으로 검증하세요.

## 상태별 허용 범위

| 기능 | PENDING_VERIFICATION | ACTIVE | DORMANT | WITHDRAWN |
| --- | --- | --- | --- | --- |
| 로그인 | 가능 | 가능 | 차단 | 차단 |
| 프로필 조회 | 가능 | 가능 | 차단 | 차단 |
| 프로필 수정 | 가능 | 가능 | 차단 | 차단 |
| 상품 조회 | 가능 | 가능 | 차단 | 차단 |
| 주문 | 차단 | 가능 | 차단 | 차단 |
| 주문 이력 조회 | 가능 | 가능 | 차단 | 차단 |
| 적립금 사용 | 차단 | 가능 | 차단 | 차단 |
| 쿠폰 사용 | 차단 | 가능 | 차단 | 차단 |
| 해지 | 가능 | 가능 | 차단 | - |

## 응답 규칙

- `PENDING_VERIFICATION` 에서 차단된 기능은 403 과 `MEMBER_5001` 을 반환합니다
- `DORMANT` 는 401 과 `AUTH_1010` 입니다. 로그인 자체가 막히므로 대부분 여기서 걸립니다
- `WITHDRAWN` 은 401 과 `AUTH_1011` 입니다

## 예외

- 운영자가 AIMS 에서 조회하는 경우는 상태와 무관하게 허용됩니다
- 정산 및 통계 배치는 상태를 검사하지 않습니다
- 고객지원팀 조회 도구는 `DORMANT` 회원 조회가 가능합니다

## 유의

이 표는 2025년 6월 기준입니다. 상태가 추가되거나 정책이 바뀌면 이 문서를 먼저 갱신하고 구현하세요. 구현이 먼저 나가서 문서와 어긋난 사례가 있었습니다.
""",
    },
]
