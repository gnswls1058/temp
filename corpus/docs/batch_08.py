"""DOC-106 ~ DOC-120 : Falcon 마무리와 Project Atlas 시작 (2026 Q1~Q2)."""

DOCUMENTS = [
    {
        "id": "DOC-106",
        "title": "부분 취소 반환 금액이 1원 어긋나는 사례",
        "department": "백엔드개발팀",
        "type": "이슈 정리",
        "project": "Project Falcon",
        "date": "2026-03-18",
        "systems": ["ORBIT", "MARS"],
        "features": ["부분 취소", "환불"],
        "participants": [],
        "concepts": ["단수 처리", "배분", "정합성"],
        "body": """## 현상

3월 16일 배포 후 부분 취소에서 반환 금액이 1원 부족하거나 1원 많은 건이 나옵니다. 3일간 22건 확인했습니다.

## 재현

3분할 이상 주문에서 순차적으로 부분 취소할 때 마지막 취소에서 발생합니다.

```text
주문 총액 34,900 / 상품 3개 (11,633 / 11,633 / 11,634)
포인트 사용 5,000

1차 취소 (11,633): 반환 1,667 (올림)
2차 취소 (11,633): 반환 1,667 (올림)
3차 취소 (11,634): 잔여 1,666 이어야 하는데 1,667 반환
```

## 분석

설계상 마지막 취소에서는 잔여 전액을 반환해야 합니다. 구현에서 "마지막"을 판별하는 조건이 문제였습니다.

```java
boolean isLast = canceledItemCount + 1 == totalItemCount;
```

`canceledItemCount` 를 취소 이력 개수로 세고 있는데, 한 번의 요청으로 여러 상품을 취소하면 개수가 맞지 않습니다. 또 이미 취소된 상품을 다시 요청하는 경우도 카운트에 들어갑니다.

## 영향 범위

- 3일간 22건, 금액 차이는 건당 1~2원
- 사용자 불이익 방향(1원 부족)이 9건, 반대가 13건

## 조치

판별 조건을 개수가 아니라 금액 기준으로 바꿨습니다.

```java
boolean isLast = payment.remainingItemAmount() == canceledItemAmount;
```

취소 후 남는 상품 금액이 0이면 마지막입니다.

## 보정

1원 부족했던 9건은 보상 지급으로 처리했습니다. 1원 많았던 13건은 회수하지 않습니다.

## 재발 방지

배분 계산에 대한 단위 테스트가 2분할 케이스만 있었습니다. 3분할 이상과 다중 상품 동시 취소 케이스를 추가했습니다.

금액 배분은 단수 때문에 실수하기 쉬운 영역입니다. 새 배분 로직을 만들 때는 합계가 원 금액과 일치하는지 검증하는 테스트를 반드시 넣기로 했습니다.
""",
    },
    {
        "id": "DOC-107",
        "title": "Falcon 배포 후 회고",
        "department": "프로젝트관리팀",
        "type": "프로젝트 진행상황",
        "project": "Project Falcon",
        "date": "2026-03-25",
        "systems": ["ORBIT", "MARS"],
        "features": ["결제", "결제 취소", "부분 취소"],
        "participants": ["임채원", "김도윤", "강민재"],
        "concepts": ["회고", "멱등성", "일정"],
        "body": """## 배포 결과

3월 16일 운영 배포했습니다. 배포 후 9일간 지표입니다.

| 지표 | 배포 전 | 배포 후 |
| --- | --- | --- |
| 결제 성공률 | 96.2% | 97.1% |
| 중복 승인 | 월 1~2건 | 0건 |
| 승인 후 저장 실패 | 월 1~2건 | 0건 (보정 배치가 4건 자동 확정) |
| 부분 취소 CS 문의 | 주 30건 | 주 4건 |

## 잘된 점

김도윤님: 중복 방지 키와 주문 단위 차단을 이중으로 둔 것이 유효했습니다. QA 에서 클라이언트가 중복 방지 키를 유실하는 경우가 실제로 나왔는데 서버 차단이 잡았습니다. 하나만 믿었으면 운영에서 터졌을 겁니다.

`PENDING` 선기록 + 보정 배치도 예상대로 동작했습니다. 배포 후 4건이 자동 확정됐는데 예전 같으면 전부 수동 처리 대상이었습니다.

강민재님: 실패 유형별 화면 문구를 나눈 뒤 결제 재시도율이 올라갔습니다. 한도 초과에서 다른 수단으로 전환하는 비율이 눈에 띄게 늘었습니다.

## 아쉬운 점

임채원님: 부분 취소 정책 확정이 두 프로젝트에 걸쳐 4개월 걸렸습니다. Nova 에서 미확정으로 남긴 항목이 Falcon 일정을 잡아먹었습니다. 미확정 항목을 넘길 때 언제까지 정할지 함께 정해야 합니다.

김도윤님: 배포 직후 1원 오차 이슈가 나왔습니다. 배분 계산 테스트가 부족했습니다. 리뷰에서 같은 문제를 한 번 지적받았는데도 케이스를 충분히 늘리지 않았습니다.

강민재님: timeout 시뮬레이션 환경을 늦게 만들어서 관련 결함을 QA 후반에야 발견했습니다.

## 남은 과제

- 부분 취소 미지원 결제 수단 안내 (프론트 미반영)
- `PENDING` 급증 시 결제 차단 여부 (미결론)
- 주문 목록 조회 성능 개선 (12월 부하 이슈의 근본 원인)
- v1 API 종료 (6월 예정)

## 다음

Atlas 에서 운영자 강제 환불 기능을 만듭니다. 이번에 정리한 결제 취소 흐름을 그대로 쓰되 승인 절차가 추가됩니다.
""",
    },
    {
        "id": "DOC-108",
        "title": "결제 장애 대응 절차",
        "department": "백엔드개발팀",
        "type": "매뉴얼 / 정의서",
        "project": None,
        "date": "2026-03-27",
        "systems": ["ORBIT"],
        "features": ["결제"],
        "participants": [],
        "concepts": ["장애 대응", "운영", "에스컬레이션"],
        "body": """## 판단 기준

| 상황 | 등급 | 초기 대응 |
| --- | --- | --- |
| 결제 성공률 90% 미만 5분 지속 | 심각 | 온콜 즉시 소집 |
| `PENDING` 건수 평소 5배 | 심각 | PG 상태 확인 |
| 특정 결제 수단만 실패 | 보통 | 해당 수단 차단 검토 |
| 개별 건 실패 문의 | 낮음 | 로그 확인 후 개별 대응 |

## 1차 확인

1. 결제 대시보드에서 성공률과 p99 확인
2. PG 상태 페이지 확인
3. `PENDING` 건수 추이 확인
4. 최근 배포 이력 확인

배포 직후라면 원인 분석보다 롤백을 먼저 고려하세요.

## PG 장애로 판단되면

- PG 담당자 연락 (연락처는 사내 연락망 참조)
- 사용자 안내 문구 노출 요청 (기획팀)
- 결제 자체를 막을지는 리드가 판단합니다. 막으면 매출이 멈추고, 안 막으면 실패가 쌓입니다

## 우리 쪽 장애로 판단되면

- 커넥션 풀, DB 슬로우 쿼리, 애플리케이션 오류율 순으로 확인
- 롤백 가능하면 롤백
- 롤백이 안 되면 영향 범위를 좁히는 조치 (특정 수단 차단 등)

## 절대 하지 말 것

- `PENDING` 건을 일괄 `FAILED` 로 바꾸지 마세요. 실제 승인된 결제를 잃습니다
- PG 관리자 화면에서 임의로 취소하지 마세요. 우리 DB 와 어긋납니다
- 결제 데이터를 직접 UPDATE 하지 마세요

상태를 바꿔야 한다면 반드시 백엔드 리드 확인 후 이력을 남기고 진행합니다.

## 복구 후

- `PENDING` 잔여 건이 모두 확정됐는지 확인
- 보정 배치 알림이 남아 있는지 확인
- 영향받은 주문 목록을 정리해 고객지원팀에 공유
- 24시간 안에 장애 보고서 작성
""",
    },
    {
        "id": "DOC-109",
        "title": "2026 코드리뷰 규칙",
        "department": "백엔드개발팀",
        "type": "팀 Ground Rule",
        "project": None,
        "date": "2026-03-30",
        "systems": [],
        "features": [],
        "participants": [],
        "concepts": ["코드리뷰", "PR", "팀 규칙"],
        "body": """## 개정 배경

2024년 규칙을 2년 가까이 썼습니다. 팀이 커지면서 리뷰어 2명 필수가 병목이 되고, 리뷰 응답 24시간 규칙이 지켜지지 않는 경우가 많았습니다. 개정합니다.

## 리뷰어 지정

- PR 당 리뷰어는 **1명 이상**입니다. 이전 규칙(2명)에서 완화합니다
- 다음 경우에는 **2명 이상**을 지정합니다
  - 결제, 인증, 개인정보를 다루는 변경
  - DB 스키마 변경
  - 서비스 간 인터페이스 변경
- 도메인 담당자가 리뷰어에 반드시 포함되어야 합니다

## 응답 시간

- 평일 기준 **하루 안에** 첫 반응을 남깁니다. 전체 리뷰가 어려우면 "언제까지 보겠다"라도 남깁니다
- 하루가 지나면 작성자가 다른 리뷰어를 추가할 수 있습니다

## PR 크기

- 변경 라인 400 이하를 권장합니다 (이전 500에서 강화)
- 리팩터링과 기능 변경은 분리합니다. 같은 PR 에 섞이면 리뷰가 어렵습니다

## 코멘트

- 반드시 고쳐야 하는 것과 취향 차이를 구분합니다. 취향은 `nit:` 을 붙입니다
- 근거를 함께 씁니다. "이렇게 하면 X 상황에서 Y 문제가 생깁니다" 형태
- 같은 지적을 3번 이상 반복하게 되면 코딩 컨벤션이나 정적 분석 규칙으로 옮깁니다

## 추가된 항목

**설계 리뷰 선행**: 새로운 도메인 로직이나 동시성이 얽힌 변경은 코드 전에 설계 문서를 공유합니다. Nova 잔액 구조를 구현 후에 갈아엎은 경험에서 나온 규칙입니다.

**테스트 케이스 확인**: 금액 계산, 상태 전이, 동시성이 걸린 변경은 리뷰어가 테스트 케이스를 함께 봅니다. 경계값과 다중 실행 케이스가 있는지 확인합니다.

## 예외

- 장애 대응 핫픽스는 Approve 1개로 머지하고 다음 영업일에 사후 리뷰를 등록합니다
- 문서와 오타 수정은 Approve 1개면 됩니다
""",
    },
    {
        "id": "DOC-110",
        "title": "Admin 개편 요구사항 수집",
        "department": "서비스기획팀",
        "type": "기획자와의 회의록",
        "project": "Project Atlas",
        "date": "2026-04-01",
        "systems": ["AIMS"],
        "features": ["운영 관리", "회원 조회"],
        "participants": ["강민재", "문가영"],
        "concepts": ["요구사항", "운영 효율", "권한"],
        "body": """## 논의 배경

AIMS 는 4년 전에 만들어진 뒤 기능만 계속 붙었습니다. 고객지원팀이 실제로 쓰면서 겪는 불편을 모았습니다.

## 현장 요구사항

문가영님이 팀 내에서 수집한 내용입니다.

| 불편 | 빈도 | 내용 |
| --- | --- | --- |
| 고객 조회가 느림 | 매일 | 이름 검색 시 10초 이상 |
| 화면을 여러 개 오가야 함 | 매일 | 고객 - 주문 - 결제가 분리 |
| 권한이 전부 아니면 전무 | 주 단위 | 조회만 필요한데 수정 권한도 같이 부여됨 |
| 누가 무엇을 했는지 모름 | 사고 시 | 조작 이력이 없음 |
| 강제 환불 시 확인 절차 없음 | 월 2~3회 | 실수 방지 장치 부재 |

## 우선순위

강민재님과 정리한 순서입니다.

1. 권한 세분화 (조회 / 수정 / 승인)
2. 조작 이력(감사 이력)
3. 고객 조회 성능
4. 통합 조회 화면
5. 강제 환불 승인 절차

1번과 2번은 함께 가야 합니다. 권한을 나눠도 이력이 없으면 사고 시 추적이 안 됩니다.

## 쟁점

문가영님은 권한을 너무 잘게 나누면 업무가 막힌다고 우려했습니다. 담당자가 자리를 비우면 아무도 처리를 못 하는 상황이 생깁니다.

강민재님은 임시 권한 위임 기능을 넣자고 했습니다. 기간을 정해 권한을 빌려주고 자동 회수되는 방식입니다.

## 미확정

임시 권한 위임은 필요성에는 동의했지만 이번 범위에 넣을지 정하지 못했습니다. 개발 범위 산정 후 판단합니다.

## Action Item

- 문가영님: 팀별 필요 권한 목록 정리
- 강민재님: 우선순위 확정 후 개발팀 전달
""",
    },
    {
        "id": "DOC-111",
        "title": "Atlas 킥오프 - AIMS 개편 범위",
        "department": "백엔드개발팀",
        "type": "개발자 회의록",
        "project": "Project Atlas",
        "date": "2026-04-06",
        "systems": ["AIMS", "NEXUS", "ORBIT"],
        "features": ["운영 관리", "권한"],
        "participants": ["노태윤", "임채원"],
        "concepts": ["범위 산정", "권한", "감사 로그"],
        "body": """## 논의 배경

기획팀이 정리한 우선순위를 받아 개발 범위를 정합니다.

## 범위

포함:

- 권한 모델 재설계 (역할 기반)
- 감사 이력
- 회원 조회 성능 개선
- 강제 환불 승인 절차

제외:

- 통합 조회 화면 (프론트 작업량이 커서 별도 과제)
- 임시 권한 위임

## 권한 모델

노태윤님이 조사한 현재 상태입니다. AIMS 사용자는 `ADMIN` 과 `VIEWER` 두 종류뿐이고, `VIEWER` 는 실제로 거의 쓰이지 않습니다. 대부분 `ADMIN` 입니다.

역할 기반으로 바꾸되 역할 정의는 기획팀이 준 목록을 따릅니다.

| 역할 | 주요 권한 |
| --- | --- |
| CS_AGENT | 회원/주문 조회, 문의 처리 |
| CS_LEAD | CS_AGENT + 환불 승인 |
| OPERATOR | 상품/이벤트 운영 |
| FINANCE | 정산 조회, 환불 승인 |
| SYSTEM_ADMIN | 계정 및 권한 관리 |

## 감사 이력

무엇을 남길지가 쟁점이었습니다. 전부 남기면 양이 많고, 조회까지 남기면 대부분이 조회 로그가 됩니다.

임채원님이 "사고가 났을 때 뭘 알고 싶은가"를 기준으로 하자고 정리했습니다. 데이터를 바꾼 행위와 민감 정보 조회는 남기고, 일반 목록 조회는 남기지 않습니다.

## 결정 사항

- 역할 5종으로 시작하고 필요 시 추가한다
- 감사 이력은 변경 행위 + 개인정보 상세 조회를 대상으로 한다
- 임시 권한 위임은 이번 범위에서 제외한다

## 일정

4스프린트, 6월 배포 목표입니다.

## Action Item

- 노태윤님: 권한 모델 및 감사 이력 설계
- 임채원님: 기존 AIMS 계정 역할 매핑 초안 요청
""",
    },
    {
        "id": "DOC-112",
        "title": "AIMS 권한 모델 설계",
        "department": "백엔드개발팀",
        "type": "비즈니스 기능 설계",
        "project": "Project Atlas",
        "date": "2026-04-09",
        "systems": ["AIMS", "BlueGate"],
        "features": ["권한", "운영 관리"],
        "participants": [],
        "concepts": ["RBAC", "권한 평가", "Edge Case"],
        "body": """## 배경

역할 기반 접근 제어로 바꿉니다. 기존 `ADMIN` / `VIEWER` 이분법은 실질적으로 전원 관리자와 같습니다.

## 모델

```text
AdminUser  --(N:M)--  Role  --(N:M)--  Permission
```

권한은 `{리소스}:{행위}` 형식입니다.

| 권한 | 설명 |
| --- | --- |
| member:read | 회원 목록 조회 |
| member:read:detail | 개인정보 포함 상세 조회 |
| member:update | 회원 정보 수정 |
| order:read | 주문 조회 |
| payment:refund:request | 환불 요청 |
| payment:refund:approve | 환불 승인 |
| reward:grant | 포인트 지급 |
| admin:manage | 계정 및 권한 관리 |

## 평가 방식

요청 시 사용자의 모든 역할에서 권한을 합집합으로 모아 확인합니다. 거부 규칙은 두지 않습니다.

거부 규칙이 있으면 "왜 안 되는지" 추적이 어렵습니다. 필요하면 역할을 빼는 방식으로 관리합니다.

## 권한 부여 경로

권한은 역할을 통해서만 부여합니다. 사용자에게 직접 권한을 주는 경로는 만들지 않습니다. 예외를 허용하면 운영이 금방 엉킵니다.

## Edge Case

- **역할 변경 즉시 반영**: 토큰에 권한을 넣으면 역할 변경이 토큰 만료까지 반영되지 않습니다. 권한은 토큰에 넣지 않고 요청마다 조회합니다. 캐시는 1분입니다.
- **권한 회수 중 진행 중인 작업**: 환불 승인 중에 권한이 회수되면 승인 시점에 다시 확인합니다. 화면 진입 시점 권한만 믿지 않습니다.
- **승인자와 요청자가 같은 사람**: 환불 요청자가 스스로 승인하는 것을 막습니다. 요청자 ID 와 승인자 ID 가 같으면 거부합니다.
- **역할이 하나도 없는 계정**: 로그인은 되지만 아무 기능도 쓸 수 없습니다. 로그인 자체를 막을지 논의가 필요합니다. 현재는 로그인 후 안내 화면을 보여줍니다.
- **SYSTEM_ADMIN 이 자신의 권한을 제거**: 마지막 `admin:manage` 보유자가 자기 역할을 빼면 아무도 권한을 관리할 수 없습니다. 최소 1명은 유지되도록 막습니다.

## 고민

권한을 코드에 상수로 둘지 DB 로 관리할지 고민했습니다. DB 로 두면 운영에서 새 권한을 만들 수 있지만, 코드가 확인하는 권한 문자열과 어긋나면 조용히 통과하거나 막힙니다.

권한 정의는 코드에 두고, 역할-권한 매핑만 DB 로 관리하는 절충안을 택했습니다. 새 권한 추가는 배포가 필요하지만 역할 조정은 운영에서 가능합니다.
""",
    },
    {
        "id": "DOC-113",
        "title": "admin_user / admin_role 테이블 설계",
        "department": "백엔드개발팀",
        "type": "DB 테이블 설계안",
        "project": "Project Atlas",
        "date": "2026-04-13",
        "systems": ["AIMS"],
        "features": ["권한", "운영 관리"],
        "participants": [],
        "concepts": ["RBAC", "Index", "이력"],
        "body": """## 테이블 구성

```text
admin_user
admin_role
admin_user_role      (N:M)
admin_role_permission
admin_role_history   (역할 변경 이력)
```

## admin_user

| 컬럼 | 타입 | 제약 | 설명 |
| --- | --- | --- | --- |
| id | BIGINT | PK | |
| employee_no | VARCHAR(20) | UNIQUE | 사번 |
| email | VARCHAR(320) | UNIQUE | 사내 계정 |
| name | VARCHAR(20) | NOT NULL | |
| department | VARCHAR(40) | NOT NULL | |
| status | VARCHAR(16) | NOT NULL | ACTIVE / SUSPENDED / RETIRED |
| last_login_at | DATETIME | NULL | |

비밀번호 컬럼은 두지 않습니다. 인증은 사내 SSO 를 통해 BlueGate 가 처리하고 AIMS 는 신원만 확인합니다.

## admin_user_role

| 컬럼 | 타입 | 설명 |
| --- | --- | --- |
| admin_user_id | BIGINT | FK |
| role_id | BIGINT | FK |
| granted_by | BIGINT | 부여한 사람 |
| granted_at | DATETIME | |
| expires_at | DATETIME | NULL 이면 무기한 |

`expires_at` 은 임시 권한 위임을 나중에 넣기 위해 미리 둡니다. 이번 범위에서는 항상 NULL 입니다.

```sql
CREATE UNIQUE INDEX uk_user_role ON admin_user_role (admin_user_id, role_id);
CREATE INDEX idx_user_role_expire ON admin_user_role (expires_at);
```

## admin_role_permission

| 컬럼 | 타입 | 설명 |
| --- | --- | --- |
| role_id | BIGINT | FK |
| permission | VARCHAR(64) | 권한 문자열 |

권한 문자열은 코드에 정의된 값과 일치해야 합니다. 코드에 없는 값이 들어가면 아무 효과가 없으므로, 기동 시 DB 의 권한 문자열을 검증하고 미정의 값이 있으면 경고 로그를 남깁니다.

## admin_role_history

권한 변경은 그 자체가 감사 대상입니다.

| 컬럼 | 설명 |
| --- | --- |
| id | PK |
| admin_user_id | 대상 |
| role_id | |
| action | GRANT / REVOKE |
| performed_by | 수행자 |
| reason | 사유 (필수 입력) |
| created_at | |

## 설계 고민

**퇴사자 처리**: `RETIRED` 상태로 두고 행은 지우지 않습니다. 감사 로그에서 누가 했는지 추적하려면 계정이 남아 있어야 합니다.

**역할을 삭제할 수 있는가**: 삭제하지 않고 비활성화만 합니다. 이력에서 참조하기 때문입니다.

**권한 조회 성능**: 요청마다 조회하되 사용자별로 1분 캐시합니다. AIMS 동시 사용자가 많지 않아 부담이 없습니다.
""",
    },
    {
        "id": "DOC-114",
        "title": "Admin 회원 조회 API 명세",
        "department": "백엔드개발팀",
        "type": "API 명세서",
        "project": "Project Atlas",
        "date": "2026-04-16",
        "systems": ["AIMS", "NEXUS"],
        "features": ["회원 조회", "권한"],
        "participants": [],
        "concepts": ["권한", "마스킹", "감사 로그"],
        "body": """## Endpoint

```http
GET /admin/members
Authorization: Bearer {adminToken}
```

## 검색 조건

| 파라미터 | 필수 | 설명 |
| --- | --- | --- |
| memberId | N | 정확 일치 |
| email | N | 정확 일치 |
| phone | N | 정확 일치 |
| name | N | 부분 일치 (2자 이상) |
| status | N | 회원 상태 |
| page, size | N | 기본 0, 20 |

검색 조건 중 하나는 반드시 있어야 합니다. 전체 조회는 허용하지 않습니다.

## 권한

| 권한 | 반환 범위 |
| --- | --- |
| member:read | 마스킹된 목록 |
| member:read:detail | 원문 포함 상세 |

`member:read` 만 있으면 이메일과 전화번호가 마스킹됩니다.

## Response

```json
{
  "content": [
    {
      "memberId": 20481,
      "email": "us***@example.com",
      "name": "홍길동",
      "phone": "010-****-1234",
      "status": "ACTIVE",
      "createdAt": "2025-01-30T10:12:00+09:00"
    }
  ],
  "totalElements": 1,
  "page": 0,
  "size": 20
}
```

## 감사 로그

`member:read:detail` 로 상세를 조회하면 감사 로그가 남습니다. 목록 조회는 남기지 않습니다.

로그에는 조회자, 조회 대상 회원 ID, 조회 시각, 검색 조건이 기록됩니다.

## Error Code

| 코드 | HTTP | 설명 |
| --- | --- | --- |
| ADMIN_1001 | 400 | 검색 조건 없음 |
| ADMIN_1002 | 400 | 이름 검색어가 2자 미만 |
| ADMIN_1003 | 403 | 권한 없음 |

## 주의사항

이름 부분 일치 검색은 결과가 많을 수 있습니다. 페이지 크기는 최대 100 입니다.

조회한 개인정보를 다른 곳에 옮겨 적거나 캡처하지 마세요. 감사 로그는 조회 사실만 남기고 이후 사용은 추적하지 못합니다.
""",
    },
    {
        "id": "DOC-115",
        "title": "[리뷰] 권한 검사 인터셉터 구현",
        "department": "백엔드개발팀",
        "type": "코드리뷰",
        "project": "Project Atlas",
        "date": "2026-04-20",
        "systems": ["AIMS"],
        "features": ["권한"],
        "participants": ["노태윤", "조아름"],
        "concepts": ["RBAC", "보안", "캐시"],
        "body": """## 현재 구현

`@RequiresPermission("member:read")` 애너테이션을 붙이면 인터셉터가 검사합니다.

```java
var required = handlerMethod.getMethodAnnotation(RequiresPermission.class);
if (required == null) {
    return true;
}
return permissionService.hasPermission(currentUser(), required.value());
```

## 리뷰 의견

조아름님: 애너테이션이 없으면 통과합니다. 실수로 애너테이션을 빼먹은 신규 엔드포인트가 무권한으로 열립니다. 기본값을 거부로 두고, 공개 엔드포인트만 명시적으로 허용 표시하는 편이 안전합니다.

노태윤님: 동의합니다. `@PublicEndpoint` 를 만들고 둘 다 없으면 거부하겠습니다. 기존 엔드포인트에 전부 붙여야 해서 작업량이 있지만 한 번 하고 나면 안전합니다.

조아름님: 권한 캐시가 1분인데 권한 회수가 급한 경우가 있습니다. 사고 대응 중에 계정을 막아야 하는데 1분을 기다려야 합니다.

노태윤님: 계정 정지(`status = SUSPENDED`)는 캐시하지 않고 매번 확인하도록 분리하겠습니다. 역할 변경만 캐시합니다.

조아름님: 권한 문자열을 애너테이션에 문자열로 넣으면 오타가 컴파일에 안 걸립니다. 상수로 정의해서 쓰는 게 좋겠습니다.

노태윤님: `Permissions.MEMBER_READ` 형태 상수를 만들고 애너테이션에서 참조하겠습니다.

## 수정 사항

- 기본 거부. `@PublicEndpoint` 로 예외 표시
- 계정 상태는 캐시 없이 매 요청 확인
- 권한 문자열을 상수로 정의
- 기동 시 DB 권한 문자열과 코드 상수 대조 후 불일치 경고

## 남은 확인

기존 엔드포인트 74개에 애너테이션을 붙여야 합니다. 누락을 막기 위해 테스트에서 모든 컨트롤러 메서드가 둘 중 하나의 애너테이션을 갖는지 검사하는 방법을 검토합니다.
""",
    },
    {
        "id": "DOC-116",
        "title": "운영자 회원 검색이 10초 이상 걸리는 문제",
        "department": "백엔드개발팀",
        "type": "이슈 정리",
        "project": "Project Atlas",
        "date": "2026-04-23",
        "systems": ["AIMS", "NEXUS"],
        "features": ["회원 조회"],
        "participants": [],
        "concepts": ["Slow Query", "Index", "조회 성능"],
        "body": """## 현상

AIMS 회원 검색에서 이름으로 검색하면 10~20초가 걸립니다. 이메일이나 회원 ID 검색은 빠릅니다.

## 분석

```sql
SELECT * FROM member
WHERE name LIKE CONCAT('%', :name, '%')
ORDER BY created_at DESC
LIMIT 20;
```

앞뒤 와일드카드 때문에 인덱스를 못 씁니다. 회원 테이블이 480만 행이라 전체 스캔에 시간이 걸립니다.

실행 계획을 보면 전체 스캔 후 정렬까지 합니다.

## 검토한 방법

**1. 앞쪽 와일드카드 제거**

`LIKE '홍길%'` 로 바꾸면 인덱스를 씁니다. 다만 "길동"으로 검색하는 경우를 지원하지 못합니다. 고객지원팀 확인 결과 성 없이 이름만 아는 경우가 실제로 있습니다.

**2. 전문 검색 인덱스**

MySQL n-gram 전문 인덱스를 쓰면 부분 일치가 가능합니다. 한글 2-gram 이면 "길동" 검색이 됩니다.

**3. 별도 검색 엔진**

범위가 커서 이번에는 제외했습니다.

## 결정

2안으로 갑니다. `name` 에 n-gram 전문 인덱스를 추가합니다.

```sql
ALTER TABLE member ADD FULLTEXT INDEX ft_member_name (name) WITH PARSER ngram;
```

검색 쿼리는 `MATCH ... AGAINST` 로 바꿉니다.

## 측정 결과

스테이징(운영 규모 복제)에서 측정했습니다.

| 방식 | 응답 시간 |
| --- | --- |
| LIKE %name% | 12.4초 |
| FULLTEXT ngram | 0.3초 |

## 남은 문제

전문 인덱스는 정확도가 LIKE 와 다릅니다. 2-gram 이라 1글자 검색은 안 되고, 일부 케이스에서 결과가 더 나올 수 있습니다. API 에서 이름 검색을 2자 이상으로 제한하기로 했습니다.

인덱스 추가에 운영 DB 에서 시간이 걸립니다. 온라인 DDL 로 가능한지 확인이 필요하며 아직 적용하지 않았습니다.
""",
    },
    {
        "id": "DOC-117",
        "title": "감사 이력 대상 범위 논의",
        "department": "정보보안팀",
        "type": "개발자 회의록",
        "project": "Project Atlas",
        "date": "2026-04-27",
        "systems": ["AIMS"],
        "features": ["운영 관리", "권한"],
        "participants": ["조아름", "노태윤"],
        "concepts": ["감사 로그", "개인정보", "보관 기간"],
        "body": """## 논의 배경

무엇을 남길지 킥오프에서 대략 정했지만 구체적인 목록이 없습니다. 오늘 확정합니다.

## 남길 것

**변경 행위 전부**

- 회원 정보 수정, 상태 변경
- 포인트 지급/회수
- 쿠폰 지급/회수
- 강제 환불 요청 및 승인
- 주문 상태 변경
- 계정 및 권한 변경

**민감 조회**

- 개인정보 포함 회원 상세 조회
- 결제 수단 정보 조회
- 환불 계좌 조회

## 남기지 않을 것

- 일반 목록 조회
- 통계 화면 조회
- 로그인/로그아웃 (별도 인증 로그에 있음)

## 기록 항목

조아름님이 요구한 최소 항목입니다.

| 항목 | 설명 |
| --- | --- |
| 수행자 | admin_user_id, 사번 |
| 행위 | 권한 문자열과 동일한 체계 |
| 대상 | 리소스 종류와 ID |
| 시각 | |
| 변경 전/후 | 변경 행위인 경우 |
| 사유 | 일부 행위는 필수 |
| 요청 IP | |

## 쟁점

변경 전/후 값을 어디까지 남길지가 쟁점이었습니다. 개인정보 수정 이력에 개인정보 원문이 남으면 감사 이력 자체가 개인정보 저장소가 됩니다.

노태윤님은 변경된 필드명만 남기자고 했고, 조아름님은 그러면 사고 조사에서 무엇이 바뀌었는지 알 수 없다고 했습니다.

절충해서 개인정보 필드는 마스킹된 값으로, 그 외 필드는 원문으로 남기기로 했습니다.

## 보관 기간

- 감사 이력: 3년
- 권한 변경 이력: 5년

3년은 내부 감사 주기를 고려한 값입니다.

## 결정 사항

위 내용대로 확정합니다. 로그는 별도 테이블에 적재하고 애플리케이션 로그와 분리합니다.

## 추후 논의

로그 양이 얼마나 될지 예측이 어렵습니다. 초기 한 달 지표를 보고 보관 방식(테이블 파티셔닝 등)을 정하기로 했습니다.
""",
    },
    {
        "id": "DOC-118",
        "title": "Admin 강제 환불 API 명세",
        "department": "백엔드개발팀",
        "type": "API 명세서",
        "project": "Project Atlas",
        "date": "2026-04-30",
        "systems": ["AIMS", "ORBIT"],
        "features": ["강제 환불", "환불", "권한"],
        "participants": [],
        "concepts": ["승인", "감사 로그", "권한"],
        "body": """## 개요

운영자가 정상 취소 경로를 거치지 않고 환불을 처리합니다. 요청과 승인이 분리되어 있습니다.

## 환불 요청

```http
POST /admin/payments/{paymentId}/force-refund
Authorization: Bearer {adminToken}
```

필요 권한: `payment:refund:request`

```json
{
  "refundAmount": 34000,
  "reason": "SYSTEM_ERROR",
  "detail": "결제 완료 후 주문 미생성. 문의 #88213"
}
```

`detail` 은 필수이며 10자 이상입니다.

응답:

```json
{
  "requestId": 4412,
  "status": "PENDING_APPROVAL",
  "requestedBy": "E20391",
  "requiredApprovals": 1
}
```

## 환불 승인

```http
POST /admin/refund-requests/{requestId}/approve
```

필요 권한: `payment:refund:approve`

```json
{
  "comment": "확인 완료. 로그상 결제 정상, 주문 미생성 확인"
}
```

## 승인 규칙

| 금액 | 필요 승인 |
| --- | --- |
| 30만원 이하 | 1명 |
| 30만원 초과 | 2명 (그중 1명은 FINANCE 역할) |

요청자는 승인할 수 없습니다. 같은 사람이 두 번 승인할 수도 없습니다.

## Error Code

| 코드 | HTTP | 설명 |
| --- | --- | --- |
| ADMIN_2001 | 403 | 권한 없음 |
| ADMIN_2002 | 409 | 요청자가 승인 시도 |
| ADMIN_2003 | 409 | 이미 승인한 요청 |
| ADMIN_2004 | 409 | 이미 처리된 요청 |
| ADMIN_2005 | 400 | 사유 미입력 또는 10자 미만 |
| ADMIN_2006 | 409 | 환불 금액이 결제 잔여 금액 초과 |

## 감사 로그

요청과 승인 모두 감사 로그에 남습니다. 요청자, 승인자, 금액, 사유, 시각이 기록됩니다.

## 주의사항

이 API 는 최후 수단입니다. 일반적인 고객 요청 취소는 정상 취소 경로를 사용하세요. 강제 환불은 PG 거래와 우리 주문 데이터가 어긋난 상황을 바로잡기 위한 것입니다.

승인 전에 반드시 결제 이력과 PG 상태를 확인하세요. 확인 없이 승인하면 이중 환불이 발생할 수 있습니다.
""",
    },
    {
        "id": "DOC-119",
        "title": "강제 환불 승인 단계 조정 논의",
        "department": "서비스기획팀",
        "type": "기획자와의 회의록",
        "project": "Project Atlas",
        "date": "2026-05-07",
        "systems": ["AIMS"],
        "features": ["강제 환불", "운영 관리"],
        "participants": ["강민재", "문가영"],
        "concepts": ["승인", "운영 효율", "정책"],
        "body": """## 논의 배경

승인 단계를 넣기로 했는데 실제 운영에서 병목이 되지 않을지 확인이 필요합니다.

## 현황

문가영님이 최근 3개월 강제 환불 건을 분석했습니다.

| 금액 구간 | 건수 | 비중 |
| --- | --- | --- |
| 5만원 이하 | 71 | 62% |
| 5만 ~ 30만원 | 36 | 31% |
| 30만원 초과 | 8 | 7% |

대부분이 소액입니다.

## 쟁점

모든 건에 승인을 요구하면 소액 처리가 느려집니다. 고객은 환불이 늦어진다고 느낍니다.

강민재님은 소액에도 승인이 필요하다는 입장이었습니다. 금액이 작아도 잘못된 환불은 정산 오류로 이어집니다.

문가영님은 리드가 자리를 비우면 처리가 멈춘다고 우려했습니다. 하루 평균 1~2건이지만 몰릴 때가 있습니다.

## 검토한 방안

1. 금액 무관 1명 승인 (현재 설계)
2. 5만원 이하는 승인 면제, 사후 확인
3. 5만원 이하는 승인자 범위를 넓힘 (CS_LEAD 외 OPERATOR 도 가능)

2안은 사후 확인이 실제로 이뤄지지 않을 가능성이 높습니다. 사후 확인 절차가 지켜지지 않은 사례가 이전에 있었습니다.

## 결정 사항

3안으로 갑니다.

| 금액 | 승인자 |
| --- | --- |
| 5만원 이하 | CS_LEAD, OPERATOR, FINANCE 중 1명 |
| 5만 ~ 30만원 | CS_LEAD 또는 FINANCE 중 1명 |
| 30만원 초과 | 2명, 그중 1명은 FINANCE |

요청자 본인 승인 불가 원칙은 모든 구간에 적용합니다.

## 미확정

승인이 오래 지연되는 건을 어떻게 알릴지 정하지 못했습니다. 알림을 보낼지, 대시보드에만 표시할지 다음에 정합니다.

## Action Item

- 강민재님: 승인 규칙 확정본 개발팀 전달
- 문가영님: 팀 내 승인 권한 대상자 목록 정리
""",
    },
    {
        "id": "DOC-120",
        "title": "Atlas Sprint 1 진행 현황",
        "department": "프로젝트관리팀",
        "type": "프로젝트 진행상황",
        "project": "Project Atlas",
        "date": "2026-05-11",
        "systems": ["AIMS"],
        "features": ["권한", "회원 조회"],
        "participants": [],
        "concepts": ["일정", "Blocker"],
        "body": """## 스프린트 개요

4월 27일 ~ 5월 8일. 권한 모델과 감사 이력 기반을 만드는 스프린트입니다.

## 완료

- 권한 모델 설계 및 테이블 정의
- 권한 검사 인터셉터 구현 (기본 거부 방식)
- 감사 이력 대상 범위 확정
- Admin 회원 조회 API v2
- 회원 이름 검색 전문 인덱스 검증

## 진행 중

- 기존 엔드포인트 74개에 권한 애너테이션 부착 (52/74)
- 감사 이력 적재 구현
- 강제 환불 요청/승인 구현

## 대기

- 기존 AIMS 계정의 역할 매핑
- 전문 인덱스 운영 적용

## Blocker

기존 계정 역할 매핑이 진행되지 않고 있습니다. 현재 AIMS 계정 118개가 대부분 `ADMIN` 이고, 누구에게 어떤 역할을 줄지 각 팀 확인이 필요합니다. 고객지원팀은 목록을 받았지만 다른 팀은 아직입니다.

매핑이 안 되면 배포 시 전원이 권한 없음 상태가 됩니다. 배포 전 필수 항목입니다.

## Risk

권한 애너테이션 누락이 가장 큰 위험입니다. 기본 거부로 바꿨기 때문에 누락되면 기능이 막힙니다. 반대로 `@PublicEndpoint` 를 잘못 붙이면 무권한으로 열립니다.

테스트로 검증하는 방법을 검토 중이지만 아직 적용 전입니다.

## 다음 스프린트

- 애너테이션 부착 완료 및 검증 테스트
- 감사 이력 완료
- 역할 매핑 취합
""",
    },
]
