"""DOC-136 ~ DOC-150 : 공통 인프라와 서비스 간 규약 (2025~2026)."""

DOCUMENTS = [
    {
        "id": "DOC-136",
        "title": "캐시 서버 연결 수가 계속 늘어나는 현상",
        "department": "DevOps팀",
        "type": "이슈 정리",
        "project": None,
        "date": "2025-06-11",
        "systems": ["NEXUS", "BlueGate"],
        "features": [],
        "participants": ["오세훈"],
        "concepts": ["커넥션 누수", "캐시", "메모리"],
        "body": """## 현상

Redis 커넥션 수가 배포 후부터 단조 증가합니다. 하루에 약 400개씩 늘고, 3일이면 상한에 닿습니다. 지난주에는 상한 도달로 신규 연결이 거부되면서 로그인 지연이 발생했습니다.

## 지표

| 시각 | 연결 수 |
| --- | --- |
| 배포 직후 | 120 |
| +6시간 | 220 |
| +24시간 | 510 |
| +48시간 | 940 |

애플리케이션 인스턴스 수는 변하지 않았습니다.

## 분석

커넥션 풀 설정을 확인했습니다. 풀 최대 크기는 인스턴스당 20이고 인스턴스는 6대이므로 최대 120이 정상입니다.

풀을 거치지 않고 직접 연결을 만드는 코드가 있는지 찾았습니다. 토큰 폐기 목록을 조회하는 부분에서 매번 새 클라이언트를 생성하고 있었습니다.

```java
var client = RedisClient.create(uri);   // 호출마다 생성
var connection = client.connect();
try {
    return connection.sync().sismember(key, token);
} finally {
    connection.close();                  // 연결은 닫지만 클라이언트는 안 닫음
}
```

`connection.close()` 는 호출하지만 `client.shutdown()` 이 없습니다. 클라이언트가 잡고 있는 이벤트 루프 스레드와 소켓이 남습니다.

## 영향 범위

- 로그인 지연 (연결 거부 시)
- 애플리케이션 메모리 증가

## 임시 대응

인스턴스를 순차 재시작해 연결을 정리했습니다. 재시작 전까지 임시로 Redis 최대 연결 수를 늘렸습니다.

## 근본 해결

공용 커넥션 풀을 주입받아 쓰도록 수정했습니다. 6월 12일 배포 완료.

## 재발 방지

리소스를 생성하는 코드가 몇 군데 더 있는지 점검했습니다. HTTP 클라이언트에서 비슷한 패턴 2건을 추가로 발견해 함께 수정했습니다.

커넥션 수 지표에 임계 알림을 추가했습니다. 예전에는 상한에 닿아야 알 수 있었습니다.
""",
    },
    {
        "id": "DOC-137",
        "title": "주문 저장 중 교착 상태 반복 발생",
        "department": "백엔드개발팀",
        "type": "이슈 정리",
        "project": None,
        "date": "2025-09-30",
        "systems": ["ORBIT"],
        "features": ["주문"],
        "participants": [],
        "concepts": ["Deadlock", "락 순서", "재시도"],
        "body": """## 현상

`Deadlock found when trying to get lock` 오류가 하루 20~40건 발생합니다. 대부분 재시도로 성공하지만 사용자에게 실패로 나가는 경우가 하루 3~5건 있습니다.

## 로그

```text
Deadlock found when trying to get lock; try restarting transaction
  UPDATE product_stock SET quantity = quantity - 1 WHERE product_id = ?
```

## 분석

주문 저장과 재고 차감 배치가 같은 `product_stock` 행을 반대 순서로 잠급니다.

주문 저장:

```text
1. order INSERT
2. order_item INSERT
3. product_stock UPDATE (상품 ID 순서 무관, 장바구니 담긴 순서)
```

재고 정합성 배치:

```text
1. product_stock UPDATE (상품 ID 오름차순)
```

주문 A 가 상품 3 → 상품 1 순으로 잠그고, 배치가 상품 1 → 상품 3 순으로 잠그면 서로를 기다립니다.

주문끼리도 발생합니다. 두 사용자가 같은 두 상품을 다른 순서로 담으면 같은 상황이 됩니다.

## 조치

**락 순서 통일**: 재고를 차감하기 전에 상품 ID 오름차순으로 정렬합니다.

```java
command.items().stream()
    .sorted(comparing(OrderItem::productId))
    .forEach(item -> stockRepository.decrease(item.productId(), item.quantity()));
```

배치도 같은 순서를 씁니다.

**재시도**: 데드락은 완전히 없앨 수 없습니다. `CannotAcquireLockException` 에 대해 최대 2회 재시도를 추가했습니다. 재시도는 트랜잭션 밖에서 감쌉니다.

## 결과

배포 후 데드락이 하루 20~40건에서 1~3건으로 줄었습니다. 남은 건은 재시도로 흡수됩니다.

## 남은 위험

정렬은 같은 테이블 안에서만 순서를 보장합니다. 여러 테이블을 잠그는 경로가 늘어나면 테이블 간 순서도 정해야 합니다.

현재 트랜잭션에서 잠그는 테이블 순서를 문서로 정리하자는 의견이 있었지만 아직 작성하지 않았습니다.
""",
    },
    {
        "id": "DOC-138",
        "title": "공통 캐시 사용 기준 정리",
        "department": "백엔드개발팀",
        "type": "개발자 회의록",
        "project": None,
        "date": "2025-10-14",
        "systems": ["NEXUS", "ORBIT", "MARS"],
        "features": [],
        "participants": ["오세훈", "김도윤"],
        "concepts": ["캐시", "TTL", "정합성"],
        "body": """## 논의 배경

서비스마다 캐시를 다르게 쓰고 있습니다. 어떤 곳은 TTL 없이 무기한이고, 어떤 곳은 매 요청 갱신합니다. Redis 연결 누수 이슈를 겪으면서 사용 기준을 정하기로 했습니다.

## 현황 조사

김도윤님이 정리한 현재 캐시 사용처입니다.

| 대상 | TTL | 무효화 |
| --- | --- | --- |
| 회원 프로필 | 10분 | 없음 |
| 상품 정보 | 5분 | 수정 시 삭제 |
| 적립금 잔액 | 없음 (캐시 안 함) | - |
| 권한 정보 | 1분 | 없음 |
| 토큰 폐기 목록 | 토큰 만료와 동일 | 로그아웃 시 추가 |
| 카테고리 | 무기한 | 수동 |

회원 프로필이 10분 캐시인데 무효화가 없습니다. 사용자가 이름을 바꿔도 10분간 예전 값이 보입니다.

## 기준

**캐시하면 안 되는 것**

- 금액, 잔액, 재고처럼 정확성이 필요한 값
- 권한 판단의 근거가 되는 상태 (계정 정지 등)

**짧은 TTL 로 캐시**

- 자주 읽고 가끔 바뀌는 값 (권한, 설정)
- 1~5분

**긴 TTL + 명시적 무효화**

- 거의 바뀌지 않는 값 (카테고리, 상품 메타)
- 변경 시 반드시 무효화 코드를 함께 넣습니다

## 결정 사항

- 회원 프로필 캐시에 수정 시 무효화 추가
- 카테고리 무기한 캐시에 24시간 TTL 부여 (무효화 누락 대비)
- 캐시 키 네이밍을 `{service}:{domain}:{id}` 로 통일
- 새 캐시를 추가할 때 TTL 과 무효화 방법을 PR 설명에 적는다

## 보류

캐시 스탬피드(만료 직후 동시 요청이 원본으로 몰리는 현상) 대응은 보류했습니다. 현재 트래픽에서 문제가 되지 않았고, 대응을 넣으면 복잡도가 올라갑니다. 문제가 관측되면 그때 넣습니다.

## Action Item

- 김도윤님: 프로필 캐시 무효화 추가
- 오세훈님: 캐시 키 네이밍 정리 및 대시보드에 히트율 추가
""",
    },
    {
        "id": "DOC-139",
        "title": "로그 조회 방법",
        "department": "DevOps팀",
        "type": "매뉴얼 / 정의서",
        "project": None,
        "date": "2025-11-20",
        "systems": ["NEXUS", "ORBIT", "MARS", "BlueGate", "AIMS"],
        "features": [],
        "participants": [],
        "concepts": ["로그", "운영", "추적"],
        "body": """## 로그 종류

| 종류 | 위치 | 보관 |
| --- | --- | --- |
| 애플리케이션 로그 | 로그 대시보드 | 30일 |
| 접근 로그 | 로그 대시보드 | 30일 |
| 감사 로그 | AIMS DB | 3년 |
| 배포 이력 | 배포 도구 | 무기한 |

## 대시보드 사용

서비스별로 인덱스가 분리되어 있습니다. 먼저 서비스를 고르고 기간을 좁히세요. 기본 기간이 15분이라 오래된 건은 기간을 직접 지정해야 합니다.

## 추적 키

요청 하나를 서비스 간에 따라가려면 `traceId` 를 사용합니다. 모든 서비스가 요청 헤더로 전달받아 로그에 남깁니다.

```text
traceId=4f2a91c8  ORBIT   결제 요청 수신
traceId=4f2a91c8  MARS    포인트 사용 처리
traceId=4f2a91c8  ORBIT   PG 승인 요청
```

`traceId` 를 모르면 도메인 키로 시작하세요.

| 상황 | 검색 키 |
| --- | --- |
| 특정 주문 문제 | `orderId` |
| 특정 회원 문제 | `memberId` |
| 결제 문제 | `paymentId`, `idempotencyKey` |
| 로그인 문제 | `memberId`, `deviceId` |

## 자주 쓰는 검색

오류만 보기:

```text
service:ORBIT AND level:ERROR
```

특정 API 의 느린 요청:

```text
service:ORBIT AND path:"/payments" AND duration:>3000
```

## 주의

- 로그에 개인정보를 남기지 않는 것이 원칙입니다. 발견하면 알려주세요
- 로그를 캡처해 외부에 공유하지 마세요
- 30일이 지난 로그는 조회할 수 없습니다. 장애 분석에 필요하면 그 안에 확보하세요

## 로그가 안 보일 때

- 서비스 이름을 확인하세요. 인덱스가 다르면 검색되지 않습니다
- 기간을 넓혀보세요
- 로그 수집 지연이 있을 수 있습니다. 1~2분 기다린 뒤 다시 확인하세요
""",
    },
    {
        "id": "DOC-140",
        "title": "Git Branch 전략",
        "department": "백엔드개발팀",
        "type": "팀 Ground Rule",
        "project": None,
        "date": "2025-05-14",
        "systems": [],
        "features": [],
        "participants": [],
        "concepts": ["Git", "배포", "팀 규칙"],
        "body": """## 브랜치 구성

```text
main       운영 배포 기준. 항상 배포 가능한 상태
develop    통합 브랜치. 스테이징 배포 대상
feature/*  기능 개발
hotfix/*   운영 긴급 수정
release/*  배포 준비
```

## 이름 규칙

```text
feature/falcon-idempotency
feature/aurora-signup-v2
hotfix/payment-timeout
release/2026-03-16
```

이슈 번호가 있으면 뒤에 붙입니다. `feature/falcon-idempotency-1284`

## 흐름

1. `develop` 에서 `feature/*` 를 분기
2. 작업 후 `develop` 으로 PR
3. 배포 시점에 `develop` 에서 `release/*` 분기
4. QA 완료 후 `main` 으로 머지하고 태그
5. `main` 을 `develop` 으로 다시 머지

## Hotfix

`main` 에서 분기해 수정하고 `main` 과 `develop` 양쪽에 머지합니다. `develop` 머지를 빠뜨리면 다음 배포에서 수정이 사라집니다. 이 실수가 실제로 두 번 있었습니다.

## 머지 방식

- `feature` → `develop`: Squash merge. 커밋 하나로 정리
- `release` → `main`: Merge commit. 이력 보존
- `hotfix`: Merge commit

## 오래된 브랜치

- 머지된 브랜치는 삭제합니다
- 2주 이상 갱신이 없는 `feature` 브랜치는 작성자에게 확인 후 정리합니다

오래 살아 있는 브랜치는 충돌이 커지고, 결국 통째로 버려지는 경우가 많습니다. 작게 나눠 자주 머지하세요.
""",
    },
    {
        "id": "DOC-141",
        "title": "Commit Message 규칙",
        "department": "백엔드개발팀",
        "type": "팀 Ground Rule",
        "project": None,
        "date": "2025-05-16",
        "systems": [],
        "features": [],
        "participants": [],
        "concepts": ["Git", "팀 규칙"],
        "body": """## 형식

```text
<type>: <제목>

<본문 (선택)>
```

제목은 50자 이내, 마침표를 붙이지 않습니다.

## type

| type | 사용 |
| --- | --- |
| feat | 기능 추가 |
| fix | 버그 수정 |
| refactor | 동작 변경 없는 구조 개선 |
| test | 테스트 추가/수정 |
| docs | 문서 |
| chore | 빌드, 설정 |

## 제목 작성

무엇을 했는지 씁니다. 왜 했는지는 본문에 씁니다.

좋은 예:

```text
fix: 결제 timeout 시 payment 상태를 PENDING 으로 유지
feat: 리워드 지급에 참조키 기반 중복 방지 추가
refactor: SignUpService 에서 비밀번호 정책 분리
```

나쁜 예:

```text
fix: 버그 수정
feat: 작업
수정함
```

## 본문

필요할 때만 씁니다. 다음 경우에는 쓰는 편이 좋습니다.

- 왜 이렇게 고쳤는지 코드만 봐서는 알기 어려울 때
- 다른 방법을 검토했다가 버렸을 때
- 관련 이슈나 문서가 있을 때

```text
fix: 결제 timeout 시 payment 상태를 PENDING 으로 유지

timeout 예외를 일반 예외와 같은 경로로 처리해 FAILED 로
마킹하고 있었음. 그 결과 주문 단위 중복 차단을 통과해
이중 승인이 발생함.

관련 문서: PG 지연 후 동일 주문 이중 승인
```

## 하지 말 것

- 여러 작업을 한 커밋에 몰아넣지 않습니다
- "리뷰 반영" 같은 커밋을 남기지 않습니다. Squash 머지를 쓰므로 PR 안에서는 자유롭지만, 최종 커밋 제목은 정리하세요
""",
    },
    {
        "id": "DOC-142",
        "title": "[리뷰] 공통 예외 처리 구조",
        "department": "백엔드개발팀",
        "type": "코드리뷰",
        "project": None,
        "date": "2025-12-05",
        "systems": ["NEXUS", "ORBIT", "MARS"],
        "features": [],
        "participants": ["김도윤", "이서연"],
        "concepts": ["예외 처리", "Error Code", "공통 모듈"],
        "body": """## 현재 구현

서비스마다 예외 처리가 달라서 공통 모듈로 뺐습니다. `BusinessException` 하나로 통일하고 에러 코드를 enum 으로 관리합니다.

```java
public class BusinessException extends RuntimeException {
    private final ErrorCode errorCode;
    private final Map<String, Object> details;
}
```

## 리뷰 의견

이서연님: `ErrorCode` enum 을 공통 모듈에 두면 서비스마다 코드를 추가할 때 공통 모듈을 고쳐야 합니다. NEXUS 에 코드를 추가하려고 ORBIT 까지 영향받는 모듈을 배포하는 건 부담입니다.

김도윤님: 인터페이스로 두고 각 서비스가 구현하는 형태로 바꾸겠습니다. 공통 모듈에는 `ErrorCode` 인터페이스와 공통 코드만 두고, 서비스별 코드는 각자 enum 으로 정의합니다.

이서연님: `details` 에 무엇이 들어가는지 통제가 없습니다. 지금 사용처를 보니 내부 금액이나 회원 정보가 들어간 곳이 있습니다. 클라이언트 응답에 그대로 나가면 곤란합니다.

김도윤님: 응답에 포함할 것과 로그에만 남길 것을 분리하겠습니다. `details` 는 로그 전용으로 두고, 클라이언트에 내보낼 값은 별도 필드로 명시하겠습니다.

이서연님: `nit:` 예외 메시지를 한국어로 쓰는 곳과 영어로 쓰는 곳이 섞여 있습니다. 로그는 영어, 사용자 노출 문구는 클라이언트가 코드로 매핑하는 방식으로 정하면 좋겠습니다.

## 수정 사항

- `ErrorCode` 를 인터페이스로 변경, 서비스별 enum 구현
- `details` 를 로그 전용으로 분리, 클라이언트 노출 필드 명시
- 예외 메시지는 영어로 통일 (사용자 문구는 클라이언트 책임)

## 남은 확인

기존 서비스 3개의 예외를 새 구조로 옮기는 작업이 남았습니다. 한 번에 하면 위험하니 서비스별로 나눠 진행합니다. NEXUS 부터 시작하고, 옮기는 동안 두 구조가 공존합니다.
""",
    },
    {
        "id": "DOC-143",
        "title": "서비스 간 통신 방식 검토",
        "department": "백엔드개발팀",
        "type": "개발자 회의록",
        "project": None,
        "date": "2026-01-08",
        "systems": ["NEXUS", "ORBIT", "MARS", "BlueGate"],
        "features": [],
        "participants": ["김도윤", "오세훈", "노태윤"],
        "concepts": ["서비스 간 호출", "이벤트", "장애 전파"],
        "body": """## 논의 배경

서비스가 5개로 늘면서 서로 호출하는 경로가 복잡해졌습니다. 한 서비스가 느려지면 호출하는 쪽이 함께 느려지는 문제가 반복됩니다.

## 현재 호출 관계

```text
ORBIT  -> NEXUS   (회원 조회)
ORBIT  -> MARS    (포인트 사용/회수)
MARS   -> NEXUS   (회원 상태 확인)
AIMS   -> 전부
BlueGate -> NEXUS (자격 검증)
```

## 문제 사례

- 탈퇴 회원 조회에서 NEXUS 가 410 을 반환하자 ORBIT 이 500 을 낸 사례
- NEXUS 지연 시 ORBIT 주문 조회가 함께 느려짐
- MARS 이벤트 유실로 탈퇴 회원 포인트가 남은 사례

## 논의 내용

**동기 호출을 줄일 수 있나**

노태윤님은 주문 조회에서 회원 이름을 매번 NEXUS 에 묻는 것이 불필요하다고 했습니다. 주문 시점에 이름을 복사해 두면 조회 때 호출이 사라집니다.

김도윤님은 데이터 복제가 늘면 정합성 문제가 생긴다고 지적했습니다. 다만 주문 시점의 수령자 이름은 오히려 그때 값이 맞습니다. 회원이 이름을 바꿔도 과거 주문의 수령자는 그대로여야 합니다.

**이벤트를 쓰는 기준**

탈퇴 이벤트처럼 "알려주기만 하면 되는 것"은 이벤트가 맞습니다. 포인트 사용처럼 "결과를 알아야 다음 단계로 갈 수 있는 것"은 동기 호출이어야 합니다.

오세훈님이 이벤트 유실 대응을 물었고, 현재는 대조 배치로 잡고 있다고 정리했습니다. 이벤트만으로 정합성을 보장하지 않고 배치가 안전망 역할을 합니다.

## 결정 사항

- 조회 시점에 필요한 값 중 "그 시점 값"이 의미 있는 것은 복사한다
- 이벤트는 통지 목적에만 쓰고, 정합성은 대조 배치가 보장한다
- 서비스 간 호출에는 타임아웃과 실패 시 동작을 반드시 정의한다

## Action Item

- 노태윤님: 주문에 수령자 정보 복사 검토
- 김도윤님: 서비스 간 재시도/타임아웃 기준 문서화
""",
    },
    {
        "id": "DOC-144",
        "title": "서비스 간 호출 재시도 및 타임아웃 기준",
        "department": "백엔드개발팀",
        "type": "비즈니스 기능 설계",
        "project": None,
        "date": "2026-01-19",
        "systems": ["NEXUS", "ORBIT", "MARS", "BlueGate"],
        "features": [],
        "participants": [],
        "concepts": ["재시도", "Timeout", "멱등성", "장애 전파"],
        "body": """## 배경

서비스 간 호출마다 타임아웃과 재시도가 제각각입니다. 기준을 정합니다.

## 분류

호출을 세 가지로 나눕니다.

| 분류 | 예 | 실패 시 |
| --- | --- | --- |
| 필수 동기 | 포인트 사용, PG 승인 | 전체 실패 |
| 보조 동기 | 회원 이름 조회 | 기본값으로 진행 |
| 비동기 통지 | 탈퇴 이벤트 | 재시도 큐 |

## 타임아웃

| 분류 | connect | read |
| --- | --- | --- |
| 필수 동기 | 2초 | 5초 |
| 보조 동기 | 1초 | 2초 |
| 외부(PG) | 5초 | 15초 |

보조 동기 호출이 느리면 전체가 느려지므로 짧게 잡습니다. 실패해도 기능이 동작해야 합니다.

## 재시도

**재시도해도 되는 경우**

- 호출이 멱등한 경우 (조회, 참조키가 있는 쓰기)
- 연결 실패, 5xx 응답

**재시도하면 안 되는 경우**

- 멱등하지 않은 쓰기
- 4xx 응답 (재시도해도 같음)
- 타임아웃이 발생한 비멱등 호출 (처리됐을 수 있음)

재시도 횟수는 1회, 간격은 200ms 입니다. 여러 번 재시도하면 장애 상황에서 부하를 키웁니다.

## 실패 시 동작

**필수 동기 실패**: 요청 전체를 실패시킵니다. 부분 성공 상태로 두지 않습니다.

**보조 동기 실패**: 기본값으로 진행합니다. 회원 이름을 못 가져오면 "정보 없음"으로 표시합니다. 화면이 안 뜨는 것보다 낫습니다.

**비동기 통지 실패**: 재시도 큐에 넣고, 큐에서도 실패하면 알림 후 대조 배치가 처리합니다.

## 오류 코드 처리

호출하는 쪽은 소비하는 API 의 오류 코드를 알아야 합니다. 특히 4xx 중 정상 흐름인 것들이 있습니다.

| 상황 | 응답 | 호출자 처리 |
| --- | --- | --- |
| 탈퇴 후 삭제된 회원 조회 | 410 | 정상. "탈퇴 회원" 표시 |
| 적립 없는 주문의 회수 요청 | 404 | 정상. 회수할 것이 없음 |
| 이미 처리된 멱등 요청 | 200 + duplicated | 정상 |

이 목록을 각 API 문서에 명시하고, 새 코드를 추가할 때 소비자에게 공유합니다.

## 남은 문제

소비자 목록을 관리하는 방법이 없습니다. API 를 바꿀 때 누가 쓰는지 알아내려면 코드를 뒤져야 합니다. 서비스 간 의존 관계를 문서로 관리하자는 의견이 있었지만 갱신이 안 될 가능성이 높습니다. 아직 결론이 없습니다.
""",
    },
    {
        "id": "DOC-145",
        "title": "내부 서비스 토큰 발급 API 명세",
        "department": "플랫폼개발팀",
        "type": "API 명세서",
        "project": None,
        "date": "2026-02-02",
        "systems": ["BlueGate"],
        "features": ["인증", "권한"],
        "participants": [],
        "concepts": ["서비스 토큰", "권한", "만료"],
        "body": """## 용도

서비스가 다른 서비스를 호출할 때 사용하는 토큰입니다. 사용자 토큰과 구분됩니다.

## Endpoint

```http
POST /auth/service-token
```

인증은 상호 TLS 로 처리합니다. 요청 본문에 자격 증명을 넣지 않습니다.

## Request

```json
{
  "serviceName": "ORBIT",
  "scopes": ["member:read", "reward:use"]
}
```

요청 가능한 scope 는 서비스별로 사전 정의되어 있습니다. 정의되지 않은 scope 를 요청하면 거부됩니다.

## Response

```json
{
  "token": "svc_...",
  "expiresIn": 3600,
  "scopes": ["member:read", "reward:use"]
}
```

## 서비스별 허용 scope

| 서비스 | scope |
| --- | --- |
| ORBIT | member:read, reward:use, reward:revoke |
| MARS | member:read |
| AIMS | member:read:detail, order:read, payment:refund |
| BlueGate | member:read, member:verify |

## 갱신

만료 10분 전부터 갱신을 시도하세요. 만료된 토큰으로 호출하면 401 이 반환되고, 이때 갱신 후 재시도하는 인터셉터를 두는 것을 권장합니다.

## Error Code

| 코드 | HTTP | 설명 |
| --- | --- | --- |
| SVC_1001 | 403 | 허용되지 않은 scope 요청 |
| SVC_1002 | 403 | 등록되지 않은 서비스 |
| SVC_1003 | 401 | 상호 TLS 검증 실패 |

## 주의사항

서비스 토큰으로 사용자 전용 API 를 호출할 수 없습니다. 반대도 마찬가지입니다. 토큰 종류가 맞지 않으면 403 이 반환됩니다.

서비스 토큰을 로그에 남기지 마세요. 만료가 1시간이라 짧지만 그 사이에 악용될 수 있습니다.
""",
    },
    {
        "id": "DOC-146",
        "title": "배포 도중 일부 요청이 응답 없이 끊기는 문제",
        "department": "DevOps팀",
        "type": "이슈 정리",
        "project": None,
        "date": "2026-03-05",
        "systems": ["ORBIT", "NEXUS"],
        "features": [],
        "participants": ["오세훈"],
        "concepts": ["배포", "graceful shutdown", "요청 유실"],
        "body": """## 현상

배포할 때마다 5xx 와 연결 끊김이 소량 발생합니다. 배포당 평균 30~60건입니다. 사용자에게는 "일시적인 오류" 화면이 나갑니다.

## 분석

인스턴스 종료 순서를 확인했습니다.

```text
1. 로드밸런서에서 제외
2. 컨테이너 종료 신호 전달
3. 프로세스 종료
```

문제는 1번과 2번 사이 간격이 없다는 점입니다. 로드밸런서 제외가 반영되기 전에 프로세스가 종료됩니다. 헬스체크 주기가 5초라 최대 5초 동안 트래픽이 계속 들어옵니다.

또 애플리케이션이 진행 중인 요청을 기다리지 않고 즉시 종료합니다. 종료 신호를 받으면 바로 컨텍스트를 닫습니다.

## 조치

**대기 시간 추가**: 종료 신호를 받으면 10초 대기 후 종료 절차를 시작합니다. 이 동안 헬스체크가 실패하고 로드밸런서가 제외합니다.

**진행 중 요청 처리**: graceful shutdown 을 활성화하고 최대 대기 시간을 20초로 뒀습니다. 진행 중인 요청이 끝날 때까지 기다립니다.

```yaml
server:
  shutdown: graceful
spring:
  lifecycle:
    timeout-per-shutdown-phase: 20s
```

**종료 순서**

```text
1. 종료 신호 수신
2. 헬스체크를 실패로 전환
3. 10초 대기 (로드밸런서 제외 반영)
4. 신규 요청 수신 중단
5. 진행 중 요청 완료 대기 (최대 20초)
6. 종료
```

## 결과

배포 후 오류가 30~60건에서 0~2건으로 줄었습니다. 남은 건은 20초 안에 끝나지 않은 장기 요청입니다.

## 남은 문제

배치나 비동기 큐 소비자는 이 처리에 포함되지 않습니다. 감사 이력 비동기 큐에서 종료 시 유실이 발생한 사례가 있었습니다. 각 비동기 처리마다 종료 시 동작을 정의해야 합니다.
""",
    },
    {
        "id": "DOC-147",
        "title": "[리뷰] 비동기 작업 종료 처리",
        "department": "백엔드개발팀",
        "type": "코드리뷰",
        "project": None,
        "date": "2026-03-10",
        "systems": ["AIMS", "ORBIT"],
        "features": [],
        "participants": ["오세훈", "노태윤"],
        "concepts": ["graceful shutdown", "비동기", "유실"],
        "body": """## 현재 구현

비동기 큐 소비자에 종료 처리를 추가했습니다.

```java
@PreDestroy
public void shutdown() {
    executor.shutdown();
}
```

## 리뷰 의견

오세훈님: `shutdown()` 은 신규 작업만 막고 진행 중인 것을 기다리지 않습니다. `awaitTermination()` 이 필요합니다. 지금 코드는 큐에 쌓인 작업이 그대로 사라집니다.

노태윤님: 추가하겠습니다. 대기 시간은 얼마가 적절할까요.

오세훈님: 애플리케이션 종료 대기가 20초이므로 그보다 짧아야 합니다. 15초 정도로 두고, 그 안에 안 끝나면 남은 작업을 로그로 남기는 편이 좋겠습니다. 유실을 조용히 넘기지 않도록요.

노태윤님: `shutdownNow()` 가 반환하는 미처리 작업 목록을 로그로 남기겠습니다.

오세훈님: 감사 로그 큐는 유실되면 안 되는 데이터입니다. 종료 시 남은 것을 동기로 저장하는 방법도 있습니다.

노태윤님: 15초 대기로 대부분 처리될 것 같습니다. 그래도 남으면 동기 저장을 시도하고, 그것도 실패하면 로그에 원본 내용을 남기겠습니다. 로그에서 복구할 수 있게요.

오세훈님: 로그에 감사 로그 내용을 남기면 개인정보가 로그로 흘러갑니다. 마스킹된 상태인지 확인해 주세요.

노태윤님: 마스킹은 큐에 넣기 전에 이미 적용됩니다. 확인 후 반영하겠습니다.

## 수정 사항

- `awaitTermination(15, SECONDS)` 추가
- 미처리 작업을 동기 저장 시도
- 실패 시 로그 기록 (마스킹 상태 확인 완료)

## 남은 확인

다른 비동기 처리에도 같은 문제가 있습니다. 메일 발송 큐, 재시도 큐가 대상입니다. 공통 베이스 클래스를 만들어 종료 처리를 한 곳에 두는 방안을 검토합니다.
""",
    },
    {
        "id": "DOC-148",
        "title": "Feature Flag 운영 방법",
        "department": "DevOps팀",
        "type": "매뉴얼 / 정의서",
        "project": None,
        "date": "2026-04-02",
        "systems": [],
        "features": [],
        "participants": [],
        "concepts": ["Feature Flag", "배포", "운영"],
        "body": """## 언제 쓰나

- 배포와 기능 공개 시점을 분리하고 싶을 때
- 일부 사용자에게만 먼저 열고 싶을 때
- 문제 발생 시 배포 없이 끄고 싶을 때

## 플래그 종류

| 종류 | 수명 | 예 |
| --- | --- | --- |
| 릴리스 | 짧음 (배포 후 제거) | 신규 결제 화면 |
| 운영 | 김 | 특정 결제 수단 차단 |
| 실험 | 실험 기간 | A/B 테스트 |

릴리스 플래그는 기능이 안정되면 반드시 제거하세요. 남아 있으면 코드에 죽은 분기가 쌓입니다.

## 명명

```text
{서비스}.{기능}.{용도}

orbit.payment.v2-enabled
mars.reward.expiry-batch-enabled
nexus.signup.email-verification-required
```

## 설정 변경

관리 화면에서 변경하며 즉시 반영됩니다. 애플리케이션 재시작이 필요 없습니다.

변경 시 사유를 남기세요. 누가 왜 껐는지 모르면 나중에 켜기 어렵습니다.

## 주의

- 플래그 값이 없을 때의 기본 동작을 반드시 정의하세요. 설정 조회가 실패해도 서비스가 동작해야 합니다
- 기본값은 "기존 동작"으로 두세요. 신규 기능이 기본으로 켜지면 플래그의 의미가 없습니다
- 플래그 안에 플래그를 넣지 마세요. 조합이 늘면 어떤 경로가 실제로 도는지 알 수 없습니다

## 제거

릴리스 플래그는 배포 후 2주 안에 제거하는 것을 목표로 합니다. 제거 시에는 남기는 쪽 코드만 두고 분기를 없앱니다.

현재 3개월 이상 된 릴리스 플래그가 4개 있습니다. 정리 대상입니다.

## 감사

플래그 변경은 이력이 남습니다. 누가 언제 무엇을 바꿨는지 조회할 수 있습니다.
""",
    },
    {
        "id": "DOC-149",
        "title": "알림 발송 채널 정책 논의",
        "department": "서비스기획팀",
        "type": "기획자와의 회의록",
        "project": None,
        "date": "2026-05-12",
        "systems": ["NEXUS"],
        "features": [],
        "participants": ["송지은", "한지민"],
        "concepts": ["알림", "수신 동의", "정책"],
        "body": """## 논의 배경

알림이 이메일, 앱 푸시, 문자로 나가는데 채널별 기준이 없습니다. 같은 내용이 세 채널로 중복 발송되는 경우도 있습니다.

## 현황

한지민님이 정리한 발송 현황입니다.

| 알림 | 이메일 | 푸시 | 문자 |
| --- | --- | --- | --- |
| 가입 인증 | O | X | X |
| 주문 완료 | O | O | X |
| 배송 시작 | O | O | O |
| 결제 실패 | X | O | X |
| 휴면 예정 | O | X | X |
| 포인트 소멸 예정 | O | O | X |
| 이벤트 안내 | O | O | X |

배송 시작이 세 채널 전부입니다. 문자는 건당 비용이 있어 부담이 큽니다.

## 분류 기준

송지은님이 제안한 분류입니다.

**필수 고지**: 서비스 이용에 필요한 정보. 수신 동의와 무관하게 발송

- 가입 인증, 비밀번호 변경, 휴면 예정, 결제 실패

**거래 알림**: 거래 진행 상황. 기본 발송, 사용자가 끌 수 있음

- 주문 완료, 배송 시작, 배송 완료

**마케팅**: 수신 동의 필요

- 이벤트, 프로모션, 추천

## 채널 선택 기준

| 분류 | 기본 채널 | 비고 |
| --- | --- | --- |
| 필수 고지 | 이메일 | 즉시성이 필요하면 푸시 추가 |
| 거래 알림 | 푸시 | 앱 미설치자는 이메일 |
| 마케팅 | 푸시 | 동의자 한정 |

문자는 원칙적으로 쓰지 않습니다. 푸시와 이메일이 모두 불가능한 경우에만 사용합니다.

## 쟁점

한지민님은 배송 시작 문자를 없애면 문의가 늘 것이라고 우려했습니다. 앱 미설치 사용자에게는 문자가 유일한 즉시 알림입니다.

송지은님은 앱 미설치자 비중을 확인한 뒤 정하자고 했습니다.

## 미확정

배송 관련 문자 폐지 여부는 데이터 확인 후 결정합니다. 그전까지 현행 유지합니다.

## Action Item

- 한지민님: 앱 미설치 사용자 비중 확인 요청
- 송지은님: 알림 설정 화면 개편안 작성
""",
    },
    {
        "id": "DOC-150",
        "title": "notification 테이블 설계",
        "department": "백엔드개발팀",
        "type": "DB 테이블 설계안",
        "project": None,
        "date": "2026-05-15",
        "systems": ["NEXUS"],
        "features": [],
        "participants": [],
        "concepts": ["알림", "발송 이력", "Index", "보관 기간"],
        "body": """## 배경

알림 발송 이력이 채널별로 흩어져 있습니다. 이메일은 메일 시스템에, 푸시는 푸시 서비스에 있고 우리 DB 에는 남지 않습니다. "알림을 못 받았다"는 문의에 답할 수 없습니다.

## notification

| 컬럼 | 타입 | 제약 | 설명 |
| --- | --- | --- | --- |
| id | BIGINT | PK | |
| member_id | BIGINT | NOT NULL | |
| category | VARCHAR(24) | NOT NULL | REQUIRED_NOTICE / TRANSACTION / MARKETING |
| template_code | VARCHAR(48) | NOT NULL | 알림 종류 |
| channel | VARCHAR(16) | NOT NULL | EMAIL / PUSH / SMS |
| status | VARCHAR(16) | NOT NULL | QUEUED / SENT / FAILED / SKIPPED |
| skip_reason | VARCHAR(32) | NULL | 수신 거부, 채널 불가 등 |
| reference_key | VARCHAR(100) | NOT NULL | 중복 발송 방지 |
| requested_at | DATETIME | NOT NULL | |
| sent_at | DATETIME | NULL | |

본문은 저장하지 않습니다. 템플릿 코드와 파라미터만 두고 필요하면 재구성합니다. 본문에 개인정보가 들어가기 때문입니다.

```sql
CREATE UNIQUE INDEX uk_notification_ref
  ON notification (template_code, channel, reference_key);
CREATE INDEX idx_notification_member
  ON notification (member_id, requested_at DESC);
CREATE INDEX idx_notification_status
  ON notification (status, requested_at);
```

## SKIPPED 를 두는 이유

수신 거부로 보내지 않은 것과 발송 실패는 다릅니다. 문의 대응에서 "동의하지 않으셔서 발송되지 않았습니다"라고 답하려면 기록이 필요합니다.

`skip_reason` 값:

```text
OPT_OUT          수신 거부
NO_CHANNEL       앱 미설치, 이메일 없음
DUPLICATE        중복 억제
INVALID_TARGET   탈퇴 회원 등
```

## 중복 방지

`reference_key` 로 같은 알림이 두 번 나가지 않게 합니다. 가입 인증 메일 중복 발송 이슈에서 배운 방식입니다.

주문 완료 알림이면 주문번호, 휴면 예정이면 `회원ID:예정일` 형태입니다.

## 보관 기간

1년으로 잡았습니다. 하루 발송량이 약 12만 건이라 1년이면 4,400만 행입니다.

파티셔닝이 필요한 규모입니다. `requested_at` 기준 월 단위 range 파티션을 적용하고, 1년 경과 파티션을 통째로 제거합니다. 행 단위 삭제보다 훨씬 빠릅니다.

## 발송 본문 재구성

문의 대응에서 실제 발송 내용을 봐야 하는 경우가 있습니다. `template_code` 와 파라미터로 재구성하는데, 템플릿이 바뀌면 과거 내용과 달라집니다.

템플릿 버전을 함께 저장할지 논의가 필요합니다. 이번 설계에는 넣지 않았습니다.
""",
    },
]
