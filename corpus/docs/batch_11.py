"""DOC-151 ~ DOC-165 : ORBIT 상품/주문 도메인과 클라이언트 (2025~2026)."""

DOCUMENTS = [
    {
        "id": "DOC-151",
        "title": "카트 담기 및 유지 정책",
        "department": "서비스기획팀",
        "type": "기획자와의 회의록",
        "project": None,
        "date": "2025-08-14",
        "systems": ["ORBIT"],
        "features": ["주문", "상품"],
        "participants": ["강민재", "최유진"],
        "concepts": ["정책", "비회원", "보관 기간"],
        "body": """## 논의 배경

카트가 로그인 상태에서만 유지됩니다. 비회원이 담은 상품은 로그인하면 사라집니다. 이 지점에서 이탈이 관측됩니다.

## 요구사항

- 비회원 카트를 로컬에 유지
- 로그인 시 기존 카트와 병합
- 담긴 상품이 품절되면 표시

## 쟁점

병합 규칙이 문제입니다. 비회원으로 A 상품 2개를 담고, 로그인한 계정에도 A 상품 1개가 있으면 결과가 몇 개여야 하는지입니다.

최유진님은 합산(3개)이 자연스럽다고 봤고, 강민재님은 사용자가 의도한 수량이 아닐 수 있다고 했습니다. 비회원 시절 담은 것은 잊고 있을 가능성이 큽니다.

## 검토안

| 방식 | 결과 | 문제 |
| --- | --- | --- |
| 합산 | 3개 | 의도치 않은 수량 |
| 큰 값 | 2개 | 설명하기 어려움 |
| 로그인 계정 우선 | 1개 | 비회원 카트가 사라짐 |
| 사용자에게 선택 | - | 단계가 늘어남 |

## 결정 사항

합산으로 갑니다. 다만 병합이 일어났을 때 "카트에 상품이 추가되었습니다" 안내를 띄웁니다. 사용자가 확인하고 수량을 조정할 수 있습니다.

수량 상한(상품당 99개)을 넘으면 상한으로 맞춥니다.

## 보관 기간

- 고객 카트: 90일
- 비회원 카트: 브라우저 로컬, 30일

90일이 지난 항목은 배치로 정리합니다. 오래된 카트는 가격과 재고가 모두 달라져 있어 유지 의미가 적습니다.

## 미확정

품절 상품을 카트에서 자동 제거할지, 표시만 하고 남길지 정하지 못했습니다. 자동 제거하면 재입고를 기다리던 사용자가 당황합니다. 남기면 주문 시 걸러야 합니다.

다음 회의에서 재입고 알림 기능과 함께 다룹니다.
""",
    },
    {
        "id": "DOC-152",
        "title": "주문 생성 처리 설계",
        "department": "백엔드개발팀",
        "type": "비즈니스 기능 설계",
        "project": None,
        "date": "2025-09-11",
        "systems": ["ORBIT", "MARS", "NEXUS"],
        "features": ["주문", "결제"],
        "participants": [],
        "concepts": ["Transaction", "재고", "Edge Case", "동시성"],
        "body": """## 배경

주문 생성은 재고, 적립금, 쿠폰, 결제가 얽혀 있습니다. 어느 단계에서 실패하면 앞 단계를 되돌려야 합니다.

## 정상 Flow

1. 주문 요청 검증 (회원 상태, 상품 판매 여부, 금액 재계산)
2. 재고 차감
3. 쿠폰 사용
4. 적립금 사용
5. 주문 생성 (`ORDERED`)
6. 결제 요청
7. 결제 성공 시 주문 확정

## 금액 재계산

클라이언트가 보낸 금액을 믿지 않습니다. 상품 가격, 할인, 쿠폰, 적립금을 서버에서 다시 계산하고 클라이언트 값과 다르면 거부합니다.

가격은 주문 시점 기준입니다. 장바구니에 담은 시점과 다를 수 있으므로 차이가 있으면 `ORDER_1005` 로 알리고 사용자에게 확인받습니다.

## 실패 시 되돌리기

| 실패 단계 | 되돌릴 것 |
| --- | --- |
| 재고 차감 | 없음 |
| 쿠폰 사용 | 재고 |
| 적립금 사용 | 재고, 쿠폰 |
| 주문 생성 | 재고, 쿠폰, 적립금 |
| 결제 | 재고, 쿠폰, 적립금, 주문 취소 |

2~5는 같은 트랜잭션이라 롤백으로 처리됩니다. 다만 쿠폰과 적립금은 MARS 호출이라 롤백 대상이 아닙니다. 명시적으로 취소를 호출해야 합니다.

## Edge Case

- **재고 차감 성공 후 결제 실패**: 재고를 복원해야 합니다. 복원이 실패하면 팔 수 있는 상품이 묶입니다. 재시도 큐로 처리하고 실패 시 알림을 보냅니다.
- **동시에 마지막 재고 주문**: 재고 차감을 조건부 갱신으로 처리합니다. `WHERE quantity >= :요청수량` 을 붙이고 영향 행이 0이면 품절로 처리합니다.
- **주문 중 상품 판매 중지**: 검증 시점과 재고 차감 시점 사이에 중지될 수 있습니다. 차감 시 판매 상태를 함께 확인합니다.
- **적립금 사용 성공, 주문 생성 실패**: 적립금 사용 취소를 호출합니다. 참조키가 주문번호라 중복 취소는 발생하지 않습니다.
- **결제 대기 중 사용자 이탈**: 주문은 `ORDERED` 로 남고 결제는 안 됩니다. 30분 뒤 자동 취소하고 재고와 적립금을 복원합니다.

## 고민

재고 차감을 결제 전에 할지 후에 할지 논의가 있었습니다. 결제 후에 하면 결제는 됐는데 품절인 상황이 생깁니다. 결제 전에 하면 결제를 안 하는 사용자 때문에 재고가 묶입니다.

결제 전 차감 + 30분 자동 취소로 정했습니다. 인기 상품에서 30분은 길 수 있어 상품별로 조정 가능하게 설정으로 뺐습니다.
""",
    },
    {
        "id": "DOC-153",
        "title": "주문 생성 API 명세",
        "department": "백엔드개발팀",
        "type": "API 명세서",
        "project": None,
        "date": "2025-09-24",
        "systems": ["ORBIT"],
        "features": ["주문"],
        "participants": [],
        "concepts": ["Validation", "Error Code", "금액 검증"],
        "body": """## Endpoint

```http
POST /orders
Authorization: Bearer {accessToken}
```

## Request

```json
{
  "items": [
    { "productId": 88201, "quantity": 2, "expectedUnitPrice": 12000 }
  ],
  "shippingAddress": {
    "receiverName": "홍길동",
    "phone": "010-0000-0000",
    "zipCode": "06234",
    "address1": "서울시 강남구",
    "address2": "101동 202호"
  },
  "couponIssueId": 5512099,
  "pointAmount": 2000,
  "expectedTotalAmount": 20000
}
```

`expectedUnitPrice` 와 `expectedTotalAmount` 는 클라이언트가 화면에서 계산한 값입니다. 서버 계산과 다르면 거부합니다.

## Response

```json
{
  "orderId": 90112,
  "status": "ORDERED",
  "totalAmount": 20000,
  "paymentDeadline": "2025-09-24T15:12:00+09:00",
  "items": [
    { "orderItemId": 551201, "productId": 88201, "quantity": 2, "itemAmount": 24000 }
  ]
}
```

`paymentDeadline` 까지 결제하지 않으면 주문이 자동 취소됩니다. 기본 30분입니다.

## Error Code

| 코드 | HTTP | 설명 |
| --- | --- | --- |
| ORDER_1001 | 400 | 필수 값 누락 |
| ORDER_1002 | 409 | 판매 중지 상품 포함 |
| ORDER_1003 | 409 | 재고 부족 |
| ORDER_1004 | 409 | 쿠폰 사용 불가 |
| ORDER_1005 | 409 | 금액 불일치 (가격 변동) |
| ORDER_1006 | 409 | 적립금 잔액 부족 |
| ORDER_1007 | 403 | 주문 불가 회원 상태 |

`ORDER_1003` 응답에는 부족한 상품 목록이 포함됩니다.

```json
{
  "code": "ORDER_1003",
  "outOfStock": [
    { "productId": 88201, "requested": 2, "available": 1 }
  ]
}
```

## 주의사항

`ORDER_1005` 는 가격이 바뀐 경우입니다. 클라이언트는 최신 가격을 다시 조회해 사용자에게 보여주고 확인받아야 합니다. 자동으로 새 가격으로 진행하면 안 됩니다.

주문 생성과 결제는 별개 호출입니다. 주문이 만들어졌다고 결제가 완료된 것은 아닙니다.
""",
    },
    {
        "id": "DOC-154",
        "title": "주문 조회 API v2 명세",
        "department": "백엔드개발팀",
        "type": "API 명세서",
        "project": None,
        "date": "2025-10-08",
        "systems": ["ORBIT"],
        "features": ["주문"],
        "participants": [],
        "concepts": ["조회 성능", "페이징", "버전 변경"],
        "body": """## 변경 배경

v1 은 주문 목록에 상품 정보를 조인해서 내려줬습니다. 주문이 많은 회원에서 응답이 느려 12월 트래픽 구간에 병목이 됐습니다.

v2 는 목록과 상세를 분리합니다.

## 목록 조회

```http
GET /orders?page=0&size=20
Authorization: Bearer {accessToken}
```

```json
{
  "content": [
    {
      "orderId": 90112,
      "status": "DELIVERED",
      "representativeProductName": "무선 이어폰 외 2건",
      "thumbnailUrl": "https://cdn.example.com/p/88201.jpg",
      "totalAmount": 20000,
      "orderedAt": "2025-09-24T14:42:00+09:00"
    }
  ],
  "totalElements": 84,
  "page": 0,
  "size": 20
}
```

대표 상품명과 썸네일은 주문 생성 시점에 복사해 둡니다. 상품이 삭제되어도 목록이 깨지지 않고, 조회 시 상품 서비스를 호출하지 않습니다.

## 상세 조회

```http
GET /orders/{orderId}
```

상세에는 전체 상품, 배송지, 결제 정보, 취소 이력이 포함됩니다.

## 변경 요약

| 항목 | v1 | v2 |
| --- | --- | --- |
| 목록에 상품 전체 | 포함 | 제외 (대표 상품만) |
| 상품 정보 출처 | 실시간 조인 | 주문 시점 복사본 |
| 기본 페이지 크기 | 50 | 20 |
| 응답 시간 p99 | 1.8초 | 0.2초 |

## 주의사항

목록의 상품명은 주문 시점 값입니다. 상품명이 나중에 바뀌어도 목록에는 옛 이름이 표시됩니다. 이것은 의도한 동작입니다. 사용자가 주문할 때 본 이름을 보여주는 것이 맞습니다.

상세에서는 현재 상품 정보를 함께 조회하므로 "판매 중지된 상품" 같은 상태가 반영됩니다.
""",
    },
    {
        "id": "DOC-155",
        "title": "product / product_stock 테이블 분리",
        "department": "백엔드개발팀",
        "type": "DB 테이블 설계안",
        "project": None,
        "date": "2025-10-21",
        "systems": ["ORBIT"],
        "features": ["상품", "주문"],
        "participants": [],
        "concepts": ["테이블 분리", "동시성", "Index"],
        "body": """## 배경

`product` 테이블에 재고 수량이 컬럼으로 들어 있습니다. 주문할 때마다 이 행을 갱신하는데, 같은 행에 상품명과 가격도 있어서 상품 정보 조회가 재고 갱신 락에 걸립니다.

## 현재 문제

- 인기 상품 주문 시 해당 상품 조회가 대기
- 상품 정보 수정과 재고 차감이 경합
- 재고 변동 이력이 남지 않음

## 분리안

### product

| 컬럼 | 타입 | 설명 |
| --- | --- | --- |
| id | BIGINT | PK |
| name | VARCHAR(200) | |
| price | INT | |
| category_id | BIGINT | |
| status | VARCHAR(16) | ON_SALE / SOLD_OUT / STOPPED |
| created_at | DATETIME | |

### product_stock

| 컬럼 | 타입 | 설명 |
| --- | --- | --- |
| product_id | BIGINT | PK, FK |
| quantity | INT | 현재 재고 |
| reserved_quantity | INT | 주문됐으나 결제 전 |
| updated_at | DATETIME | |

### product_stock_history

| 컬럼 | 설명 |
| --- | --- |
| id | PK |
| product_id | |
| change_type | ORDER / CANCEL / RESTOCK / ADJUST |
| quantity_delta | 증감 |
| reference_key | 주문번호 등 |
| created_at | |

```sql
CREATE INDEX idx_product_status ON product (status, category_id);
CREATE UNIQUE INDEX uk_stock_history_ref
  ON product_stock_history (change_type, reference_key, product_id);
```

## reserved_quantity 를 두는 이유

주문 생성 시 재고를 차감하고 결제 전에 취소되면 복원합니다. 이 사이 상태를 구분하지 않으면 "실제 팔린 것"과 "묶인 것"을 알 수 없습니다.

판매 가능 수량은 `quantity - reserved_quantity` 입니다.

## 동시성

재고 차감은 조건부 갱신으로 합니다.

```sql
UPDATE product_stock
SET reserved_quantity = reserved_quantity + :qty, updated_at = NOW()
WHERE product_id = :productId
  AND quantity - reserved_quantity >= :qty;
```

영향 행이 0이면 재고 부족입니다. 조회 후 판단하는 방식은 쓰지 않습니다.

## 이력의 unique 제약

같은 주문으로 재고가 두 번 차감되지 않도록 `(change_type, reference_key, product_id)` 에 unique 를 겁니다. 재시도 상황에서 중복 차감을 막습니다.

## 마이그레이션

`product.stock_quantity` 를 읽어 `product_stock` 을 채웁니다. 이관 중 재고가 바뀌면 어긋나므로 짧은 점검 시간이 필요합니다. 상품 수가 12만 건이라 1분 이내로 예상합니다.
""",
    },
    {
        "id": "DOC-156",
        "title": "한정 수량 상품에서 재고보다 많은 주문 접수",
        "department": "백엔드개발팀",
        "type": "이슈 정리",
        "project": None,
        "date": "2025-11-06",
        "systems": ["ORBIT"],
        "features": ["주문", "상품"],
        "participants": [],
        "concepts": ["동시성", "재고", "Race Condition"],
        "body": """## 현상

11월 5일 한정 수량 이벤트 상품(재고 200개)에서 주문이 214건 접수되었습니다. 14건은 재고 없이 주문된 상태입니다.

## 재현

동시 요청 100건을 보내면 재고보다 많이 통과합니다.

## 분석

재고 차감 코드가 조회 후 판단하는 구조였습니다.

```java
var stock = stockRepository.findByProductId(productId);
if (stock.available() < quantity) {
    throw new OutOfStockException();
}
stock.decrease(quantity);
stockRepository.save(stock);
```

10월에 `product_stock` 분리 설계를 하면서 조건부 갱신으로 바꾸기로 했는데, 테이블만 분리되고 차감 로직은 그대로였습니다.

## 영향 범위

- 초과 주문 14건
- 그중 결제 완료 11건

## 대응

11건은 개별 연락 후 취소하고 보상 쿠폰을 지급했습니다. 재고를 추가 확보할 수 없는 한정 상품이라 다른 방법이 없었습니다.

## 근본 해결

조건부 갱신으로 변경했습니다.

```sql
UPDATE product_stock
SET reserved_quantity = reserved_quantity + :qty
WHERE product_id = :productId
  AND quantity - reserved_quantity >= :qty;
```

영향 행이 0이면 품절 처리합니다. 11월 6일 배포했고, 부하 테스트에서 동시 500 요청에도 재고를 초과하지 않는 것을 확인했습니다.

## 재발 방지

설계 문서에 적힌 내용이 구현에 반영됐는지 확인하는 절차가 없었습니다. 설계 문서와 구현 PR 을 연결하고, 리뷰어가 설계의 핵심 결정이 반영됐는지 확인하기로 했습니다.

"조회 후 판단하고 저장"하는 패턴을 다른 곳에서도 찾았습니다. 쿠폰 사용에서 같은 문제가 있어 별도 이슈로 등록했습니다.
""",
    },
    {
        "id": "DOC-157",
        "title": "재고 차감 및 복원 설계",
        "department": "백엔드개발팀",
        "type": "비즈니스 기능 설계",
        "project": None,
        "date": "2025-11-13",
        "systems": ["ORBIT"],
        "features": ["상품", "주문", "주문 취소"],
        "participants": [],
        "concepts": ["동시성", "재고", "Edge Case", "보상 처리"],
        "body": """## 배경

초과 판매 이슈 이후 재고 처리 전반을 정리합니다.

## 재고 상태

```text
quantity            총 재고
reserved_quantity   주문됐으나 결제 미완료
available           quantity - reserved_quantity  (판매 가능)
```

## 차감 시점

주문 생성 시 `reserved_quantity` 를 늘립니다. 결제가 완료되면 `quantity` 를 줄이고 `reserved_quantity` 를 되돌립니다.

```text
주문 생성:  reserved += n
결제 완료:  quantity -= n,  reserved -= n
주문 취소:  reserved -= n            (결제 전)
결제 후 취소: quantity += n           (결제 후)
```

## 조건부 갱신

모든 변경은 조건을 쿼리에 넣습니다. 조회 후 판단하지 않습니다.

```sql
-- 예약
UPDATE product_stock
SET reserved_quantity = reserved_quantity + :qty
WHERE product_id = :id AND quantity - reserved_quantity >= :qty;

-- 확정
UPDATE product_stock
SET quantity = quantity - :qty, reserved_quantity = reserved_quantity - :qty
WHERE product_id = :id AND reserved_quantity >= :qty;
```

## Edge Case

- **결제 대기 중 타임아웃**: 30분 뒤 자동 취소 배치가 `reserved` 를 되돌립니다. 배치가 늦으면 재고가 묶입니다. 배치 주기는 1분입니다.
- **자동 취소와 결제 완료 동시 발생**: 배치가 취소하려는 순간 결제가 완료될 수 있습니다. 취소는 주문 상태를 조건에 넣어 `ORDERED` 인 것만 취소합니다. 결제 완료로 상태가 바뀌었으면 취소가 실패하고 정상입니다.
- **재고 복원 실패**: 재시도 큐로 처리합니다. `product_stock_history` 의 unique 제약이 중복 복원을 막습니다.
- **음수 재고**: 조건부 갱신이라 정상 경로에서는 발생하지 않습니다. 운영자가 재고를 줄이는 조정에서 발생할 수 있어 조정 시에도 조건을 겁니다.
- **판매 중지 후 기존 예약**: 상품을 중지해도 이미 예약된 주문은 진행됩니다. 중지는 신규 주문만 막습니다.

## 재고 조정

운영자가 재고를 직접 수정하는 경우가 있습니다. 입고, 실사 반영 등입니다.

조정도 이력을 남기고, 조정 결과 `available` 이 음수가 되는 요청은 거부합니다. 이미 예약된 수량보다 적게 조정할 수 없습니다.

## 대사

`product_stock` 과 `product_stock_history` 합계를 일 1회 비교합니다. 어긋나면 알림을 보내고 자동 보정하지 않습니다.
""",
    },
    {
        "id": "DOC-158",
        "title": "[리뷰] 재고 차감 조건부 갱신 적용",
        "department": "백엔드개발팀",
        "type": "코드리뷰",
        "project": None,
        "date": "2025-11-19",
        "systems": ["ORBIT"],
        "features": ["상품", "주문"],
        "participants": ["노태윤", "김도윤"],
        "concepts": ["동시성", "재고", "이력"],
        "body": """## 현재 구현

조건부 갱신으로 바꾸고 이력을 함께 남깁니다.

```java
int affected = stockRepository.reserve(productId, quantity);
if (affected == 0) {
    throw new OutOfStockException(productId);
}
historyRepository.save(StockHistory.reserve(productId, quantity, orderId));
```

## 리뷰 의견

김도윤님: 여러 상품을 주문할 때 앞 상품은 성공하고 뒤 상품이 실패하면 앞 상품 예약이 남습니다. 같은 트랜잭션이면 롤백되지만, 트랜잭션 경계가 어디인지 코드에서 안 보입니다.

노태윤님: 주문 생성 전체가 하나의 트랜잭션입니다. 서비스 메서드에 `@Transactional` 이 붙어 있습니다.

김도윤님: 그러면 여러 상품을 잠그게 되는데 락 순서가 정해져 있나요. 9월 데드락 이슈와 같은 상황이 생길 수 있습니다.

노태윤님: 정렬이 빠져 있습니다. 상품 ID 오름차순으로 정렬해서 처리하겠습니다.

김도윤님: 이력 저장에 `reference_key` 로 주문번호를 쓰는데, 한 주문에 같은 상품이 두 줄로 들어오는 경우가 있나요. 카트에서 같은 상품을 다른 옵션으로 담으면 그럴 수 있습니다.

노태윤님: unique 제약이 `(change_type, reference_key, product_id)` 라 같은 주문의 같은 상품이 두 번이면 위반이 납니다. 확인해 보니 현재는 담을 때 합쳐지지만, 옵션이 생기면 문제가 됩니다.

김도윤님: 지금 구조에서는 괜찮지만 주석이라도 남겨두면 좋겠습니다.

## 수정 사항

- 상품 ID 오름차순 정렬 후 처리
- 이력 unique 제약의 전제(주문 내 상품 중복 없음)를 주석으로 명시
- `affected == 0` 일 때 어떤 상품이 부족한지 예외에 포함

## 남은 확인

여러 상품 중 일부만 부족한 경우 응답에 전부 표시하려면 실패 즉시 중단하지 말고 전체를 확인해야 합니다. 현재는 첫 실패에서 중단합니다. UX 관점에서 어느 쪽이 나은지 기획팀 확인이 필요합니다.
""",
    },
    {
        "id": "DOC-159",
        "title": "품절 상품 노출 기준 논의",
        "department": "프론트엔드개발팀",
        "type": "개발자 회의록",
        "project": None,
        "date": "2025-12-11",
        "systems": ["ORBIT"],
        "features": ["상품"],
        "participants": ["강민재", "정하늘"],
        "concepts": ["UX", "재고", "정책"],
        "body": """## 논의 배경

품절 상품이 목록에 계속 노출됩니다. 사용자가 눌러서 들어가면 품절 안내가 나옵니다. 이탈 지점 분석에서 이 경로가 상위에 있습니다.

## 현황

목록 API 는 `status = ON_SALE` 인 상품을 반환하는데, 재고가 0이어도 상태가 자동으로 바뀌지 않습니다. 운영자가 수동으로 `SOLD_OUT` 으로 바꿔야 합니다.

## 검토안

정하늘님이 정리한 방안입니다.

| 방안 | 장점 | 단점 |
| --- | --- | --- |
| 재고 0이면 자동 SOLD_OUT | 정확 | 재입고 시 되돌려야 함 |
| 목록에서 제외 | 단순 | 재입고 알림 신청 경로가 사라짐 |
| 목록 뒤로 밀고 표시 | 노출 유지 | 정렬 복잡도 |

## 쟁점

강민재님은 품절 상품도 노출되어야 한다는 입장입니다. 재입고 알림 신청이 마케팅 자산이고, 인기 상품이 목록에서 사라지면 브랜드 인상이 나빠집니다.

정하늘님은 목록에 품절 표시가 없어서 사용자가 알 수 없다는 점을 지적했습니다. 표시만 확실하면 노출을 유지해도 됩니다.

## 결정 사항

- 재고 0이면 목록 응답에 `soldOut: true` 를 내려준다 (상태 변경 없이 계산)
- 프론트에서 품절 배지와 흐림 처리를 적용한다
- 정렬에서 품절 상품을 뒤로 보낸다
- 상세 화면에 재입고 알림 신청 버튼을 노출한다

상품 상태를 자동으로 바꾸지 않는 이유는 재입고 시 되돌리는 처리가 필요하고, 운영자가 의도적으로 `STOPPED` 로 둔 상품과 구분이 어려워지기 때문입니다.

## Action Item

- 정하늘님: 목록 정렬 및 배지 적용
- 강민재님: 재입고 알림 기획 요건 정리

## 추후 논의

재입고 알림 기능 자체는 아직 없습니다. 별도 과제로 등록했습니다.
""",
    },
    {
        "id": "DOC-160",
        "title": "[리뷰] 주문서 화면 상태 관리 정리",
        "department": "프론트엔드개발팀",
        "type": "코드리뷰",
        "project": "Project Falcon",
        "date": "2026-01-15",
        "systems": ["ORBIT"],
        "features": ["주문", "결제"],
        "participants": ["최유진", "정하늘"],
        "concepts": ["상태 관리", "멱등성", "UX"],
        "body": """## 현재 구현

주문서 화면 상태가 컴포넌트마다 흩어져 있습니다. 이번 PR 에서 하나의 스토어로 모았습니다.

```text
orderSheet: {
  items, coupon, pointAmount, paymentMethod,
  idempotencyKey, isSubmitting
}
```

## 리뷰 의견

최유진님: `idempotencyKey` 를 스토어에 두는 건 좋은데, 페이지를 새로 고치면 사라집니다. 서버 팀에서 정한 규칙은 "주문 ID 확보 직후 생성하고 보관"입니다. 로컬 스토리지에 함께 저장해야 합니다.

정하늘님: 반영하겠습니다. 주문 ID 를 키로 저장하고, 결제 완료 시 삭제하겠습니다.

최유진님: `isSubmitting` 으로 중복 제출을 막는데, 이 값이 false 로 돌아오는 시점이 응답 수신 후입니다. 타임아웃으로 요청이 오래 걸리면 사용자가 기다리다 새로고침하고, 그러면 상태가 초기화됩니다.

정하늘님: 202(처리 중) 응답을 받으면 결제 상태 조회 화면으로 넘기도록 되어 있습니다. 다만 타임아웃 자체는 클라이언트에서도 발생할 수 있어서 그 경우 처리가 없네요.

최유진님: 클라이언트 타임아웃 시에도 결제 상태 조회로 보내는 게 맞습니다. 요청이 서버에 도달했을 수 있으니까요. 실패로 처리하고 재시도 버튼을 주면 중복 결제 위험이 있습니다.

정하늘님: 수정하겠습니다.

최유진님: `nit:` 포인트 입력이 문자열로 들어와서 계산에 쓰기 전에 매번 변환합니다. 스토어에 넣을 때 숫자로 정규화하는 게 낫겠습니다.

## 수정 사항

- 멱등키를 로컬 스토리지에 주문 ID 기준으로 저장
- 클라이언트 타임아웃 시 결제 상태 조회 화면으로 이동
- 포인트 입력값을 스토어 진입 시 숫자로 정규화

## 남은 확인

로컬 스토리지 정리 시점이 애매합니다. 결제 완료 시 삭제하지만 사용자가 이탈하면 남습니다. 주문 ID 기준이라 충돌은 없지만 쌓입니다. 오래된 항목을 정리하는 처리를 다음 PR 에 넣기로 했습니다.
""",
    },
    {
        "id": "DOC-161",
        "title": "일부 안드로이드 기기에서 주문서가 비어 보이는 문제",
        "department": "모바일개발팀",
        "type": "이슈 정리",
        "project": None,
        "date": "2026-02-06",
        "systems": ["ORBIT"],
        "features": ["주문"],
        "participants": ["한지민"],
        "concepts": ["클라이언트", "캐시", "호환성"],
        "body": """## 현상

특정 안드로이드 기기에서 주문서 화면이 빈 상태로 표시됩니다. 상품 목록과 금액이 모두 보이지 않습니다. 앱 재설치하면 정상입니다.

접수된 기기는 모두 안드로이드 10 이하입니다.

## 재현

한지민님이 안드로이드 10 에뮬레이터에서 재현했습니다.

1. 카트에 상품 담기
2. 앱 종료 후 24시간 이상 경과
3. 앱 실행 후 주문서 진입 → 빈 화면

## 분석

주문서는 카트 정보를 로컬 캐시에서 읽습니다. 캐시 만료 시 서버에서 다시 받아오는데, 이 분기에서 예외가 발생하고 있었습니다.

```text
java.time.format.DateTimeParseException:
  Text '2026-02-05T14:22:31+09:00' could not be parsed
```

서버가 내려주는 시각 형식을 파싱하는 부분에서 실패합니다. 안드로이드 10 이하는 `java.time` 백포트 라이브러리를 쓰는데, 이 라이브러리 버전이 오프셋 표기 일부를 처리하지 못합니다.

24시간이 조건인 이유는 캐시 만료가 24시간이고, 만료 후에만 이 경로를 타기 때문입니다.

## 영향 범위

- 안드로이드 10 이하 사용자 (전체의 약 6%)
- 앱 재설치로 임시 회피 가능하나 24시간 뒤 재발

## 조치

- 백포트 라이브러리 버전 업데이트
- 시각 파싱 실패 시 예외를 삼키지 않고 기본값으로 진행하도록 수정
- 파싱 실패를 오류 수집 도구로 보고

두 번째 항목이 중요합니다. 파싱 하나가 실패해서 화면 전체가 안 뜨는 구조였습니다.

## 배포

2월 10일 앱 릴리스에 포함합니다. 심사 기간을 고려하면 사용자 도달까지 3~4일 걸립니다.

그동안 안내가 필요해서 고객지원팀에 재설치 안내를 전달했습니다.

## 재발 방지

안드로이드 10 이하 기기가 CI 테스트 대상에 없었습니다. 최소 지원 버전을 테스트 매트릭스에 추가합니다.
""",
    },
    {
        "id": "DOC-162",
        "title": "모바일 앱 배포 절차",
        "department": "모바일개발팀",
        "type": "매뉴얼 / 정의서",
        "project": None,
        "date": "2026-02-13",
        "systems": [],
        "features": [],
        "participants": [],
        "concepts": ["배포", "앱 심사", "롤백"],
        "body": """## 서버 배포와 다른 점

앱은 배포해도 즉시 반영되지 않습니다. 스토어 심사와 사용자 업데이트가 필요합니다.

| 단계 | 소요 |
| --- | --- |
| 빌드 및 업로드 | 30분 |
| 심사 (iOS) | 1~3일 |
| 심사 (Android) | 수 시간 ~ 1일 |
| 사용자 업데이트 도달 50% | 3~5일 |
| 도달 90% | 2~3주 |

서버 변경이 앱 변경을 필요로 하면 이 기간을 반드시 고려하세요.

## 배포 절차

1. `release/*` 브랜치에서 버전 태그
2. 빌드 및 내부 테스트 배포
3. QA 확인
4. 스토어 제출
5. 심사 통과 후 단계적 출시

## 단계적 출시

Android 는 10% → 50% → 100% 로 나눠 출시합니다. 각 단계에서 하루씩 지표를 봅니다.

- 크래시율
- 주요 화면 진입 성공률
- 오류 보고 건수

iOS 는 단계적 출시를 쓰되 기간을 7일로 설정합니다.

## 롤백

앱은 롤백이 사실상 불가능합니다. 이미 업데이트한 사용자를 되돌릴 수 없습니다.

대응 방법은 두 가지입니다.

- 단계적 출시 중이면 출시 중단
- 이미 배포됐으면 수정 버전을 긴급 배포

따라서 서버에서 끌 수 있는 기능은 Feature Flag 로 감싸 두는 것이 중요합니다. 앱 코드가 배포된 뒤에도 서버에서 기능을 끌 수 있어야 합니다.

## 강제 업데이트

치명적 결함이 있으면 강제 업데이트를 걸 수 있습니다. 앱 실행 시 최소 버전을 확인하고 미달이면 업데이트 화면을 띄웁니다.

강제 업데이트는 사용자 경험이 나쁘므로 기준을 지켜서 사용합니다. 기준은 별도 문서에 있습니다.

## 체크리스트

- [ ] 최소 지원 OS 버전에서 테스트했는가
- [ ] 서버 API 하위 호환이 유지되는가
- [ ] Feature Flag 로 끌 수 있는가
- [ ] 크래시 수집이 정상 동작하는가
""",
    },
    {
        "id": "DOC-163",
        "title": "앱 강제 업데이트 적용 기준",
        "department": "모바일개발팀",
        "type": "개발자 회의록",
        "project": None,
        "date": "2026-03-03",
        "systems": [],
        "features": [],
        "participants": ["한지민", "임채원"],
        "concepts": ["앱 배포", "정책", "하위 호환"],
        "body": """## 논의 배경

강제 업데이트를 언제 쓸지 기준이 없습니다. 지난달 주문서 빈 화면 이슈 때 강제 업데이트를 걸지 논의했는데 판단 근거가 없어 시간이 걸렸습니다.

## 논의 내용

한지민님이 정리한 강제 업데이트의 비용입니다.

- 업데이트 화면에서 이탈하는 사용자가 있습니다
- 네트워크가 느린 환경에서는 업데이트 자체가 부담입니다
- 스토어 접근이 어려운 사용자가 있습니다

임채원님은 반대로 구버전을 계속 지원하는 비용도 있다고 했습니다. 서버가 여러 버전을 동시에 지원하면 분기가 늘고 테스트가 어려워집니다.

## 결정 사항

강제 업데이트 기준을 세 가지로 정했습니다.

**1. 보안 결함**

즉시 강제 업데이트합니다. 논의 없이 진행합니다.

**2. 데이터 정합성에 영향을 주는 결함**

중복 결제, 잘못된 금액 표시처럼 데이터가 잘못되는 경우입니다. 리드 판단으로 강제 업데이트합니다.

**3. 서버 하위 호환 종료**

구버전 API 를 종료해야 할 때입니다. 최소 3개월 전 공지하고, 해당 버전 사용자 비중이 5% 미만이 된 뒤 적용합니다.

## 권장 업데이트

강제까지는 아니지만 안내가 필요한 경우 "권장 업데이트"를 씁니다. 앱 실행 시 안내를 띄우되 건너뛸 수 있습니다. 주 1회만 노출합니다.

주문서 빈 화면 같은 기능 결함은 여기에 해당합니다.

## 하위 호환 원칙

서버 API 는 최소 6개월 하위 호환을 유지합니다. 필드 추가는 자유롭게 하되, 필드 제거나 의미 변경은 새 버전으로 만듭니다.

## Action Item

- 한지민님: 권장 업데이트 노출 로직 구현
- 임채원님: API 버전 종료 공지 절차 정리
""",
    },
    {
        "id": "DOC-164",
        "title": "앱 버전 확인 API 명세",
        "department": "모바일개발팀",
        "type": "API 명세서",
        "project": None,
        "date": "2026-03-12",
        "systems": [],
        "features": [],
        "participants": [],
        "concepts": ["앱 배포", "버전 관리"],
        "body": """## Endpoint

```http
GET /app/version?platform=ANDROID&currentVersion=3.14.2
```

앱 실행 시 호출합니다. 인증이 필요 없습니다.

## Response

```json
{
  "latestVersion": "3.16.0",
  "minimumVersion": "3.10.0",
  "updateType": "RECOMMENDED",
  "message": "주문서 화면 오류가 수정되었습니다.",
  "storeUrl": "https://play.example.com/store/apps/details?id=com.nexbridge.app"
}
```

## updateType

| 값 | 동작 |
| --- | --- |
| NONE | 최신 버전. 아무것도 하지 않음 |
| RECOMMENDED | 안내 표시. 건너뛰기 가능. 주 1회 노출 |
| FORCED | 업데이트 화면 표시. 건너뛸 수 없음 |

`currentVersion` 이 `minimumVersion` 미만이면 `FORCED` 입니다.

## 판정 규칙

```text
current < minimum          -> FORCED
current < latest           -> RECOMMENDED
current >= latest          -> NONE
```

버전 비교는 semver 규칙을 따릅니다. `3.9.0` 과 `3.10.0` 을 문자열로 비교하면 안 됩니다.

## 운영

`minimumVersion` 과 `latestVersion` 은 Feature Flag 관리 화면에서 변경합니다. 배포 없이 즉시 반영됩니다.

강제 업데이트는 영향이 크므로 변경 시 승인이 필요합니다.

## 주의사항

이 API 가 실패하면 앱은 정상 진행해야 합니다. 버전 확인이 안 된다고 앱을 막으면 API 장애가 전체 서비스 중단이 됩니다.

응답을 캐시하지 마세요. 긴급 상황에서 즉시 반영되어야 합니다. 대신 앱 실행 시 1회만 호출합니다.
""",
    },
    {
        "id": "DOC-165",
        "title": "2026 상반기 기술 부채 정리 현황",
        "department": "백엔드개발팀",
        "type": "프로젝트 진행상황",
        "project": None,
        "date": "2026-06-05",
        "systems": ["NEXUS", "ORBIT", "MARS", "BlueGate"],
        "features": [],
        "participants": [],
        "concepts": ["기술 부채", "우선순위"],
        "body": """## 배경

프로젝트 사이사이에 미뤄둔 항목을 정리합니다. 분기마다 한 번씩 현황을 남깁니다.

## 완료

| 항목 | 출처 |
| --- | --- |
| 재고 조건부 갱신 적용 | 초과 판매 이슈 |
| 쿠폰 사용 조건부 갱신 | 쿠폰 중복 사용 이슈 |
| Graceful shutdown | 배포 중 요청 유실 |
| 데드락 락 순서 통일 | 주문 저장 데드락 |
| 공통 예외 구조 (NEXUS) | 코드리뷰 |

## 진행 중

| 항목 | 상태 |
| --- | --- |
| 공통 예외 구조 (ORBIT, MARS) | ORBIT 60% |
| 정합성 대조 배치 주기 단축 (일 1회 → 6시간) | 설계 완료, 미배포 |
| 감사 이력 마스킹 화이트리스트 전환 | 착수 전 |

## 미착수

| 항목 | 출처 | 비고 |
| --- | --- | --- |
| 주문 목록 조회 성능 (v2 미적용 경로) | 연말 부하 이슈 | Admin 쪽은 완료, 사용자 앱 일부 남음 |
| 오래된 Feature Flag 제거 | Feature Flag 매뉴얼 | 4개 |
| 임시 권한 위임 | Atlas 회고 | 온콜 공용 계정 대체 |
| 서비스 간 의존 관계 문서화 | 서비스 간 통신 회의 | 관리 방법 미정 |
| 트랜잭션 내 테이블 락 순서 문서 | 데드락 이슈 | |
| 부분 취소 미지원 결제 수단 안내 | Falcon 회고 | 프론트 |

## 판단

미착수 항목 중 임시 권한 위임이 가장 시급합니다. 공용 계정을 쓰는 상태가 두 달째입니다.

Feature Flag 제거는 위험이 낮고 작업량도 적습니다. 다음 스프린트 여유 시간에 처리합니다.

서비스 간 의존 관계 문서화는 방법이 정해지지 않아 계속 밀립니다. 문서로 관리하면 갱신이 안 되고, 코드에서 추출하려면 도구가 필요합니다. 결론을 내지 못하면 계속 남을 항목입니다.

## 다음

7월 중 임시 권한 위임 설계를 시작합니다. 나머지는 분기말에 다시 점검합니다.
""",
    },
]
