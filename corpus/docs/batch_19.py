"""DOC-271 ~ DOC-285 : 판매자 연동, 정산, 클라이언트 품질 (2026)."""

DOCUMENTS = [
    {
        "id": "DOC-271",
        "title": "판매자 시스템 연동 표준화 논의",
        "department": "백엔드개발팀",
        "type": "개발자 회의록",
        "project": None,
        "date": "2026-01-27",
        "systems": ["ORBIT"],
        "features": ["주문", "주문 취소"],
        "participants": ["노태윤", "임채원"],
        "concepts": ["외부 연동", "표준화", "기술적 제약"],
        "body": """## 논의 배경

주문 취소와 배송지 변경에서 계속 "연동된 판매자만 가능"이라는 제약이 나옵니다. 연동 방식이 판매자마다 달라 생기는 문제입니다.

## 현황

노태윤님이 조사한 연동 형태입니다.

| 방식 | 판매자 수 | 주문 비중 |
| --- | --- | --- |
| API 연동 | 42 | 71% |
| 파일 전송 (일 1회) | 88 | 21% |
| 수동 (관리자 화면) | 214 | 8% |

주문 건수 기준으로는 API 가 대부분이지만 판매자 수로는 소수입니다.

## 문제

**파일 전송**: 하루 한 번이라 실시간 취소 요청이 불가능합니다.

**수동**: 판매자가 화면에 들어와야 주문을 봅니다. 취소 요청을 인지하는 데 시간이 걸립니다.

**API 연동도 제각각**: 42곳이 각자 다른 스펙을 씁니다. 우리가 판매자마다 다른 어댑터를 만들고 있습니다.

## 검토안

**1. 표준 API 정의 후 전환 요구**

깔끔하지만 판매자가 개발을 해야 합니다. 소규모 판매자는 어렵습니다.

**2. 어댑터 계층 정리**

우리 쪽에서 판매자별 차이를 흡수합니다. 판매자 부담이 없지만 우리 코드가 계속 늘어납니다.

**3. 표준 API + 대체 수단**

표준을 정의하되, 못 하는 판매자는 관리자 화면을 개선해 대응합니다.

## 결정 사항

**3안으로 갑니다.**

- 표준 연동 API 를 정의하고 문서를 공개합니다
- 신규 판매자는 표준 API 만 지원합니다
- 기존 API 연동 판매자는 1년 안에 전환을 권고합니다
- 파일/수동 판매자는 관리자 화면을 개선해 대응 속도를 높입니다

## 관리자 화면 개선

수동 판매자가 취소 요청을 빨리 인지하도록 알림을 보냅니다. 이메일과 관리자 화면 배지입니다.

임채원님은 알림을 보내도 확인하지 않으면 소용없다고 했습니다. 일정 시간 무응답이면 자동 수락으로 처리하는 방안을 검토합니다.

## 미확정

자동 수락 여부와 대기 시간을 정하지 못했습니다. 판매자 동의가 필요한 사안이라 사업팀 확인이 필요합니다.

## Action Item

- 노태윤님: 표준 연동 API 설계
- 임채원님: 자동 수락 정책 사업팀 협의
""",
    },
    {
        "id": "DOC-272",
        "title": "판매자 주문 전송 API 명세",
        "department": "백엔드개발팀",
        "type": "API 명세서",
        "project": None,
        "date": "2026-02-09",
        "systems": ["ORBIT"],
        "features": ["주문"],
        "participants": [],
        "concepts": ["외부 연동", "표준화", "Webhook"],
        "body": """## 개요

판매자 시스템이 우리 주문을 받아가는 표준 방식입니다. 조회 방식과 수신 방식 두 가지를 제공합니다.

## 방식 1: 조회 (Polling)

```http
GET /partner/orders?status=NEW&from=2026-02-09T00:00:00%2B09:00
Authorization: Bearer {partnerToken}
```

```json
{
  "orders": [
    {
      "orderNumber": "ORD-2026-0209-0031",
      "orderedAt": "2026-02-09T10:22:00+09:00",
      "items": [
        { "itemNumber": "ITEM-551201", "productCode": "SELLER-SKU-01", "quantity": 2 }
      ],
      "receiver": {
        "name": "홍길동",
        "phone": "010-****-1234",
        "address": "서울시 강남구 ..."
      },
      "status": "NEW"
    }
  ],
  "nextCursor": "eyJpZCI6OTAxMTJ9"
}
```

연락처는 마스킹해서 전달합니다. 배송에 필요한 전체 번호는 송장 등록 시 별도 API 로 조회합니다.

## 방식 2: 수신 (Webhook)

판매자가 엔드포인트를 등록하면 주문 발생 시 전송합니다.

```http
POST {판매자 엔드포인트}
X-Signature: {HMAC-SHA256}
```

응답으로 2xx 를 받지 못하면 재시도합니다.

```text
1차: 즉시
2차: 1분 후
3차: 5분 후
4차: 30분 후
이후: 조회 방식으로 대체
```

## 상태 전달

판매자가 처리 결과를 알려줍니다.

```http
POST /partner/orders/{orderNumber}/status
```

```json
{
  "status": "PREPARING",
  "estimatedShipDate": "2026-02-10"
}
```

허용 상태: `PREPARING`, `SHIPPED`, `REJECTED`

## 취소 요청 수신

우리가 취소를 요청하면 판매자가 수락 또는 거절합니다.

```http
POST /partner/orders/{orderNumber}/cancel-request/respond
```

```json
{
  "accepted": true,
  "reason": null
}
```

응답 기한은 2시간입니다. 기한 내 응답이 없으면 정책에 따라 처리됩니다.

## 인증

파트너 토큰을 발급합니다. 판매자별로 다르며 유효기간은 1년입니다.

Webhook 서명은 HMAC-SHA256 이며 비밀키는 판매자별로 발급합니다.

## Error Code

| 코드 | HTTP | 설명 |
| --- | --- | --- |
| PARTNER_1001 | 401 | 토큰 오류 |
| PARTNER_1002 | 403 | 다른 판매자의 주문 |
| PARTNER_1003 | 409 | 허용되지 않는 상태 전이 |
| PARTNER_1004 | 410 | 응답 기한 경과 |
""",
    },
    {
        "id": "DOC-273",
        "title": "seller / seller_integration 테이블 설계",
        "department": "백엔드개발팀",
        "type": "DB 테이블 설계안",
        "project": None,
        "date": "2026-02-23",
        "systems": ["ORBIT"],
        "features": ["주문"],
        "participants": [],
        "concepts": ["외부 연동", "설정", "확장성"],
        "body": """## 배경

판매자별 연동 방식이 코드에 하드코딩되어 있습니다. 새 판매자가 들어올 때마다 배포가 필요합니다.

## seller

| 컬럼 | 타입 | 설명 |
| --- | --- | --- |
| id | BIGINT | PK |
| code | VARCHAR(32) | 판매자 식별 코드, UNIQUE |
| name | VARCHAR(100) | |
| status | VARCHAR(16) | ACTIVE / SUSPENDED / TERMINATED |
| settlement_cycle | VARCHAR(16) | WEEKLY / BIWEEKLY / MONTHLY |
| commission_rate | DECIMAL(5,4) | 수수료율 |
| created_at | DATETIME | |

## seller_integration

| 컬럼 | 타입 | 설명 |
| --- | --- | --- |
| seller_id | BIGINT | PK, FK |
| method | VARCHAR(16) | API_POLL / API_WEBHOOK / FILE / MANUAL |
| webhook_url | VARCHAR(500) | WEBHOOK 인 경우 |
| webhook_secret_encrypted | VARBINARY(256) | 서명 키 (암호화) |
| supports_cancel | BOOLEAN | 취소 요청 수신 가능 |
| supports_address_change | BOOLEAN | 배송지 변경 수신 가능 |
| cancel_response_hours | INT | 응답 기한 (기본 2) |
| legacy_adapter | VARCHAR(32) | 구 방식 어댑터 식별자 |

```sql
CREATE UNIQUE INDEX uk_seller_code ON seller (code);
CREATE INDEX idx_integration_method ON seller_integration (method);
```

## supports_* 컬럼

주문 취소나 배송지 변경 화면에서 이 값을 확인해 가능 여부를 미리 표시합니다.

지금은 코드에서 판매자 ID 를 하드코딩해 판단하고 있었습니다. 설정으로 옮깁니다.

## legacy_adapter

기존 API 연동 판매자 42곳은 각자 다른 스펙을 씁니다. 어댑터 식별자를 두고 코드에서 대응하는 구현을 찾습니다.

표준 API 로 전환하면 이 값을 비우고 `method` 만 씁니다. 전환 진행 상황을 이 컬럼으로 파악할 수 있습니다.

## 비밀키 암호화

Webhook 서명 키를 평문으로 두지 않습니다. 마스터 키로 암호화합니다. 서명 키 관리에서 쓴 방식과 같습니다.

## 판매자 상태

`SUSPENDED` 는 일시 중지입니다. 신규 주문을 보내지 않지만 기존 주문 처리는 계속합니다.

`TERMINATED` 는 계약 종료입니다. 주문 전송을 중단하고 정산만 남습니다. 행은 삭제하지 않습니다. 과거 주문이 참조합니다.

## 조회 캐시

주문 처리마다 판매자 설정을 읽습니다. 자주 바뀌지 않으므로 10분 캐시합니다.

연동 중단 같은 긴급 상황에서는 즉시 반영이 필요하므로 관리 화면에서 변경 시 캐시를 무효화합니다.
""",
    },
    {
        "id": "DOC-274",
        "title": "[리뷰] 판매자 연동 어댑터 구조",
        "department": "백엔드개발팀",
        "type": "코드리뷰",
        "project": None,
        "date": "2026-03-09",
        "systems": ["ORBIT"],
        "features": ["주문"],
        "participants": ["노태윤", "김도윤"],
        "concepts": ["외부 연동", "인터페이스", "재시도"],
        "body": """## 현재 구현

판매자별 연동을 어댑터로 분리했습니다.

```java
public interface SellerAdapter {
    String adapterId();
    void sendOrder(Order order, SellerIntegration config);
    CancelResponse requestCancel(Order order, SellerIntegration config);
}
```

## 리뷰 의견

김도윤님: `sendOrder` 가 void 입니다. 전송 실패를 어떻게 아나요.

노태윤님: 예외를 던집니다.

김도윤님: 예외로 흐름을 제어하면 재시도 대상인지 구분이 어렵습니다. 결과 객체를 반환하고 재시도 가능 여부를 담는 편이 낫습니다. 서비스 간 호출 기준 문서에서도 재시도 가능/불가능을 구분하기로 했습니다.

노태윤님: `SendResult` 를 반환하도록 바꾸겠습니다.

김도윤님: `MANUAL` 방식 판매자는 어댑터가 뭘 하나요.

노태윤님: 아무것도 하지 않고 주문 상태만 대기로 둡니다.

김도윤님: 그러면 인터페이스에 맞지 않는 구현이 생깁니다. `MANUAL` 은 어댑터를 두지 않고 서비스 계층에서 분기하는 게 명확할 것 같습니다.

노태윤님: 동의합니다. 어댑터를 찾지 못하면 수동 처리 대기로 두겠습니다.

김도윤님: Webhook 서명 키를 복호화해서 어댑터에 넘기는데, 설정 객체에 평문 키가 담깁니다. 로그로 찍히면 노출됩니다.

노태윤님: 설정 객체의 `toString` 에서 키를 제외하겠습니다.

김도윤님: 어댑터 구현이 42개가 되면 관리가 어렵습니다. 공통 부분을 추상 클래스로 빼는 게 좋겠습니다. HTTP 호출, 재시도, 로깅은 공통입니다.

## 수정 사항

- `SendResult` 반환으로 변경, 재시도 가능 여부 포함
- `MANUAL` 을 어댑터에서 제외하고 서비스 분기
- 설정 객체 `toString` 에서 비밀키 제외
- 공통 HTTP 어댑터 추상 클래스 도입

## 남은 확인

기존 42개 연동을 새 구조로 옮기는 작업이 큽니다. 표준 API 전환을 유도하면서 점진적으로 줄이는 편이 낫습니다.

당장 전부 옮기지 않고, 손댈 일이 있을 때 함께 전환하기로 했습니다.
""",
    },
    {
        "id": "DOC-275",
        "title": "판매자 정산 주기 조정 논의",
        "department": "서비스기획팀",
        "type": "기획자와의 회의록",
        "project": None,
        "date": "2026-03-23",
        "systems": ["ORBIT"],
        "features": ["환불"],
        "participants": ["강민재", "배현우"],
        "concepts": ["정산", "정책", "집계"],
        "body": """## 논의 배경

정산 주기가 판매자마다 다릅니다. 주간, 격주, 월간이 섞여 있고 기준일도 제각각입니다.

## 현황

배현우님이 정리한 분포입니다.

| 주기 | 판매자 수 | 기준 |
| --- | --- | --- |
| 주간 | 18 | 배송 완료일 |
| 격주 | 64 | 배송 완료일 |
| 월간 | 262 | 주문일 |

기준이 다른 것이 더 큰 문제입니다. 월간 판매자는 주문일 기준이라 배송이 다음 달로 넘어가면 정산과 실제 매출이 어긋납니다.

## 쟁점

**기준일 통일**

배현우님은 배송 완료일 기준이 맞다고 봤습니다. 취소와 반품이 대부분 배송 전후에 발생하므로, 배송 완료 후에 정산해야 조정이 줄어듭니다.

강민재님은 판매자가 정산을 늦게 받게 된다고 우려했습니다. 주문일 기준이면 더 빨리 받습니다.

**반품 기간**

배송 완료 후 7일간 반품이 가능합니다. 정산을 배송 완료 즉시 하면 반품 시 회수해야 합니다.

## 결정 사항

- 기준일을 **배송 완료일**로 통일합니다
- 배송 완료 후 **7일이 지난 건**만 정산 대상으로 삼습니다
- 주기는 판매자 계약에 따라 유지하되 기준일만 통일합니다

7일을 기다리면 반품 대부분이 정리되어 사후 조정이 줄어듭니다.

## 사후 조정

7일 이후 반품이나 환불이 발생하면 다음 정산에서 차감합니다. 차감액이 정산액을 넘으면 마이너스 정산이 되는데, 이 경우 판매자에게 청구합니다.

강민재님은 마이너스 정산이 분쟁 소지가 있다고 했습니다. 실제 발생 빈도를 확인한 뒤 처리 방식을 정하기로 했습니다.

## 미확정

마이너스 정산 처리 방식이 미확정입니다. 다음 정산에서 상계할지, 별도 청구할지 정하지 않았습니다.

기존 판매자의 기준일 변경 시 이행 방법도 정해야 합니다. 전환 시점에 정산이 겹치거나 빠지는 구간이 생깁니다.

## Action Item

- 배현우님: 마이너스 정산 발생 빈도 확인
- 강민재님: 기준일 변경 안내 및 이행 방안
""",
    },
    {
        "id": "DOC-276",
        "title": "정산 내역 조회 API 명세",
        "department": "백엔드개발팀",
        "type": "API 명세서",
        "project": None,
        "date": "2026-04-06",
        "systems": ["ORBIT"],
        "features": [],
        "participants": [],
        "concepts": ["정산", "권한", "집계"],
        "body": """## Endpoint

```http
GET /partner/settlements?from=2026-03-01&to=2026-03-31
Authorization: Bearer {partnerToken}
```

판매자가 자기 정산 내역을 조회합니다. 운영자는 `/admin/settlements` 를 사용합니다.

## Response

```json
{
  "period": { "from": "2026-03-01", "to": "2026-03-31" },
  "settlements": [
    {
      "settlementId": 8812,
      "cycleStart": "2026-03-01",
      "cycleEnd": "2026-03-15",
      "status": "COMPLETED",
      "salesAmount": 12400000,
      "commissionAmount": 1240000,
      "adjustmentAmount": -320000,
      "settlementAmount": 10840000,
      "scheduledAt": "2026-03-20",
      "completedAt": "2026-03-20T14:02:00+09:00"
    }
  ]
}
```

| 항목 | 설명 |
| --- | --- |
| salesAmount | 정산 대상 매출 (배송 완료 + 7일 경과) |
| commissionAmount | 수수료 |
| adjustmentAmount | 사후 조정 (반품, 환불). 음수 |
| settlementAmount | 실지급액 |

## 상세 조회

```http
GET /partner/settlements/{settlementId}/details
```

정산에 포함된 주문 목록을 반환합니다. 주문별 금액과 수수료를 확인할 수 있습니다.

조정 내역도 함께 반환합니다. 어느 주문의 반품으로 얼마가 차감됐는지 알 수 있습니다.

## 상태

| 상태 | 의미 |
| --- | --- |
| PENDING | 집계 중 |
| CONFIRMED | 금액 확정, 지급 대기 |
| COMPLETED | 지급 완료 |
| HOLD | 보류 (분쟁, 확인 필요) |

## 정산 기준

- 대상: 배송 완료 후 7일이 지난 주문
- 제외: 취소, 반품 완료 건
- 조정: 7일 이후 발생한 반품/환불은 다음 정산에서 차감

## Error Code

| 코드 | HTTP | 설명 |
| --- | --- | --- |
| SETTLE_1001 | 400 | 조회 기간 오류 (최대 1년) |
| SETTLE_1002 | 403 | 다른 판매자의 정산 |
| SETTLE_1003 | 404 | 정산 내역 없음 |

## 주의사항

`PENDING` 상태의 금액은 확정되지 않았습니다. 집계가 끝나면 바뀔 수 있으므로 참고용으로만 사용하세요.

조정액이 커서 `settlementAmount` 가 음수가 되는 경우가 있습니다. 이때 처리 방식은 계약에 따릅니다.
""",
    },
    {
        "id": "DOC-277",
        "title": "판매자 연동 확대 현황",
        "department": "프로젝트관리팀",
        "type": "프로젝트 진행상황",
        "project": None,
        "date": "2026-05-04",
        "systems": ["ORBIT"],
        "features": ["주문", "주문 취소"],
        "participants": [],
        "concepts": ["외부 연동", "일정", "Risk"],
        "body": """## 범위

- 표준 연동 API 정의 및 공개
- 판매자 설정 테이블화
- 어댑터 구조 정리
- 수동 판매자 대응 개선

## 완료

- 표준 연동 API 문서 공개
- `seller` / `seller_integration` 테이블
- 어댑터 인터페이스 정리
- 관리자 화면 취소 요청 알림

## 진행 중

- 신규 판매자 표준 API 온보딩 (3곳)
- 기존 API 판매자 전환 안내

## 대기

- 자동 수락 정책 (사업팀 협의 중)
- 파일 전송 판매자 대응

## 전환 현황

| 방식 | 2월 | 5월 |
| --- | --- | --- |
| 표준 API | 0 | 7 |
| 구 API (어댑터) | 42 | 38 |
| 파일 전송 | 88 | 88 |
| 수동 | 214 | 217 |

표준 API 로 7곳이 전환했습니다. 신규 3곳과 기존 4곳입니다.

목표는 1년 안에 구 API 42곳을 모두 전환하는 것인데, 3개월에 4곳이면 속도가 부족합니다.

## Risk

판매자가 전환할 동기가 약합니다. 지금도 동작하는데 개발 비용을 들일 이유가 없습니다.

전환을 유도할 방법이 필요합니다. 표준 API 에서만 제공하는 기능(취소 요청 수신, 수령지 변경)을 강조하고 있지만 충분하지 않습니다.

자동 수락 정책이 두 달째 협의 중입니다. 판매자 계약 조건과 연결되어 사업팀 판단이 필요합니다.

## 취소 요청 개선 효과

수동 판매자에게 알림을 보낸 뒤 평균 응답 시간이 줄었습니다.

| 항목 | 이전 | 이후 |
| --- | --- | --- |
| 평균 응답 시간 | 9.2시간 | 4.1시간 |
| 24시간 무응답 비율 | 31% | 12% |

여전히 12% 는 하루가 지나도 응답이 없습니다. 자동 수락이 필요한 이유입니다.

## 다음

- 자동 수락 정책 확정 요청
- 전환 유도 방안 사업팀과 논의
""",
    },
    {
        "id": "DOC-278",
        "title": "판매자 문의 대응 가이드",
        "department": "고객지원팀",
        "type": "매뉴얼 / 정의서",
        "project": None,
        "date": "2026-05-18",
        "systems": ["ORBIT", "AIMS"],
        "features": ["주문"],
        "participants": [],
        "concepts": ["운영", "판매자", "정산"],
        "body": """## 문의 유형

판매자 문의는 고객 문의와 창구가 다릅니다. 판매자 지원 채널로 들어옵니다.

| 유형 | 비중 |
| --- | --- |
| 정산 관련 | 38% |
| 주문 전송 누락 | 24% |
| 취소/반품 처리 | 19% |
| 계정/권한 | 12% |
| 기타 | 7% |

## 정산 문의

**"금액이 예상과 다릅니다"**

1. AIMS 정산 상세에서 대상 주문 목록 확인
2. 조정 내역(반품, 환불) 확인
3. 수수료율이 계약과 맞는지 확인

가장 흔한 원인은 조정액입니다. 이전 정산 이후 발생한 반품이 차감된 경우입니다.

**"정산이 안 됐습니다"**

- 배송 완료 후 7일이 지났는지 확인
- 정산 상태가 `HOLD` 인지 확인
- `HOLD` 면 사유를 확인하고 담당자에게 전달

## 주문 전송 누락

**"주문이 안 들어왔습니다"**

1. AIMS 에서 해당 주문의 판매자 전송 이력 확인
2. 전송 실패면 재시도 이력과 오류 확인
3. Webhook 방식이면 판매자 엔드포인트 응답 확인

판매자 서버가 응답하지 않아 실패한 경우가 많습니다. 4회 재시도 후 조회 방식으로 전환되므로 판매자가 직접 조회하도록 안내합니다.

## 취소 요청

**"취소 요청을 못 봤습니다"**

- 알림 발송 이력 확인
- 수동 판매자는 관리자 화면에서 확인해야 함을 안내
- 응답 기한(2시간)이 지난 건은 정책에 따라 처리됨을 설명

## 계정 문의

판매자 계정 권한 변경은 사업팀 승인이 필요합니다. 고객지원팀에서 직접 처리하지 않습니다.

파트너 토큰 재발급은 요청받아 개발팀에 전달합니다.

## 에스컬레이션

- 정산 금액 이의 제기: 재무팀
- 계약 조건 관련: 사업팀
- 연동 오류: 개발팀
- 반복 문의(같은 판매자 주 3회 이상): 사업팀 공유

## 주의

판매자에게 다른 판매자 정보를 노출하지 마세요. 수수료율은 계약마다 다르며 대외비입니다.
""",
    },
    {
        "id": "DOC-279",
        "title": "앱 초기 실행 속도 개선 방향",
        "department": "모바일개발팀",
        "type": "개발자 회의록",
        "project": None,
        "date": "2026-04-06",
        "systems": [],
        "features": [],
        "participants": ["한지민", "서지호"],
        "concepts": ["성능", "앱", "측정"],
        "body": """## 논의 배경

앱 실행 후 첫 화면이 보이기까지 평균 2.8초입니다. 경쟁 서비스 대비 느리다는 지적이 있었습니다.

## 측정 결과

한지민님이 구간별로 측정했습니다.

| 구간 | 소요 |
| --- | --- |
| 앱 프로세스 시작 | 0.4초 |
| 초기화 (SDK, 설정) | 0.9초 |
| 토큰 확인 및 갱신 | 0.6초 |
| 홈 데이터 조회 | 0.7초 |
| 렌더링 | 0.2초 |

초기화와 토큰 확인이 절반을 차지합니다.

## 초기화

SDK 초기화가 순차적으로 일어납니다. 분석, 크래시 수집, 푸시, 결제 SDK 를 차례로 초기화합니다.

이 중 첫 화면에 필요한 것은 없습니다. 크래시 수집만 먼저 하고 나머지는 화면 표시 후로 미룰 수 있습니다.

## 토큰 확인

앱 실행 시 토큰 유효성을 확인하고 만료됐으면 갱신합니다. 이 과정이 끝나야 홈 데이터를 조회합니다.

서지호님이 물었습니다. 홈 화면이 로그인 없이도 보이는 화면인데 왜 토큰을 먼저 확인하나요.

한지민님 답은 개인화 데이터(최근 본 상품 등) 때문입니다. 다만 개인화 없이 먼저 보여주고 나중에 채우는 방식이 가능합니다.

## 결정 사항

- 크래시 수집 외 SDK 초기화를 화면 표시 후로 지연
- 홈 데이터를 토큰 확인과 병렬로 조회
- 개인화 영역은 토큰 확인 후 채움
- 목표: 첫 화면 1.5초

## 측정 체계

개선 효과를 알려면 지속적인 측정이 필요합니다. 지금은 수동으로 측정했습니다.

앱에서 구간별 시간을 수집해 지표로 보내기로 했습니다. 기기 성능 편차가 크므로 기기 등급별로 나눠 봅니다.

## 서지호님 의견

저사양 기기에서 더 느립니다. 테스트 기기가 모두 최신 기종이라 실제 사용자 경험과 다를 수 있습니다.

QA 테스트 기기에 저사양 모델을 추가하기로 했습니다.

## Action Item

- 한지민님: 초기화 지연 및 병렬 조회
- 서지호님: 저사양 기기 확보 및 테스트 기준 정리
""",
    },
    {
        "id": "DOC-280",
        "title": "[리뷰] 앱 이미지 캐시 정책",
        "department": "모바일개발팀",
        "type": "코드리뷰",
        "project": None,
        "date": "2026-04-21",
        "systems": [],
        "features": ["상품"],
        "participants": ["한지민", "최유진"],
        "concepts": ["캐시", "성능", "앱"],
        "body": """## 현재 구현

이미지 캐시 정책을 조정했습니다. 메모리와 디스크 캐시 크기, 만료 기간을 설정합니다.

```text
메모리 캐시: 앱 가용 메모리의 25%
디스크 캐시: 200MB
만료: 7일
```

## 리뷰 의견

최유진님: 디스크 200MB 는 큽니다. 저장 공간이 부족한 기기에서 문제가 될 수 있습니다. 사용자가 "앱이 용량을 많이 차지한다"고 느낍니다.

한지민님: 상품 이미지가 많아서 크게 잡았습니다. 줄이면 캐시 미스가 늘어납니다.

최유진님: 캐시 히트율을 측정해 봤나요. 200MB 를 다 쓰는지, 100MB 로도 충분한지 데이터가 필요합니다.

한지민님: 측정하지 않았습니다. 100MB 로 낮추고 히트율을 보겠습니다.

최유진님: 메모리 25% 도 큽니다. 이미지 캐시가 메모리를 많이 쓰면 다른 작업에서 부족해집니다. 저사양 기기에서 앱이 종료될 수 있습니다.

한지민님: 15% 로 낮추겠습니다.

최유진님: 만료 7일이면 상품 이미지가 바뀌어도 일주일간 옛 이미지가 보입니다. 판매자가 이미지를 교체하는 경우가 있습니다.

한지민님: 이미지 URL 에 버전이 포함되어 있어서 교체하면 URL 이 바뀝니다. 옛 이미지가 보이지 않습니다.

최유진님: 그러면 만료를 더 길게 잡아도 되겠네요. URL 이 바뀌면 자연히 새로 받으니까요. 다만 옛 URL 캐시가 계속 남아 용량을 차지합니다. LRU 로 밀려나긴 하겠지만요.

## 수정 사항

- 디스크 캐시 100MB 로 축소
- 메모리 캐시 15% 로 축소
- 만료 30일로 연장 (URL 버전 관리에 의존)
- 캐시 히트율 지표 수집 추가

## 남은 확인

히트율 데이터를 2주간 모은 뒤 크기를 재조정합니다. 100MB 로 히트율이 크게 떨어지면 다시 늘립니다.

설정에서 캐시를 지울 수 있는 메뉴가 있는지 확인이 필요합니다. 없으면 추가하는 편이 좋겠습니다.
""",
    },
    {
        "id": "DOC-281",
        "title": "앱 초기 설정 조회 API 명세",
        "department": "모바일개발팀",
        "type": "API 명세서",
        "project": None,
        "date": "2026-05-15",
        "systems": [],
        "features": [],
        "participants": [],
        "concepts": ["앱", "설정", "성능"],
        "body": """## 배경

앱 실행 시 여러 API 를 호출해 설정을 가져왔습니다. 버전 확인, Feature Flag, 공지, 배너를 각각 호출했습니다.

하나로 합칩니다.

## Endpoint

```http
GET /app/bootstrap?platform=ANDROID&version=3.16.0
```

인증이 필요 없습니다. 로그인 전에 호출합니다.

## Response

```json
{
  "version": {
    "latestVersion": "3.16.0",
    "minimumVersion": "3.10.0",
    "updateType": "NONE"
  },
  "features": {
    "guestOrderEnabled": true,
    "easyPayBEnabled": true,
    "reviewRewardEnabled": true
  },
  "notice": {
    "id": 412,
    "type": "MAINTENANCE",
    "title": "5월 20일 새벽 점검 안내",
    "displayUntil": "2026-05-20T06:00:00+09:00"
  },
  "config": {
    "imageBaseUrl": "https://cdn.example.com",
    "supportUrl": "https://help.example.com"
  },
  "serverTime": "2026-05-15T10:22:31+09:00"
}
```

## 캐시

응답을 5분 캐시합니다. 앱은 실행 시 1회 호출하며 이후에는 캐시된 값을 씁니다.

긴급 상황(강제 업데이트, 점검 공지)에서는 즉시 반영이 필요합니다. 이때는 캐시를 무효화합니다.

## serverTime

앱이 서버 시각을 알 수 있게 합니다. 기기 시각이 틀린 경우 만료 판정이 어긋나는 문제가 있었습니다.

앱은 서버 시각과 기기 시각의 차이를 저장하고 시각 계산에 반영합니다.

## 실패 시 동작

**이 API 가 실패해도 앱은 정상 동작해야 합니다.**

- 버전 확인 실패: 업데이트 안내를 표시하지 않고 진행
- Feature Flag 실패: 기본값(모두 비활성) 사용
- 공지 실패: 표시하지 않음

기본값은 "기존 동작"입니다. 새 기능이 기본으로 켜지지 않습니다.

## Error Code

오류 응답은 정의하지 않습니다. 실패 시 앱이 기본값으로 진행하므로 오류 코드를 구분할 필요가 없습니다.

다만 서버 오류는 로그로 남깁니다. 이 API 실패율이 높으면 다른 문제의 신호입니다.
""",
    },
    {
        "id": "DOC-282",
        "title": "앱 크래시 분석 방법",
        "department": "모바일개발팀",
        "type": "매뉴얼 / 정의서",
        "project": None,
        "date": "2026-06-05",
        "systems": [],
        "features": [],
        "participants": [],
        "concepts": ["앱", "크래시", "디버깅"],
        "body": """## 어디서 보나

크래시 수집 도구 대시보드에서 확인합니다. 플랫폼별로 분리되어 있습니다.

## 지표 기준

| 지표 | 정상 범위 |
| --- | --- |
| 크래시 없는 세션 비율 | 99.5% 이상 |
| 크래시 없는 사용자 비율 | 99.0% 이상 |

이 아래로 떨어지면 원인 파악이 필요합니다. 릴리스 직후에는 일시적으로 낮아질 수 있습니다.

## 우선순위

크래시를 볼 때 순서입니다.

1. 영향받은 사용자 수가 많은 것
2. 최근 버전에서 새로 생긴 것
3. 결제나 주문 화면에서 발생한 것

발생 횟수보다 사용자 수가 중요합니다. 한 사용자가 100번 겪는 것과 100명이 한 번씩 겪는 것은 다릅니다.

## 분석 순서

**1. 스택 트레이스 확인**

우리 코드에서 발생했는지 라이브러리에서 발생했는지 봅니다. 라이브러리 내부라도 우리가 잘못 호출한 경우가 많습니다.

**2. 발생 조건 파악**

- 특정 OS 버전에만 발생하는가
- 특정 기기에만 발생하는가
- 특정 화면에서만 발생하는가
- 특정 앱 버전부터 발생했는가

**3. 재현**

조건을 좁힌 뒤 재현을 시도합니다. 재현되지 않으면 로그를 추가해 다음 릴리스에서 정보를 더 모읍니다.

## 자주 나오는 유형

| 유형 | 흔한 원인 |
| --- | --- |
| NullPointerException | 서버 응답의 선택 필드 처리 누락 |
| OutOfMemory | 큰 이미지 처리, 캐시 과다 |
| 파싱 오류 | 서버 응답 형식 변경, 구버전 라이브러리 |
| ANR | 메인 스레드에서 네트워크나 파일 작업 |

서버 응답의 선택 필드를 필수로 다루는 경우가 가장 많습니다. API 문서에서 nullable 인지 확인하세요.

## 로그 추가

재현이 안 되면 로그를 추가합니다. 다만 개인정보를 남기지 않도록 주의하세요. 크래시 리포트는 외부 서비스로 전송됩니다.

## 배포 후 확인

릴리스 후 24시간, 72시간 시점에 크래시 지표를 확인합니다. 급증하면 단계적 출시를 중단하거나 긴급 수정을 배포합니다.

## 기록

주요 크래시는 원인과 조치를 기록으로 남깁니다. 같은 유형이 반복되는지 확인할 수 있습니다.
""",
    },
    {
        "id": "DOC-283",
        "title": "접근성 개선 범위 논의",
        "department": "프론트엔드개발팀",
        "type": "개발자 회의록",
        "project": None,
        "date": "2026-06-19",
        "systems": [],
        "features": [],
        "participants": ["최유진", "서지호"],
        "concepts": ["접근성", "우선순위", "테스트"],
        "body": """## 논의 배경

접근성 관련 문의가 접수됐습니다. 스크린 리더로 주문이 불가능하다는 내용입니다.

## 현황 점검

최유진님이 주요 화면을 스크린 리더로 확인했습니다.

| 화면 | 상태 |
| --- | --- |
| 로그인 | 대체로 동작 |
| 상품 목록 | 이미지 대체 텍스트 없음 |
| 상품 상세 | 가격 정보를 읽지 못함 |
| 장바구니 | 수량 조절 버튼 인식 안 됨 |
| 주문서 | 폼 레이블 누락 다수 |
| 결제 | 진행 상태를 알 수 없음 |

주문 흐름 전체가 사실상 사용 불가입니다.

## 쟁점

서지호님은 전면 개선이 필요하다고 봤지만, 최유진님은 화면이 60개가 넘어 한 번에 하기 어렵다고 했습니다.

## 우선순위

핵심 흐름부터 개선합니다.

1. 상품 검색 및 목록
2. 상품 상세
3. 장바구니
4. 주문서
5. 결제

이 다섯 화면으로 구매가 가능해집니다. 나머지는 이후에 진행합니다.

## 개선 항목

**이미지**: 모든 이미지에 대체 텍스트를 넣습니다. 장식용 이미지는 빈 대체 텍스트로 표시해 읽지 않게 합니다.

**폼**: 모든 입력 요소에 레이블을 연결합니다. 오류 메시지를 입력 요소와 연결해 읽히게 합니다.

**버튼**: 아이콘만 있는 버튼에 레이블을 넣습니다. 공통 버튼 컴포넌트에서 이미 강제하고 있습니다.

**상태 변경**: 결제 진행 같은 상태 변화를 알립니다.

**포커스**: 모달이 열리면 포커스를 모달로 옮기고, 닫으면 원래 위치로 돌립니다.

## 테스트

서지호님이 QA 시나리오에 접근성 항목을 넣기로 했습니다. 스크린 리더로 주문 완료까지 가능한지 확인합니다.

자동 검사 도구도 도입합니다. 대체 텍스트 누락 같은 것은 자동으로 잡힙니다.

## 결정 사항

- 핵심 5개 화면을 우선 개선
- 공통 컴포넌트에 접근성 속성 포함
- QA 시나리오에 접근성 항목 추가
- 자동 검사 도구를 CI 에 추가

## Action Item

- 최유진님: 핵심 화면 개선 및 공통 컴포넌트 반영
- 서지호님: 접근성 QA 시나리오 작성
""",
    },
    {
        "id": "DOC-284",
        "title": "다크 모드 도입 검토",
        "department": "서비스기획팀",
        "type": "기획자와의 회의록",
        "project": None,
        "date": "2026-07-02",
        "systems": [],
        "features": [],
        "participants": ["송지은", "정하늘"],
        "concepts": ["UX", "우선순위", "작업량"],
        "body": """## 논의 배경

다크 모드 요청이 앱 리뷰에 꾸준히 있습니다. 도입을 검토합니다.

## 요청 현황

송지은님이 정리한 자료입니다. 최근 6개월 앱 리뷰 중 다크 모드 언급이 82건입니다. 기능 요청 중 3위입니다.

1위는 재입고 알림(이미 개발 중), 2위는 배송 조회 개선(완료), 3위가 다크 모드입니다.

## 작업량

정하늘님이 추정한 내용입니다.

색상이 코드에 직접 들어가 있습니다. 다크 모드를 하려면 먼저 색상을 토큰으로 빼야 합니다.

| 작업 | 예상 |
| --- | --- |
| 색상 토큰 정의 및 적용 | 3주 |
| 다크 모드 색상 설계 | 1주 (디자인) |
| 화면별 적용 및 검수 | 4주 |
| 이미지 자산 대응 | 1주 |

총 2개월 정도입니다.

## 쟁점

색상 토큰 작업은 다크 모드가 아니어도 필요합니다. 공통 컴포넌트 정리에서도 같은 요구가 나왔습니다.

정하늘님은 토큰 작업을 먼저 하고 다크 모드는 그 위에 얹는 순서를 제안했습니다. 토큰만 해도 화면 일관성이 좋아집니다.

송지은님은 사용자에게 보이는 성과가 없는 작업에 3주를 쓰는 것이 정당화되는지 물었습니다.

## 결정 사항

- **색상 토큰 정리를 먼저 진행합니다.** 다크 모드와 무관하게 필요한 작업입니다
- 다크 모드는 토큰 완료 후 재검토합니다
- 이번 분기에는 다크 모드를 약속하지 않습니다

## 근거

토큰 작업은 공통 컴포넌트, 접근성(명암비), 다크 모드에 모두 필요합니다. 세 가지가 같은 기반을 요구합니다.

다크 모드만 보고 판단하면 우선순위가 낮지만, 공통 기반으로 보면 할 이유가 충분합니다.

## 이미지 자산

다크 모드에서 로고나 아이콘이 배경과 섞이는 문제가 있습니다. 자산을 두 벌 준비해야 합니다.

디자인 리소스가 필요한데 현재 전담 인력이 없습니다. 이 부분이 실제 제약입니다.

## 미확정

다크 모드 도입 시점을 정하지 않았습니다. 토큰 작업 완료 후 디자인 리소스 확보 상황을 보고 판단합니다.
""",
    },
    {
        "id": "DOC-285",
        "title": "클라이언트 품질 개선 현황",
        "department": "프로젝트관리팀",
        "type": "프로젝트 진행상황",
        "project": None,
        "date": "2026-07-08",
        "systems": [],
        "features": [],
        "participants": [],
        "concepts": ["성능", "품질", "일정"],
        "body": """## 범위

- 앱 초기 실행 속도
- 이미지 캐시 정책
- 공통 컴포넌트
- 접근성

## 완료

- 앱 초기화 지연 및 병렬 조회
- 앱 초기 설정 API 통합
- 이미지 캐시 정책 조정
- 공통 버튼 컴포넌트

## 진행 중

- 색상 토큰 정의
- 접근성 핵심 화면 개선 (5개 중 2개)
- 공통 폼 입력 컴포넌트

## 대기

- 다크 모드 (토큰 완료 후 재검토)
- 나머지 화면 접근성

## 지표

**앱 초기 실행**

| 구간 | 개선 전 | 개선 후 |
| --- | --- | --- |
| 첫 화면 표시 | 2.8초 | 1.4초 |
| 완전 로딩 | 3.5초 | 2.1초 |

목표(1.5초)를 달성했습니다.

저사양 기기에서는 여전히 2.6초입니다. 개선 전 4.9초에서 줄었지만 최신 기기와 차이가 큽니다.

**이미지 캐시**

디스크 캐시를 200MB 에서 100MB 로 줄인 뒤 히트율을 측정했습니다.

| 크기 | 히트율 |
| --- | --- |
| 200MB | 87% |
| 100MB | 81% |

6%p 감소했지만 체감 차이는 크지 않습니다. 100MB 를 유지합니다.

**크래시**

| 시점 | 크래시 없는 세션 |
| --- | --- |
| 3월 | 99.2% |
| 7월 | 99.7% |

주문서 빈 화면 이슈 수정과 파싱 오류 처리 개선이 반영됐습니다.

## Risk

디자인 리소스가 없어 색상 토큰의 다크 모드 값을 정하지 못합니다. 라이트 모드 토큰만 먼저 정리하고 있습니다.

접근성 개선이 화면당 예상보다 오래 걸립니다. 기존 마크업 구조가 접근성을 고려하지 않아 손볼 곳이 많습니다.

## 다음

- 색상 토큰 라이트 모드 완료
- 접근성 나머지 3개 화면
- 저사양 기기 추가 개선 검토
""",
    },
]
