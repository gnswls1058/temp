"""DOC-286 ~ DOC-300 : 2026 하반기 - 임시 권한 위임, 알림 정책, 회고."""

DOCUMENTS = [
    {
        "id": "DOC-286",
        "title": "2026 하반기 로드맵 논의",
        "department": "프로젝트관리팀",
        "type": "기획자와의 회의록",
        "project": None,
        "date": "2026-07-10",
        "systems": ["NEXUS", "ORBIT", "MARS", "AIMS"],
        "features": [],
        "participants": ["강민재", "임채원"],
        "concepts": ["우선순위", "일정", "기술 부채"],
        "body": """## 논의 배경

상반기 프로젝트가 마무리되어 하반기 우선순위를 정합니다.

## 후보

기획 요구와 기술 부채를 함께 놓고 봅니다.

**기획 요구**

| 항목 | 요청 배경 |
| --- | --- |
| 정기 결제 / 정기 배송 | 신규 사업 |
| 선착순 쿠폰 확대 | 이벤트 효과 |
| 다크 모드 | 앱 리뷰 요청 |
| 통합 조회 화면 (AIMS) | 운영 효율 |
| 닉네임 도입 | 리뷰 표시 |

**기술 부채**

| 항목 | 배경 |
| --- | --- |
| 임시 권한 위임 | 온콜 공용 계정 대체 |
| 색상 토큰 / 공통 컴포넌트 | 여러 작업의 선행 조건 |
| 접근성 개선 | 문의 접수 |
| 검색 엔진 도입 | 오타 보정 필요 |
| 서비스 간 의존 관계 관리 | 계속 미뤄짐 |

## 쟁점

강민재님은 정기 결제가 사업 일정과 묶여 있어 우선이라고 했습니다.

임채원님은 정기 결제 설계에 미확정 항목이 두 달째 남아 있다고 지적했습니다. 해지 시 환불 기준과 재고 확보 방식입니다. 이것부터 정해야 착수할 수 있습니다.

임시 권한 위임은 정보보안팀이 임시 조치로 받아들인 공용 계정이 넉 달째 유지되는 상황입니다. 더 미루기 어렵습니다.

## 결정 사항

**1순위 (7~8월)**

- 임시 권한 위임
- 정기 결제 미확정 항목 확정 및 착수
- 색상 토큰

**2순위 (9~10월)**

- 정기 결제 구현 완료
- 통합 조회 화면
- 접근성 나머지 화면

**3순위 (11~12월)**

- 검색 엔진 도입 검토
- 다크 모드
- 닉네임

## 하지 않기로 한 것

선착순 쿠폰 확대는 첫 이벤트 결과를 본 뒤 판단합니다. 아직 한 번도 운영하지 않았습니다.

서비스 간 의존 관계 관리는 방법이 정해지지 않아 계속 미뤄집니다. 이번에도 넣지 않고, 관리 방법을 먼저 정하는 것만 과제로 둡니다.

## Action Item

- 강민재님: 정기 결제 미확정 항목 회신 (7월 17일까지)
- 임채원님: 1순위 항목 일정 산정
""",
    },
    {
        "id": "DOC-287",
        "title": "임시 권한 위임 설계 검토",
        "department": "백엔드개발팀",
        "type": "개발자 회의록",
        "project": "Project Atlas",
        "date": "2026-07-13",
        "systems": ["AIMS"],
        "features": ["권한", "운영 관리"],
        "participants": ["노태윤", "조아름"],
        "concepts": ["권한", "만료", "감사 로그"],
        "body": """## 논의 배경

Atlas 회고에서 남은 과제입니다. 온콜 담당자가 개인정보 상세 조회를 하려면 공용 계정을 써야 합니다. 넉 달째 이 상태입니다.

## 요구사항

- 기간을 정해 권한을 빌려줍니다
- 기간이 지나면 자동 회수됩니다
- 누가 누구에게 무엇을 언제까지 줬는지 기록합니다

## 쟁점

**누가 위임할 수 있나**

조아름님은 아무나 위임하면 권한 통제가 무의미해진다고 했습니다. 위임할 수 있는 권한을 제한해야 합니다.

노태윤님은 자기가 가진 권한만 위임할 수 있게 하면 된다고 봤습니다. 없는 권한을 줄 수는 없습니다.

조아름님은 그것만으로는 부족하다고 했습니다. `CS_LEAD` 가 자기 권한을 모두에게 나눠주면 통제가 사라집니다. 위임 자체에 별도 권한을 두자고 제안했습니다.

**재위임**

위임받은 권한을 다시 위임할 수 있는지 정해야 합니다. 허용하면 추적이 어려워집니다.

## 결정 사항

- `permission:delegate` 권한이 있는 사람만 위임할 수 있습니다
- 자기가 실제로 보유한 권한만 위임할 수 있습니다
- **재위임은 금지합니다.** 위임받은 권한으로 다시 위임할 수 없습니다
- 최대 위임 기간은 7일입니다
- 위임과 회수 모두 감사 이력에 남습니다

## 만료 처리

만료 시각이 지나면 자동으로 무효가 됩니다. 배치로 정리하지 않고 권한 평가 시점에 만료 여부를 확인합니다.

배치에 의존하면 배치가 밀렸을 때 권한이 남습니다. 평가 시점 확인이 안전합니다.

## 온콜 적용

온콜 담당자에게 근무 기간 동안 위임합니다. 온콜 일정과 연동해 자동 위임하는 방안도 나왔지만, 자동화는 다음 단계로 미룹니다.

먼저 수동으로 운영하며 실제로 잘 동작하는지 확인합니다.

## Action Item

- 노태윤님: 설계 문서 작성
- 조아름님: 위임 가능 권한 목록 정리
""",
    },
    {
        "id": "DOC-288",
        "title": "임시 권한 위임 설계",
        "department": "백엔드개발팀",
        "type": "비즈니스 기능 설계",
        "project": "Project Atlas",
        "date": "2026-07-16",
        "systems": ["AIMS"],
        "features": ["권한", "운영 관리"],
        "participants": [],
        "concepts": ["권한", "만료", "Edge Case", "감사 로그"],
        "body": """## 배경

기간 한정으로 권한을 빌려주는 기능입니다. 온콜 대응, 담당자 부재 시 대행이 주 용도입니다.

## 모델

역할 위임이 아니라 **권한 단위 위임**입니다. 역할을 통째로 주면 필요 이상의 권한이 넘어갑니다.

```text
Delegation:
  from_admin_user_id   위임자
  to_admin_user_id     수임자
  permission           위임 권한 (단일)
  reason               사유 (필수)
  starts_at
  expires_at           최대 7일
  revoked_at           조기 회수
```

## 권한 평가

기존 평가에 위임을 더합니다.

```text
유효 권한 = 역할 기반 권한
          ∪ 유효한 위임 권한 (starts_at <= now < expires_at, revoked_at IS NULL)
```

위임은 항상 추가만 합니다. 위임으로 권한을 뺏을 수는 없습니다.

## 위임 조건

1. 위임자가 `permission:delegate` 권한 보유
2. 위임하려는 권한을 위임자가 **역할로** 보유 (위임받은 권한은 재위임 불가)
3. 기간이 7일 이내
4. 사유 10자 이상
5. 같은 사람에게 같은 권한이 이미 유효하면 거부

## Edge Case

- **위임자의 권한이 회수됨**: 위임은 유지됩니다. 위임 시점에 정당했기 때문입니다. 다만 감사 관점에서 이상 신호이므로 알림을 보냅니다.
- **위임자가 퇴사**: 계정이 `RETIRED` 가 되면 해당 위임을 모두 무효화합니다. 퇴사자가 준 권한이 남아 있으면 안 됩니다.
- **수임자가 정지됨**: 계정 상태가 우선입니다. 위임이 있어도 정지된 계정은 아무것도 할 수 없습니다.
- **만료 직전 작업 진행 중**: 승인 절차처럼 여러 단계가 있는 작업에서 중간에 만료될 수 있습니다. 각 단계마다 권한을 확인하므로 만료 후 단계는 거부됩니다. 작업이 중간에 멈추지만, 만료된 권한으로 계속하는 것보다 낫습니다.
- **시계 오차**: 인스턴스 간 시각이 다르면 만료 판정이 엇갈립니다. 유예를 두지 않고 서버 시각을 그대로 씁니다. 몇 초 차이는 문제되지 않습니다.
- **위임 중 역할 변경으로 수임자가 이미 권한을 갖게 됨**: 중복이지만 문제없습니다. 위임이 만료되어도 역할 권한이 남습니다.

## 캐시

권한 캐시가 1분입니다. 위임 회수가 최대 1분 늦게 반영됩니다.

긴급 회수 시 캐시를 즉시 무효화하는 경로를 둡니다. 계정 정지처럼 위임 회수도 즉시 반영이 필요할 수 있습니다.

## 감사

위임 생성, 회수, 만료를 모두 기록합니다. 위임된 권한으로 수행한 작업의 감사 로그에는 "위임 권한 사용"을 표시합니다.

나중에 "이 조치를 왜 이 사람이 했나"를 볼 때 위임 이력을 함께 확인할 수 있어야 합니다.

## 하지 않는 것

**자동 위임**: 온콜 일정과 연동한 자동 위임은 이번 범위가 아닙니다. 수동으로 운영하며 검증한 뒤 검토합니다.

**역할 위임**: 권한 단위만 지원합니다.
""",
    },
    {
        "id": "DOC-289",
        "title": "permission_delegation 테이블 설계",
        "department": "백엔드개발팀",
        "type": "DB 테이블 설계안",
        "project": "Project Atlas",
        "date": "2026-07-20",
        "systems": ["AIMS"],
        "features": ["권한"],
        "participants": [],
        "concepts": ["권한", "만료", "Index"],
        "body": """## 테이블

| 컬럼 | 타입 | 제약 | 설명 |
| --- | --- | --- | --- |
| id | BIGINT | PK | |
| from_admin_user_id | BIGINT | NOT NULL | 위임자 |
| to_admin_user_id | BIGINT | NOT NULL | 수임자 |
| permission | VARCHAR(64) | NOT NULL | 권한 문자열 |
| reason | VARCHAR(500) | NOT NULL | 사유 |
| starts_at | DATETIME | NOT NULL | |
| expires_at | DATETIME | NOT NULL | 최대 starts_at + 7일 |
| revoked_at | DATETIME | NULL | 조기 회수 |
| revoked_by | BIGINT | NULL | 회수자 |
| created_at | DATETIME | NOT NULL | |

```sql
CREATE INDEX idx_delegation_active
  ON permission_delegation (to_admin_user_id, expires_at, revoked_at);
CREATE INDEX idx_delegation_from
  ON permission_delegation (from_admin_user_id, created_at DESC);
```

## 중복 방지

같은 수임자에게 같은 권한이 이미 유효한 상태에서 다시 위임하는 것을 막습니다.

DB unique 로는 표현하기 어렵습니다. 기간이 겹치는지 확인해야 하기 때문입니다. 애플리케이션에서 검사합니다.

```sql
SELECT COUNT(*) FROM permission_delegation
WHERE to_admin_user_id = :to
  AND permission = :permission
  AND revoked_at IS NULL
  AND expires_at > NOW()
  AND starts_at < :newExpiresAt;
```

## 조회 쿼리

권한 평가에서 매 요청마다(캐시 미스 시) 실행됩니다.

```sql
SELECT permission FROM permission_delegation
WHERE to_admin_user_id = :userId
  AND revoked_at IS NULL
  AND starts_at <= NOW()
  AND expires_at > NOW();
```

`idx_delegation_active` 로 처리됩니다. 위임 건수가 많지 않아(동시 수십 건 수준) 부담이 없습니다.

## 만료 데이터

만료된 행을 삭제하지 않습니다. 감사 이력이므로 남깁니다.

권한 변경 이력과 같이 5년 보관합니다. `admin_role_history` 와 동일한 기준입니다.

행이 계속 쌓이지만 연간 수천 건 수준이라 문제되지 않습니다.

## 퇴사자 처리

위임자가 `RETIRED` 가 되면 해당 위임을 무효화합니다. `revoked_at` 을 채우고 `revoked_by` 에 시스템을 나타내는 값을 넣습니다.

계정 상태 변경 시 트리거되는 처리로 구현합니다.

## FK

`admin_user` 에 FK 를 걸지 않습니다. 퇴사자 계정을 삭제하지 않으므로 실제로는 문제가 없지만, 이력 테이블에는 FK 를 걸지 않는 기존 방침을 따릅니다.
""",
    },
    {
        "id": "DOC-290",
        "title": "[리뷰] 권한 위임 만료 판정",
        "department": "백엔드개발팀",
        "type": "코드리뷰",
        "project": "Project Atlas",
        "date": "2026-07-23",
        "systems": ["AIMS"],
        "features": ["권한"],
        "participants": ["노태윤", "김도윤"],
        "concepts": ["권한", "캐시", "만료"],
        "body": """## 현재 구현

권한 평가에서 역할 권한과 위임 권한을 합칩니다.

```java
public Set<String> effectivePermissions(long userId) {
    var rolePermissions = rolePermissionRepository.findByUserId(userId);
    var delegated = delegationRepository.findActive(userId, Instant.now());
    return Sets.union(rolePermissions, delegated);
}
```

이 결과를 1분 캐시합니다.

## 리뷰 의견

김도윤님: 캐시에 만료 시각 정보가 없습니다. 위임이 30초 뒤 만료되는데 지금 캐시하면 최대 1분 30초 동안 유효한 것으로 판정됩니다.

노태윤님: 캐시 TTL 을 가장 이른 만료 시각까지로 줄이겠습니다. 위임이 없으면 1분, 있으면 `min(1분, 가장 이른 만료까지)` 입니다.

김도윤님: 좋습니다. 다만 만료가 임박하면 TTL 이 1초가 되어 캐시 효과가 없어집니다. 그 정도는 감수해야겠네요.

김도윤님: 조기 회수는 어떻게 반영되나요.

노태윤님: 회수 시 해당 사용자의 캐시를 무효화합니다.

김도윤님: 무효화가 실패하면 최대 1분간 남습니다. 계정 정지처럼 즉시성이 필요한 경우는 캐시 없이 매번 확인하기로 했는데, 위임 회수도 같은 수준인가요.

노태윤님: 위임 회수는 계정 정지만큼 급하지 않다고 봅니다. 1분은 감수할 수 있습니다. 다만 무효화 실패를 로그로 남기겠습니다.

김도윤님: `findActive` 에 `Instant.now()` 를 넘기는데, 캐시된 결과를 쓸 때는 이 시각이 캐시 생성 시점입니다. 위 TTL 조정으로 해결되지만 의도가 드러나도록 주석을 남겨 주세요.

김도윤님: 위임 권한으로 수행한 작업을 감사 로그에서 구분한다고 했는데, 여기서는 합집합이라 출처를 알 수 없습니다.

노태윤님: 반환 타입을 바꿔 출처를 함께 담겠습니다. 감사 로그 기록 시 참조합니다.

## 수정 사항

- 캐시 TTL 을 가장 이른 만료 시각까지로 제한
- 무효화 실패 시 경고 로그
- 권한에 출처(ROLE / DELEGATION) 정보 포함
- 캐시와 시각 판정의 관계를 주석으로 명시

## 남은 확인

출처 정보를 담으면 반환 타입이 바뀌어 호출부를 수정해야 합니다. 호출부가 6곳입니다. 이번 PR 에서 함께 처리합니다.
""",
    },
    {
        "id": "DOC-291",
        "title": "권한 위임 API 명세",
        "department": "백엔드개발팀",
        "type": "API 명세서",
        "project": "Project Atlas",
        "date": "2026-07-27",
        "systems": ["AIMS"],
        "features": ["권한", "운영 관리"],
        "participants": [],
        "concepts": ["권한", "만료", "감사 로그"],
        "body": """## 위임 생성

```http
POST /admin/permission-delegations
Authorization: Bearer {adminToken}
```

필요 권한: `permission:delegate`

```json
{
  "toEmployeeNo": "E20391",
  "permission": "member:read:detail",
  "reason": "8월 1주차 온콜 대응",
  "startsAt": "2026-08-03T10:00:00+09:00",
  "expiresAt": "2026-08-10T10:00:00+09:00"
}
```

`startsAt` 을 생략하면 즉시 시작합니다.

## Response

```json
{
  "delegationId": 1204,
  "toEmployeeNo": "E20391",
  "permission": "member:read:detail",
  "startsAt": "2026-08-03T10:00:00+09:00",
  "expiresAt": "2026-08-10T10:00:00+09:00",
  "status": "SCHEDULED"
}
```

| 상태 | 의미 |
| --- | --- |
| SCHEDULED | 시작 전 |
| ACTIVE | 유효 |
| EXPIRED | 만료 |
| REVOKED | 회수됨 |

## 회수

```http
DELETE /admin/permission-delegations/{delegationId}
```

```json
{
  "reason": "온콜 교대 조기 종료"
}
```

위임자 본인 또는 `SYSTEM_ADMIN` 이 회수할 수 있습니다.

## 조회

```http
GET /admin/permission-delegations?to=E20391&status=ACTIVE
GET /admin/permission-delegations/me
```

`/me` 는 자신이 받은 위임과 자신이 준 위임을 함께 반환합니다.

## 제약

| 항목 | 값 |
| --- | --- |
| 최대 기간 | 7일 |
| 사유 최소 길이 | 10자 |
| 재위임 | 불가 |
| 동시 중복 위임 | 불가 |

위임할 수 있는 권한은 위임자가 **역할로** 보유한 것에 한합니다. 위임받은 권한은 다시 위임할 수 없습니다.

## Error Code

| 코드 | HTTP | 설명 |
| --- | --- | --- |
| DELEG_1001 | 403 | 위임 권한 없음 |
| DELEG_1002 | 403 | 위임하려는 권한을 보유하지 않음 |
| DELEG_1003 | 403 | 위임받은 권한은 재위임 불가 |
| DELEG_1004 | 400 | 기간이 7일 초과 |
| DELEG_1005 | 409 | 동일 권한이 이미 유효 |
| DELEG_1006 | 400 | 사유 미입력 또는 10자 미만 |
| DELEG_1007 | 404 | 대상 계정 없음 |

## 감사

생성, 회수, 만료가 모두 감사 로그에 남습니다. 위임 권한으로 수행한 작업에는 감사 로그에 위임 사용 표시가 붙습니다.

## 주의사항

위임은 최소 범위로 사용하세요. 권한 하나씩 위임하는 것이 원칙입니다. 여러 권한이 필요하면 여러 건으로 나눠 요청합니다.
""",
    },
    {
        "id": "DOC-292",
        "title": "위임 만료 후에도 권한이 유지되는 사례",
        "department": "백엔드개발팀",
        "type": "이슈 정리",
        "project": "Project Atlas",
        "date": "2026-07-30",
        "systems": ["AIMS"],
        "features": ["권한"],
        "participants": [],
        "concepts": ["권한", "캐시", "만료"],
        "body": """## 현상

스테이징 테스트 중 위임이 만료됐는데도 권한이 계속 유효한 경우가 확인됐습니다.

## 재현

1. 만료 시각을 1분 뒤로 설정해 위임
2. 만료 직전에 권한이 필요한 API 호출 (캐시 생성)
3. 만료 후 같은 API 호출
4. 여전히 성공

## 분석

캐시 TTL 을 "가장 이른 만료 시각까지"로 제한했는데, 이 계산이 위임이 있을 때만 동작합니다.

문제가 된 경로는 다른 곳이었습니다. 감사 이력에 권한 출처를 담기 위해 반환 타입을 바꿨는데, 캐시 키가 그대로였습니다.

이전 버전에서 캐시된 값(출처 정보 없는 형태)이 남아 있었고, 역직렬화에 실패하자 예외를 잡고 캐시를 무시한 채 **기본 허용**으로 동작했습니다.

```java
try {
    return cache.get(userId);
} catch (Exception e) {
    log.warn("캐시 조회 실패", e);
    return PermissionSet.allowAll();   // 문제
}
```

`allowAll()` 이 왜 들어갔는지 커밋 이력을 보니, 캐시 장애 시 운영이 멈추는 것을 막으려는 의도였습니다.

## 심각도

높음입니다. 캐시 문제로 권한 검사가 무력화됩니다. 만료된 위임뿐 아니라 모든 권한 검사가 통과합니다.

운영에는 배포 전이라 실제 영향은 없습니다.

## 조치

```java
} catch (Exception e) {
    log.error("캐시 조회 실패, DB 직접 조회", e);
    return permissionRepository.findEffective(userId);
}
```

캐시 실패 시 DB 를 직접 조회합니다. 그것도 실패하면 예외를 던져 요청을 거부합니다.

**권한 검사에서 실패는 거부입니다.** 가용성을 위해 허용하면 안 됩니다.

## 캐시 키

버전 정보를 캐시 키에 넣었습니다. 반환 타입이 바뀌면 키도 바뀌어 옛 캐시를 읽지 않습니다.

## 재발 방지

권한 관련 코드에서 "실패 시 허용"이 있는지 전수 확인했습니다. 한 곳 더 발견해 함께 수정했습니다.

Atlas 에서 "기본 거부" 원칙을 세웠는데, 예외 처리 경로에서 그 원칙이 깨져 있었습니다. 정상 경로만 보면 놓칩니다.

QA 시나리오에 "캐시 장애 시 권한 동작"을 추가했습니다.
""",
    },
    {
        "id": "DOC-293",
        "title": "Atlas 2차 진행 현황",
        "department": "프로젝트관리팀",
        "type": "프로젝트 진행상황",
        "project": "Project Atlas",
        "date": "2026-08-03",
        "systems": ["AIMS"],
        "features": ["권한", "운영 관리"],
        "participants": [],
        "concepts": ["일정", "권한", "Risk"],
        "body": """## 범위

- 임시 권한 위임
- 통합 조회 화면 (회원 상세 요약 영역)
- 감사 이력 마스킹 화이트리스트 전환

## 완료

- 권한 위임 설계 및 테이블
- 위임 API
- 캐시 만료 판정 이슈 대응

## 진행 중

- 위임 관리 화면
- QA (권한 시나리오 중심)

## 대기

- 통합 조회 화면
- 감사 이력 마스킹 전환

## 발견 이슈

7월 30일 캐시 예외 처리에서 권한 검사가 무력화되는 문제를 발견했습니다. 배포 전이라 영향은 없었습니다.

같은 패턴을 전수 확인해 한 곳을 더 수정했습니다. 심각도가 높아 QA 시나리오를 보강했습니다.

## Risk

권한 관련 변경은 잘못되면 영향이 큽니다. 위임 기능 자체보다 기존 권한 평가 로직을 건드리는 것이 위험합니다.

배포는 단계적으로 진행합니다.

```text
1주차: 위임 기능 배포하되 위임 권한을 아무에게도 부여하지 않음
2주차: SYSTEM_ADMIN 에게만 위임 권한 부여, 시범 운영
3주차: CS_LEAD 등으로 확대
```

1주차에는 기능이 코드에만 있고 실제로 쓰이지 않습니다. 권한 평가 로직 변경의 영향만 관찰합니다.

## 공용 계정 폐기

위임이 안정화되면 온콜 공용 계정을 폐기합니다. 넉 달간 유지된 임시 조치입니다.

폐기 시점은 3주차 이후로 잡았습니다. 정보보안팀과 협의했습니다.

## 다음

- 8월 중 위임 배포 (단계적)
- 9월 공용 계정 폐기
- 통합 조회 화면 착수
""",
    },
    {
        "id": "DOC-294",
        "title": "상품 문의 답변 목표 시간 조정",
        "department": "서비스기획팀",
        "type": "기획자와의 회의록",
        "project": None,
        "date": "2026-08-06",
        "systems": ["ORBIT"],
        "features": [],
        "participants": ["강민재", "문가영"],
        "concepts": ["SLA", "운영 효율", "정책"],
        "body": """## 논의 배경

문의 응답 목표가 유형과 무관하게 영업일 1일입니다. 실제로는 유형별로 처리 난이도가 크게 다릅니다.

## 현황

문가영님이 정리한 유형별 실제 처리 시간입니다.

| 유형 | 평균 | 목표 달성률 |
| --- | --- | --- |
| 배송 | 3.2시간 | 94% |
| 결제/환불 | 2.1시간 | 97% |
| 상품 문의 | 18.4시간 | 61% |
| 계정 | 4.8시간 | 89% |
| 포인트/쿠폰 | 5.2시간 | 87% |

상품 문의만 목표를 크게 벗어납니다.

## 원인

상품 문의는 판매자에게 확인해야 하는 경우가 많습니다. 재고, 입고 예정, 상품 사양은 우리가 답할 수 없습니다.

판매자 응답이 늦으면 문의도 늦어집니다. 우리가 통제할 수 없는 구간입니다.

## 검토안

**1. 상품 문의 목표를 2일로 연장**

현실을 반영하지만 사용자 경험이 나빠집니다.

**2. 판매자에게 직접 전달**

상품 문의를 판매자가 직접 답하게 합니다. 중간 단계가 사라집니다.

**3. 1차 답변 분리**

"확인 중입니다" 1차 답변을 자동으로 보내고, 최종 답변 목표를 따로 둡니다.

## 쟁점

강민재님은 2안이 근본적이라고 봤습니다. 다만 판매자 관리 화면에 문의 기능이 없고, 판매자가 답변 품질을 보장하지 않습니다.

문가영님은 판매자가 직접 답하면 부적절한 답변이 나갈 수 있다고 우려했습니다. 검수가 필요하면 결국 중간 단계가 남습니다.

## 결정 사항

- **상품 문의 목표를 영업일 2일로 조정**합니다
- 접수 시 "판매자 확인이 필요해 시간이 걸릴 수 있습니다" 안내를 표시합니다
- 판매자 직접 답변은 별도 과제로 검토합니다

## 판매자 직접 답변

장기적으로는 필요합니다. 다만 판매자 관리 화면 개발과 답변 검수 정책이 함께 필요해 범위가 큽니다.

판매자 연동 표준화와 함께 검토하기로 했습니다.

## 미확정

판매자 응답이 없을 때 어떻게 할지 정하지 않았습니다. 현재는 고객지원팀이 반복 요청하는데 한계가 있습니다.

일정 시간 무응답이면 "판매자 확인이 어렵습니다"로 답변하는 방안이 나왔지만, 사용자 입장에서 만족스럽지 않습니다.
""",
    },
    {
        "id": "DOC-295",
        "title": "신규 API 개발 체크리스트",
        "department": "백엔드개발팀",
        "type": "매뉴얼 / 정의서",
        "project": None,
        "date": "2026-08-10",
        "systems": ["NEXUS", "ORBIT", "MARS", "BlueGate", "AIMS"],
        "features": [],
        "participants": [],
        "concepts": ["API", "체크리스트", "설계"],
        "body": """## 설계 단계

- [ ] 이 기능이 기존 API 로 해결되지 않는가
- [ ] 조회인가 변경인가. HTTP 메서드가 의미와 맞는가
- [ ] 응답이 커질 수 있는가. 페이징이 필요한가
- [ ] 다른 서비스를 호출하는가. 실패 시 동작을 정의했는가
- [ ] 멱등해야 하는가. 그렇다면 중복 방지 키나 참조키가 있는가

변경 API 는 재시도 가능성을 항상 고려하세요. 네트워크는 언제든 끊깁니다.

## 권한

- [ ] 필요한 권한을 정의했는가
- [ ] 애너테이션을 붙였는가 (기본 거부이므로 누락 시 막힙니다)
- [ ] 본인 데이터만 접근 가능한가. 다른 사람 ID 로 호출하면 막히는가
- [ ] 서비스 토큰과 사용자 토큰 중 무엇을 받는가

## 검증

- [ ] 모든 입력을 서버에서 검증하는가
- [ ] 금액을 클라이언트 값으로 믿지 않는가
- [ ] 경계값(0, 최대, 초과)을 처리하는가
- [ ] 조회 조건 없는 전체 조회를 막았는가

## 개인정보

- [ ] 응답에 필요 이상의 개인정보가 없는가
- [ ] 권한에 따라 마스킹하는가
- [ ] 로그에 개인정보가 남지 않는가
- [ ] 감사 로그 대상인가

## 동시성

- [ ] 조회 후 판단하고 저장하는 패턴이 없는가
- [ ] 조건을 갱신 쿼리에 넣었는가
- [ ] 여러 자원을 잠근다면 순서가 일관된가

이 항목은 세 번 반복해서 사고가 났습니다. 재고, 쿠폰, 포인트에서 각각 발생했습니다.

## 오류

- [ ] 오류 코드를 정의했는가
- [ ] 정상 흐름인 4xx 가 있는가 (410, 404 등). 호출자에게 공유했는가
- [ ] 오류 응답에 내부 구조가 노출되지 않는가

## 문서

- [ ] 요청/응답 예시가 있는가
- [ ] 오류 코드 표가 있는가
- [ ] 제한(한도, 크기, 기간)을 명시했는가
- [ ] 주의사항을 적었는가

주의사항은 실제로 사고가 났거나 날 뻔한 것을 적으세요. 형식적인 문장은 아무도 읽지 않습니다.

## 배포

- [ ] 하위 호환을 유지하는가. 아니면 새 버전인가
- [ ] 소비자에게 공유했는가
- [ ] 스키마 변경이 선행 배포되었는가
- [ ] Feature Flag 로 끌 수 있는가

## 테스트

- [ ] 금액 계산, 상태 전이, 경계값 테스트가 있는가
- [ ] 권한별 접근 테스트가 있는가
- [ ] 동시 요청 시나리오를 확인했는가
""",
    },
    {
        "id": "DOC-296",
        "title": "알림 야간 발송 차단 설계",
        "department": "백엔드개발팀",
        "type": "비즈니스 기능 설계",
        "project": None,
        "date": "2026-08-13",
        "systems": ["NEXUS"],
        "features": [],
        "participants": [],
        "concepts": ["알림", "정책", "Edge Case"],
        "body": """## 배경

배송 출발 알림이 새벽에 발송되는 경우가 있습니다. 택배사 처리 시각을 따라가기 때문입니다.

야간 발송을 제한합니다.

## 차단 시간

기본 21:00 ~ 08:00 입니다. 사용자가 설정에서 끌 수 있습니다.

## 분류별 적용

| 분류 | 야간 차단 |
| --- | --- |
| 필수 고지 | 적용하지 않음 |
| 거래 알림 | 적용 |
| 마케팅 | 적용 |

필수 고지는 즉시성이 중요합니다. 결제 실패나 보안 관련 알림을 아침까지 미룰 수 없습니다.

## 처리 방식

차단 시간에 발생한 알림은 버리지 않고 **다음 발송 가능 시각으로 예약**합니다.

```text
02:30 배송 출발 알림 발생
  -> 08:00 발송 예약
08:00 발송
```

버리면 사용자가 알림을 못 받습니다. 미루는 것이 맞습니다.

## Edge Case

- **예약 대기 중 상태 변경**: 배송 출발 알림이 대기 중인데 이미 배송 완료가 되면 발송할 의미가 없습니다. 발송 직전에 유효성을 확인합니다.
- **같은 알림이 여러 건 대기**: 밤사이 배송 상태가 여러 번 바뀌면 알림이 쌓입니다. 같은 유형은 마지막 것만 발송합니다.
- **차단 시각 경계**: 20:59 에 발생한 알림은 즉시 발송됩니다. 발송 처리가 지연되어 21:00 을 넘겨도 이미 발송 결정이 났으므로 진행합니다.
- **사용자가 야간 차단을 끔**: 대기 중인 알림을 즉시 발송할지 다음 시각까지 기다릴지 정해야 합니다. 즉시 발송합니다. 사용자가 끈 시점에 받고 싶다는 의사를 표시한 것입니다.
- **시간대**: 회원의 시간대 정보가 없습니다. 모두 한국 시각 기준으로 처리합니다. 해외 사용자는 고려하지 않습니다.

## 예약 저장

`notification` 테이블에 `scheduled_at` 을 추가합니다. `status` 에 `SCHEDULED` 를 추가합니다.

발송 배치가 1분마다 `scheduled_at <= now` 이고 `SCHEDULED` 인 건을 처리합니다.

## 중복 억제

같은 유형의 대기 건이 여러 개면 마지막만 남깁니다. 새 알림이 예약될 때 같은 `(member_id, template_code)` 의 기존 `SCHEDULED` 건을 `SKIPPED` 로 바꿉니다.

`skip_reason` 은 `SUPERSEDED` 입니다.

## 고민

08:00 에 알림이 몰립니다. 밤사이 쌓인 것이 한 번에 나갑니다.

발송량이 많으면 08:00~08:30 에 분산합니다. 사용자별로 임의 지연을 두는 방식입니다.

첫 운영에서 실제 발송량을 보고 분산 여부를 정합니다.
""",
    },
    {
        "id": "DOC-297",
        "title": "알림 설정 조회 및 변경 API 명세",
        "department": "백엔드개발팀",
        "type": "API 명세서",
        "project": None,
        "date": "2026-08-17",
        "systems": ["NEXUS"],
        "features": [],
        "participants": [],
        "concepts": ["알림", "수신 동의", "설정"],
        "body": """## 조회

```http
GET /members/me/notification-settings
Authorization: Bearer {accessToken}
```

```json
{
  "categories": [
    {
      "code": "REQUIRED_NOTICE",
      "name": "필수 안내",
      "editable": false,
      "push": true,
      "email": true
    },
    {
      "code": "TRANSACTION",
      "name": "주문/배송",
      "editable": true,
      "push": true,
      "email": true
    },
    {
      "code": "REWARD",
      "name": "포인트/쿠폰",
      "editable": true,
      "push": true,
      "email": false
    },
    {
      "code": "MARKETING",
      "name": "이벤트/혜택",
      "editable": true,
      "push": false,
      "email": false
    }
  ],
  "quietHours": {
    "enabled": true,
    "startHour": 21,
    "endHour": 8
  },
  "devicePushPermission": "GRANTED"
}
```

`devicePushPermission` 은 클라이언트가 OS 권한 상태를 함께 조회해 채웁니다. 서버는 `UNKNOWN` 을 반환하며 클라이언트가 덮어씁니다.

## 변경

```http
PATCH /members/me/notification-settings
```

```json
{
  "categories": [
    { "code": "MARKETING", "push": true, "email": true }
  ],
  "quietHours": { "enabled": true, "startHour": 22, "endHour": 7 }
}
```

바꾸려는 항목만 보냅니다.

`editable: false` 인 분류를 변경하려 하면 거부합니다.

## 마케팅 동의

`MARKETING` 을 켜면 수신 동의로 기록됩니다. 동의 시각을 별도로 남깁니다. 법적으로 동의 이력이 필요합니다.

끄면 철회로 기록됩니다.

## 야간 차단

`quietHours` 는 필수 고지에 적용되지 않습니다. 설정 화면에 이 점을 표시하세요.

시작과 종료 시각은 정시 단위입니다. 분 단위는 지원하지 않습니다.

## Error Code

| 코드 | HTTP | 설명 |
| --- | --- | --- |
| NOTI_1001 | 400 | 알 수 없는 분류 코드 |
| NOTI_1002 | 403 | 변경할 수 없는 분류 |
| NOTI_1003 | 400 | 시각 범위 오류 |

## 주의사항

설정을 껐다고 이미 예약된 알림이 취소되지는 않습니다. 발송 직전 설정을 다시 확인하므로 대부분 반영되지만, 발송 처리 중인 건은 나갈 수 있습니다.
""",
    },
    {
        "id": "DOC-298",
        "title": "야간 차단 적용 후 결제 실패 알림 미도달",
        "department": "백엔드개발팀",
        "type": "이슈 정리",
        "project": None,
        "date": "2026-08-21",
        "systems": ["NEXUS", "ORBIT"],
        "features": ["결제"],
        "participants": [],
        "concepts": ["알림", "분류", "정책"],
        "body": """## 현상

야간 차단을 적용한 뒤 결제 실패 알림이 다음 날 아침에 도착합니다. 사용자가 밤에 결제하다 실패했는데 알림을 8시에 받습니다.

## 분석

설계상 필수 고지는 야간 차단 대상이 아닙니다. 그런데 결제 실패 알림의 분류가 `TRANSACTION` 으로 등록되어 있었습니다.

알림 채널 정책 문서를 보니 결제 실패는 "필수 고지"로 분류되어 있습니다. 문서와 실제 등록이 다릅니다.

템플릿 등록 시점에 분류를 잘못 지정한 것으로 보입니다.

## 영향 범위

8월 18~20일 야간 발생 결제 실패 알림 41건이 지연됐습니다.

지연으로 인한 직접적인 피해는 확인되지 않았습니다. 결제 실패 시 화면에서 즉시 안내되므로 알림은 보조 수단입니다.

## 조치

**즉시**: 결제 실패 알림의 분류를 `REQUIRED_NOTICE` 로 변경했습니다. 설정 변경이라 배포가 필요 없습니다.

**전수 확인**: 등록된 템플릿 34개의 분류를 정책 문서와 대조했습니다. 3건이 더 어긋나 있었습니다.

| 템플릿 | 등록 분류 | 정책 분류 |
| --- | --- | --- |
| 결제 실패 | TRANSACTION | REQUIRED_NOTICE |
| 비밀번호 변경 완료 | TRANSACTION | REQUIRED_NOTICE |
| 휴면 전환 예정 | MARKETING | REQUIRED_NOTICE |
| 쿠폰 만료 임박 | REQUIRED_NOTICE | REWARD |

휴면 전환 예정이 `MARKETING` 이었습니다. 마케팅 수신을 거부한 사용자는 휴면 안내를 받지 못했다는 뜻입니다.

작년에 같은 문제를 한 번 겪었습니다. 그때는 수신 동의 필터가 원인이었고, 분류를 추가해 해결했습니다. 그런데 분류만 만들고 기존 템플릿을 옮기지 않았습니다.

## 근본 문제

템플릿 분류가 자유롭게 지정 가능하고 검증이 없습니다. 정책 문서와 실제 설정이 어긋나도 알 수 없습니다.

## 조치

- 4건 분류 수정
- 템플릿 등록/수정 시 분류를 정책 목록에서만 선택하도록 제한
- 정책 문서와 등록 현황을 비교하는 점검을 분기 1회 수행

## 배운 점

"정책은 문서에 있는데 구현이 없다"는 패턴이 반복됩니다. 개인정보 보관 기간 점검에서도 같은 문제가 있었습니다.

문서만 만들고 대조하지 않으면 시간이 지나면서 어긋납니다. 대조하는 절차나 자동 검증이 함께 있어야 합니다.
""",
    },
    {
        "id": "DOC-299",
        "title": "2026 하반기 기술 부채 점검",
        "department": "백엔드개발팀",
        "type": "개발자 회의록",
        "project": None,
        "date": "2026-08-25",
        "systems": ["NEXUS", "ORBIT", "MARS", "BlueGate", "AIMS"],
        "features": [],
        "participants": ["김도윤", "노태윤", "박준호"],
        "concepts": ["기술 부채", "우선순위", "반복 문제"],
        "body": """## 논의 배경

분기 점검입니다. 상반기 목록에서 무엇이 해결됐고 무엇이 남았는지 봅니다.

## 해결된 것

| 항목 | 비고 |
| --- | --- |
| 임시 권한 위임 | 8월 배포, 공용 계정 9월 폐기 예정 |
| 오래된 Feature Flag 제거 | 4개 정리 |
| 공통 예외 구조 | NEXUS, ORBIT 완료. MARS 진행 중 |
| 대사 배치 주기 단축 | 6시간으로 변경 |
| 트랜잭션 락 순서 문서 | 작성 완료 |

## 남은 것

| 항목 | 상태 | 미해결 기간 |
| --- | --- | --- |
| 서비스 간 의존 관계 관리 | 방법 미정 | 8개월 |
| 부분 취소 미지원 수단 안내 | 프론트 미착수 | 5개월 |
| 감사 이력 마스킹 화이트리스트 | 미착수 | 5개월 |
| 주문 목록 조회 성능 (앱 일부) | 미착수 | 8개월 |
| 검색 엔진 도입 | 검토 중 | 3개월 |

## 반복되는 문제

박준호님이 지적한 패턴입니다. 올해 발생한 사고를 유형별로 보면 반복이 있습니다.

**1. 조회 후 판단하고 저장** — 재고, 쿠폰, 포인트, 결제에서 각각 발생

체크리스트에 넣었고 이후 신규 코드에서는 발생하지 않았습니다. 다만 기존 코드에 남아 있는 것을 전수 확인하지는 않았습니다.

**2. 정책은 있는데 구현이 없음** — 개인정보 보관, 알림 분류

문서와 구현을 대조하는 절차가 없습니다. 8월 알림 분류 이슈가 최근 사례입니다.

**3. 예외 처리 경로에서 원칙이 깨짐** — 권한 캐시 실패 시 허용, 트랜잭션 REQUIRES_NEW

정상 경로만 리뷰하고 예외 경로를 대충 보는 경향이 있습니다.

## 논의

김도윤님은 2번이 가장 위험하다고 봤습니다. 아무도 모르는 채로 오래 지속됩니다.

노태윤님은 자동 검증이 가능한 것부터 하자고 제안했습니다. 알림 분류처럼 목록으로 관리되는 것은 대조 스크립트를 만들 수 있습니다.

## 결정 사항

- 정책-구현 대조 항목을 목록화하고 가능한 것은 자동 검증을 추가합니다
- 예외 처리 경로를 리뷰 체크리스트에 명시합니다
- "조회 후 판단" 패턴을 기존 코드에서 전수 확인합니다 (9월 중)

## 우선순위

남은 부채 중 감사 이력 마스킹을 우선합니다. 보안 관련이고 미해결 기간이 깁니다.

서비스 간 의존 관계는 이번에도 방법이 정해지지 않았습니다. 8개월째입니다. 완벽한 방법을 찾기보다 불완전해도 시작하기로 했습니다. API 문서에 소비자 목록을 수동으로 적는 것부터 합니다.
""",
    },
    {
        "id": "DOC-300",
        "title": "2026 상반기 전체 회고",
        "department": "프로젝트관리팀",
        "type": "프로젝트 진행상황",
        "project": None,
        "date": "2026-07-15",
        "systems": ["NEXUS", "ORBIT", "MARS", "BlueGate", "AIMS"],
        "features": [],
        "participants": [],
        "concepts": ["회고", "일정", "품질"],
        "body": """## 상반기 완료 프로젝트

| 프로젝트 | 배포 | 결과 |
| --- | --- | --- |
| Falcon (결제 고도화) | 3월 16일 | 중복 승인 0건, 부분 취소 지원 |
| Atlas (Admin 개편) | 6월 23일 | ADMIN 계정 118 → 2, 감사 이력 도입 |
| 검색 개선 1차 | 5월 20일 | 결과 0건 비율 18% → 7% |
| 상품 상세 개편 | 5월 26일 | 첫 화면 3.2초 → 0.9초 |
| 결제 수단 확대 | 5월 25일 | 간편결제 B 추가 |

## 지표 요약

| 지표 | 1월 | 6월 |
| --- | --- | --- |
| 결제 성공률 | 96.2% | 97.3% |
| 앱 크래시 없는 세션 | 99.2% | 99.7% |
| 주문 전환율 | 11.2% | 15.1% |
| CS 문의 (주문 대비) | 4.8% | 3.1% |

## 잘된 점

**설계 문서를 먼저 쓰는 습관이 자리잡았습니다.** Nova 에서 구조를 갈아엎은 경험 이후 동시성이 얽힌 기능은 설계를 먼저 공유합니다. Falcon 과 Atlas 모두 설계 단계에서 문제를 발견했습니다.

**정합성 대조 배치가 여러 번 사고를 조기에 잡았습니다.** 리워드 불일치 12건, 등급 산정 누락 3만 건이 모두 정합성 대조나 검증으로 발견됐습니다.

**이중 방어가 실제로 작동했습니다.** Falcon 에서 중복 방지 키와 주문 단위 차단을 둘 다 넣었는데, 클라이언트가 중복 방지 키를 유실하는 상황이 실제로 나왔고 서버 차단이 잡았습니다.

## 아쉬운 점

**미확정 항목이 프로젝트를 넘어 다닙니다.** 부분 취소 정책은 Nova 에서 Falcon 으로 넘어가 4개월이 걸렸습니다. 정기 결제 미확정 항목도 두 달째입니다.

미확정으로 넘길 때 기한을 함께 정하지 않는 것이 문제입니다.

**같은 유형의 결함이 반복됩니다.** 조회 후 판단하고 저장하는 패턴이 네 번 나왔습니다. 체크리스트에 넣었지만 기존 코드는 그대로입니다.

**정책과 구현이 어긋난 채 방치됩니다.** 개인정보 보관 기간, 알림 분류에서 발견됐습니다. 문서만 만들고 대조하지 않았습니다.

**조직 정보가 필요한 작업이 항상 늦습니다.** Atlas 역할 매핑이 두 스프린트 연속 Blocker 였습니다. 개발 일정과 별개로 일찍 시작해야 합니다.

## 하반기 방향

- 미확정 항목에 기한을 함께 정합니다
- 정책-구현 대조를 정기 점검에 넣습니다
- 반복 결함 유형을 기존 코드에서 전수 확인합니다

## 남은 큰 과제

- 정기 결제 (사업 일정 연동)
- 검색 엔진 도입 여부
- 서비스 경계 재정리 (당장은 분리하지 않되 내부 정리)
""",
    },
]
