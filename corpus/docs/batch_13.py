"""DOC-181 ~ DOC-195 : 코드리뷰와 배송 도메인 (2025~2026)."""

DOCUMENTS = [
    {
        "id": "DOC-181",
        "title": "[리뷰] 회원 프로필 캐시 무효화 추가",
        "department": "백엔드개발팀",
        "type": "코드리뷰",
        "project": None,
        "date": "2025-10-16",
        "systems": ["NEXUS"],
        "features": ["프로필"],
        "participants": ["이서연", "김도윤"],
        "concepts": ["캐시", "정합성", "이벤트"],
        "body": """## 현재 구현

프로필 수정 시 캐시를 삭제합니다.

```java
@Transactional
public void updateProfile(long memberId, UpdateProfileCommand command) {
    var member = memberRepository.findById(memberId);
    member.update(command);
    cacheManager.getCache("member:profile").evict(memberId);
}
```

## 리뷰 의견

김도윤님: 캐시 삭제가 트랜잭션 안에 있습니다. 커밋 전에 캐시가 지워지고, 그 사이 다른 요청이 조회하면 아직 커밋되지 않은 DB 값을 읽어 다시 캐시합니다. 결과적으로 옛 값이 캐시에 남습니다.

이서연님: 커밋 후로 옮기겠습니다. `TransactionSynchronization` 을 쓰면 됩니다.

김도윤님: 커밋 후 삭제가 실패하면 어떻게 되나요. 트랜잭션은 이미 끝났으니 롤백할 수 없습니다.

이서연님: TTL 이 10분이라 최대 10분간 옛 값이 보입니다. 완전히 막을 수는 없습니다.

김도윤님: 그 정도면 받아들일 수 있습니다. 다만 삭제 실패를 로그로 남겨서 빈도를 알 수 있게 해주세요.

김도윤님: 프로필을 수정하는 경로가 여기 하나인가요. 관리자 화면에서도 수정할 수 있는데 그쪽에도 무효화가 있나요.

이서연님: 확인해 보니 없습니다. AIMS 에서 수정하면 캐시가 남습니다. 같이 넣겠습니다.

## 수정 사항

- 캐시 삭제를 커밋 후로 이동
- 삭제 실패 시 경고 로그
- AIMS 수정 경로에도 무효화 추가

## 남은 확인

캐시 무효화를 각 수정 경로에 넣는 방식은 누락되기 쉽습니다. 도메인 이벤트를 발행하고 이벤트 구독자가 캐시를 지우는 구조가 낫습니다.

이번 PR 범위를 넘어서 별도 과제로 등록했습니다. 지금은 경로가 두 개뿐이라 직접 호출로 둡니다.
""",
    },
    {
        "id": "DOC-182",
        "title": "[리뷰] 주문 목록 조회 쿼리 분리",
        "department": "백엔드개발팀",
        "type": "코드리뷰",
        "project": None,
        "date": "2026-01-23",
        "systems": ["ORBIT"],
        "features": ["주문"],
        "participants": ["노태윤", "배현우"],
        "concepts": ["Query 최적화", "N+1", "Index"],
        "body": """## 현재 구현

목록에서 조인을 제거하고 대표 상품 정보를 주문 테이블에서 읽도록 바꿨습니다.

```sql
SELECT id, status, representative_product_name, thumbnail_url,
       total_amount, ordered_at
FROM orders
WHERE member_id = :memberId
ORDER BY ordered_at DESC
LIMIT :size OFFSET :offset;
```

## 리뷰 의견

배현우님: 인덱스가 `(member_id, ordered_at DESC)` 인지 확인했나요. 실행 계획을 보여주세요.

노태윤님: 첨부했습니다. 인덱스를 타고 정렬도 인덱스로 해결됩니다.

배현우님: OFFSET 페이징은 뒤쪽 페이지에서 느려집니다. 주문이 많은 회원이 100페이지를 보면 앞의 2,000건을 건너뛰어야 합니다.

노태윤님: 실제 사용 패턴을 보니 3페이지 이상 보는 비율이 0.4% 입니다. 커서 페이징으로 바꾸면 API 스펙이 달라져서 클라이언트 작업이 생깁니다.

배현우님: 지금은 괜찮지만 상한을 두는 게 좋겠습니다. `page` 가 100 을 넘으면 거부하거나 커서 방식으로 유도하세요.

노태윤님: 상한 100 을 두겠습니다.

배현우님: `representative_product_name` 을 주문 생성 시 복사하는데, 상품이 한 건일 때와 여러 건일 때 형식이 다릅니다. "무선 이어폰 외 2건" 형태 생성 로직이 어디 있나요.

노태윤님: 주문 생성 서비스에 있습니다.

배현우님: 그러면 상품명이 긴 경우 컬럼 길이를 넘을 수 있습니다. 자르는 처리가 있나요.

노태윤님: 없습니다. 200자로 자르고 말줄임표를 붙이겠습니다.

## 수정 사항

- `page` 상한 100
- 대표 상품명 200자 절단 처리
- 실행 계획 첨부

## 남은 확인

커서 페이징 전환은 별도 과제입니다. 상한에 걸리는 요청이 실제로 발생하는지 지표로 확인한 뒤 판단합니다.
""",
    },
    {
        "id": "DOC-183",
        "title": "테스트 코드 작성 기준 논의",
        "department": "백엔드개발팀",
        "type": "개발자 회의록",
        "project": None,
        "date": "2025-07-15",
        "systems": [],
        "features": [],
        "participants": ["김도윤", "이서연", "윤수빈"],
        "concepts": ["테스트", "팀 규칙", "커버리지"],
        "body": """## 논의 배경

테스트 작성 기준이 사람마다 다릅니다. 어떤 PR 은 테스트가 촘촘하고 어떤 PR 은 없습니다. 기준을 정합니다.

## 현황

이서연님이 조사한 결과 전체 커버리지는 54% 입니다. 도메인 계층은 71%, 서비스 계층은 48%, 컨트롤러는 22% 입니다.

## 쟁점

**커버리지 목표를 둘 것인가**

김도윤님은 숫자 목표를 반대했습니다. 목표를 정하면 의미 없는 테스트로 숫자를 채우게 됩니다. getter 테스트 같은 것들입니다.

이서연님은 기준이 없으면 아무도 안 쓴다고 했습니다.

**무엇을 반드시 테스트할 것인가**

윤수빈님이 QA 관점에서 의견을 냈습니다. QA 에서 반복적으로 발견되는 결함 유형을 보면 금액 계산, 상태 전이, 경계값이 대부분입니다. 이 영역만 확실히 테스트해도 QA 부담이 크게 줄어듭니다.

## 결정 사항

커버리지 숫자 목표는 두지 않습니다. 대신 다음은 반드시 테스트를 작성합니다.

- 금액 계산 (할인, 배분, 적립)
- 상태 전이 (허용/비허용 전이 모두)
- 경계값 (0, 최소, 최대, 초과)
- 동시성이 있는 로직
- 버그 수정 (재현 테스트를 먼저 작성)

컨트롤러 테스트는 요청 검증과 응답 형식만 확인합니다. 비즈니스 로직은 서비스 테스트에서 봅니다.

## 버그 수정 시 재현 테스트

버그를 고칠 때 재현 테스트를 먼저 작성하고 실패를 확인한 뒤 고칩니다. 이 규칙은 예외 없이 적용합니다.

## 테스트하지 않아도 되는 것

- 단순 위임 메서드
- getter/setter
- 프레임워크 동작에 의존하는 부분

## Action Item

- 이서연님: 위 기준을 PR 템플릿 체크리스트에 반영
- 윤수빈님: QA 에서 반복되는 결함 유형을 분기마다 공유
""",
    },
    {
        "id": "DOC-184",
        "title": "PR 작성 규칙",
        "department": "백엔드개발팀",
        "type": "팀 Ground Rule",
        "project": None,
        "date": "2026-04-08",
        "systems": [],
        "features": [],
        "participants": [],
        "concepts": ["PR", "코드리뷰", "팀 규칙"],
        "body": """## 제목

```text
[프로젝트] 무엇을 했는지
```

예:

```text
[Falcon] 결제 timeout 시 PENDING 유지 처리
[Atlas] 권한 검사 인터셉터 기본 거부로 전환
```

## 본문 구성

```markdown
## 배경
왜 이 변경이 필요한지

## 변경 내용
무엇을 바꿨는지

## 확인 방법
리뷰어가 어떻게 확인할 수 있는지

## 영향 범위
다른 기능이나 서비스에 미치는 영향
```

## 배경을 쓰는 이유

코드만 봐서는 왜 그렇게 했는지 알 수 없습니다. 리뷰어가 "이렇게 하면 되지 않나요"라고 물었을 때 이미 검토했다가 버린 방법이라면 배경에 적어 두세요.

관련 설계 문서나 이슈가 있으면 링크를 넣습니다.

## 확인 방법

리뷰어가 로컬에서 확인해야 하는 경우 절차를 적습니다. 테스트로 검증되는 것은 어떤 테스트를 보면 되는지 알려주세요.

## 크기

- 400줄 이하를 권장합니다
- 넘으면 왜 나눌 수 없었는지 본문에 적습니다
- 리팩터링과 기능 변경을 한 PR 에 섞지 않습니다

## 체크리스트

PR 템플릿에 포함된 항목입니다.

- [ ] 금액 계산, 상태 전이, 경계값에 테스트를 작성했는가
- [ ] 버그 수정이라면 재현 테스트를 먼저 작성했는가
- [ ] DB 스키마 변경이 있다면 선행 배포가 필요한가
- [ ] 서비스 간 인터페이스가 바뀌었다면 소비자에게 공유했는가
- [ ] 설계 문서의 결정 사항이 구현에 반영되었는가

마지막 항목은 재고 초과 판매 이슈 이후 추가됐습니다. 설계에서 정한 것이 구현에 빠진 사례가 있었습니다.

## Draft

작업 중인 PR 은 Draft 로 올려도 됩니다. 방향만 먼저 확인받고 싶을 때 유용합니다. Draft 는 리뷰 응답 시간 규칙이 적용되지 않습니다.
""",
    },
    {
        "id": "DOC-185",
        "title": "[리뷰] 알림 발송 큐 재시도 처리",
        "department": "백엔드개발팀",
        "type": "코드리뷰",
        "project": None,
        "date": "2026-06-02",
        "systems": ["NEXUS"],
        "features": [],
        "participants": ["노태윤", "오세훈"],
        "concepts": ["재시도", "비동기", "중복"],
        "body": """## 현재 구현

알림 발송 실패 시 재시도합니다.

```java
public void send(NotificationMessage message) {
    for (int attempt = 1; attempt <= 3; attempt++) {
        try {
            channelSender.send(message);
            return;
        } catch (Exception e) {
            sleep(1000L * attempt);
        }
    }
    notificationRepository.markFailed(message.id());
}
```

## 리뷰 의견

오세훈님: 재시도 사이에 스레드가 `sleep` 합니다. 큐 소비자 스레드가 최대 6초씩 묶입니다. 발송이 몰릴 때 처리량이 크게 떨어집니다.

노태윤님: 지연 큐로 다시 넣는 방식이 낫겠네요. 즉시 실패 처리하고 지연 재시도 큐에 적재하겠습니다.

오세훈님: 재시도할 수 없는 실패도 3번 시도합니다. "존재하지 않는 기기 토큰" 같은 것은 재시도해도 같습니다.

노태윤님: 예외를 분류하겠습니다. 재시도 가능(네트워크, 5xx)과 불가능(잘못된 대상, 4xx)으로 나누고 불가능은 즉시 실패 처리합니다.

오세훈님: 재시도할 때 같은 알림이 두 번 발송될 가능성은 없나요. 발송은 됐는데 응답만 실패한 경우요.

노태윤님: 채널마다 다릅니다. 푸시는 멱등키를 지원하지 않아서 중복 발송이 가능합니다. 이메일은 메시지 ID 로 중복을 막을 수 있습니다.

오세훈님: 푸시 중복은 사용자에게 알림이 두 번 오는 정도라 감수할 수 있겠네요. 다만 그런 특성을 주석이나 문서에 남겨 두세요.

## 수정 사항

- `sleep` 재시도를 지연 큐 방식으로 변경
- 예외를 재시도 가능/불가능으로 분류
- 채널별 중복 발송 가능성을 문서화

## 남은 확인

지연 큐를 새로 만들면 인프라가 추가됩니다. 기존 큐에 지연 속성을 쓸 수 있는지 확인이 필요합니다. 안 되면 DB 에 재시도 예정 시각을 두고 스케줄러가 읽는 방식으로 대체합니다.
""",
    },
    {
        "id": "DOC-186",
        "title": "shipping / shipping_tracking 테이블 설계",
        "department": "백엔드개발팀",
        "type": "DB 테이블 설계안",
        "project": None,
        "date": "2025-12-16",
        "systems": ["ORBIT"],
        "features": ["주문"],
        "participants": [],
        "concepts": ["외부 연동", "이력", "Index"],
        "body": """## 배경

배송 정보가 `order` 테이블에 컬럼으로 들어 있습니다. 송장번호 하나만 저장할 수 있어서 부분 배송을 표현하지 못합니다.

## 현재 문제

- 주문 하나에 송장이 여러 개인 경우를 처리할 수 없음
- 배송 상태 변경 이력이 없어 "언제 배송이 시작됐는지" 추적 불가
- 택배사 조회 결과를 매번 외부 호출

## shipping

| 컬럼 | 타입 | 제약 | 설명 |
| --- | --- | --- | --- |
| id | BIGINT | PK | |
| order_id | BIGINT | NOT NULL | |
| carrier_code | VARCHAR(16) | NOT NULL | 택배사 |
| tracking_number | VARCHAR(40) | NOT NULL | 송장번호 |
| status | VARCHAR(24) | NOT NULL | READY / PICKED_UP / IN_TRANSIT / OUT_FOR_DELIVERY / DELIVERED / FAILED |
| shipped_at | DATETIME | NULL | |
| delivered_at | DATETIME | NULL | |
| last_synced_at | DATETIME | NULL | 마지막 외부 조회 시각 |

## shipping_item

한 배송에 어떤 주문 상품이 들어갔는지 표현합니다.

| 컬럼 | 설명 |
| --- | --- |
| shipping_id | FK |
| order_item_id | FK |
| quantity | 부분 배송 시 수량 |

## shipping_tracking

택배사에서 받은 상태 변경 이력입니다.

| 컬럼 | 설명 |
| --- | --- |
| id | PK |
| shipping_id | FK |
| status | |
| location | 배송 위치 |
| occurred_at | 택배사 기준 발생 시각 |
| synced_at | 우리가 받은 시각 |

```sql
CREATE UNIQUE INDEX uk_shipping_tracking
  ON shipping (carrier_code, tracking_number);
CREATE INDEX idx_shipping_order ON shipping (order_id);
CREATE INDEX idx_shipping_sync ON shipping (status, last_synced_at);
CREATE UNIQUE INDEX uk_tracking_event
  ON shipping_tracking (shipping_id, status, occurred_at);
```

## 설계 고민

**tracking_number unique**: 같은 택배사에 같은 송장번호는 하나뿐입니다. 운영자가 실수로 같은 송장을 두 주문에 입력하는 것을 막습니다.

**idx_shipping_sync**: 상태 동기화 배치가 이 인덱스로 대상을 찾습니다. 배송 완료된 건은 더 조회하지 않으므로 `status` 를 앞에 뒀습니다.

**tracking 이벤트 unique**: 폴링으로 같은 이벤트를 여러 번 받습니다. 중복 저장을 막기 위해 `(shipping_id, status, occurred_at)` 조합에 unique 를 겁니다.

## 마이그레이션

기존 `order.tracking_number` 를 읽어 `shipping` 을 만듭니다. 송장이 없는 주문은 건너뜁니다.

`shipping_item` 은 주문의 모든 상품을 하나의 배송에 넣습니다. 과거 데이터는 부분 배송 정보가 없기 때문입니다.
""",
    },
    {
        "id": "DOC-187",
        "title": "배송 조회 API 명세",
        "department": "백엔드개발팀",
        "type": "API 명세서",
        "project": None,
        "date": "2026-01-12",
        "systems": ["ORBIT"],
        "features": ["주문"],
        "participants": [],
        "concepts": ["외부 연동", "캐시", "부분 배송"],
        "body": """## Endpoint

```http
GET /orders/{orderId}/shipments
Authorization: Bearer {accessToken}
```

## Response

```json
{
  "orderId": 90112,
  "shipments": [
    {
      "shipmentId": 4412,
      "carrierName": "가나택배",
      "trackingNumber": "1234-5678-9012",
      "status": "IN_TRANSIT",
      "statusLabel": "배송 중",
      "shippedAt": "2026-01-10T09:20:00+09:00",
      "estimatedDeliveryDate": "2026-01-12",
      "items": [
        { "orderItemId": 551201, "productName": "무선 이어폰", "quantity": 1 }
      ],
      "tracking": [
        { "status": "PICKED_UP", "location": "서울 강남", "occurredAt": "2026-01-10T09:20:00+09:00" },
        { "status": "IN_TRANSIT", "location": "대전 물류센터", "occurredAt": "2026-01-11T02:14:00+09:00" }
      ]
    }
  ]
}
```

주문 하나에 배송이 여러 건일 수 있습니다. 부분 배송인 경우 `items` 로 어떤 상품이 포함됐는지 확인하세요.

## 갱신 주기

배송 상태는 배치가 주기적으로 택배사에서 조회해 저장합니다. 이 API 는 저장된 값을 반환하며 실시간 조회를 하지 않습니다.

| 상태 | 조회 주기 |
| --- | --- |
| READY | 조회하지 않음 |
| PICKED_UP, IN_TRANSIT | 6시간 |
| OUT_FOR_DELIVERY | 2시간 |
| DELIVERED, FAILED | 조회 중단 |

`lastSyncedAt` 을 함께 내려주므로 화면에 "N시간 전 기준"으로 표시할 수 있습니다.

## Error Code

| 코드 | HTTP | 설명 |
| --- | --- | --- |
| ORDER_2001 | 404 | 주문 없음 |
| ORDER_2002 | 403 | 본인 주문이 아님 |
| ORDER_2003 | 404 | 배송 정보 없음 (아직 출고 전) |

`ORDER_2003` 은 오류가 아니라 정상 상태입니다. 클라이언트는 "배송 준비 중"으로 표시하세요.

## 주의사항

`estimatedDeliveryDate` 는 택배사가 제공하는 예상일입니다. 보장되지 않으므로 "예상"임을 명시해 표시하세요.
""",
    },
    {
        "id": "DOC-188",
        "title": "배송 상태 동기화 방식 검토",
        "department": "백엔드개발팀",
        "type": "개발자 회의록",
        "project": None,
        "date": "2025-12-09",
        "systems": ["ORBIT"],
        "features": ["주문"],
        "participants": ["노태윤", "오세훈"],
        "concepts": ["외부 연동", "폴링", "Webhook"],
        "body": """## 논의 배경

배송 상태를 어떻게 가져올지 정합니다. 현재는 사용자가 조회할 때마다 택배사 API 를 호출합니다.

## 현재 문제

- 택배사 API 호출 제한에 걸립니다 (일 10만 건)
- 택배사가 느리면 우리 화면이 함께 느려집니다
- 배송 완료를 우리가 인지하지 못해 구매 확정 처리가 늦습니다

## 검토안

**1. Webhook**

택배사가 상태 변경 시 알려줍니다. 가장 효율적이지만 지원하는 택배사가 3곳 중 1곳뿐입니다.

**2. 폴링**

주기적으로 조회합니다. 모든 택배사에 적용 가능합니다. 호출량이 많아집니다.

**3. 혼합**

지원하는 곳은 Webhook, 나머지는 폴링입니다.

## 폴링 호출량 추정

노태윤님이 계산했습니다. 배송 중인 주문이 평균 3만 건이고 6시간마다 조회하면 하루 12만 건입니다. 제한을 넘습니다.

상태별로 주기를 다르게 하면 줄일 수 있습니다. 배송 중은 6시간, 배송 출발은 2시간, 완료는 조회 중단입니다. 이렇게 하면 하루 6만 건 수준입니다.

## 쟁점

오세훈님은 혼합 방식이 코드가 두 갈래로 갈라진다고 우려했습니다. Webhook 을 받는 경로와 폴링 경로가 따로 생깁니다.

노태윤님은 저장 로직을 공통으로 두고 유입 경로만 나누면 된다고 했습니다. 둘 다 `shipping_tracking` 에 이벤트를 넣는 형태입니다.

## 결정 사항

- 혼합 방식으로 갑니다
- 유입 경로는 다르되 저장 로직은 공통
- 폴링 주기는 상태별로 차등 적용
- Webhook 은 서명 검증 후 수용

## Webhook 신뢰성

Webhook 이 유실될 수 있습니다. Webhook 을 지원하는 택배사라도 하루 1회는 폴링으로 대조합니다.

## Action Item

- 노태윤님: 폴링 스케줄러 및 저장 로직
- 오세훈님: Webhook 수신 엔드포인트 및 서명 검증
""",
    },
    {
        "id": "DOC-189",
        "title": "일부 주문에서 배송 상태가 갱신되지 않음",
        "department": "백엔드개발팀",
        "type": "이슈 정리",
        "project": None,
        "date": "2026-02-25",
        "systems": ["ORBIT"],
        "features": ["주문"],
        "participants": [],
        "concepts": ["외부 연동", "폴링", "예외 처리"],
        "body": """## 현상

배송 중 상태에서 멈춰 있는 주문이 쌓입니다. 실제로는 배송이 완료됐는데 화면에는 "배송 중"으로 남아 있습니다.

2월 24일 기준 1,842건입니다.

## 분석

폴링 스케줄러 로그를 확인했습니다.

```text
shipping.poll batch=500 success=487 failed=13
shipping.poll error carrier=DAHAN trackingNumber=xxx
  status=404 body={"code":"NOT_FOUND"}
```

특정 택배사에서 404 가 반복됩니다. 조회했을 때 송장번호를 찾을 수 없다는 응답입니다.

해당 송장을 택배사 사이트에서 직접 조회하니 정상 조회됩니다. API 와 사이트의 결과가 다릅니다.

택배사에 문의한 결과, 배송 완료 후 30일이 지나면 API 조회에서 제외된다고 합니다. 사이트는 더 오래 보관합니다.

## 원인

배송 완료 상태를 우리가 받지 못한 채 30일이 지나면 이후로는 영원히 조회할 수 없습니다. 폴링이 계속 404 를 받고 상태는 갱신되지 않습니다.

왜 완료 상태를 못 받았는지는 별개 문제입니다. 로그를 보니 1월 중 택배사 API 장애가 이틀 있었고, 그 기간에 완료된 건들이 대부분입니다. 장애 기간 폴링이 실패했고 이후 재조회에서 이미 30일이 다 되어 갔습니다.

## 영향 범위

- 1,842건이 배송 중으로 표시
- 구매 확정이 자동으로 되지 않아 정산이 지연

## 조치

**즉시**: 배송 시작 후 30일이 지나고 상태가 완료가 아닌 건을 `DELIVERED` 로 일괄 처리했습니다. 실제로 미배송인 건이 섞일 수 있으나, 30일 이상 배송 중인 정상 케이스는 없습니다.

**폴링 개선**: 404 가 3회 연속이면 폴링을 중단하고 알림을 보냅니다. 무한히 재시도하지 않습니다.

**장애 대응**: 택배사 API 장애 시 실패한 건을 별도로 기록하고, 복구 후 우선 조회합니다.

## 남은 문제

30일이 지나 조회 불가한 건은 실제 상태를 알 방법이 없습니다. 배송 완료로 가정하는 것 외에 대안이 없습니다.

Webhook 을 지원하는 택배사로 전환을 늘리는 것이 근본 대책이라는 의견이 있었습니다. 계약 문제라 개발팀 결정 범위를 넘습니다.
""",
    },
    {
        "id": "DOC-190",
        "title": "배송 지연 안내 기준 논의",
        "department": "서비스기획팀",
        "type": "기획자와의 회의록",
        "project": None,
        "date": "2026-03-05",
        "systems": ["ORBIT"],
        "features": ["주문"],
        "participants": ["강민재", "문가영"],
        "concepts": ["알림", "정책", "고객 문의"],
        "body": """## 논의 배경

배송이 늦어지면 문의가 들어옵니다. 문가영님 집계로 배송 관련 문의가 전체의 34% 이고, 그중 "언제 오냐"가 대부분입니다.

## 현황

배송 시작 후 평균 2.1일에 도착합니다. 다만 분포가 넓습니다.

| 소요 | 비중 |
| --- | --- |
| 1일 | 18% |
| 2일 | 41% |
| 3일 | 27% |
| 4일 이상 | 14% |

4일 이상인 건에서 문의가 집중됩니다.

## 기획안

강민재님 제안은 배송 시작 후 3일이 지나면 안내 알림을 보내는 것입니다.

```text
"주문하신 상품의 배송이 예상보다 지연되고 있습니다.
 현재 위치: 대전 물류센터"
```

## 쟁점

문가영님은 안내를 받고 오히려 문의하는 경우가 있다고 했습니다. 모르고 있던 사람에게 지연을 알려주면 문의가 늘 수 있습니다.

강민재님은 그래도 알리는 편이 낫다고 봤습니다. 알림 없이 늦으면 불신이 생깁니다.

두 번째 쟁점은 기준일입니다. 3일이면 27% 가 안내 대상이 되어 발송량이 많습니다. 4일로 하면 14% 입니다.

## 결정 사항

- 배송 시작 후 **4일** 경과 시 안내
- 안내에 현재 배송 위치를 포함
- 안내에서 바로 문의할 수 있는 버튼 제공
- 주말과 공휴일은 기준일 계산에서 제외

주말 제외는 택배 미운영일을 지연으로 보지 않기 위함입니다.

## 효과 측정

안내 발송 후 문의율 변화를 봅니다. 문의가 오히려 늘면 기준을 조정하거나 중단합니다.

## 미확정

배송 실패(주소 오류, 부재)에 대한 안내는 별도로 다뤄야 합니다. 지연과 실패는 사용자 조치가 다릅니다. 다음 회의에서 정합니다.

## Action Item

- 강민재님: 알림 문구 확정
- 문가영님: 안내 발송 후 문의율 모니터링
""",
    },
    {
        "id": "DOC-191",
        "title": "[리뷰] 배송 상태 폴링 스케줄러",
        "department": "백엔드개발팀",
        "type": "코드리뷰",
        "project": None,
        "date": "2026-03-19",
        "systems": ["ORBIT"],
        "features": ["주문"],
        "participants": ["노태윤", "김도윤"],
        "concepts": ["배치", "외부 연동", "재시도"],
        "body": """## 현재 구현

상태별 주기에 따라 조회 대상을 뽑아 택배사 API 를 호출합니다.

```java
var targets = shippingRepository.findPollingTargets(now, limit);
for (var shipping : targets) {
    var result = carrierClient.track(shipping.carrier(), shipping.trackingNumber());
    trackingService.apply(shipping.id(), result);
}
```

## 리뷰 의견

김도윤님: 한 건이 실패하면 예외가 올라가 나머지가 처리되지 않습니다. 개별 실패를 잡고 계속 진행해야 합니다.

노태윤님: try-catch 를 안쪽에 넣겠습니다.

김도윤님: 순차 호출이라 500건이면 응답 시간만큼 곱해집니다. 택배사 응답이 1초면 8분이 넘습니다. 병렬 처리가 필요해 보입니다.

노태윤님: 택배사별로 호출 제한이 있어서 무작정 병렬화하면 429 가 납니다. 택배사별로 동시 실행 수를 제한하는 형태로 가겠습니다.

김도윤님: 404 연속 3회면 중단한다고 했는데 카운트를 어디에 저장하나요.

노태윤님: `shipping` 에 `consecutive_failure_count` 컬럼을 추가하겠습니다. 성공하면 0으로 초기화합니다.

김도윤님: 404 와 5xx 를 같은 카운터로 세면 안 됩니다. 5xx 는 택배사 장애라 복구되면 정상 조회됩니다. 404 는 영구적일 가능성이 높습니다.

노태윤님: 분리하겠습니다. 404 는 3회에 중단, 5xx 는 카운트하되 중단하지 않고 알림만 보냅니다.

## 수정 사항

- 개별 실패 격리
- 택배사별 동시 실행 수 제한
- 404 와 5xx 카운터 분리
- 404 3회 연속 시 폴링 중단 및 알림

## 남은 확인

폴링 중단된 건을 다시 살리는 경로가 없습니다. 운영자가 수동으로 재개할 수 있어야 합니다. AIMS 화면에 재개 버튼을 추가하는 작업을 별도 등록했습니다.
""",
    },
    {
        "id": "DOC-192",
        "title": "배송 문의 대응 가이드",
        "department": "고객지원팀",
        "type": "매뉴얼 / 정의서",
        "project": None,
        "date": "2026-04-21",
        "systems": ["ORBIT", "AIMS"],
        "features": ["주문"],
        "participants": [],
        "concepts": ["운영", "고객 문의", "배송"],
        "body": """## 문의 유형별 확인 순서

### "언제 도착하나요"

1. AIMS 에서 주문 조회
2. 배송 탭에서 송장번호와 현재 상태 확인
3. `lastSyncedAt` 확인. 6시간 이상 지났으면 최신이 아닐 수 있음
4. 필요 시 택배사 사이트에서 직접 조회

우리 화면이 실시간이 아니라는 점을 유의하세요. 배송 완료됐는데 화면은 배송 중일 수 있습니다.

### "배송 중인데 상태가 안 바뀌어요"

- 폴링이 중단된 건일 수 있습니다. 배송 탭에 "동기화 중단" 표시를 확인하세요
- 중단된 건은 재개 버튼으로 다시 조회를 시도할 수 있습니다
- 재개해도 안 되면 택배사 사이트에서 확인 후 수동으로 상태를 변경하세요

### "받지 못했는데 배송 완료로 나와요"

1. 배송 이력에서 완료 시각과 위치 확인
2. 택배사에 확인 요청
3. 분실로 확인되면 재발송 또는 환불 처리

이 경우 강제 환불이 필요할 수 있습니다. 승인 절차를 따르세요.

### "주소를 잘못 입력했어요"

- 배송 준비 중이면 수령지 변경 가능
- 이미 출고됐으면 변경 불가. 택배사에 반송 요청 안내

## 상태 표시 대응

| 시스템 상태 | 고객 안내 |
| --- | --- |
| READY | 배송 준비 중입니다 |
| PICKED_UP | 상품이 택배사에 인계되었습니다 |
| IN_TRANSIT | 배송 중입니다 |
| OUT_FOR_DELIVERY | 오늘 배송 예정입니다 |
| DELIVERED | 배송이 완료되었습니다 |
| FAILED | 배송에 문제가 있습니다 |

## 하지 말 것

- 예상 도착일을 확정적으로 안내하지 마세요. 택배사 제공 값이며 보장되지 않습니다
- 상태를 임의로 변경하지 마세요. 변경이 필요하면 사유를 남기고 처리합니다
- 송장번호를 다른 주문에 복사하지 마세요. 중복 송장은 시스템에서 거부됩니다
""",
    },
    {
        "id": "DOC-193",
        "title": "배송 연동 개선 진행 현황",
        "department": "프로젝트관리팀",
        "type": "프로젝트 진행상황",
        "project": None,
        "date": "2026-04-28",
        "systems": ["ORBIT"],
        "features": ["주문"],
        "participants": [],
        "concepts": ["일정", "외부 연동", "Risk"],
        "body": """## 범위

- 배송 정보 테이블 분리 (부분 배송 지원)
- 폴링 + Webhook 혼합 동기화
- 배송 지연 안내
- 수령지 변경

## 완료

- `shipping` / `shipping_item` / `shipping_tracking` 테이블
- 마이그레이션 (기존 송장 41만 건)
- 폴링 스케줄러
- 배송 조회 API
- 폴링 중단 및 알림

## 진행 중

- Webhook 수신 (택배사 1곳, 서명 검증 구현 중)
- 배송 지연 안내 발송
- 수령지 변경 API

## 대기

- 폴링 재개 운영 화면
- 배송 실패 안내 (정책 미확정)

## 지표

폴링 전환 후 택배사 API 호출량입니다.

| 시점 | 일 호출량 |
| --- | --- |
| 전환 전 (실시간 조회) | 94,000 |
| 전환 후 (폴링) | 58,000 |

제한(10만) 대비 여유가 생겼습니다. Webhook 이 적용되면 더 줄어듭니다.

## Risk

Webhook 을 지원하는 택배사가 1곳뿐입니다. 물량 기준으로 전체의 22% 라 효과가 제한적입니다.

배송 실패 안내는 정책이 정해지지 않아 착수하지 못했습니다. 3월 회의에서 다음에 정하기로 했는데 아직 회의가 잡히지 않았습니다.

## 다음

- 5월 중 Webhook 적용
- 배송 실패 정책 회의 요청
""",
    },
    {
        "id": "DOC-194",
        "title": "배송지 변경 API 명세",
        "department": "백엔드개발팀",
        "type": "API 명세서",
        "project": None,
        "date": "2026-05-06",
        "systems": ["ORBIT"],
        "features": ["주문"],
        "participants": [],
        "concepts": ["Validation", "상태 전이", "제약"],
        "body": """## Endpoint

```http
PATCH /orders/{orderId}/shipping-address
Authorization: Bearer {accessToken}
```

## 변경 가능 조건

주문의 모든 상품이 다음 상태여야 합니다.

| 상태 | 변경 가능 |
| --- | --- |
| ORDERED | 가능 |
| PREPARING | 가능 (판매자 연동 시 확인 필요) |
| SHIPPING | 불가 |
| DELIVERED | 불가 |
| CANCELED | 불가 |

부분 배송으로 일부만 출고된 경우 변경할 수 없습니다. 출고된 것과 아닌 것의 주소가 달라지면 혼란스럽습니다.

## Request

```json
{
  "receiverName": "홍길동",
  "phone": "010-0000-0000",
  "zipCode": "06234",
  "address1": "서울시 강남구",
  "address2": "101동 202호",
  "deliveryMemo": "부재 시 경비실"
}
```

전체 주소를 보냅니다. 일부만 바꾸는 것은 지원하지 않습니다. 부분 수정은 검증이 복잡하고 실수 여지가 큽니다.

## Response

```json
{
  "orderId": 90112,
  "updatedAt": "2026-05-06T11:02:00+09:00",
  "shippingFeeChanged": false
}
```

`shippingFeeChanged` 가 true 면 도서산간 등으로 배송비가 달라진 경우입니다. 이때는 추가 결제나 환불이 필요하며 현재 지원하지 않습니다.

## Error Code

| 코드 | HTTP | 설명 |
| --- | --- | --- |
| ORDER_3001 | 409 | 변경 불가 상태 |
| ORDER_3002 | 409 | 일부 상품이 이미 출고됨 |
| ORDER_3003 | 400 | 주소 형식 오류 |
| ORDER_3004 | 409 | 배송비가 달라지는 주소 |
| ORDER_3005 | 409 | 판매자가 변경을 수락하지 않음 |

## 판매자 연동

`PREPARING` 상태에서는 판매자에게 변경 요청을 보내고 수락을 받아야 합니다. 연동이 없는 판매자는 변경할 수 없으며 `ORDER_3005` 가 반환됩니다.

## 감사

배송지 변경은 이력에 남습니다. 변경 전후 주소와 변경 시각이 기록됩니다. 운영자가 변경한 경우 감사 로그에도 기록됩니다.
""",
    },
    {
        "id": "DOC-195",
        "title": "배송지 변경 가능 시점 논의",
        "department": "백엔드개발팀",
        "type": "개발자 회의록",
        "project": None,
        "date": "2026-04-30",
        "systems": ["ORBIT"],
        "features": ["주문"],
        "participants": ["노태윤", "강민재"],
        "concepts": ["정책", "판매자 연동", "기술적 제약"],
        "body": """## 논의 배경

배송지 변경 문의가 꾸준합니다. 현재는 고객지원팀이 판매자에게 개별 연락해 처리합니다. 자동화하려면 어디까지 변경을 허용할지 정해야 합니다.

## 상태별 판단

노태윤님이 정리한 기술적 제약입니다.

**ORDERED**: 아직 판매자에게 전달 전이므로 자유롭게 변경 가능합니다.

**PREPARING**: 판매자가 준비를 시작했습니다. 송장을 이미 출력했을 수 있습니다. 판매자 수락이 필요합니다.

**SHIPPING**: 택배사에 인계됐습니다. 우리 시스템에서 변경할 방법이 없습니다.

## 쟁점

강민재님은 `PREPARING` 에서도 대부분 변경이 가능할 것이라고 봤습니다. 실제로 고객지원팀이 연락하면 판매자가 대개 수락합니다.

노태윤님은 판매자 연동이 없는 곳이 문제라고 했습니다. 주문 취소 정책 논의에서도 같은 제약이 나왔습니다. 연동 없는 판매자는 자동 요청을 받을 수 없습니다.

## 확인 필요

연동된 판매자 비중을 확인했습니다. 주문 건수 기준 71% 입니다. 나머지 29% 는 수동 처리가 유지됩니다.

## 결정 사항

- `ORDERED` 는 즉시 변경
- `PREPARING` 은 연동된 판매자에 한해 변경 요청 후 수락 시 반영
- 연동 없는 판매자는 기존대로 고객지원팀 수동 처리
- 화면에서 변경 가능 여부를 미리 표시

## 배송비 변경

주소가 바뀌어 배송비가 달라지는 경우는 이번 범위에서 제외합니다. 추가 결제나 부분 환불이 얽혀 복잡합니다.

도서산간으로 바꾸는 요청은 거부하고 안내합니다. 반대 방향(도서산간에서 일반 지역)도 환불이 필요해 같이 막습니다.

## 추후 논의

부분 배송된 주문의 배송지 변경은 지원하지 않기로 했습니다. 남은 상품만 다른 주소로 보내는 것이 기술적으로는 가능하지만, 사용자가 어떤 상품이 어디로 가는지 혼동합니다.

수요가 확인되면 다시 봅니다.
""",
    },
]
