"""DOC-091 ~ DOC-105 : Falcon 중반 - 중복 결제 발견, 멱등성, 부분 취소 (2026 Q1)."""

DOCUMENTS = [
    {
        "id": "DOC-091",
        "title": "QA 중 동일 주문 2회 승인 확인",
        "department": "QA팀",
        "type": "이슈 정리",
        "project": "Project Falcon",
        "date": "2026-01-09",
        "systems": ["ORBIT"],
        "features": ["결제"],
        "participants": ["윤수빈", "김도윤"],
        "concepts": ["중복 요청", "멱등성", "Race Condition"],
        "body": """## 현상

스테이징 QA 중 하나의 주문에 결제 승인이 두 번 일어났습니다. `payment` 행이 두 개 생겼고 PG 거래도 두 건입니다.

## 재현 조건

윤수빈님이 재현 절차를 정리했습니다.

1. 주문서에서 결제하기를 누른다
2. 응답이 오기 전(약 2초 이내)에 한 번 더 누른다
3. 두 요청 모두 승인된다

10회 시도 중 6회 재현됩니다.

## 분석

v2 설계에는 중복 방지 키가 들어 있습니다. 그런데 재현된 요청 두 건의 중복 방지 키가 서로 달랐습니다.

```text
14:02:11 POST /payments Idempotency-Key: idem-90881-a1f2
14:02:12 POST /payments Idempotency-Key: idem-90881-b7c4
```

프론트가 버튼 클릭마다 새 중복 방지 키를 생성하고 있었습니다. 중복 방지 키는 "같은 결제 시도"를 식별해야 하는데 "같은 요청"을 식별하고 있습니다.

주문 단위 중복 확인(`PAY_1002`)도 있는데, 첫 요청이 아직 `PENDING` 이라 승인된 결제로 판정되지 않았습니다.

## 영향 범위

스테이징에서만 확인했습니다. 운영은 아직 v1 이라 이 경로가 없습니다. 다만 v1 에도 주문 단위 중복 승인 문제는 존재하며 지난 3개월간 5건 발생했습니다.

## 원인

두 가지입니다.

1. 중복 방지 키 생성 주체와 범위가 정의되지 않음
2. `PENDING` 상태의 동일 주문 결제를 차단하지 않음

## 조치 방향

김도윤님 의견으로 1번은 클라이언트와 협의가 필요하고, 2번은 서버에서 즉시 막을 수 있습니다. 주문 ID 에 대해 `PENDING` 또는 `APPROVED` 인 결제가 있으면 새 요청을 거부하도록 합니다.

1월 13일에 중복 방지 키 정의 회의를 잡았습니다.

## 상태

조사 완료, 조치 진행 중입니다.
""",
    },
    {
        "id": "DOC-092",
        "title": "중복 방지 키 생성 주체와 범위 정리",
        "department": "백엔드개발팀",
        "type": "개발자 회의록",
        "project": "Project Falcon",
        "date": "2026-01-13",
        "systems": ["ORBIT"],
        "features": ["결제"],
        "participants": ["김도윤", "한지민", "정하늘"],
        "concepts": ["멱등성", "클라이언트 협의", "재시도"],
        "body": """## 논의 배경

QA 에서 나온 중복 승인은 중복 방지 키가 요청마다 새로 생성돼서 발생했습니다. 누가 언제 만들고 언제까지 유지하는지 정합니다.

## 쟁점

**언제 생성하나**

정하늘님은 주문서 진입 시 생성하는 안을 냈습니다. 화면이 살아 있는 동안 같은 키를 유지합니다.

한지민님은 앱에서는 화면이 백그라운드로 갔다 오면 상태가 초기화될 수 있다고 지적했습니다. 주문 ID 를 받은 시점에 생성해 로컬에 저장해야 안전합니다.

**언제 바꾸나**

결제가 실패해서 사용자가 다른 카드로 다시 시도하면 새 결제 시도입니다. 이때는 새 키가 필요합니다. 같은 키를 쓰면 이전 실패 결과가 그대로 반환됩니다.

김도윤님이 정리한 기준은 "사용자가 결제 수단이나 금액을 바꾸면 새 키, 같은 조건으로 재시도하면 같은 키" 입니다.

**서버가 만들면 안 되나**

서버가 만들면 네트워크 실패로 응답을 못 받은 클라이언트가 같은 키를 알 수 없습니다. 재시도할 때 새 키를 쓰게 되어 의미가 없습니다. 클라이언트 생성이 맞습니다.

## 결정 사항

- 중복 방지 키는 **클라이언트가 생성**한다
- 생성 시점은 **주문 ID 확보 직후**, 로컬 저장소에 보관한다
- 결제 수단이나 금액 변경 시 새로 생성한다
- 형식은 `{orderId}-{uuid 앞 8자리}` 로 통일한다
- 서버는 주문 단위 중복 승인도 별도로 차단한다

## 서버 보완

중복 방지 키와 무관하게, 같은 주문에 `PENDING` 또는 `APPROVED` 결제가 있으면 새 승인 요청을 거부합니다. `PAY_1003` 을 반환하고 클라이언트는 결제 상태 조회로 유도합니다.

## Action Item

- 한지민님, 정하늘님: 클라이언트 중복 방지 키 생성 및 보관 구현
- 김도윤님: 주문 단위 중복 차단 추가, API 문서 갱신
""",
    },
    {
        "id": "DOC-093",
        "title": "결제 멱등성 처리 설계",
        "department": "백엔드개발팀",
        "type": "비즈니스 기능 설계",
        "project": "Project Falcon",
        "date": "2026-01-15",
        "systems": ["ORBIT"],
        "features": ["결제"],
        "participants": [],
        "concepts": ["멱등성", "동시성", "Edge Case", "재시도"],
        "body": """## 배경

멱등키 정의가 확정되어 서버 처리를 설계합니다. 목표는 같은 결제 시도가 몇 번 들어오든 승인은 한 번만 일어나는 것입니다.

## 저장 구조

멱등 처리 상태를 별도로 관리합니다.

| 컬럼 | 설명 |
| --- | --- |
| idempotency_key | PK |
| request_hash | 요청 본문 해시 |
| status | IN_PROGRESS / COMPLETED |
| payment_id | 완료 시 결과 |
| response_body | 완료 시 응답 스냅샷 |
| created_at | |
| expires_at | 생성 후 24시간 |

## 처리 Flow

1. 멱등키로 기존 레코드 조회
2. 없으면 `IN_PROGRESS` 로 삽입 (unique 제약이 동시 진입을 막음)
3. 삽입 성공 시 실제 결제 처리 진행
4. 완료 시 `COMPLETED` 로 갱신하고 응답 저장
5. 이미 있으면 상태에 따라 분기

```text
IN_PROGRESS -> 409 PAY_1003 (재시도 안내)
COMPLETED   -> 저장된 응답 그대로 반환 (200)
```

## 요청 본문 검증

같은 멱등키로 다른 내용이 오면 클라이언트 버그입니다. `request_hash` 를 비교해 다르면 `PAY_1004` 로 거부합니다. 이걸 허용하면 첫 요청의 결과가 엉뚱한 요청에 반환됩니다.

## Edge Case

- **처리 중 서버 다운**: `IN_PROGRESS` 로 남습니다. 결제 자체는 보정 배치가 `payment.status` 로 확정합니다. 멱등 레코드는 10분 이상 `IN_PROGRESS` 면 해당 `payment` 상태를 보고 정리합니다.
- **멱등키 만료 후 재요청**: 24시간이 지나면 레코드가 없습니다. 새 요청으로 처리되지만 주문 단위 중복 차단에 걸립니다. 이중 방어입니다.
- **같은 키로 취소 요청**: 승인과 취소는 다른 엔드포인트이므로 키 네임스페이스를 분리합니다. `approve:{key}` 와 `cancel:{key}` 로 저장합니다.
- **응답 스냅샷이 큰 경우**: 결제 응답은 작아서 문제되지 않습니다. 다만 오류 응답까지 저장할지 결정이 필요합니다. 저장하면 재시도해도 계속 같은 오류가 반환되어 복구가 안 됩니다.

## 오류 응답 처리

**저장하지 않습니다.** 승인 거절(`PAY_1010`)은 클라이언트가 다른 수단으로 재시도해야 하고, 그때는 새 멱등키를 씁니다. 시스템 오류는 재시도가 성공할 수 있어야 합니다.

따라서 실패 시 멱등 레코드를 삭제합니다. 단 PG 승인이 실제로 일어났는지 불확실한 경우(timeout)는 삭제하지 않고 `IN_PROGRESS` 로 두어 중복 승인을 막습니다.

## 고민

멱등 레코드를 DB 에 둘지 Redis 에 둘지 고민했습니다. Redis 가 빠르지만 장애 시 멱등 보장이 사라집니다. 결제는 정확성이 우선이라 DB 를 선택했습니다. `payment` 와 같은 트랜잭션에 넣을 수 있는 것도 장점입니다.
""",
    },
    {
        "id": "DOC-094",
        "title": "[리뷰] 멱등 레코드 저장 및 정리",
        "department": "백엔드개발팀",
        "type": "코드리뷰",
        "project": "Project Falcon",
        "date": "2026-01-20",
        "systems": ["ORBIT"],
        "features": ["결제"],
        "participants": ["노태윤", "김도윤"],
        "concepts": ["멱등성", "Transaction", "동시성"],
        "body": """## 현재 구현

```java
public PaymentResult approve(String key, ApproveCommand command) {
    var record = idempotencyRepository.findByKey(key);
    if (record != null) {
        return handleExisting(record, command);
    }
    idempotencyRepository.save(IdempotencyRecord.inProgress(key, command));
    try {
        var result = paymentProcessor.process(command);
        idempotencyRepository.complete(key, result);
        return result;
    } catch (Exception e) {
        idempotencyRepository.delete(key);
        throw e;
    }
}
```

## 리뷰 의견

노태윤님: 조회 후 저장 사이에 동시 요청이 들어오면 둘 다 통과합니다. 설계 문서에는 unique 제약으로 막는다고 되어 있는데, 코드는 조회를 먼저 합니다. 조회 없이 바로 삽입하고 예외를 잡는 형태여야 제약이 의미가 있습니다.

김도윤님: 맞습니다. 삽입 먼저 시도하고 `DataIntegrityViolationException` 이면 기존 레코드를 조회하는 순서로 바꾸겠습니다.

노태윤님: `catch (Exception e)` 에서 무조건 삭제합니다. 설계에는 timeout 인 경우 삭제하지 않는다고 되어 있습니다. 지금 코드로는 PG timeout 후 재시도가 이중 승인이 됩니다.

김도윤님: 예외 타입을 나눠서 `PgTimeoutException` 은 레코드를 유지하겠습니다.

노태윤님: 삭제가 실패하면 어떻게 되나요. 원래 예외가 삼켜집니다.

김도윤님: 삭제 실패는 로그만 남기고 원래 예외를 던지도록 하겠습니다. 남은 레코드는 정리 배치가 처리합니다.

## 수정 사항

- 삽입 우선, unique 위반 시 기존 레코드 조회
- 예외 타입별 분기 (timeout 은 레코드 유지)
- 삭제 실패 시 원래 예외 보존
- 10분 이상 `IN_PROGRESS` 레코드 정리 배치 추가

## 남은 확인

정리 배치가 `payment.status` 를 보고 판단하는데, `payment` 행이 아직 없는 경우(삽입 직후 장애)가 있습니다. 이때는 PG 요청 이력도 없으므로 안전하게 삭제해도 됩니다. 판별 로직에 이 케이스를 추가했는지 다음 PR 에서 확인이 필요합니다.
""",
    },
    {
        "id": "DOC-095",
        "title": "결제 승인 API v2.1 변경 내역",
        "department": "백엔드개발팀",
        "type": "API 명세서",
        "project": "Project Falcon",
        "date": "2026-01-22",
        "systems": ["ORBIT"],
        "features": ["결제"],
        "participants": [],
        "concepts": ["멱등성", "버전 변경", "Error Code"],
        "body": """## 변경 요약

v2 초안 대비 멱등 처리와 오류 코드가 정리되었습니다. 초안을 기준으로 구현 중인 클라이언트는 아래 항목을 확인하세요.

| 항목 | v2 초안 | v2.1 |
| --- | --- | --- |
| 멱등키 형식 | 자유 | `{orderId}-{uuid8}` 권장 |
| 멱등키 유효기간 | 미정의 | 24시간 |
| 요청 본문 불일치 | 미정의 | `PAY_1004` 추가 |
| 주문 단위 중복 | 미정의 | `PAY_1003` 으로 차단 |
| 포인트 사용 | 요청에 포함 | 요청에 포함 (확정) |

## 멱등키 규칙

- 클라이언트가 생성합니다
- 주문 ID 확보 직후 만들고 로컬에 보관합니다
- 결제 수단이나 금액이 바뀌면 새로 만듭니다
- 같은 조건으로 재시도할 때는 같은 키를 씁니다
- 24시간이 지나면 서버에서 만료됩니다

## 응답 재현

`COMPLETED` 상태의 멱등키로 재요청하면 최초 응답을 그대로 반환합니다. HTTP 200 이며 본문은 첫 승인 결과와 동일합니다.

실패 응답은 저장하지 않습니다. 승인 거절 후 같은 키로 재요청하면 새로 처리됩니다. 다만 이 경우 대부분 주문 단위 중복 차단에 걸리므로, 다른 수단으로 재시도할 때는 새 키를 사용하세요.

## 추가된 Error Code

| 코드 | HTTP | 설명 | 클라이언트 조치 |
| --- | --- | --- | --- |
| PAY_1003 | 409 | 동일 주문에 처리 중이거나 완료된 결제 존재 | 결제 상태 조회 |
| PAY_1004 | 409 | 동일 멱등키에 다른 요청 내용 | 새 멱등키로 재요청 |

## 마이그레이션 안내

v2 초안으로 구현한 클라이언트는 멱등키 생성 시점만 바꾸면 됩니다. 요청 클릭마다 생성하던 것을 주문 단위로 바꾸세요. 이 변경 없이는 중복 승인이 발생합니다.

v1 은 예정대로 2026년 6월 종료합니다.
""",
    },
    {
        "id": "DOC-096",
        "title": "중복 방지 키 충돌로 정상 결제가 차단되는 사례",
        "department": "백엔드개발팀",
        "type": "이슈 정리",
        "project": "Project Falcon",
        "date": "2026-01-27",
        "systems": ["ORBIT"],
        "features": ["결제"],
        "participants": [],
        "concepts": ["멱등성", "Edge Case", "클라이언트"],
        "body": """## 현상

스테이징에서 정상적인 결제가 `PAY_1004`(중복 방지 키에 다른 요청 내용)로 거부되는 사례가 나왔습니다.

## 재현

1. 주문서에서 포인트 1,000점 사용을 선택하고 결제 시도
2. 카드 한도 초과로 실패
3. 포인트 사용을 취소하고 다시 결제 시도
4. `PAY_1004` 발생

## 분석

3번에서 클라이언트가 중복 방지 키를 바꾸지 않았습니다. 규칙상 "결제 수단이나 금액이 바뀌면 새 키"인데, 사용자가 포인트 사용량만 바꾼 경우가 규칙에 명시되지 않았습니다.

클라이언트 입장에서는 결제 수단(카드)도 같고 주문 금액도 같습니다. 실제 청구 금액만 달라집니다.

## 원인

중복 방지 키 갱신 조건이 모호했습니다. "결제 수단이나 금액"이라고 썼는데 어떤 금액인지 명확하지 않습니다.

## 조치

규칙을 다시 정의했습니다.

> 요청 본문(`request_hash`)에 포함되는 값 중 하나라도 바뀌면 새 중복 방지 키를 생성한다.

대상은 `orderId`, `method`, `totalAmount`, `pointAmount`, `couponIssueId` 입니다. 클라이언트가 요청을 만들 때 이 값들의 해시를 함께 계산하고, 이전과 다르면 키를 새로 만듭니다.

## 대안 검토

서버가 요청 내용이 바뀌면 자동으로 새 결제로 처리하는 방안도 검토했습니다. 그러면 중복 방지 키의 의미가 사라집니다. 같은 키로 다른 요청을 보내는 것은 클라이언트 오류이므로 거부하는 게 맞습니다.

## 남은 작업

API 문서에 갱신 조건을 명확히 기재했습니다. 클라이언트 구현 반영은 진행 중이며 앱은 다음 릴리스에 포함됩니다. 그때까지 스테이징에서 이 케이스가 계속 나올 수 있습니다.
""",
    },
    {
        "id": "DOC-097",
        "title": "Falcon Sprint 3 - QA 진입 전 확인사항",
        "department": "프로젝트관리팀",
        "type": "프로젝트 진행상황",
        "project": "Project Falcon",
        "date": "2026-01-30",
        "systems": ["ORBIT", "MARS"],
        "features": ["결제", "결제 취소"],
        "participants": ["임채원", "윤수빈"],
        "concepts": ["QA", "체크리스트", "Blocker"],
        "body": """## 스프린트 개요

1월 19일 ~ 1월 30일. 승인 경로를 QA 에 넘길 수 있는 상태로 만드는 것이 목표였습니다.

## 진입 조건

- [x] 승인 API v2.1 스테이징 배포
- [x] 멱등 처리 구현 및 리뷰 반영
- [x] 주문 단위 중복 차단
- [x] 보정 배치 구현
- [ ] 클라이언트 중복 방지 키 생성 규칙 반영
- [ ] 부분 취소 구현

## Blocker

클라이언트 중복 방지 키 규칙 반영이 앱 릴리스 일정에 묶여 있습니다. 웹은 이번 주 반영 가능하지만 앱은 2월 둘째 주입니다.

QA 는 웹 기준으로 먼저 시작하고, 앱은 반영 후 별도 회차로 진행합니다. 윤수빈님과 협의했습니다.

## QA 범위 (1차, 웹)

| 영역 | 시나리오 | 비고 |
| --- | --- | --- |
| 정상 승인 | 12 | 카드, 간편결제, 포인트 병행 |
| 중복 요청 | 9 | 동시 클릭, 재시도, 키 재사용 |
| 실패 처리 | 15 | 거절 코드별 화면 확인 |
| timeout | 6 | PG 지연 시뮬레이션 |
| 취소 | 8 | 전액 취소만. 부분 취소 제외 |

## Risk

부분 취소가 다음 스프린트로 밀렸습니다. 적립 회수 기준이 2월 3일 기획 회의에서 정해질 예정이라 그전에는 구현을 확정할 수 없습니다.

PG timeout 시뮬레이션 환경이 아직 없습니다. PG 스테이징에 지연 주입 기능이 없어서 프록시로 흉내내는 방법을 검토 중입니다. 이게 안 되면 timeout 경로 검증이 어렵습니다.

## 다음 스프린트

- 부분 취소 구현
- 앱 중복 방지 키 반영 후 QA 2차
- timeout 시뮬레이션 환경 구성
""",
    },
    {
        "id": "DOC-098",
        "title": "부분 취소 시 적립 회수 기준 확정",
        "department": "서비스기획팀",
        "type": "기획자와의 회의록",
        "project": "Project Falcon",
        "date": "2026-02-03",
        "systems": ["ORBIT", "MARS"],
        "features": ["부분 취소", "리워드 회수", "환불"],
        "participants": ["강민재", "송지은"],
        "concepts": ["부분 취소", "배분", "정책 확정"],
        "body": """## 논의 배경

작년 10월 회의에서 부분 취소 시 포인트 반환은 금액 비율 배분으로 정했지만, 적립 회수 기준은 미확정으로 남았습니다. Falcon 구현이 이 결정을 기다리고 있어 오늘 마무리합니다.

## 두 가지 방식

주문 100,000원, 적립 1,000점(1%), 30,000원 상품 취소 상황입니다.

**A. 취소 상품 적립분만 회수**

30,000원에 해당하는 300점을 회수합니다. 계산이 단순합니다.

**B. 남은 금액으로 재계산 후 차액 회수**

남은 70,000원에 대한 적립은 700점입니다. 1,000 - 700 = 300점을 회수합니다.

이 예시에서는 결과가 같습니다.

## 결과가 달라지는 경우

적립률에 구간이 있으면 달라집니다. 예를 들어 5만원 이상 2%, 미만 1% 라면,

- 주문 60,000원 → 적립 1,200점
- 20,000원 취소 → 남은 40,000원

A 방식: 20,000원의 2% 인 400점 회수 → 800점 남음
B 방식: 40,000원의 1% 인 400점이 정상 → 800점 회수 → 400점 남음

차이가 큽니다.

## 결정 사항

**B 방식(재계산)으로 확정합니다.**

근거는 적립 조건을 충족하지 못하게 된 주문에 조건 혜택을 남겨두면 어뷰징이 가능하기 때문입니다. 6만원어치 주문하고 2만원을 취소해 2% 적립을 유지하는 패턴이 실제로 관측됩니다.

## 예외

- 재계산 결과 회수 금액이 사용 가능 잔액을 초과하면 잔액만큼만 회수합니다. 음수 잔액은 만들지 않습니다
- 이미 소멸된 적립분은 회수하지 않습니다
- 회수 불가 금액은 별도 집계합니다

## 사용자 안내

취소 화면에 "취소 후 적립 예정 포인트가 조정됩니다" 문구를 넣습니다. 구체적 금액은 표시하지 않습니다. 계산이 복잡해 오히려 혼란을 줄 수 있다는 송지은님 의견을 반영했습니다.

## Action Item

- 강민재님: 취소 화면 문구 확정
- 개발: 재계산 로직 구현 및 회수 불가 집계 지표 추가
""",
    },
    {
        "id": "DOC-099",
        "title": "부분 취소 및 환불 처리 설계",
        "department": "백엔드개발팀",
        "type": "비즈니스 기능 설계",
        "project": "Project Falcon",
        "date": "2026-02-05",
        "systems": ["ORBIT", "MARS"],
        "features": ["부분 취소", "환불", "리워드 회수"],
        "participants": [],
        "concepts": ["부분 취소", "배분", "보상 처리", "Edge Case"],
        "body": """## 배경

적립 회수 기준이 확정되어 부분 취소 전체 흐름을 설계합니다. 금액 배분, PG 취소, 포인트 처리가 순서대로 얽혀 있습니다.

## 금액 배분

취소 대상 상품의 금액을 기준으로 나눕니다.

```text
상품 A 10,000 / B 20,000 / C 30,000  (합계 60,000)
포인트 사용 6,000, 실제 결제 54,000
B 취소 시:
  B 비율 = 20,000 / 60,000 = 1/3
  포인트 반환 = 6,000 * 1/3 = 2,000
  PG 취소 = 54,000 * 1/3 = 18,000
```

단수는 포인트 반환에서 올림, PG 취소에서 내림으로 처리해 사용자에게 유리하게 합니다. 합계가 원 금액을 넘지 않도록 마지막 취소에서 잔액 전부를 처리합니다.

## 처리 순서

1. 취소 가능 여부 확인 (상태, 잔여 금액, 기간)
2. 배분 계산
3. PG 부분 취소 요청
4. `payment` 상태 및 누적 취소액 갱신
5. MARS 포인트 반환
6. MARS 적립 회수 (재계산 기준)
7. 주문 상태 갱신

3번이 실패하면 이후 단계를 진행하지 않습니다. 4번 이후 실패는 재시도 큐로 넘깁니다.

## Edge Case

- **마지막 상품 취소**: 배분 잔여를 전부 반영합니다. 앞선 취소들의 올림/내림으로 생긴 오차가 여기서 정리됩니다.
- **PG 부분 취소 미지원 수단**: 일부 간편결제는 부분 취소가 안 됩니다. 전액 취소 후 재결제해야 하는데 이 흐름은 위험합니다. 해당 수단은 부분 취소를 막고 안내합니다.
- **적립 회수 대상이 이미 사용됨**: 회수 불가 금액으로 집계하고 진행합니다. 취소 자체를 막지 않습니다.
- **포인트 반환 후 적립 회수 실패**: 사용자에게 유리한 쪽만 처리된 상태입니다. 재시도 큐가 처리하며 참조키로 중복을 막습니다.
- **동시 부분 취소 요청**: 두 상품을 동시에 취소하면 배분 계산이 겹칩니다. `payment` 행을 잠그고 순차 처리합니다.
- **취소 후 남은 금액이 0**: 상태를 `CANCELED` 로 전이합니다.
- **취소 가능 기간 경과**: PG 취소가 불가능합니다. 환불 계좌를 받아 수동 처리하며 시스템에서는 `PAY_2006` 을 반환합니다.

## 환불 수단

카드 결제는 승인 취소로 처리되어 카드사 정책에 따라 3~5영업일이 걸립니다. 계좌 이체는 환불 계좌가 필요합니다.

포인트로만 결제한 부분은 즉시 반환됩니다. 사용자에게는 수단별 소요 시간을 안내합니다.

## 고민

배분 계산을 주문 시점에 미리 저장할지, 취소 시점에 계산할지 고민했습니다. 미리 저장하면 빠르지만 주문 변경(수량 변경 등)이 있으면 어긋납니다. 취소 시점 계산으로 정했고, 계산 결과는 `payment_history` 에 남겨 추적 가능하게 합니다.
""",
    },
    {
        "id": "DOC-100",
        "title": "order / order_item 구조 검토",
        "department": "백엔드개발팀",
        "type": "DB 테이블 설계안",
        "project": "Project Falcon",
        "date": "2026-02-10",
        "systems": ["ORBIT"],
        "features": ["주문", "부분 취소"],
        "participants": [],
        "concepts": ["부분 취소", "상태값 관리", "Index", "정합성"],
        "body": """## 배경

부분 취소를 지원하려면 상품 단위 상태가 필요합니다. 지금은 주문 단위 상태만 있어서 "3개 중 1개만 취소됨"을 표현할 수 없습니다.

## 현재 구조

`order.status` 하나로 관리합니다. 부분 취소가 들어오면 이 값이 애매해집니다.

## 변경안

주문 상태는 유지하되 상품 단위 상태를 추가합니다.

### order

| 컬럼 | 타입 | 설명 |
| --- | --- | --- |
| id | BIGINT | PK |
| member_id | BIGINT | |
| status | VARCHAR(24) | 파생 상태 (아래 규칙) |
| total_amount | INT | |
| point_amount | INT | |
| ordered_at | DATETIME | |

### order_item

| 컬럼 | 타입 | 설명 |
| --- | --- | --- |
| id | BIGINT | PK |
| order_id | BIGINT | FK |
| product_id | BIGINT | |
| quantity | INT | |
| unit_price | INT | |
| item_amount | INT | 수량 반영 금액 |
| status | VARCHAR(24) | ORDERED / PREPARING / SHIPPING / DELIVERED / CANCELED |
| canceled_at | DATETIME | |

## 주문 상태 파생 규칙

```text
모든 item 이 CANCELED        -> order.status = CANCELED
일부 item 만 CANCELED         -> order.status = PARTIAL_CANCELED
그 외                        -> item 중 가장 앞선 단계를 따름
```

주문 상태를 저장하지 않고 매번 계산하는 안도 있었지만, 주문 목록 조회에서 item 을 전부 읽어야 해서 비용이 큽니다. 저장하고 item 변경 시 갱신합니다.

## 인덱스

```sql
CREATE INDEX idx_order_member ON orders (member_id, ordered_at DESC);
CREATE INDEX idx_item_order ON order_item (order_id);
CREATE INDEX idx_item_status ON order_item (status, canceled_at);
```

`idx_order_member` 는 주문 목록 조회용입니다. 12월 부하 이슈에서 이 조회가 병목이었는데, 조인 없이 이 인덱스만으로 목록을 만들고 상품 정보는 별도 조회로 분리합니다.

## 정합성

`order.status` 는 파생 값이라 `order_item` 과 어긋날 수 있습니다. 일 1회 정합성 대조 배치로 검증합니다. 리워드와 결제에서 쓴 방식과 동일합니다.

## 마이그레이션

기존 `order_item` 에 `status` 를 추가하고 주문 상태를 복사합니다. 이미 취소된 주문은 모든 item 을 `CANCELED` 로 채웁니다. 부분 취소가 없었으므로 단순합니다.
""",
    },
    {
        "id": "DOC-101",
        "title": "[리뷰] 부분 취소 금액 배분 계산",
        "department": "백엔드개발팀",
        "type": "코드리뷰",
        "project": "Project Falcon",
        "date": "2026-02-12",
        "systems": ["ORBIT", "MARS"],
        "features": ["부분 취소"],
        "participants": ["노태윤", "박준호"],
        "concepts": ["배분", "단수 처리", "정합성"],
        "body": """## 현재 구현

```java
int pointToRestore = (int) Math.ceil(
    payment.pointAmount() * ((double) canceledItemAmount / payment.totalItemAmount())
);
int pgCancelAmount = payment.totalAmount() - pointToRestore;
```

## 리뷰 의견

박준호님: `pgCancelAmount` 계산이 잘못됐습니다. 전체 결제 금액에서 반환 포인트를 빼고 있는데, 부분 취소이므로 취소 대상 금액에서 빼야 합니다. 지금 코드로는 첫 부분 취소에서 거의 전액이 PG 취소됩니다.

노태윤님: 확인했습니다. 취소 대상 금액을 먼저 배분하고 그 안에서 포인트와 PG 를 나누는 순서로 고치겠습니다.

박준호님: 두 번째로, 여러 번 부분 취소할 때 올림이 누적됩니다. 3분할 주문에서 매번 올림하면 반환 포인트 합계가 원래 사용액보다 커질 수 있습니다.

```text
6,000점, 3분할 (1/3 씩)
매번 올림 -> 2,000 + 2,000 + 2,000 = 6,000  (이 경우는 괜찮음)
5,000점, 3분할
매번 올림 -> 1,667 + 1,667 + 1,667 = 5,001  (1점 초과)
```

노태윤님: 마지막 취소에서 잔여 전부를 반환하는 처리가 설계에 있는데 구현에 빠졌습니다. 추가하겠습니다.

박준호님: 적립 회수 재계산은 MARS 쪽 API 를 쓰나요. 회수 금액을 ORBIT 이 계산해서 넘기면 적립률 규칙이 두 곳에 생깁니다.

노태윤님: 회수 금액은 MARS 가 계산하는 게 맞습니다. ORBIT 은 "취소 후 최종 주문 금액"만 넘기고 MARS 가 재계산하도록 인터페이스를 바꾸겠습니다.

## 수정 사항

- 배분 순서 수정 (취소 대상 금액 → 포인트/PG 분배)
- 마지막 취소 시 잔여 전액 처리
- 적립 회수 계산을 MARS 로 이관
- 배분 결과를 `payment_history` 에 기록

## 남은 확인

적립률 규칙이 MARS 로 가면 ORBIT 은 최종 금액만 알면 됩니다. 다만 취소가 여러 번 일어날 때 "최종 금액"이 매번 바뀌므로, MARS 가 이전 회수분을 고려해 차액만 처리해야 합니다. MARS 쪽 구현에서 확인이 필요합니다.
""",
    },
    {
        "id": "DOC-102",
        "title": "환불 API 명세",
        "department": "백엔드개발팀",
        "type": "API 명세서",
        "project": "Project Falcon",
        "date": "2026-02-17",
        "systems": ["ORBIT", "AIMS"],
        "features": ["환불", "결제 취소"],
        "participants": [],
        "concepts": ["환불", "수동 처리", "승인"],
        "body": """## 언제 쓰나

PG 취소가 불가능한 경우에 사용합니다. 결제일로부터 365일이 지났거나, 카드사 사정으로 승인 취소가 되지 않는 경우입니다.

일반적인 취소는 `POST /payments/{paymentId}/cancel` 을 사용하세요. 이 API 는 운영자 전용입니다.

## Endpoint

```http
POST /admin/refunds
Authorization: Bearer {adminToken}
```

## Request

```json
{
  "paymentId": 771903,
  "refundAmount": 34000,
  "reason": "PG_CANCEL_UNAVAILABLE",
  "bankAccount": {
    "bankCode": "088",
    "accountNumber": "masked-on-storage",
    "holderName": "홍길동"
  },
  "approverNote": "카드사 승인취소 불가. 고객 계좌 환불 처리"
}
```

계좌번호는 저장 시 암호화되며 조회 시 마스킹됩니다.

## Response

```json
{
  "refundId": 3312,
  "status": "REQUESTED",
  "refundAmount": 34000,
  "expectedCompletedAt": "2026-02-20"
}
```

## 상태

| 상태 | 의미 |
| --- | --- |
| REQUESTED | 접수됨. 승인 대기 |
| APPROVED | 승인됨. 정산 처리 대기 |
| COMPLETED | 이체 완료 |
| REJECTED | 반려 |

## 승인 절차

- 30만원 이하: 고객지원팀 리드 승인
- 30만원 초과: 재무팀 추가 승인

승인은 AIMS 화면에서 수행합니다. API 로 직접 승인할 수 없습니다.

## Error Code

| 코드 | HTTP | 설명 |
| --- | --- | --- |
| REFUND_1001 | 404 | 결제 건 없음 |
| REFUND_1002 | 409 | 이미 PG 취소된 결제 |
| REFUND_1003 | 400 | 환불 금액이 결제 잔여 금액 초과 |
| REFUND_1004 | 409 | 동일 결제에 처리 중인 환불 존재 |

## 주의사항

이 API 로 처리한 환불은 PG 거래와 무관합니다. 정산 대사에서 별도로 확인해야 하므로 반드시 사유와 근거를 남기세요.

계좌 정보를 화면 캡처하거나 채팅으로 공유하지 마세요.
""",
    },
    {
        "id": "DOC-103",
        "title": "PG 지연 후 동일 주문 이중 승인",
        "department": "백엔드개발팀",
        "type": "이슈 정리",
        "project": "Project Falcon",
        "date": "2026-02-19",
        "systems": ["ORBIT"],
        "features": ["결제"],
        "participants": [],
        "concepts": ["Timeout", "중복 요청", "보정 배치"],
        "body": """## 현상

스테이징 timeout 시뮬레이션 중 같은 주문에 PG 승인이 두 건 생성되었습니다. 멱등 처리가 들어간 이후라 발생하면 안 되는 상황입니다.

## 재현

1. 프록시로 PG 응답을 20초 지연시킴
2. 결제 요청 (승인 timeout 15초로 실패, `PENDING` 유지)
3. 사용자가 결제 화면을 새로 고침하고 다시 결제

## 분석

3번에서 클라이언트가 새 중복 방지 키를 생성했습니다. 화면을 새로 고치면서 로컬 저장소를 읽지 못한 것으로 보입니다.

새 중복 방지 키이므로 멱등 레코드 검사는 통과합니다. 그다음 주문 단위 중복 차단이 걸려야 하는데 통과했습니다.

차단 조건을 확인했습니다.

```java
boolean exists = paymentRepository
    .existsByOrderIdAndStatusIn(orderId, List.of(APPROVED, PENDING));
```

조건 자체는 맞습니다. 그런데 첫 결제의 `payment` 상태가 `PENDING` 이 아니라 `FAILED` 였습니다.

timeout 예외 처리에서 `payment` 를 `FAILED` 로 마킹하고 있었습니다. 설계에는 timeout 시 `PENDING` 유지라고 되어 있는데, 구현이 일반 예외와 timeout 을 같은 경로로 처리했습니다.

## 실제 원인

두 가지가 겹쳤습니다.

1. timeout 시 `payment` 를 `FAILED` 로 잘못 마킹 (서버)
2. 화면 새로고침 시 중복 방지 키 유실 (클라이언트)

1번이 없었다면 2번이 있어도 차단됐습니다.

## 조치

- timeout 예외를 별도 처리해 `PENDING` 유지 (수정 완료)
- 보정 배치가 이 건을 PG 조회로 확정하는지 확인 완료
- 클라이언트 중복 방지 키를 세션 저장소가 아닌 로컬 저장소에 보관하도록 요청

## 남은 위험

클라이언트가 중복 방지 키를 유실하는 경우는 계속 있을 수 있습니다. 서버의 주문 단위 차단이 최종 방어선이므로 이 로직의 상태 조건이 정확해야 합니다. `FAILED` 상태여도 `payment_history` 에 `APPROVE_REQUESTED` 만 있고 결과가 없으면 차단 대상에 포함할지 검토 중입니다.
""",
    },
    {
        "id": "DOC-104",
        "title": "결제 보정 배치 운영 기준 정리",
        "department": "백엔드개발팀",
        "type": "개발자 회의록",
        "project": "Project Falcon",
        "date": "2026-02-24",
        "systems": ["ORBIT"],
        "features": ["결제"],
        "participants": ["김도윤", "오세훈"],
        "concepts": ["보정 배치", "운영 대응", "알림"],
        "body": """## 논의 배경

보정 배치가 구현됐는데 운영 기준이 정해지지 않았습니다. 실행 주기, 알림 조건, 수동 개입 시점을 정합니다.

## 배치가 하는 일

`PENDING` 상태로 일정 시간 이상 머문 결제를 PG 에 조회해 상태를 확정합니다.

## 논의 내용

**실행 주기**

오세훈님은 1분마다 돌리자고 했습니다. 사용자가 "처리 중" 화면에서 오래 기다리지 않아야 합니다.

김도윤님은 PG 조회 API 에도 호출 제한이 있다는 점을 지적했습니다. 대상이 많을 때 1분마다 전부 조회하면 제한에 걸립니다.

절충해서 대상 선정 기준을 두 단계로 나눴습니다.

| 경과 시간 | 처리 |
| --- | --- |
| 1분 ~ 5분 | 1분마다 조회 |
| 5분 ~ 30분 | 5분마다 조회 |
| 30분 초과 | 조회 중단, 알림 발송 |

**PG 조회 실패**

PG 자체가 응답하지 않으면 확정할 수 없습니다. 재시도하되 30분이 지나면 사람이 개입합니다.

**자동 확정 금지**

30분이 지나도 자동으로 `FAILED` 처리하지 않습니다. 실제 승인된 결제를 실패로 만들면 돈은 빠져나갔는데 주문이 없는 상태가 됩니다.

## 결정 사항

- 위 표대로 단계별 주기 적용
- 30분 초과 건은 온콜 알림
- 자동 확정은 하지 않음
- 배치 실행 결과를 지표로 남김 (확정 성공, 조회 실패, 알림 발송 건수)

## Action Item

- 오세훈님: 알림 연동 및 지표 대시보드
- 김도윤님: 단계별 주기 구현

## 추후 논의

`PENDING` 건이 갑자기 늘면 PG 장애일 가능성이 높습니다. 임계값을 넘으면 결제 자체를 잠시 막을지 논의가 필요한데, 오늘은 결론을 내지 못했습니다.
""",
    },
    {
        "id": "DOC-105",
        "title": "Falcon Sprint 4 진행 현황",
        "department": "프로젝트관리팀",
        "type": "프로젝트 진행상황",
        "project": "Project Falcon",
        "date": "2026-02-27",
        "systems": ["ORBIT", "MARS"],
        "features": ["결제", "부분 취소"],
        "participants": [],
        "concepts": ["일정", "Risk", "QA"],
        "body": """## 스프린트 개요

2월 16일 ~ 2월 27일. 부분 취소 구현과 QA 2차가 목표였습니다.

## 완료

- 부분 취소 금액 배분 구현 및 리뷰 반영
- 적립 회수 재계산 MARS 이관
- 환불 API (운영자용)
- timeout 처리 수정 (`PENDING` 유지)
- 보정 배치 운영 기준 확정

## 진행 중

- QA 2차 (앱 포함), 시나리오 50건 중 31건 수행
- `order_item` 상태 마이그레이션 검증

## 대기

- 운영 배포 승인
- 정산 정합성 대조 확인

## QA 현황

지금까지 발견된 결함 7건입니다.

| 심각도 | 건수 | 비고 |
| --- | --- | --- |
| 높음 | 1 | timeout 후 이중 승인 (수정 완료) |
| 보통 | 4 | 화면 문구, 금액 표시 오류 |
| 낮음 | 2 | |

## Risk

부분 취소가 안 되는 결제 수단이 있습니다. 해당 수단 사용자는 전액 취소만 가능한데, 앱에서 이를 사전에 안내하지 않으면 취소 버튼을 눌렀다가 실패합니다. 프론트 반영이 필요하고 아직 착수 전입니다.

정산 정합성 대조는 재무팀 확인이 필요한데 일정을 잡지 못했습니다. 부분 취소로 인해 정산 데이터 형태가 바뀌므로 배포 전 확인이 필수입니다.

## 다음 일정

- 3월 6일 QA 완료 목표
- 3월 중순 운영 배포 예정
""",
    },
]
