# Python 3.10 권장 (konlpy/JPype1, gensim 휠 호환성)
# Komoran 실행에는 JDK 8+ 필요 (현재 환경: OpenJDK 17 확인됨)

# --- 설정 / 유틸 ---
PyYAML==6.0.2
python-dotenv==1.0.1

# --- Confluence ---
requests==2.32.3

# --- 전처리 ---
beautifulsoup4==4.12.3
lxml==5.3.0
kiwipiepy==0.20.4 ; python_version >= "3.8"   # 문장 분리 보조(선택). 없으면 규칙 기반 fallback 사용

# --- 형태소 분석 ---
konlpy==0.6.0
JPype1==1.5.0

# --- 복합어 / 임베딩 ---
gensim==4.3.3
numpy<2.0                                      # gensim 4.3.x 호환

# --- LLM ---
anthropic==0.39.0

# --- 테스트 ---
pytest==8.3.3
